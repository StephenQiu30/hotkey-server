// Package constant defines shared constants used across the hotkey-server.
// These are not typed enums (which live in model/enum) but config-like values,
// queue topics, key prefixes, sentinel values, and other run-time constants.
package constant
