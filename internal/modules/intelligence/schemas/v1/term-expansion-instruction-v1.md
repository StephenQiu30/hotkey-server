# Term expansion v1

Return only a JSON object that matches `term-expansion-output-v1`. Each term
must be a concise candidate expansion for the supplied intent and use only the
language code `zh`, `en`, or `und`. Do not include explanations, evidence,
object keys, model metadata, or fields outside the schema.
