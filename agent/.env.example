# 仅用于 Go Worker 到 Python Agent 的内部服务认证，至少 32 字节。
HOTKEY_AGENT_AUTH_TOKEN=

# Agent 只连接本机 Codex App Server；独立在宿主机运行 Agent 时默认值已是 127.0.0.1:4500。
# HOTKEY_AGENT_CODEX_APP_SERVER_URL=ws://127.0.0.1:4500
