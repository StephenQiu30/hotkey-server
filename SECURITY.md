# 安全说明

当前工作树仅保留规范，以下是后续实现的安全要求，不代表已有安全机制或验收结论。

不要在公开 Issue 中提交密码、Cookie、会话、真实用户评论或私有连接地址。报告问题时提供脱敏复现步骤、影响范围和版本。

部署配置应默认只绑定回环地址，密码仅适合本机学习环境。公开访问需要 HTTPS 终止、独立随机凭据、精确允许来源和生产覆盖配置。生产配置必须强制 Secure Cookie 和 HTTPS 来源；账号应由本机 CLI 初始化，不提供开放注册。

会话应使用高熵随机令牌，数据库应只保存摘要；密码应使用 Argon2id。写请求应校验 Origin 和绑定会话的 CSRF 令牌。连续五次失败应锁定单 owner 登录 15 分钟；此全局限流适用于个人环境，不替代公网网关防护。

采集仅针对公开或获授权内容。后续需要落实凭据隔离、限流、证据脱敏与数据删除机制后，才能宣布来源可用。生产 TLS、备份恢复和持续运行必须提供独立验证证据。
