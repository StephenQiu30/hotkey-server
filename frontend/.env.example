# HotKey Web 日常环境模板
#
# 复制为 .env 后可直接启动（首选在 backend/.env 已配置好后端）。
# 下面全部是可选覆盖项，不需要逐项填写；不配置时使用程序默认值。

# ===== 后端 API 地址 =====

# 可选：后端不在本机 8866 端口时取消注释并修改。
# 仅供 Next.js 服务端代理 /api、/healthz 使用，不会以 NEXT_PUBLIC_* 暴露给浏览器，
# 也不向浏览器泄漏后端内部地址或密钥。
# 默认：http://127.0.0.1:8866
# HOTKEY_API_ORIGIN=http://127.0.0.1:8866

# ===== Docker 端口 =====

# Docker 构建和宿主机端口覆盖由仓库根目录的 Compose 与环境模板统一管理（见根 .env.example 的 WEB_PORT），
# 本文件不再重复维护。

# 生产环境模板见仓库根目录 .env.prod.example（配合 docker-compose-prod.yml）。
