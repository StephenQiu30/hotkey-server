# HotKey Web 日常环境模板
# 复制为 .env 后可直接启动；下面全部是可选覆盖项，不需要逐项填写。

# 可选：后端不在本机 8080 端口时取消注释并修改。
# 仅供 Next.js 服务端代理 /api、/healthz 使用，不会暴露给浏览器。
# HOTKEY_API_ORIGIN=http://127.0.0.1:8080

# Docker 构建和端口覆盖由仓库根目录的 Compose 与环境模板统一管理。
