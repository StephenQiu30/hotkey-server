# HotKey 日常 Docker Compose 可选覆盖项。
# 日常启动不要求创建任何环境文件，直接执行：
# docker compose -f docker-compose.yml up --build --detach --wait
#
# 只有端口冲突或同机运行多套实例时才复制本文件为 `.env` 并取消相应注释。
# 生产环境使用 `.env.prod.example`，不得复用这里的开发默认值。

# HOTKEY_CONTAINER_PREFIX=hotkey
# HOTKEY_HTTP_PORT=8866
# WEB_PORT=8010

# 仅在明确执行 003 真实模型 Shadow 评估时配置；普通开发使用内置 deterministic 分析。
# HOTKEY_AGENT_RUNTIME=openai_compatible
# HOTKEY_AGENT_MODEL_BASE_URL=https://approved-model.example/v1
# HOTKEY_AGENT_MODEL_API_KEY=
# HOTKEY_AGENT_MODEL_NAME=
# HOTKEY_AGENT_MODEL_VERSION=
