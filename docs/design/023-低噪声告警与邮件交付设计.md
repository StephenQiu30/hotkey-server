---
layer: Design
scope: shared
doc_no: "023"
title: 低噪声告警与邮件交付设计
status: proposed
version: v1.0
owner: HotKey Team
phase: P0
canonical_path: docs/design/023-低噪声告警与邮件交付设计.md
prd: docs/prd/023-低噪声告警与邮件交付.md
plan: docs/plans/023-低噪声告警与邮件交付计划.md
---

# 低噪声告警与邮件交付设计

## 现状

项目已有基于 EventUpdate 的告警评估、Alert Thread、acknowledge/resolve/suppress 和审计；SMTP 当前主要用于身份邮件与报告投递，热点告警邮件链路待补。

## 目标

只在监控事件发生实质变化且超过用户阈值时创建站内告警，并按 high/urgent 策略可靠发送邮件。

## 非目标

不在本条复制相邻领域的数据或服务，不以纯前端假数据宣称功能完成，也不引入第二套事实源。

## 核心决策

- 告警以 Monitor+Event 形成 Thread，Occurrence 记录每次变化；冷却窗口内更新线程而不制造通知风暴。
- 策略可配置最低热度、动量、来源宽度、重要性、严重级别、冷却时间和邮件开关。
- 邮件只由持久 Delivery 任务发送，使用 idempotency key、最大重试和最终失败状态；不在请求内直接 SMTP。
- 用户可确认、解决、抑制；新实质变化可按策略重新打开，全部状态变化使用乐观锁与审计。

## 数据、接口与交互

- 低噪声阈值与告警线程
- 站内告警列表和详情
- high/urgent 邮件模板与异步投递
- 确认、解决、抑制、重开和失败诊断

所有状态变化保存明确版本、时间和操作者；跨端字段由 Schema、后端 DTO 和生成 OpenAPI 驱动，前端不得手写重复类型。

## 安全、失败与降级

阈值过低会迅速失去用户信任；默认策略必须偏保守并展示触发原因。

外部 AI、邮件、实时连接或归档不可用时，业务事实先可靠落库，异步重试或降级读取，不在请求链路丢失结果。

## 设计验收边界

- 同一变化并发评估只产生一个 Occurrence 和一封邮件
- 冷却期内相似变化不重复打扰
- SMTP 故障不回滚告警事实且可安全重试
