---
layer: Design
scope: shared
doc_no: "002"
title: 信息获取主链路设计
status: accepted
version: v1.1
date: 2026-09-26
owner: HotKey Team
canonical_path: docs/design/002-信息获取主链路设计.md
parent: docs/design/001-热点舆情监控平台总体设计.md
prd: docs/prd/002-信息获取主链路需求.md
plan_index: docs/plan/README.md
---

# 信息获取主链路设计

本 Design 是 M1 Epic，从 [总设计 001 v3.2](001-热点舆情监控平台总体设计.md) 正向拆解；总设计现为 v4.0。需求为 [PRD 002](../prd/002-信息获取主链路需求.md)，逐 Issue 执行计划见 [Plan 001—009、031—041及共享051](../plan/README.md)。本文中的“已有”仅表示代码或指定运行证据，不代表产品验收。

## 1. 范围与不包含

四个关键词来源（HN Algolia、Google News 搜索 RSS、本机 SearXNG 新闻、本机 RSSHub 36Kr 快讯）、HN 评论父链、六个公开热榜、来源覆盖查询及本机 Codex 相关性构成 M1。每个来源、能力、时间窗单独验收。不包含本人账号 B 站、其他登录平台、事件归并、报告、知识库、推送、告警与导出。对应 [PRD 002](../prd/002-信息获取主链路需求.md) 与[完整Issue清单](../plan/README.md)。

## 2. 当前能力与证据分层

| 能力 | 代码/受控 | 真实运行 | 产品验收 |
|---|---|---|---|
| 四来源搜索、持久化、调度 | 已有代码及受控测试 | 2026-09-26 可重建开发库约 4 小时：Google News 365、SearXNG 94、HN 61、36Kr 8 条 | 同窗 72 小时未通过 |
| HN 评论 | `source.comments` 已有代码及受控测试 | 较早一轮旧库 45 线程，库已重建，不与当前库统计合并 | 分页、父链、重放与连续性未通过 |
| 六榜 | 快照、条目、调度、最新榜查询已有代码及受控测试 | 开发库 59 快照；最近 2 小时 23 成功、1 失败；首轮 12 命中、7 条 Codex 判相关 | 六榜逐榜与同窗 72 小时未通过 |
| Codex 相关性 | `ai`/`analysis`/`content_annotations` 已有代码及受控测试 | 开发库 738 条标注中 3 条相关字段为空 | 异常排查与质量抽检未通过 |
| 统一来源覆盖查询 | 部分事实表/查询已有 | 尚无完整查询与页面 | 未通过 |

上述数值是总设计 v3.2 的 2026-09-26 开发记录，未在本次重跑；代码、受控、真实、产品四层不可互相替代。

## 3. 架构与数据流

`monitors` 设主题规则，`connections` 原子应用来源预设，独立调度进程扫到期表并由 `jobs` 同事务创建 Job/Outbox，Kafka 唤醒单宿主机 Worker。`sources/adapters` 请求来源，`content` 保存来源身份、版本、发现关系、评论父链和热榜快照；`analysis` 经 `ai` 的本机 Codex 标注不可变内容版本。`jobs` 持有覆盖窗口与预算事实。API 只经所属服务聚合 DTO；浏览查询不请求源站。事务、重投与跨领域依赖沿用[总设计 §3—5](001-热点舆情监控平台总体设计.md)。

### 3.1 来源预设与连接

`python -m cli sources preset apply hackernews` 应原子应用连接、策略和预算；应用预设只说明配置就绪，来源须经真实采集与持久化验收才标记可用。

- **预设定义**在 `connections/presets.py`：来源键、能力、主机白名单、字段用途、保留天数、组件版本/许可、执行策略与预算。`SourcePresetService.apply_in_transaction` 将连接版本、准入、保留、组件与预算放在同一事务；重复同一配置幂等，改变配置升连接版本。`config JSONB` 仅存白名单许可的非机密端点字段，`source_connection_versions.execution_policy JSONB` 保存严格验证的非秘密执行策略快照；普通连接可以没有预设策略。该新增列只进入全新空库 Schema，保留库先走 051 恢复路径。
- **关键词来源实际入口**：`hackernews` 为 Algolia 公开 API；`google_news` 为 Google News 搜索 RSS；`news_search` 为本机 SearXNG `127.0.0.1:8888` 的 `duckduckgo news` 引擎；`rss_36kr` 为本机 RSSHub `127.0.0.1:1200/36kr/newsflashes`，按主题过滤快讯，不走 36Kr 原站 feed。`web` 仍专指单网页正文采集。
- **节奏契约（DEC-001-213）**：版本化 `SourceExecutionPolicy` 包含最短间隔、带时区起止的静默窗口、每轮词数、每词条数和请求上限、Job `max_seconds`、进程 `hard_timeout_seconds`、最大并发及开关；静默窗口支持跨午夜。B 站预设为 6 小时、Asia/Shanghai 00:00—08:00、3 词、每词 5 条/26 次请求、220 秒 Job、240 秒进程与并发 1。四关键词来源沿用现有 100 条、3 请求、90 秒策略及原日预算；六榜分别保存默认 1800 秒间隔。Plan 002 只持久化策略，现有调度硬编码的消费迁 031；调度届时先检查开关和静默，再取 `max(主题频率, 来源最短间隔)`，词数、条数、请求数与时间上限取保守值。M1 验收窗口按 §3.5 冻结。
- **版本语义**：策略随连接版本保存，可按 owner、连接 ID、指定版本读取；Job 输入保留受理时的连接版本。安全停用状态仍从当前连接行检查，预设重应用不能解除停用。来源日预算以 owner、source、metric、窗口规则对应的稳定 policy 行和窗口累计，变更连接版本或非秘密策略不建立新额度；同一来源/指标/窗口规则的替代预算键被拒绝。调度消费及旧 Job 执行裁决由 031 完成，重试周期由 032 完成，不能把本策略快照当作已运行的调度证据。

### 3.2 适配器与安全

| 适配器 | 状态 | 说明 |
|---|---|---|
| `http_source.py` | 代码已合入 | 公共基类：预算授权、截止、取消、游标防循环、响应上限和错误映射 |
| `hackernews.py` | 代码已合入 | Algolia 搜索、分页与评论父链；端到端另验 |
| `rss.py` | 代码已合入 | Google News RSS 与本机 36Kr RSSHub；逐来源另验 |
| `web_search.py` | 代码已合入 | SearXNG JSON，`duckduckgo news` 引擎；逐来源另验 |
| `rsshub_hotlist.py` | 代码已合入 | 六榜 RSSHub 入口，条目顺序为排名；逐榜另验 |
| `mediacrawler.py` | 代码已合入 | B 站宿主机子进程与同次运行缓存评论；真实修复后验收待做 |

安全规则：

- 适配器构造时接收**主机白名单**（来自预设），请求 URL 与每一跳重定向的主机都必须在白名单内，否则 `access_denied`；禁止私网地址（复用 `sources/adapters/web_targets.py` 已有规则）。
- 本地服务（SearXNG `127.0.0.1:8888`、RSSHub `127.0.0.1:1200`）以显式条目加入对应预设白名单，由同级 `Docker` 的独立 Compose 项目管理；Firecrawl 独立编排。MediaCrawler 的 HotKey 入口固定为宿主机子进程，不以它的容器构建或 CLI 帮助通过代替平台采集。

### 3.3 时间口径

- 契约允许 `published_at=None`。关键词发现提交时：有发布时间的按发布时间判断是否落在任务窗口；没有发布时间的可作为发现时观察纳入，记录 `content_discoveries.first_observed_at`，不得伪造发布时间。
- 报告与统计的时间依据 = `COALESCE(发布时间, 首次发现时间)`，并对"按发现时间计"的条数单独计数、写入覆盖说明。
- **三种频率分开**：主题/来源的调度频率决定何时尝试；`HOTKEY_COLLECTION_LOOKBACK_SECONDS` 默认 86,400 秒、最多 7 天，决定当前任务向前查询的窗口；来源自身发布、缓存或榜单更新时间由来源控制。代码用上一窗口终点或首轮间隔起点叠加回看，按身份去重；重复扫描和 24 小时回看不能证明源站历史完整、分页尾段已见或中断缺口已补。无可信终止证据的 `COMPLETE/EMPTY` 仍为部分覆盖。

### 3.4 评论采集

- `source.comments` 执行器已有代码：锁租约、校验连接版本和准入、逐条入库、记录游标与计量；真实来源的页尾和父链仍分别验收。
- 评论 ID 保留来源原生不透明字符串；作品、线程根、直接父节点和回复目标分开记录。关系未知或父节点缺失须显式保留状态与缺口，不根据正文或提及用户名猜测父链；每根评论的回复分页与停止原因分别计量。
- 评论上限按来源预设或能力设置。HN 可按候选策略采评论与楼中楼；本人账号 B 站试点当前只取同轮搜索缓存的**一级评论每帖最多 20 条**，不宣称楼中楼覆盖，不为评论 Job 再发平台请求。微博暂不接入。

### 3.5 六个公开热榜

| 来源键 | 本机 RSSHub 路由 | 排名与时间 |
|---|---|---|
| `hotlist_weibo` | `/weibo/search/hot` | Feed 顺序为排名；缺发布时间时统计按观察时间，`published_at` 仍为空 |
| `hotlist_baidu` | `/baidu/top` | 同上 |
| `hotlist_zhihu` | `/zhihu/hot` | 同上 |
| `hotlist_bilibili` | `/bilibili/popular/all` | 同上 |
| `hotlist_36kr` | `/36kr/hot-list` | 同上；与关键词 `rss_36kr` 快讯入口不同 |
| `hotlist_thepaper` | `/thepaper/featured` | 同上 |

- 已有 `SourceCapability.HOTLIST`、能力 CHECK、`source.hotlist`、`RsshubHotlistAdapter`、`hotlist_snapshots` 与 `hotlist_entries`。调度按 `hotlist_interval_seconds` 创建来源级 Job，默认 1800 秒且可配置。M1 共同 72 小时窗口开始时须记录实际生效的间隔、配置版本及各榜到期相位并冻结；逐榜按该间隔与相位统计 `[T0,T1)` 内实际应到期的桶数，达标数取 `ceil(0.9 × 桶数)`。窗口内配置变化则窗口作废，从新 T0 重开，不拼接不同间隔的桶；仅1800秒且T0与到期点对齐时每榜为144桶、至少130桶。适配器仅访本机 RSSHub，最多取前 100 条；一次成功响应按实际顺序保留排名、标题、链接、可选摘要/热度/发布时间。快照记录 `observed_at`、来源和 Job；条目记录排名、命中主题、可选内容 ID。与**同来源前一快照**按 URL 比较得 `new/up/down/same`，不把热度或发布时间当排名。
- 主题规则复用 `evaluate_monitor_rules`：对标题与摘要作任一/全部/排除匹配；中文可子串匹配，英文/ASCII 词须在字母数字边界形成整词，避免 `AI` 命中 `daily`。命中条目经来源准入后按 URL 身份入库、建立发现关系，再进入 Codex 相关性队列；未命中条目只保留榜单快照。榜单时间以本次 `observed_at` 表示观察，不把来源未给出的发布时间伪造为观察时间。
- `GET /hotlists/sources` 查询已应用来源与最近快照时间；`GET /hotlists/{source_key}` 分页返回最新快照、条目、命中与排名变化。读取不访问 RSSHub。代码当前将空榜作为 `hotlist_source_empty` 失败，且不保存空快照；后续应保留“成功取到空榜”和网络/解析/准入失败的可查询差异，空榜也不能证明来源覆盖完整。
- 开发库真实运行在 15:00 的 `hotlist_36kr` 时间桶出现一次 `hotlist_upstream_error`，最近 2 小时统计为 23 成功、1 失败，下一时间桶恢复。该失败桶保留上游错误和覆盖缺口；若重试，只能关联原时间桶的 operation ID，不以后一时间桶的成功覆盖前一桶或把跨桶数据伪记为原桶结果。
- **独立 AC**：六条路由逐一真实采到带观察时间的快照，复扫不重建同一 Job，排名变化与空榜/失败可辨；命中主题的条目落入内容库并获得 Codex 相关性标注，不命中者不进入内容库。事件归并与事件热度属于后续能力，不作为本 AC 条件；RSS 可解析或快照存在本身不能代替入库与分析证据。

### 3.6 运行时前置条件

Codex app-server 初始化必须声明 `experimentalApi`，以支持当前 `thread/start.environments`；`AiService` 与管理命令使用同一个 `codex.app-server` 组件键。部署者在唯一 owner 下运行 `jobs budget-baseline`，单次事务写入全局网络请求、分析尝试每日上限与本机 Codex 组件策略，命令不输出凭据，未初始化 owner 时失败。独立调度进程先导入完整 ORM 注册表再配置映射。`HOTKEY_COLLECTION_LOOKBACK_SECONDS` 默认 24 小时、最多七天；回看不能证明分页尾段或完整覆盖。36Kr 关键词入口固定本机 RSSHub `/36kr/newsflashes`，异常保留失败状态。以上为原总设计 v3.2 §21 的运行校准，不是三日验收证据。

## 4. 领域、任务与数据状态

`keyword.search`、`source.comments`、`source.hotlist`、`analysis.annotate` 为本里程碑任务；通用 `webpage.collect` 是已有基础能力，不能替代逐来源验收。`monitor_schedules` 唯一键 `(owner_id, topic_id, source_key, capability)`；Job/`job_attempts`、`coverage_windows`/页检查点和 `resource_budget_*` 记录执行与预算；帖子保留来源原生 ID、作者 ID 与昵称，`content_discoveries.first_observed_at` 与可空 `published_at` 分开；`content_threads` 保留稳定身份与父链；`hotlist_snapshots`、`hotlist_entries` 保存观察时间、排名与命中；`content_annotations` 按内容版本、主题规则和提示词版本唯一。

覆盖 `COMPLETE/EMPTY/PARTIAL/STOPPED` 必须附可信页尾或缺口说明；空榜成功与网络/解析失败分开，后一个时间桶不能补写前一桶。`content_annotations` 空相关字段归“标注异常”，不是相关、不相关或完成。状态与字段由唯一 DDL `backend/database/schema.sql` 落地；新增事实表须先按数据库重建门槛设计。

## 5. 接口与页面

现有 `GET /hotlists/sources`、`GET /hotlists/{source_key}` 返回应用来源、最新快照与分页条目，读取不得访问 RSSHub。Plan039补快照历史与指定快照读取；Plan041补内容、评论、标注与来源缺口阅读。Plan004规定 `GET /api/collection-coverage`、`/{window_id}`；Plan006规定静态 `/metrics`（优先注册），同一服务供CLI复用。查询按owner、source、capability和[start,end)筛选，单次最多31天、默认20/最多100条并使用游标；同一只读事务从各领域DTO聚合，不在路由联表SQL。Plan034负责来源覆盖工作台，展示正常、空、加载、错误、部分和无权限状态，不把空列表当完整覆盖。所有接口复用现行ErrorView和生成客户端。

## 6. 调度、执行与截止

### 6.1 主题与调度

`monitors` 的主题选择来源和采集频率；默认 30 分钟、最短 10 分钟只是主题请求频率，实际运行需与 [Design 002 §3.1](002-信息获取主链路设计.md#31-来源预设与连接) 的来源节奏合成。报告时间、周报与推送目标属于各自后续结果配置，不定义信息获取“就绪”。主题具备已应用来源、有效规则和预算可用，仍只算配置就绪；真实连接与采集状态由来源能力证据决定。

Plan 001 将 `monitor_topic_versions` 扩为采集配置快照：关键词三组、按键排序去重的 `source_keys`、`collection_interval_seconds` 随版本保存；规则、来源或主题间隔变化才升版，名称及报告/推送偏好变化不升版。旧 Job 的主题版本与连接版本不变，克隆产生新主题版本 1，归档保留旧版本。创建/更新来源选择须符合当前已应用搜索预设、准入策略和启用的执行策略；恢复另核对当前来源网络预算可用，重复恢复幂等。配置就绪不等于来源持久读取证据，页面分别展示。唯一 DDL 仍为 `backend/database/schema.sql`，仅在新空库应用；存量库按恢复流程处理。

新增主题的首个有效来源周期保留原 PRD 的真实帖子目标：至少一个已准入关键词来源须有可打开原帖的入库结果；若真实来源为空或失败，记录窗口状态与缺口，该验收项不通过，不以模拟帖子补足。

- 入口 `python -m worker.scheduler`，与 Worker 分开运行（Kafka 消费是同步逐条执行的，不能把调度塞进消费循环）。
- 每 30 秒一轮，独立事务领取到期行并用 `JobService.accept_in_transaction` 建 Job、推进 `next_run_at`；行锁、B 站来源级 advisory lock 与确定性 operation ID 阻止重复受理。预设节奏通用化后仍须保持同样的并发证明。
- `monitor_schedules` 以 `(owner_id, topic_id, source_key, capability)` 唯一，保存间隔、下次到期、开关及最近 Job。Job 固定来源连接版本和 `scheduled`/`manual` 入口，事后不按当前主题设置重释旧结果。

### 6.2 来源覆盖与运行查询

- 以**来源 × 能力 × 时间窗**为主键展示到期计划、实际请求与页数、起止及最后成功时间、`COMPLETE/EMPTY/PARTIAL/STOPPED`、停止/失败原因、已入库数与按来源身份去重数、分析待处理/失败/**标注异常**数、预算上限/预留/实际使用和未确认缺口。查询还显示当前连接版本、Job/尝试 ID 与来源是否暂停，允许从时间窗追到快照或内容；不以空结果覆盖缺口。
- 事实分别来自 `monitor_schedules`、Job/`job_attempts`、`coverage_windows`/页检查点、内容发现与版本、`content_annotations`、`resource_budget_*`、热榜快照。API 由所属服务组合只读 DTO，不在路由拼 SQL；Web 来源详情或任务详情按时间窗展示，CLI/受控查询提供同一口径。现有 Job 状态、预算快照、报告覆盖及热榜读取只覆盖部分字段，统一来源能力时间窗查询与页面仍待实现。
- 覆盖状态只有收到该适配器针对固定时间窗的可信尾段证据才能确认；回看、`COMPLETE/EMPTY`、RSS 头部或固定数量截断不构成完整覆盖。请求失败保留既有内容和缺口；分析积压单列，避免把“已入库”误读成“已判断相关”。
- **验收**：对成功、有结果但页数/预算截断、空榜、限流、登录失效和中途失败各造受控场景，再以真实来源窗口抽查请求、页数、入库/去重、分析积压和预算与数据库一致；展示能定位缺口起止和停止原因。三日连续运行是产品验收证据，受控查询只验证口径。

### 6.3 截止与重试

关键词/评论默认进程硬截止 90 秒，热榜 60 秒、请求预算 45 秒，分析 600 秒；来源预算更保守时优先。`jobs.started_at` 是首次领取，`job_attempts.started_at` 是本次尝试，采集预算起点另记；自动重试沿用剩余预算，人工重试经权限核验后在下一次领取开新采集预算周期，但不返还来源日预算。旧代码可能让迟到的人工重试立即耗尽；改动和失败测试见总设计 DEC-001-217。

## 7. 失败语义与恢复

分页截断、未确认尾段、429、访问拒绝、RSSHub 空榜及网络/解析错误分别保留状态、时间桶和原因；已有内容不因后续失败删除。重放按来源稳定 ID、operation ID 和唯一约束幂等；消费位点仅在业务事务成功后连续提交。预设关闭阻止新受理，运行中的 Job 依取消与进程硬截止回收。来源状态只在持久化真实采集证据通过后转可用。

## 8. 安全、风控与可观测

适配器接收主机白名单、校验每一跳重定向，禁止非准入目标和越预算访问；本机 RSSHub/SearXNG 以显式条目配置。日志只保留请求 ID、路由模板、状态、耗时与脱敏错误。按来源/能力/时间窗暴露到期、请求/页、停止原因、预算、入库/去重、标注异常与未确认缺口；不得把回看或 HTTP 200 推断为完整性。

## 9. 验收设计要点

先以受控测试覆盖空榜/失败桶、父链、缺口、预算与人工/自动重试；再逐一核对四个真实入口、六榜排名与命中入库、Codex 标注、来源状态和数据库事实。产品验收要求同窗连续 72 小时，HN 评论分页、旧帖新回复及重放单独核对；真实运行和产品结论写入 M1 Acceptance，不复用旧开发库计数作三日证据。

## 10. 决策与历史约束

沿用总设计 DEC-001-203/205/206/207/209/212/213/215/216/217/218；DEC-001-215 替代“热榜条目直接进入事件”，DEC-001-216 替代“报告覆盖说明代表采集覆盖”，DEC-001-213 替代“主题频率单独决定采集”。本次拆分不新建 DEC-002-xxx；以上编号仍在[总设计决策表](001-热点舆情监控平台总体设计.md#8-全局决策与去向)。历史参考/约束来源：旧 Design 008 评论与回复采集（已删除，见 Git 历史）、旧 Design 010 增量更新与历史回补（已删除，见 Git 历史）、旧 Design 037 费用与资源约束（已删除，见 Git 历史）、旧 Design 046 全局异常与响应契约（已删除，见 Git 历史）、旧 Design 047 本地网页与浏览器采集（已删除，见 Git 历史）

## 11. 风险与总设计对照

### 本轮执行合同补全（待实现）

- Plan002固定 `SourceExecutionPolicy` 版本字段；相同预设幂等、变更升连接版本。Job在受理时固定配置，执行时仍检查当前安全停用状态；日预算身份不含连接版本，换版不能重置日额度。Plan031负责调度与手动受理，Plan032负责人工重试周期，避免同一份Plan混合三种状态机。
- Plan033新增 `collection_due_windows`，唯一(owner_id,schedule_key,due_at)，到期点与 `[window_start,window_end)` 分开固定；持久化 pending/accepted/skipped/missed 与原因、非秘密 `SourceExecutionPolicy` 快照、连接版本和可空 Job 关联。Job 只在同一受理事务实际创建后关联；恢复逐到期点使用当时版本，最近到期点保留待受理，较早未受理点记 missed。既有 Job 请求数、`coverage_windows` 页数、内容观察、attempt 与预算账本各自拥有事实；原始页观察数随检查点同事务保存，不新建第二套计量账本。没有Job时指标为未知 null，已受理且确无请求/页时为真实 0。
- 采集周期编号/开始时间与attempt分开。自动重试沿用周期剩余额度；人工重试在领取时开新周期，来源日额度不返还。页面提交、内容持久化与检查点在同一事务中，重放不重复统计。
- Plan006以due_at进入[start,end)确定时效分母；未终态窗口保留未完成/超时及延迟下界，不从中位数样本静默消失。无法确定中位数时返回null与下界，不判通过。分析按内容版本/主题/规则/提示词身份计数，60分钟成熟窗外单列pending_observation，72小时结束后继续等最后一批成熟。
- Plan035—037分别闭合另外三个关键词来源；Plan038独立闭合HN分页父链与旧帖新回复；Plan040独立闭合Codex批量调度。全部共享服务、真实来源逐项证据与页面闭合后，由Plan009汇总M1 AC，运行库保留前置为051。
- HN评论把现有每主题20帖、6小时刷新、24小时自动候选期、10页/10请求、200根/根下20回复固定为预设初值；按互动分数及稳定ID排序。超过自动候选期的已入库帖可通过POST `/api/contents/{content_id}/comment-runs`显式复采，仍受6小时间隔/预算/owner与CSRF约束；041提供按钮，038真实核对跨24小时旧帖的新回复。候选年龄不构成已覆盖全部评论的声明。

来源缓存、有限页与 RSS 头部不证明完整；限流、评论父链错误、预算耗尽和标注空值均可产生未确认缺口。对应原总设计 v3.2 §4.1—4.4、§4.6、§5、§6 中采集截止、§7—8 中相关性和 §21；全局资源/Worker 规则仍以 v4.0 总设计为准。
