---
layer: Plan
scope: shared
doc_no: "042"
title: 容量与部署可重复性计划
status: in_progress
version: v1.4
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/042-容量与部署可重复性计划.md
design: docs/design/042-容量与部署可重复性设计.md
prd: docs/prd/042-容量与部署可重复性.md
source_requirement: NFR-001-016
backlog: BACKLOG.md
priority: P0
milestone: M5
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。本文状态与验收进度不变；Kafka 重试、消费位点与 Redis 失效场景需在实现前补齐设计。

# 容量与部署可重复性执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

日期：2026-09-21。状态：`in_progress`。来源：[042 PRD](../prd/042-容量与部署可重复性.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

## 1. 交付目标与当前差距

完成 NFR-001-016 的实现和证据闭环，保持 P0。当前已有 API/Worker/CLI、Web、唯一根 Compose、隔离依赖与基础 CI；S00/S01 已完成，业务容量保护及完整验收仍未交付。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把运行基线当作本项已验收。本文尚未建立 Acceptance。

建议责任角色：**后端＋前端＋运维**，实际执行人于开工时登记；从 M1 开始准备/基础接入，归属里程碑 **M5：首版质量验收与交付**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。它包含第 4 节规格、S01—S03 具体切片、S04 的原始 AC 证据，以及可执行的停止/回退说明。PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

[同编号 Design](../design/042-容量与部署可重复性设计.md) 已接受 S00/S01 的运行基线决策。它不冻结尚无业务数据的完整 B0 参数，也不授权真实来源实采；S02—S04 仍须在各自门禁满足后实施。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| 046 S03；001 S01 内部设计 | 统一契约修复与验收通过，工程目录与运行约束就绪；不等待001外部来源准入 | 已满足：046 Acceptance passed；001 Design proposed |

以上是**切片依赖**。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

开工时处理 PRD 中 `OPEN-042-001`，冻结会影响本切片的默认值、访问条件、资源和验收口径。已确定技术栈沿用 AGENTS；新增平台、付费依赖或框架等实质选择按项目规范处理，普通实现细节由执行者负责。

## 3. 预计变更文件与归属

以下均为**拟建/拟改路径**，不是现存实现。只在切片实际开始时创建必要文件；先登记新领域职责和结构检查，再加入代码。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| backend/app/main.py；backend/app/core/config.py；backend/app/db/；backend/app/worker/app.py；backend/app/cli/__main__.py；compose.yaml；backend/pyproject.toml；frontend/package.json | 复用现有应用工厂及锁文件，补齐唯一编排、契约生成与真实依赖验证 |
| backend/tests/unit/test_deployment_capacity.py | 规则与状态的针对性失败/通过验证 |
| backend/tests/integration/test_deployment_capacity.py | 适用的真实 PostgreSQL/Redis/Kafka、权限、幂等或边界验证；纯前端/人工项写明适用范围 |
| frontend/tests/e2e/deployment_capacity.spec.ts（仅存在相关界面时） | 验证受影响业务页面；不为质量项额外建业务页面 |
| backend/app/api/router.py、dependencies.py；运行时 /openapi.json（自动生成）；frontend/src/api/ | 若 HTTP 契约变化，注册路由及唯一生成链；无变化则记录不适用原因 |
| backend/database/schema.sql；根 Compose 和配置样例 | 只在确需数据/配置变化时修改；SQL 与 ORM 同批更新，禁止覆盖用户配置或删除持久卷 |
| docs/design/042-容量与部署可重复性设计.md；对应 Acceptance（真实验证后创建） | 开工前设计与交付后证据，使用同编号，不预填通过 |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-042-API-001 | 建立唯一 FastAPI 应用工厂及基本健康契约，区分进程、依赖和业务可读；前端同源 /api，客户端从运行时 OpenAPI 生成。 |
| SPEC-042-DATA-001 | 复用已锁定依赖与唯一 schema.sql，补齐隔离测试资源；B0 数据生成器可配置正文、评论、快照分布及临时空间。 |
| SPEC-042-JOB-001 | 建立唯一 Compose、Worker/CLI 入口和资源上限；只在真实创建模块时登记边界，不提前创建空目录。 |
| SPEC-042-UI-001 | 改进本质量要求影响的实际业务页面、状态或运维记录，不默认新建一个独立质量功能页面；无界面变化时以书面理由说明该 UI 分支不适用，不能跳过实际受影响界面。 |
| SPEC-042-SEC-001 | 先检查当前身份及资源权限；配置版本不能绕过撤权、删除、停用和更严格预算。秘密不可回读，正文与外部输入只作数据。关键变更留操作者、对象、时间与版本。 |
| SPEC-042-OBS-001 | 两干净环境复现，测整套4vCPU/8GiB/SSD负载及增长；候选80%预警/90%暂停非必要工作，保留读写与清理资源。 |
| SPEC-042-OPS-001 | 在隔离依赖验证后渐进启用本能力；保持历史资料可读。配置缺失或外部条件不满足时明确拒绝/延期。变更前评估数据兼容与回退，停止新增操作优先于破坏性迁移。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-042-S00-T01 | 阅读 PRD 与关联设计，关闭本期必要未决项，补同编号 Design；明确未纳入本期的分支 | 需求/设计评审记录、边界与风险 |
| TASK-042-S00-T02 | 核查先行切片，登记文件职责与主责，冻结 SPEC、样本、接口及测试命令 | 可执行切片清单、隔离资源和验证清单 |

- [x] `CHK-042-G0-001` → AC-042-001、AC-042-002、AC-042-003、AC-042-004、AC-042-005、AC-042-006；S01 范围和 Design 已接受，完整 B0 与实采仍留待后续；证据 `EV-042-001`（042 Design v1.0）。
- [x] `CHK-042-G1-001` → AC-042-001、AC-042-002、AC-042-003、AC-042-004、AC-042-005、AC-042-006；046/001 前置与隔离 Docker 环境可用，来源条件不冒充运行依赖；证据 `EV-042-002`（046 Acceptance、001 Design、Docker Engine 29.8.0 / Compose 5.5.1）。
- [x] `CHK-042-G2-001` → AC-042-001、AC-042-002、AC-042-003、AC-042-004、AC-042-005、AC-042-006；S01 文件、版本、资源、空库、健康与契约命令已冻结；证据 `EV-042-003`（042 Design 第 2—5 节）。

### S01 运行基线与容量测量工具

前置：S00 完成。本切片覆盖 NFR-042-001、NFR-042-002；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-042-S01-T01 | Red：以 AC-042-002 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-042-S01-T02 | Green：在046 S03通过后复用 API/Worker/CLI、Web及契约生成/CI检查；补根 Compose、空库 schema.sql 原子执行、真实隔离依赖，并验证同一生成链在部署环境可用。准备容量测量工具，业务数据接齐后在 S02/S04 测完整 B0，不以 B0 实测作为下游开始业务开发的前置。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-042-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-042-G3-001` → AC-042-002；任务 TASK-042-S01-T01：2026-09-21 根目录执行 `docker compose config --quiet` 退出 1，原因为不存在配置文件；证据 `EV-042-004`（冻结输入为提交 `63bfb38c`）。
- [x] `CHK-042-G4-001` → AC-042-002；任务 TASK-042-S01-T02、TASK-042-S01-T03：唯一 Compose、事务化空库、真实依赖、API/Web/Worker、资源快照和部署态 OpenAPI 链通过；证据 `EV-042-007`（本切片提交及下列运行记录）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

S01 先行输出的关闭范围：同提交工程可启动、空库结构可验证、必需依赖检查、生成客户端与隔离测试链可执行；现有文件逐项复核并补缺，不重新建一套底座。AC-042-002 的 B0 完整规模测量在业务对象接齐后的 S02/S04 复验，S01 证据不得提前关闭该 AC。

2026-09-21 运行证据：独立项目 `hotkey-s01-verify` 的 PostgreSQL、Redis、Kafka、API、Web 五服务均 healthy；数据库查询、Redis PONG、Kafka 元数据命令、API health/readiness、Web health 与同源 API 均成功；无 handler Worker 记录原因后退出 0。一次瞬时快照约为 API 88 MiB、Web 43 MiB、Kafka 368 MiB、PostgreSQL 30 MiB、Redis 10 MiB，仅证明命令和资源上限有效。后端 26 tests、Ruff/mypy、前端 10 tests、ESLint/TypeScript/Prettier/build、部署态 `openapi:check` 通过；验证专用卷已删除。

### S02 高水位与并行负载

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 NFR-042-003、NFR-042-004；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-042-S02-T01 | Red：以 AC-042-003、AC-042-004 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-042-S02-T02 | Green：业务接齐后施加受控磁盘压力和前后台共同负载，校验不丢确认数据及读取目标。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-042-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-042-G3-002` → AC-042-003、AC-042-004；任务 TASK-042-S02-T01：失败验证确实证明当前行为缺失；预期证据 `EV-042-005`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-042-G4-002` → AC-042-003、AC-042-004；任务 TASK-042-S02-T02、TASK-042-S02-T03：切片闭环与必要回归通过；预期证据 `EV-042-008`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S03 可重复部署与回滚交接

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 NFR-042-005、NFR-042-006；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-042-S03-T01 | Red：以 AC-042-005、AC-042-006 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-042-S03-T02 | Green：在两独立环境按说明部署、缺配置失败、升级回退并核对历史资料。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-042-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-042-G3-003` → AC-042-005、AC-042-006；任务 TASK-042-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-042-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-042-G4-003` → AC-042-005、AC-042-006；任务 TASK-042-S03-T02、TASK-042-S03-T03：切片闭环与必要回归通过；预期证据 `EV-042-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-042-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-042-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-042-G5-001` → AC-042-001；**总纲场景继承**：Given 存储接近限额且正在历史回填；When 继续正常浏览和调度；Then 回填有界延期、已有资料可查，资源压力和处理记录可见。预期证据 `EV-042-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-042-G5-002` → AC-042-002；**声明与实测**：Given B0 数据集和整套 4 vCPU/8 GiB 资源已配置；When 运行负载并核对占用；Then 环境、数据分布、全部服务与临时空间计入，增长假设可核查；预期证据 `EV-042-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-042-G5-003` → AC-042-003；**磁盘高水位**：Given 存储达到预警和暂停阈值且回填运行中；When 继续调度并浏览资料；Then 回填有界延期，已有资料可读，无法保存时不虚假确认，清理仍可执行；预期证据 `EV-042-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-042-G5-004` → AC-042-004；**后台共同负载**：Given 采集、分析、备份与清理产生代表性负载；When 执行规定读取窗口；Then 读取符合 030 且资源不超预算，不能隐藏失败或停后台后替代结果；预期证据 `EV-042-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-042-G5-005` → AC-042-005；**干净环境配置**：Given 两个隔离环境仅具备声明前置条件；When 按同一说明部署，并分别测试缺少必要配置；Then 配置正确时核心可读；缺失时明确失败，日志无秘密，版本可核对；预期证据 `EV-042-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-042-G5-006` → AC-042-006；**升级与交接**：Given 已备份合法样本且存在兼容性变化；When 按文档升级并执行回退/恢复核对；Then 历史资料与配置可读，步骤和限制完整，不依赖作者临时手工修复；预期证据 `EV-042-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-042-G6-001` → AC-042-001、AC-042-002、AC-042-003、AC-042-004、AC-042-005、AC-042-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-042-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-042-G6-002` → AC-042-001、AC-042-002、AC-042-003、AC-042-004、AC-042-005、AC-042-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-042-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| NFR-042-001 | 基线完整声明 | TASK-042-S01-T02 | AC-042-002 | CHK-042-G4-001 |
| NFR-042-002 | 容量测量与增长 | TASK-042-S01-T02 | AC-042-002 | CHK-042-G4-001 |
| NFR-042-003 | 高水位保护 | TASK-042-S02-T02 | AC-042-003 | CHK-042-G4-002 |
| NFR-042-004 | 并行负载隔离 | TASK-042-S02-T02 | AC-042-004 | CHK-042-G4-002 |
| NFR-042-005 | 可重复部署与配置 | TASK-042-S03-T02 | AC-042-005 | CHK-042-G4-003 |
| NFR-042-006 | 升级回滚与交接 | TASK-042-S03-T02 | AC-042-006 | CHK-042-G4-003 |

父级综合场景 AC-042-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| backend/ | `uv run ruff check .`；`uv run ruff format --check .`；`uv run mypy` | 既定静态与边界要求，依赖须先由 042 锁定安装 |
| backend/ | `uv run pytest tests/unit/test_deployment_capacity.py` | 本切片规则验证 |
| backend/ | `uv run pytest tests/integration/test_deployment_capacity.py` | 隔离的真实依赖与边界验证 |
| backend/ | `uv run pytest tests/architecture` | 新模块必须受结构/依赖门禁覆盖 |
| 根目录 | `docker compose config --quiet`；`docker compose build`；`docker compose up --detach --wait`；标准依赖 CLI、`curl` 与 `docker stats` | 唯一编排、真实依赖、健康链、空 Worker 与有界资源快照；不新增验证脚本 |
| frontend/ | `HOTKEY_OPENAPI_URL=<部署态 API> pnpm openapi:check`；`pnpm test`；`pnpm lint`；`pnpm typecheck`；`pnpm format:check`；`pnpm build` | 同一运行时契约、生成客户端和 Web 质量门禁；S01 无业务页面变化，不虚构 E2E |
| 对应业务/验证环境 | 两干净环境复现，测整套4vCPU/8GiB/SSD负载及增长；候选80%预警/90%暂停非必要工作，保留读写与清理资源。 | 真实来源、B0/Q0、故障、恢复或用户证据按本项范围执行 |

通用静态检查和基础测试入口已存在；本项专用测试、契约漂移及 E2E 入口须按实际切片补齐。本文件列出验证要求，不宣称命令已运行。若某项纯人工或无对应界面，G2 写出不适用理由和替代证据；不能删掉必要质量要求来让结果通过。涉及持久化/消息用真实 PostgreSQL/Redis/Kafka，涉及文件证据用获授权隔离 MinIO；不复用生产卷或在真实平台压测。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-21 | v1.4 | 接受同编号 Design，完成 S00/S01 的失败基线、唯一 Compose、空库事务初始化、真实依赖、资源快照及部署态契约验证；完整 AC 仍为 0/6。 |

文档复核（2026-09-21）：需求 ID、优先级及必要 AC 不变；S01 是下游底座前置，不是 042 产品验收通过。
