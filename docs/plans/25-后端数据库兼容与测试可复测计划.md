# 25 后端数据库兼容与测试可复测计划

## 目标

将后端数据库连接与模型层在生产、测试、开发的行为统一为可复现路径：

- 生产：继续使用 PostgreSQL + `sql/001_init_schema.sql`。
- 测试/本地：直接覆盖 PostgreSQL 初始化链路，验证数据库 URL 与 schema 初始化策略，不使用 SQLite fallback。

## 任务拆解

### D0-1 数据库连接策略封装
- 修改：
  - 新建 `server/app/db/connection.py`
  - 重构 `server/app/db/session.py`
  - 重构 `server/app/db/init_schema.py`
- 实现点：
  - 新增 `resolve_database_url()`；
  - session 与 init 初始化统一通过解析后的 URL。
- 测试：
  - `tests/test_db_connection.py::test_resolve_database_url_keeps_postgresql_setting`
  - `tests/test_db_connection.py::test_resolve_database_url_rejects_unsupported_scheme`

### D0-2 PostgreSQL 初始化链路
- 修改：
  - `server/app/db/init_schema.py`
- 实现点：
  - PostgreSQL：继续执行 `sql/001_init_schema.sql`。
- 测试：
  - `tests/test_db_connection.py::test_initialize_database_uses_sql_script_for_postgresql`
  - `tests/test_db_connection.py::test_initialize_database_raises_for_unsupported_scheme`

### D0-3 JSON/查询兼容收口
- 修改：
  - `server/app/models/hotspot.py`
  - `server/app/models/source.py`
  - `server/app/models/setting.py`
  - `server/app/models/ai_analysis.py`
  - `server/app/api/routes/hotspots.py`
  - `server/app/services/check_runner.py`
- 实现点：
  - JSON 字段改为 `JSON().with_variant(JSONB, "postgresql")`；
  - 替换 `.astext` 相关查询 cast 写法。
- 测试：
  - 已含数据库回归用例（`tests/test_db_connection.py` + `tests/test_mvp_services.py`）。

## 依赖关系与顺序

1. 先实现连接策略；
2. 再完成初始化策略联通；
3. 最后修复 JSON 查询/字段兼容。

## 验收

- `python3 -m pytest -q tests/test_db_connection.py` 通过；
- `python3 -m pytest -q` 全量通过；
- `git diff` 无 SQLite 回退路径，且仅保留 PostgreSQL 初始化闭环。

## 交付要求

- 若有问题复现：提供失败命令与回退建议；
- 在 PR 中补充验证说明（见 `AGENTS.md` 的 PR 模板）。

## Issue 关联

- 父 Issue：#37
- 子 Issue：#36（连接策略与 URL 解析）、#38（PostgreSQL 初始化链路联通）、#39（JSON 与查询兼容收口）
- 本次 PR 需在 PR 描述中关联上述 Issue（已在当前提交中一并关联）。
