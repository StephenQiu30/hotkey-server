---
layer: Plan
scope: shared
doc_no: "032"
title: 备份与恢复计划
status: in_progress
version: v1.12
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/032-备份与恢复计划.md
design: docs/design/032-备份与恢复设计.md
prd: docs/prd/032-备份与恢复.md
source_requirement: NFR-001-006
backlog: BACKLOG.md
priority: P0
milestone: M5
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。S01 只备份 PostgreSQL 并核对 MinIO 引用；Redis 可重建状态与 Kafka 运行消息不进入候选包。

# 备份与恢复执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

日期：2026-09-17，2026-09-23、2026-09-25 更新。状态：`in_progress`；S00/S01 已完成，S02 的 PostgreSQL + MinIO 失败保护/恢复技术门禁已通过隔离 CI（`CHK-032-G4-002`）。首次 CI 暴露 `pg_dump` 主版本不匹配，后续发现并同步了两张既有 schema 表的测试预期；修复后全套隔离验证全绿，证据见 `EV-032-019`、`EV-032-020`。本机唯一 `hotkey-server` 数据库未连接或改动；S03 G2 设计尚未接受：恢复工具目前只验证并删除临时数据库，删除请求只写 PostgreSQL，恢复入口不暂停主题；唯一 `hotkey-evidence` bucket 未启用 versioning/Object Lock/lifecycle/bucket encryption，且 036 生命周期清理拒绝版本化 bucket。维护者要求项目只保留一个固定 MinIO bucket，本期复用现有 bucket，不另建或替换；本机 PostgreSQL 与 MinIO 同属 Mac 故障域，锁定 MinIO SDK 不提供普通 `put_object` 条件写入参数，bucket 前缀不能称为 WORM 或独立灾备。进入 S03 Red 前还需明确是否有现存独立故障域的备份挂载路径、最长可恢复周期与密钥恢复责任。S03/S04、总门禁与产品 AC 仍未完成，产品 AC 为 0/6。来源：[032 PRD](../prd/032-备份与恢复.md)；设计：[032 Design](../design/032-备份与恢复设计.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

## 1. 交付目标与当前差距

完成 NFR-001-006 的实现和证据闭环，保持 P0。S01 已建立数据库候选归档、同快照清单、MinIO 引用核对和秘密隔离；S02 的真实 PostgreSQL + MinIO 失败保护/恢复技术门禁已通过隔离 CI。独立介质、上一完整有效副本晋升、删除重放、B0 RPO/RTO、S03/S04 及完整验收仍未交付。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把候选包当作本项已验收。本文未建立 Acceptance。

建议责任角色：**运维＋后端＋恢复验证**，实际执行人于开工时登记；从 M1 开始准备/基础接入，归属里程碑 **M5：首版质量验收与交付**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。S01/S02 只建立候选包与数据库恢复验证契约；完整交付仍包含 S03、S04 原始 AC 证据及可执行的停止/回退说明。PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

[同编号 Design](../design/032-备份与恢复设计.md) v1.2 已接受 S00/S01、S02 数据库子片及 MinIO 内容归档/临时回读子片：复用官方 `pg_dump`/`pg_restore`、锁定的 MinIO Python SDK `7.2.20` 和管理 CLI，不新增脚本、依赖或服务。候选目录仍不是完整已验证备份，也不选择最终生产介质。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| [042 容量与部署可重复性](042-容量与部署可重复性计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 `09e0ca69` |
| [034 凭据与应用安全](034-凭据与应用安全计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 `beb88768` |
| [036 数据访问与生命周期](036-数据访问与生命周期计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 `8d17af90` |

以上是**切片依赖**。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

`OPEN-032-001` 在 S01 保持开放：独立介质、加密材料恢复方式和最终保留天数需要用户提供自有资源与维护责任。候选命令要求显式目标目录，没有新增平台、付费依赖、框架或默认生产路径。

## 3. 预计变更文件与归属

下表登记本计划的文件职责。S01 行已按实际实现回写；后续切片仍只在开始时创建必要文件。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| backend/app/backups/schemas.py、services.py、restore.py、adapters/ | S01 候选清单与同快照编排；S02 隔离恢复及 PostgreSQL/MinIO 薄适配，包括对象流式归档、内容摘要与临时回读；不建立第二套导出格式 |
| backend/app/evidence/schemas.py、services.py | 由证据领域提供 MinIO 引用和删除状态，备份领域不直接读取证据 ORM |
| backend/app/cli/commands.py | 复用 `python -m cli` 的 `backup create-candidate`、`backup verify-restore`；未创建 `scripts/` 或 `.sh` |
| backend/tests/unit/test_backup_restore.py | 清单状态、对象三态、归档路径/摘要、备份/恢复临时凭据与坏归档规则验证 |
| backend/tests/integration/test_backup_restore.py | 复用现有 PostgreSQL/MinIO 和官方工具验证候选包、真实临时库恢复、MinIO 内容往返、损坏保护和随机测试前缀清理；本机 PostgreSQL 集成仅可使用明确隔离的 HOTKEY_TEST_DATABASE_URL |
| frontend/tests/e2e/backup_restore.spec.ts（仅存在相关界面时） | S01 无业务页面，未创建空 E2E；后续状态接入实际运维入口时再验证 |
| backend/app/api/router.py、dependencies.py；frontend/src/api/ | S01 无 HTTP/OpenAPI 变化，均未改 |
| backend/database/schema.sql；根 Compose 和配置样例 | S01 无 DDL/配置/服务变化，均未改；现有 `.env` 只在本机读取 |
| docs/design/032-备份与恢复设计.md；对应 Acceptance（真实验证后创建） | 开工前设计与交付后证据，使用同编号，不预填通过 |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-032-API-001 | 运维 CLI/受控任务负责备份和恢复，不向普通用户开放原始备份下载；状态页面只暴露授权范围内摘要。 |
| SPEC-032-DATA-001 | 备份清单包含配置、业务关系、证据对象清单、版本及一致性时间点；密钥材料与备份分开。 |
| SPEC-032-JOB-001 | 计划备份校验后才替代上一有效副本；恢复在隔离实例进行，先重放删除/撤权再允许用户访问。 |
| SPEC-032-UI-001 | 改进本质量要求影响的实际业务页面、状态或运维记录，不默认新建一个独立质量功能页面；无界面变化时以书面理由说明该 UI 分支不适用，不能跳过实际受影响界面。 |
| SPEC-032-SEC-001 | 先检查当前身份及资源权限；配置版本不能绕过撤权、删除、停用和更严格预算。秘密不可回读，正文与外部输入只作数据。关键变更留操作者、对象、时间与版本。 |
| SPEC-032-OBS-001 | B0 实际演练 RPO≤24小时、RTO≤2小时候选目标；核心读写核对完成才停止计时，恢复采集保持暂停。 |
| SPEC-032-OPS-001 | 在隔离依赖验证后渐进启用本能力；保持历史资料可读。配置缺失或外部条件不满足时明确拒绝/延期。变更前评估数据兼容与回退，停止新增操作优先于破坏性迁移。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-032-S00-T01 | 阅读 PRD 与关联设计，关闭本期必要未决项，补同编号 Design；明确未纳入本期的分支 | 需求/设计评审记录、边界与风险 |
| TASK-032-S00-T02 | 核查先行切片，登记文件职责与主责，冻结 SPEC、样本、接口及测试命令 | 可执行切片清单、隔离资源和验证清单 |

- [x] `CHK-032-G0-001` → AC-032-001、AC-032-002、AC-032-003、AC-032-004、AC-032-005、AC-032-006；`EV-032-001`：PRD v1.2、Design v1.0 已冻结 S00—S01；独立介质、真实恢复、删除重放、B0 和产品 AC 保持未完成。
- [x] `CHK-032-G1-001` → AC-032-001、AC-032-002、AC-032-003、AC-032-004、AC-032-005、AC-032-006；`EV-032-002`：042 S01 `09e0ca69`、034 S01 `beb88768`、036 S01 `8d17af90` 和 046 S03 已取得；复用既有 `.env`、PostgreSQL 18.4 与 MinIO，未启动或重启服务。
- [x] `CHK-032-G2-001` → AC-032-001、AC-032-002、AC-032-003、AC-032-004、AC-032-005、AC-032-006；`EV-032-003`（2026-09-25 扩展）：变更限定为 `backups` 领域、证据引用 DTO/查询、MinIO 归档/回读适配、既有 CLI 和专用测试；DDL、HTTP/UI、依赖、Compose、脚本均 N/A。Design v1.2 保留旧 `inventory_only` 清单读取、固定对象版本或 If-Match，并限制临时对象前缀和逐对象清理。

### S01 备份清单与受控存取

前置：S00 完成。本切片覆盖 NFR-032-001、NFR-032-002；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-032-S01-T01 | Red：以 AC-032-002、AC-032-003 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-032-S01-T02 | Green：建立最小备份路径、清单/校验及凭据独立恢复流程；上线写数据前可执行。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-032-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-032-G3-001` → AC-032-002、AC-032-003；`EV-032-004`：专用单元测试首次收集因缺少 `backups` 模块失败（1 collection error），准确证明候选备份能力缺失。
- [x] `CHK-032-G4-001` → AC-032-002、AC-032-003；`EV-032-007`：4 个专用单元测试、1 个真实集成测试、114 个后端全量测试、12 个架构测试及 Ruff/format/mypy 全绿；真实 CLI 生成同快照 custom archive，19 表计数、MinIO 引用、SHA-256、`0700/0600` 权限、秘密隔离与 `pg_restore --list` 通过。输出明确 `candidate`、证据 `inventory_only`、`restore_verified=false`，不证明 S02/S03、B0 或产品 AC。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S02 失败保护与真实恢复

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 NFR-032-003、NFR-032-004；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-032-S02-T01 | Red：以 AC-032-003、AC-032-004 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-032-S02-T02 | Green：验证损坏备份不覆盖旧副本，在隔离环境恢复数据库；归档快照所指向的 MinIO 对象内容，并在随机临时对象名前缀回读核验大小/SHA-256 后清理。 | 该范围从输入到可查看结果可运行；缺失/删除对象保持显式缺口，失败/权限分支可见；不把同 bucket 往返冒充独立介质或完整 B0 恢复 |
| TASK-032-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-032-G3-002` → AC-032-003、AC-032-004；`EV-032-005`：先加入损坏候选拒绝与隔离真实恢复测试，专用测试收集因 `backups.restore` 缺失而失败（1 个 `ModuleNotFoundError`）；QA 库旧结构造成的后续 `coverage_windows` 缺表单独记录为环境问题，不计入 Red。
- [x] `CHK-032-G4-002` → AC-032-003、AC-032-004：隔离 CI 真实验证 PostgreSQL + MinIO 备份/恢复技术门禁通过（`EV-032-020`）；仅关闭 S02 技术门禁，不代表 B0、独立介质、S03/S04 或产品验收完成。
  - `EV-032-008`（既有数据库子片）：复用隔离 `hotkey_qa_032_20260923` 运行真实 `pg_dump`/`pg_restore`，损坏候选拒绝、失败新候选保留旧候选、同库目标拒绝、31 表计数与事务内读写探针通过，临时恢复库回收；CLI 输出 `tables: 31; duration seconds: 0.841; complete backup verified: false`。该记录不单独证明 S02 完成。
  - `EV-032-018`（2026-09-25 MinIO 内容子片）：Red 先以 `MinioEvidenceRestoreVerifier` 缺失失败；Green 单测 9 passed、MinIO 隔离随机前缀归档/回读/清理集成 1 passed，合并定向测试 10 passed/4 skipped。历史后端全量 387 passed/195 skipped，Ruff、format、mypy 通过。
  - `EV-032-019`（2026-09-25 首轮隔离 CI）：run `36087709530` 暴露 PostgreSQL 17 服务与 runner 默认 `pg_dump` 16 不兼容；安装并断言 PostgreSQL 17 客户端后，run `36091137377` 的候选归档成功，573 passed/8 skipped，只有表清单预期漏列当前 schema 的 `followed_accounts` 与 `followed_account_aliases`。上述两项已修正。两轮 CI 均只使用 Actions 隔离 PostgreSQL/MinIO；本机唯一 `hotkey-server` 数据库未连接、修改或另建。
  - `EV-032-020`（2026-09-25 全绿隔离验证，提交 `4c26632a`）：backend run `36091748228` 的 PostgreSQL 17 安装/版本断言、临时 schema 初始化、MinIO 启停、Ruff、format、mypy 和 pytest 全通过；pytest 574 passed/8 skipped，`tests/integration/test_backup_restore.py` 5 项全部通过。runtime `36091748237` 与 contract `36091748194` 亦成功。仅证明本 S02 技术范围，不替代生产故障域、B0 RPO/RTO 或用户 AC。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S03 删除重放与业务核对

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 NFR-032-005、NFR-032-006；涉及的完整规则以对应 PRD 为准。

2026-09-25 G2 设计准备审查（尚未关闭）：`verify-restore` 是临时数据库技术验证，不产生可上线恢复环境；删除事实目前只写入 PostgreSQL，无法独立证明快照后删除日志覆盖；恢复入口未强制 `monitor_topics` 暂停或 Worker 屏障。维护者要求项目仅使用唯一 `hotkey-evidence` bucket，不新增/替换 bucket；它当前未版本化、无 Object Lock/lifecycle/SSE，036 清理器拒绝版本化 bucket。PostgreSQL 与 MinIO 均为本机进程，FileVault 开启但属同一主机故障域；锁定的 MinIO SDK `put_object` 不提供通用条件写入参数，故同 bucket 前缀不能声称 WORM 或独立灾备。进入 S03 Red 前需明确快照外删除日志、覆盖完整性/失败关闭语义、暂停调度顺序，以及是否提供现有独立故障域备份路径、最长可恢复周期与密钥恢复责任。未收齐设计输入前，不登记 G2/G3/G4 通过，不创建 Acceptance。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-032-S03-T01 | Red：以 AC-032-005、AC-032-002、AC-032-004、AC-032-006 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-032-S03-T02 | Green：接入所有对象后的完整 B0 恢复，检查权限、证据和暂停调度，列明损失窗口。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-032-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-032-G3-003` → AC-032-005、AC-032-002、AC-032-004、AC-032-006；任务 TASK-032-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-032-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-032-G4-003` → AC-032-005、AC-032-002、AC-032-004、AC-032-006；任务 TASK-032-S03-T02、TASK-032-S03-T03：切片闭环与必要回归通过；预期证据 `EV-032-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-032-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-032-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-032-G5-001` → AC-032-001；**总纲场景继承**：Given 隔离环境丢失业务存储；When 使用已保存备份恢复；Then 在目标范围内恢复；已到期删除数据不会因恢复长期重新出现。预期证据 `EV-032-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-032-G5-002` → AC-032-002；**备份范围完整**：Given 数据含主题、事件、评论关系与外部证据附件；When 生成并核查备份清单；Then 一致性时间点、各类对象及缺失附件均可核对；预期证据 `EV-032-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-032-G5-003` → AC-032-003；**坏备份与权限**：Given 新备份损坏且普通读者尝试下载；When 执行校验与访问；Then 拒绝未授权下载，坏备份不覆盖上一有效备份，风险提示可见；预期证据 `EV-032-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-032-G5-004` → AC-032-004；**完整恢复计时**：Given B0 数据备份和隔离的新环境已指定；When 从恢复处置开始直到核心读取、受控本地写入及核对完成；Then 包含准备和校验的 RTO 与真实 RPO 有记录，达到目标才判通过；外部采集仍按恢复启用规则暂停；预期证据 `EV-032-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-032-G5-005` → AC-032-005；**删除重放**：Given 备份含后来已删除的对象或已撤销分享；When 恢复并准备开放读取；Then 先应用删除和撤权；缺少必要记录时阻止开放相关资料；预期证据 `EV-032-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-032-G5-006` → AC-032-006；**恢复后调度**：Given 恢复数据包含原来的运行中监控；When 启动恢复环境；Then 用户可核对资料，采集保持暂停直到连接、权限和预算确认有效；预期证据 `EV-032-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-032-G6-001` → AC-032-001、AC-032-002、AC-032-003、AC-032-004、AC-032-005、AC-032-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-032-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-032-G6-002` → AC-032-001、AC-032-002、AC-032-003、AC-032-004、AC-032-005、AC-032-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-032-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| NFR-032-001 | 完整范围与一致性 | TASK-032-S01-T02 | AC-032-002 | CHK-032-G4-001 |
| NFR-032-002 | 安全存取 | TASK-032-S01-T02 | AC-032-003 | CHK-032-G4-001 |
| NFR-032-003 | 计划与失败 | TASK-032-S02-T02 | AC-032-003 | CHK-032-G4-002 |
| NFR-032-004 | 真实恢复与计时 | TASK-032-S02-T02 | AC-032-004 | CHK-032-G4-002 |
| NFR-032-005 | 删除与权限重放 | TASK-032-S03-T02 | AC-032-005 | CHK-032-G4-003 |
| NFR-032-006 | 恢复结果核对 | TASK-032-S03-T02 | AC-032-002、AC-032-004、AC-032-006 | CHK-032-G4-003 |

父级综合场景 AC-032-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| backend/ | `uv run ruff check .`；`uv run ruff format --check .`；`uv run mypy` | 既定静态与边界要求，依赖须先由 042 锁定安装 |
| backend/ | `uv run --no-sync pytest tests/unit/test_backup_restore.py -q` | 本切片规则验证 |
| backend/ | `uv run --no-sync --env-file .env pytest tests/integration/test_backup_restore.py -q` | MinIO 使用随机隔离前缀；PostgreSQL 用例仅在显式配置指向隔离库的 HOTKEY_TEST_DATABASE_URL 时运行，禁止指向 `hotkey-server` |
| backend/ | `uv run pytest tests/architecture` | 新模块必须受结构/依赖门禁覆盖 |
| backend/ | `PYTHONPATH=app uv run --env-file .env python -m cli backup create-candidate --destination <existing-directory>` | 维护者显式创建候选包；不配置自动路径或调度 |
| backend/ | `PYTHONPATH=app uv run --env-file .env python -m cli backup verify-restore --candidate <candidate-directory> --isolation-url-env HOTKEY_TEST_DATABASE_URL` | S02 在现有 PostgreSQL 服务内生成、验证并回收唯一临时库；URL 值只从环境读取 |
| backend/ | `uv run --no-sync pytest -q` | 后端全量回归；有安全隔离 URL 时另运行真实 PostgreSQL 集成，不启动第二套环境 |
| frontend/ | N/A | S01/S02 没有 HTTP 或 UI 变化，不创建空页面、客户端或 E2E |
| 对应业务/验证环境 | B0 实际演练 RPO≤24小时、RTO≤2小时候选目标；核心读写核对完成才停止计时，恢复采集保持暂停。 | 真实来源、B0/Q0、故障、恢复或用户证据按本项范围执行 |

S01/S02 已运行的命令与结果分别记录在 `EV-032-007`、`EV-032-008`、`EV-032-018`、`EV-032-019`、`EV-032-020`；未勾选切片仍只是验证要求，不宣称已执行。后续专用测试、契约漂移及 E2E 入口须按实际切片补齐。若某项纯人工或无对应界面，G2 写出不适用理由和替代证据；不能删掉必要质量要求来让结果通过。涉及持久化/消息用真实 PostgreSQL/Redis/Kafka，涉及文件证据用获授权隔离 MinIO；不复用生产卷或在真实平台压测。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-21 | v1.4 | 完成 S00/S01：接受 Design v1.0，建立同快照候选归档、证据引用清单和秘密隔离；专用 5 tests、后端 114 tests 及静态门禁通过，产品 AC 保持 0/6。 |
| 2026-09-23 | v1.5 | S02 数据库子片的失败保护与真实隔离恢复通过：独立 QA 库、损坏拒绝、逐表核对、读写探针、临时库清理和 CLI 计时；总门禁因无完整有效副本/独立介质/MinIO 内容保持未关闭。 |
| 2026-09-25 | v1.6 | 扩展 S02 MinIO 内容子片并冻结复用 SDK 7.2.20 的版本/ETag 条件读取、流式归档、摘要核验及随机临时前缀回读；开始 Red/Green。唯一 `hotkey-server` 业务库不用于测试，MinIO 测试只用随机对象并清理本次版本；独立介质、B0 与产品 AC 不变。 |
| 2026-09-25 | v1.7 | MinIO 内容子片 Red/Green 完成；9 项单测和现有 MinIO 随机前缀回读集成通过，修复响应清理与 CLI 结果措辞。因没有专用隔离 PostgreSQL URL，完整数据库+MinIO 集成未运行，故 G4/总门禁仍开放；后端全量 387 passed/195 skipped，Ruff/format/mypy 通过。 |
| 2026-09-25 | v1.8 | 隔离 CI 确认 PostgreSQL 17 客户端修复并实际执行全量后端测试；归档成功，573 passed/8 skipped，只有清单测试漏列两张现有 schema 表。同步测试预期和证据 EV-032-019，等待修正后的 CI 全绿；G4 与产品 AC 保持开放。 |
| 2026-09-25 | v1.9 | PostgreSQL 17 + MinIO 隔离 CI 全绿：backend 574 passed/8 skipped，备份/恢复集成 5 passed；runtime/contract 通过。关闭 S02 技术门禁 CHK-032-G4-002；S03/S04、独立介质、B0 与产品 AC 仍未完成。 |
| 2026-09-25 | v1.10 | 依据当前源码核对补充 S03 G2 准入事实：临时恢复验证不等于生产恢复；删除事实无快照外日志，恢复命令不暂停监控；独立保存目标、故障域、保留期及密钥恢复责任待确认。未运行 S03 Red/Green，S03/S04 与产品 AC 仍未完成。 |
| 2026-09-25 | v1.11 | 记录维护者要求 HotKey 使用唯一固定 MinIO bucket；保留现有 unversioned bucket+前缀与迁移为启用锁定的唯一 bucket 两种方案待选择，后一方案须先设计 036 兼容及数据切换/回退。S03 G2/S04/产品 AC 未通过。 |
| 2026-09-25 | v1.12 | 核实 PostgreSQL/MinIO 同机故障域、FileVault 状态与 MinIO SDK `put_object` 条件写入能力；固定唯一 bucket 不等于独立灾备/WORM。完整恢复仍需独立目标路径、最长保留周期和密钥恢复责任，未关闭 S03 门禁。 |

文档复核（2026-09-21）：同步现有工程基线、实际检查入口和 App Router 路径；需求 ID、优先级及必要 AC 不变，未补记产品通过证据。
