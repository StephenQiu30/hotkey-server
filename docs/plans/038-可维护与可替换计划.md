---
layer: Plan
scope: shared
doc_no: "038"
title: 可维护与可替换计划
status: in_progress
version: v1.12
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/038-可维护与可替换计划.md
design: docs/design/038-可维护与可替换设计.md
prd: docs/prd/038-可维护与可替换.md
source_requirement: NFR-001-012
backlog: BACKLOG.md
priority: P0
milestone: M5
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。本文状态与验收进度不变；Kafka 重试、消费位点与 Redis 失效场景需在实现前补齐设计。

# 可维护与可替换执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

日期：2026-09-17。状态：`in_progress`；2026-09-21 完成 S00/S01，2026-09-22 接入 047 S01 的网页适配器局部先行输出；2026-09-25 关闭 S02 G3/G4 技术门禁，Firecrawl API 日志脱敏补丁已部署；上游代理与合成 DNS 的一致性仍待验证，产品 AC 仍为 0/6。来源：[038 PRD](../prd/038-可维护与可替换.md)；设计：[038 Design](../design/038-可维护与可替换设计.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

## 1. 交付目标与当前差距

完成 NFR-001-012 的实现和证据闭环，保持 P0。当前已有 API/Worker/CLI 与 Web 工程底座、依赖锁和基础 CI；S01 已建立纯来源契约，047 S01 又增加固定版本 Firecrawl 的受控网页映射。2026-09-25 Firecrawl 与 HotKey 适配器的 `example.com` 显式探针均返回正文；固定公开响应样本回归、HotKey 适配器单测、Firecrawl Playwright 测试/build 及 loopback 拒绝通过。Firecrawl 官方 `v2.11.162` tag 与 AGPL-3.0-or-later 许可证已核对，S02 G3/G4 技术门禁已关闭；后续 API 日志修补与健康检查不改变该门禁，也不代表上游代理/DNS 边界已验收。S03 回退/替换演练、S04 完整验收与维护责任仍未闭环。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把工程存在当作本项已验收。本文未建立 Acceptance。

建议责任角色：**来源接入＋后端**，实际执行人于开工时登记；从 M0 开始准备/基础接入，归属里程碑 **M5：首版质量验收与交付**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。它包含第 4 节规格、S01—S03 具体切片、S04 的原始 AC 证据，以及可执行的停止/回退说明。PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

同编号 [Design](../design/038-可维护与可替换设计.md) v1.1 已接受 S00—S01，并接受 047 S01 的独立网页契约与默认关闭适配器基础。Firecrawl 版本/许可、固定样本、受限探针与修复后运行态证据已按 S02 归档并通过 G3/G4；该证据只覆盖受控公开网页，不代表任何平台获准实采。最新 API 日志补丁及健康检查是运行维护证据；上游代理对合成 DNS 目标的限制仍需单独验证，不改变已归档的 S02 结论。S03 回退/替换演练和 S04 完整验收仍未完成。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| [042 容量与部署可重复性](042-容量与部署可重复性计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已由 `09e0ca69` 提供依赖锁、本地拓扑和验证入口 |

以上是**切片依赖**。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

`OPEN-038-001` 对 S01 的统一 DTO、分页和停止契约部分关闭；首批组件、固定版本/许可、脱敏样本文件、合法真实探针和维护责任人保留给 S02。已确定技术栈沿用 AGENTS；不新增付费依赖或框架。

## 3. 预计变更文件与归属

以下均为**拟建/拟改路径**，不是现存实现。只在切片实际开始时创建必要文件；先登记新领域职责和结构检查，再加入代码。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| backend/app/sources/__init__.py、contracts.py | S01 纯能力枚举、受判别请求、统一作品/评论、页状态与适配器 Protocol；不放 ORM 或业务状态 |
| backend/app/evidence/schemas.py、services.py | 改为复用 `sources.contracts.SourceCapability`，不保留平行枚举 |
| backend/tests/unit/test_adapter_compatibility.py | S01 请求、缺失值/父链、分页/停止和 Protocol 针对性验证 |
| backend/tests/architecture/test_structure.py | S01 强制 `sources` 不依赖 HTTP、ORM、Worker 或其他业务领域 |
| backend/app/sources/adapters/；backend/tests/integration/test_adapter_compatibility.py | S02 及真实适配器切片按职责创建；S01 不建空目录或伪集成测试 |
| frontend/tests/e2e/adapter_compatibility.spec.ts（仅存在相关界面时） | 验证受影响业务页面；不为质量项额外建业务页面 |
| backend/app/api/router.py、dependencies.py；运行时 /openapi.json（自动生成）；frontend/src/api/ | 若 HTTP 契约变化，注册路由及唯一生成链；无变化则记录不适用原因 |
| backend/database/schema.sql；根 Compose 和配置样例 | 只在确需数据/配置变化时修改；SQL 与 ORM 同批更新，禁止覆盖用户配置或删除持久卷 |
| docs/design/038-可维护与可替换设计.md；对应 Acceptance（真实验证后创建） | 开工前设计与交付后证据，使用同编号，不预填通过 |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-038-API-001 | 来源契约为内部端口，外部能力目录通过 004；不开放上传任意代码或运行任意适配器的用户接口。 |
| SPEC-038-DATA-001 | 固定组件版本、许可证及能力清单；纯 sources 契约/适配器不依赖 ORM，业务持久化归所属领域。 |
| SPEC-038-JOB-001 | 统一输入、稳定ID、分页、停止原因和错误语义；受控样本验证与真实探针分别执行，替换继承预算/删除约束。 |
| SPEC-038-UI-001 | 改进本质量要求影响的实际业务页面、状态或运维记录，不默认新建一个独立质量功能页面；无界面变化时以书面理由说明该 UI 分支不适用，不能跳过实际受影响界面。 |
| SPEC-038-SEC-001 | 先检查当前身份及资源权限；配置版本不能绕过撤权、删除、停用和更严格预算。秘密不可回读，正文与外部输入只作数据。关键变更留操作者、对象、时间与版本。 |
| SPEC-038-OBS-001 | 依赖锁定、兼容性回归与回退演练，记录实际维护工时；停用适配器仍能读既有资料。 |
| SPEC-038-OPS-001 | 在隔离依赖验证后渐进启用本能力；保持历史资料可读。配置缺失或外部条件不满足时明确拒绝/延期。变更前评估数据兼容与回退，停止新增操作优先于破坏性迁移。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-038-S00-T01 | 阅读 PRD 与关联设计，关闭本期必要未决项，补同编号 Design；明确未纳入本期的分支 | 需求/设计评审记录、边界与风险 |
| TASK-038-S00-T02 | 核查先行切片，登记文件职责与主责，冻结 SPEC、样本、接口及测试命令 | 可执行切片清单、隔离资源和验证清单 |

- [x] `CHK-038-G0-001` → AC-038-001、AC-038-002、AC-038-003、AC-038-004、AC-038-005、AC-038-006；`EV-038-001`：PRD v1.3 与 Design v1.0 接受 S00—S01，冻结纯来源契约和证据边界；版本/许可、样本、真实探针及回退保留给 S02—S03。
- [x] `CHK-038-G1-001` → AC-038-001、AC-038-002、AC-038-003、AC-038-004、AC-038-005、AC-038-006；`EV-038-002`：046 S03 已通过，042 S01 由 `09e0ca69` 提供依赖锁与验证入口；复用本机现有 `.env` 与服务，不新建环境。当前无授权真实来源、固定样本或适配器，S01 只做纯契约。
- [x] `CHK-038-G2-001` → AC-038-001、AC-038-002、AC-038-003、AC-038-004、AC-038-005、AC-038-006；`EV-038-003`：变更限定为 `sources/contracts.py`、`evidence` 能力枚举引用、单元/架构测试及 038 文档；无 DDL、HTTP/OpenAPI、Worker、UI、外部 SDK 或新依赖，不建空 adapter 目录。

### S01 统一语义与能力契约

前置：S00 完成。本切片覆盖 NFR-038-001、NFR-038-002；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-038-S01-T01 | Red：以 AC-038-002 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-038-S01-T02 | Green：冻结纯来源 DTO、分页/停止状态和基础测试，供 X 专项与国内来源共同实现。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-038-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-038-G3-001` → AC-038-002；`EV-038-004`：冻结四类能力请求、显式缺失值/父链、页状态和结构化端口后执行 `uv run pytest tests/unit/test_adapter_compatibility.py -q`，收集期因 `sources` 包不存在失败（exit 2），证明来源契约缺失，不是环境未启动。
- [x] `CHK-038-G4-001` → AC-038-002；`EV-038-007`：Design v1.0 下完成 Pydantic 严格冻结 DTO、四类受判别请求、不透明分页/水位、作品/评论统一语义、五类页状态、稳定停止原因及非运行时 `Protocol`；拒绝缺席点赞数/父链、负数、无时区时间、跨来源或错误对象类型。新增 5 tests，相关 23 tests、后端全量 92 tests、Ruff format/check 与 mypy（44 source files）全通过。全量回归复用现有 `.env` 和已启动 PostgreSQL/Redis/Kafka/MinIO；S01 无专项持久化或外部调用，无 HTTP/OpenAPI/UI/Worker 变更，专项集成测试、客户端生成与浏览器验证 N/A。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S02 版本许可与双重验证

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 NFR-038-003、NFR-038-004；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-038-S02-T01 | Red：以 AC-038-003、AC-038-004 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-038-S02-T02 | Green：记录固定版本及许可范围，建立样本回归和低频获准探针两套独立结论。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-038-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-038-G3-002` → AC-038-003、AC-038-004；`EV-038-005`：2026-09-24 修复前以冻结公开输入 `https://example.com` 运行现有 Firecrawl 网页链，实际返回 HTTP 500 `SCRAPE_RETRY_LIMIT`/`document_antibot` 且无正文，证明当前受控网页无法满足文档提取结果；047 Plan 同日记录适配器失败优先分类用例先复现旧 `upstream_error`、再验证结构化反爬拒绝被稳定分类。环境为既有本机服务；该失败是来源/运行态证据，不把服务未启动当作业务失败，也不扩展为平台失败结论。
- [x] `CHK-038-G4-002` → AC-038-003、AC-038-004；`EV-038-008`：Firecrawl 固定版本 `v2.11.162`、官方 [LICENSE](https://github.com/firecrawl/firecrawl/blob/v2.11.162/LICENSE) 为 AGPL-3.0-or-later；2026-09-25 在既有本机服务以同一 `example.com` 受限探针取得 HTTP 200/180 字符，适配器报告 `firecrawl/2.11.162`，目标请求数未知且未创建 Job/写数据库。固定响应样本回归覆盖标题、规范化 URL、正文与提取器版本；适配器文件 21 passed，后端 unit+architecture 356 passed（2 个第三方弃用警告），Firecrawl Playwright 3 tests 与 TypeScript build 通过，loopback 拒绝复验通过。证据限于固定版本、公开样本与本机技术链，不证明长期可用或任何平台能力；不含数据库集成门禁。

2026-09-25 Firecrawl 自托管 API 日志凭据脱敏（补充运行态安全证据，不改变 S02 G3/G4 判定）：独立 Firecrawl `main` 提交 `9262e24`；回归测试先复现结构化 API key/access token/client secret/password 与文本 Bearer/token/api_key 明文，再验证脱敏，logger 3 tests passed。API Dockerfile build 中 TypeScript build 与该测试通过；仅原位重建 API 并以 `--no-deps --force-recreate` 替换既有容器，readiness HTTP 200，带 URL/query 与请求头 canary 的 `example.com` 受限抓取成功（180 字符），API/Playwright 均未出现 canary 且保留运行事件。未更改 Firecrawl 数据库、代理/SSRF 规则或依赖容器；本次不增加/关闭 038 门禁或产品 AC。

2026-09-25 Firecrawl 安全补充修复：独立 Firecrawl `main` 提交 `a25f95b` 为 `Authorization: Basic` 文本赋值补充脱敏回归，并更正上游 HTTP 代理解析边界说明；logger 3 tests、代理选项 3 tests、Playwright TypeScript 检查及 API Dockerfile 的 TypeScript build/logger 测试通过。API 原位 `--no-deps` 重建后 liveness/readiness 均为 HTTP 200；Playwright 与依赖服务未重启。本机 API 单独 `tsc --noEmit` 受未安装的 `@mendable/firecrawl-rs` 阻断，Docker build 已通过。安全差异复核无已确认报告项，仍有上游代理目标校验与本地 DNS 范围配置待核对；本次未发外部抓取，不改变 S02 G3/G4、S03/S04 或产品 AC。

047 S01 局部先行输出：已固定 `firecrawl/2.11.162` 文档映射、精确域名/默认端口、固定请求体、缓存关闭、响应/正文上限及稳定失败原因，并以 MockTransport 受控测试验证；Red 为缺少适配器模块的收集失败。相关 22 tests、后端全量 284 tests、Ruff format/check、mypy、临时空库 DDL、运行时 OpenAPI/生成客户端及前端全量门禁通过。后续服务、失败输入、成功固定样本及回归分别在本 S02 证据中记录；此处保留该阶段历史状态。

2026-09-24 S02 前置核验（非 G3/G4 通过证据）：Firecrawl 官方 `v2.11.162` tag 指向 `7666c1f9ae8720a6bba271e0f60b6a217f8a5210`，该 tag 的根 `LICENSE` 为 AGPL-3.0-or-later（[release](https://github.com/firecrawl/firecrawl/releases/tag/v2.11.162)、[license](https://github.com/firecrawl/firecrawl/blob/v2.11.162/LICENSE)）。本地 Firecrawl 仓库 HEAD `6d9fb16514eb3e9e47c5a0b0915b8774dbfc6e51` 的父提交与上述 tag 一致。最初运行的 API 镜像 `sha256:5a2daaa22e9085a781a2f6d4c82ee49b0ef5a7748f992b7879e604f9a1e3d34a` 创建时间早于该补丁提交，无法证明其对应关系；随后确认 Firecrawl 根 `.env` 权限为 `0600`，当前 Compose 解析出的 API/Playwright 环境变量分别与运行容器 46/46、8/8 完全匹配。基于干净的 `main`（`6d9fb16`）成功重建 API 与 Playwright 镜像，并仅用 `--no-deps --force-recreate` 原位替换这两个容器，未启动或重建依赖；新镜像摘要分别为 `sha256:a65554b4f692db3aa93b1443b5b6c4301c43ba3aad5a6f8d557116a26e91644d` 与 `sha256:9819d871c32e82bdeaf7c7fa3b03ca9822e274185d75981bcd7105448042140e`。重建后 HotKey 的一次受限无持久化探针仍返回 `upstream_error`，目标请求数未知；重建前 `/v2/scrape` 状态为 HTTP 500，`GET /health` 返回 404。PostgreSQL、Redis、RabbitMQ 的容器内 TCP 检查可达，不等于 Firecrawl 服务就绪。Compose 对未配置的可选集成变量仍有警告。固定输出样本尚未建立，G3/G4、EV-038-005/008 与产品 AC 仍未关闭。

2026-09-24 S02 运行态/出口诊断（只读，非 G3/G4 通过证据）：现有 Firecrawl readiness 与 Playwright `/health` 均为 HTTP 200；Playwright 显示 `active_pages=0/max_pages=1`。对受控 `example.com` 的 Firecrawl `/v2/scrape` 仍返回 HTTP 500 `SCRAPE_RETRY_LIMIT`/`document_antibot`，无正文；内部 Browser `/scrape` 返回 HTTP 200 但页面状态为 403，安全分类为 `private_block`。现有出口代理 CONNECT 探针为 200；浏览器 DNS 使用合成地址，DoH 返回的 4 个地址经当前 `ipaddr.js` 检查均为公网地址。只读源代码审查发现出口路径与既有代理策略的一致性仍需安全复核；具体实现路径与复现细节暂不放入公开项目文档，且尚不能确认其与当前 403/采集失败的因果关系。不得关闭 SSRF/目标白名单或直接放宽出口；任何修复均须兼容现有目标策略并经批准。未改代码、数据库或服务，未重启容器；S02 样本、G3/G4、EV-038-005/008 与产品 AC 仍未关闭。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S03 回退兼容与替换演练

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 NFR-038-005、NFR-038-006；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-038-S03-T01 | Red：以 AC-038-005、AC-038-006 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-038-S03-T02 | Green：模拟字段变化和少量新版本写入，证明回退可读且不重复导入或复活已删数据。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-038-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-038-G3-003` → AC-038-005、AC-038-006；任务 TASK-038-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-038-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-038-G4-003` → AC-038-005、AC-038-006；任务 TASK-038-S03-T02、TASK-038-S03-T03：切片闭环与必要回归通过；预期证据 `EV-038-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-038-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-038-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-038-G5-001` → AC-038-001；**总纲场景继承**：Given 一个来源版本升级后字段变化；When 执行兼容性验证并回滚；Then 错误被发现且旧版本可恢复，来源变化不悄悄修改产品指标口径。预期证据 `EV-038-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-038-G5-002` → AC-038-002；**字段语义变化**：Given 新版本把缺失点赞数返回为空且父评论字段改名；When 执行统一契约回归；Then 不能把空转零或丢失父子关系，阻止错误版本启用；预期证据 `EV-038-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-038-G5-003` → AC-038-003；**许可与固定版本**：Given 候选组件只有部分目录声明许可证；When 登记选型与准备部署；Then 记录实际版本及许可范围，未确认部分不能按已授权复用处理；预期证据 `EV-038-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-038-G5-004` → AC-038-004；**样本与真实访问**：Given 固定样本通过但当前真实来源拒绝访问；When 汇总验证状态；Then 规则回归通过与来源受限分别报告，不标整体接入通过；预期证据 `EV-038-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-038-G5-005` → AC-038-005；**升级回退**：Given 新版本已写入少量合法资料后出现兼容问题；When 暂停并回退；Then 旧版本或既定恢复路径可读取资料，无需删除全部历史；预期证据 `EV-038-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-038-G5-006` → AC-038-006；**替换不重复**：Given 旧适配器已有去重与删除记录；When 接入兼容替代组件继续采集；Then 沿用权限、预算和对象标识，不复活删除资料或重复全量导入，维护耗时可记录；预期证据 `EV-038-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-038-G6-001` → AC-038-001、AC-038-002、AC-038-003、AC-038-004、AC-038-005、AC-038-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-038-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-038-G6-002` → AC-038-001、AC-038-002、AC-038-003、AC-038-004、AC-038-005、AC-038-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-038-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| NFR-038-001 | 业务语义稳定 | TASK-038-S01-T02 | AC-038-002 | CHK-038-G4-001 |
| NFR-038-002 | 能力契约验证 | TASK-038-S01-T02 | AC-038-002 | CHK-038-G4-001 |
| NFR-038-003 | 版本与许可可追踪 | TASK-038-S02-T02 | AC-038-003 | CHK-038-G4-002 |
| NFR-038-004 | 回归与真实探针 | TASK-038-S02-T02 | AC-038-004 | CHK-038-G4-002 |
| NFR-038-005 | 升级回退与数据兼容 | TASK-038-S03-T02 | AC-038-005 | CHK-038-G4-003 |
| NFR-038-006 | 维护与替换决策 | TASK-038-S03-T02 | AC-038-006 | CHK-038-G4-003 |

父级综合场景 AC-038-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| backend/ | `uv run ruff check .`；`uv run ruff format --check .`；`uv run mypy` | 既定静态与边界要求，依赖须先由 042 锁定安装 |
| backend/ | `uv run pytest tests/unit/test_adapter_compatibility.py` | 本切片规则验证 |
| backend/ | `uv run pytest tests/integration/test_adapter_compatibility.py` | S02 或首个真实适配器出现时创建；S01 纯契约无外部依赖，N/A |
| backend/ | `uv run pytest tests/architecture` | 新模块必须受结构/依赖门禁覆盖 |
| 根目录 | `pnpm openapi:check` | 仅当 HTTP 变化时检查运行契约与生成客户端；S01 无 HTTP 变化，N/A |
| frontend/ | `pnpm test`、`pnpm lint`、`pnpm typecheck`、`pnpm format:check`、`pnpm build` 与适用 E2E | 仅在 UI 范围执行；S01 无 UI 变化，N/A |
| 对应业务/验证环境 | 依赖锁定、兼容性回归与回退演练，记录实际维护工时；停用适配器仍能读既有资料。 | 真实来源、B0/Q0、故障、恢复或用户证据按本项范围执行 |

通用静态检查和基础测试入口已存在；本项专用测试、契约漂移及 E2E 入口须按实际切片补齐。本文件列出验证要求，不宣称命令已运行。若某项纯人工或无对应界面，G2 写出不适用理由和替代证据；不能删掉必要质量要求来让结果通过。涉及持久化/消息用真实 PostgreSQL/Redis/Kafka，涉及文件证据用获授权隔离 MinIO；不复用生产卷或在真实平台压测。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-21 | v1.4 | 完成 S00：接受 Design v1.0，冻结 S01 纯来源 DTO、分页/停止状态、结构化端口、目录与验证边界。 |
| 2026-09-21 | v1.5 | 完成 S01：实现四类请求、统一作品/评论、分页/水位、页状态/停止原因与结构化适配器端口；产品 AC 仍为 0/6。 |
| 2026-09-22 | v1.6 | 接入 047 S01 网页契约与固定 Firecrawl 适配器局部输出；真实服务/样本双重验证、维护与回退仍未完成，S02 门禁及产品 0/6 AC 不变。 |
| 2026-09-24 | v1.7 | 回写 Firecrawl 与 Browser 受控运行态复验及合成 DNS/代理出口风险；保持 SSRF 与目标白名单，安全出口修复待设计/授权，S02 门禁与产品 AC 不变。 |
| 2026-09-25 | v1.8 | 记录获授权的 Firecrawl 本机修复、3 项单测/build 与受控页面/loopback 拒绝复验；仅恢复技术冒烟，不关闭固定样本、G3/G4 或产品 AC。 |
| 2026-09-25 | v1.9 | 核对 Firecrawl v2.11.162/AGPL-3.0-or-later，并记录 HotKey 适配器 20 项回归及无持久化在线探针成功；target request count 未知，待固定样本及 G3/G4 证据归档。 |
| 2026-09-25 | v1.10 | 归档修复前失败输入、固定 Firecrawl 响应样本、适配器回归与受控在线探针，关闭 S02 G3/G4；不变更 0/6 产品 AC，S03/S04 与回退仍开放。 |
| 2026-09-25 | v1.11 | 更正 S02 当前前置状态，明确 9 月 24 日失败诊断为历史并已由受限修复后复测取代；保留 G3/G4 证据、S03/S04 与产品 AC 未完成状态。 |
| 2026-09-25 | v1.12 | 记录独立 Firecrawl `9262e24` 凭据日志脱敏加固及 API-only 原位复验；不改 038 S02 G3/G4、S03/S04 或产品 AC 状态。 |
| 2026-09-25 | v1.13 | 记录独立 Firecrawl `a25f95b` Basic 凭据脱敏回归、代理边界文档修正及 API-only 部署；上游代理/DNS 一致性仍待核验，计划门禁及产品 AC 状态不变。 |

文档复核（2026-09-21）：同步现有工程基线、实际检查入口和 App Router 路径；需求 ID、优先级及必要 AC 不变，未补记产品通过证据。
