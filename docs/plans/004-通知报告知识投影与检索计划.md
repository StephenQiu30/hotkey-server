---
layer: Plan
scope: shared
doc_no: "004"
title: 通知、报告、知识投影与检索计划
status: in_progress
version: v1.0
owner: HotKey Team
canonical_path: docs/plans/004-通知报告知识投影与检索计划.md
design: docs/design/004-通知报告知识投影与检索设计.md
prd: docs/prd/004-通知报告知识投影与检索.md
---

# 通知、报告、知识投影与检索计划

## 当前实现与差距

### 当前实现

- `notification`、`alert` 与 `delivery` 模块已保存 Notification Outbox、User Notification、Notification Read Receipt、Delivery Attempt、短期 Delivery Claim、WebSocket/REST 读取和邮件等能力；已读游标以服务端追加事实收敛，lost-ack 会按 Provider 能力对账或形成持久 `unknown` 并停止盲目重发；
- `report` 模块已有报告构建、存储、River Job、候选读取和 HTTP 入口；
- `knowledge` 模块已有文档、Proposal、Snapshot、Reconciliation、MinIO 与 Vault Writer；
- `search` 模块已注册认证后的 `GET /api/v1/search`，由 `ingestion`、`event` 和 `knowledge` 拥有模块分别读取 PostgreSQL FTS/`pg_trgm` 字段，统一完成筛选、排序、安全高亮和当前权限重检；原 `POST /api/v1/search` 外部来源即时搜索保持兼容，但前端默认读路径已切换到 PostgreSQL；
- `backend/db/schema.sql` 已包含通知 Outbox、用户通知、报告、知识文档/Revision、Vault Sync 和投递记录；
- 前端已有通知、日报、知识投影、搜索、热点和内容详情页面，以及 WebSocket 状态与安全 Markdown 组件；日报通知使用受控深链进入指定 Revision，知识治理入口只对 Editor/Admin 可见。

### 主要差距

- 通知事实、River 投递、WebSocket 推送、REST `after_id` 补拉和服务端 Read Receipt 已落地；仍需在 S05 以同一端到端 Fixture 证明中断、恢复和跨设备收敛，且 WebSocket 始终不是事实源；
- 日报中的事实性 Claim 必须 100% 引用有效 Evidence，审批后冻结不可变 Revision，重新生成不能覆盖已批准版本；
- Vault 自动区域和人工区域需要稳定边界、原子写入、Hash/Revision 冲突和 Inbox 流程；自动发布不能覆盖人工编辑；
- PostgreSQL FTS/`pg_trgm`、过滤、排序、安全高亮和双边界权限重检已落地，且默认读路径不使用 Elasticsearch、Embedding、向量召回或 RAG；固定三类语料、查询集、结果稳定性、脱敏查询计划和 warm-cache 容量/索引可见性候选基线已落地，仍需统一链路、冷缓存和生产同构环境批准；
- Notification Outbox、User Notification、Notification Read Receipt 和 Delivery Attempt 已是追加式持久事实与游标/交付审计锚点，Fencing、Provider 能力和 lost-ack `unknown` 停止语义已通过故障注入。S05 仍需证明恢复只清理 Delivery Claim/租约、WebSocket 会话、客户端缓存、Vault 自动区域和搜索投影，并完成报告、Vault、搜索从 PostgreSQL/受保护 Vault 的统一重排、对账与差异报告；
- 四角色权限和前端五态需要同时覆盖通知、报告审批、知识冲突和搜索结果撤权；
- 通知可见、报告生成、Vault 发布和搜索索引延迟需要固定基准环境；
- 周报、评论报告、外部 Webhook 和新增通知通道不进入 24 周 P0。既有邮件能力保持兼容但不在本计划扩大。

## 前置门禁

1. 001 已冻结 API、Schema、River、Vault、PostgreSQL FTS 和角色边界；
2. 002 的 Document、Evidence、Event 成员和来源权利可追踪；
3. 003 的 Event、Heat、Claim/Evidence、摘要和治理 Revision 稳定，AI 失败状态可识别；
4. 004 Design=`accepted`、PRD=`approved`；
5. Vault 测试目录、可丢弃 MinIO/PostgreSQL/Redis 环境和 WebSocket/浏览器测试工具可用；
6. 报告审批人、知识冲突处理人、搜索撤权责任人和数据保留规则明确；
7. 不以 Elasticsearch、Kafka、向量库或 RAG 作为任何验收前提。

## 阶段、依赖与出口

| 阶段 | 周次 | 依赖 | 主要工作 | 阶段出口 |
|---|---:|---|---|---|
| S01 站内通知 | W15 | 001–003 | 通知事实、Read Receipt、Claim Fencing、River 投递、WebSocket、REST 重放与权限 | 断线、重连、跨设备已读、租约竞态、lost-ack 和游标补拉可复测 |
| S02 日报与审批 | W15–W16 | S01、003 | 时间窗冻结、Evidence 校验、Revision、审批与重新生成 | 日报事实性结论 Evidence 覆盖率 100% |
| S03 Vault 投影 | W16–W17 | S02 | 自动/人工区域、原子写、冲突、Proposal、重建与对账 | 人工编辑不丢失，重复发布不重复 |
| S04 PostgreSQL 全文检索 | W17–W18 | S02–S03 | FTS/Trigram、筛选、排序、高亮、权限与撤权 | 内容、事件、知识可检索且不依赖向量 |
| S05 端到端收敛 | W18 | S01–S04 | 通知→日报→审批→Vault→检索、故障与延迟基准 | M4 八条 AC 形成同一闭环证据 |

005 负责生产级备份恢复、保留删除、容量和发布门禁；本计划必须先提供可调用的重建、对账和安全停用入口。

## 预计变更文件清单

```text
backend/db/schema.sql
backend/internal/bootstrap/alert_delivery.go
backend/internal/bootstrap/worker.go
backend/internal/platform/queue/
backend/internal/platform/scheduler/report_due.go
backend/internal/modules/notification/domain/
backend/internal/modules/notification/application/
backend/internal/modules/notification/infrastructure/jobs/
backend/internal/modules/notification/infrastructure/postgres/
backend/internal/modules/notification/transport/http/
backend/internal/modules/alert/
backend/internal/modules/delivery/
backend/internal/modules/report/domain/
backend/internal/modules/report/application/
backend/internal/modules/report/infrastructure/jobs/
backend/internal/modules/report/infrastructure/postgres/
backend/internal/modules/report/transport/http/
backend/internal/modules/knowledge/domain/
backend/internal/modules/knowledge/application/
backend/internal/modules/knowledge/infrastructure/jobs/
backend/internal/modules/knowledge/infrastructure/postgres/
backend/internal/modules/knowledge/infrastructure/vault/
backend/internal/modules/knowledge/transport/http/
backend/internal/modules/search/domain/
backend/internal/modules/search/application/
backend/internal/modules/search/transport/http/
backend/internal/modules/event/infrastructure/postgres/lexical_search.go
backend/internal/modules/ingestion/infrastructure/postgres/lexical_search.go
backend/internal/modules/knowledge/infrastructure/postgres/lexical_search.go
backend/internal/modules/event/application/content_search.go
backend/internal/modules/event/transport/http/
backend/internal/modules/ingestion/application/query.go
backend/internal/modules/ingestion/infrastructure/postgres/
backend/internal/modules/ingestion/transport/http/
backend/test/_suite/internal/modules/notification/
backend/test/_suite/internal/modules/report/
backend/test/_suite/internal/modules/knowledge/
backend/test/_suite/internal/modules/search/
backend/test/_suite/internal/modules/event/
backend/test/_suite/internal/modules/ingestion/
backend/test/integration/
backend/openapi/docs.go
docs/openapi/swagger.json
frontend/src/app/dashboard/notifications/page.tsx
frontend/src/app/dashboard/search/page.tsx
frontend/src/app/dashboard/events/page.tsx
frontend/src/app/dashboard/reports/
frontend/src/app/dashboard/knowledge/
frontend/src/components/notifications/
frontend/src/components/content/SafeMarkdown.tsx
frontend/src/lib/notificationStream.ts
frontend/src/stores/notificationStore.ts
frontend/src/services/hotkey/hotkey-server/
frontend/test/unit/app/dashboard-notifications-page.test.tsx
frontend/test/unit/app/dashboard-search-page.test.tsx
frontend/test/unit/components/notifications/
```

## 测试先行任务

### S01：站内通知与重放

1. `TASK-004-S01-T01`：先写同一 Event Update/触发窗口在最小通知间隔内只创建一个用户通知事实、重复 River Job 被抑制，且交付前重读用户当前通道偏好的测试，再收敛 Outbox、去重窗口与投影事务。映射 `AC-004-001`。
2. `TASK-004-S01-T02`：先覆盖 WebSocket 鉴权、Workspace/用户隔离、心跳、容量、恶意帧、慢消费者和断线，再实现或收敛实时流。映射 `AC-004-001`、`AC-004-007`。
3. `TASK-004-S01-T03`：先模拟连接在 ID N 后中断、期间新增通知、REST `after_id=N` 补拉和重连重复消息，断言无丢失、无重复 Toast。映射 `AC-004-001`。
4. `TASK-004-S01-T04`：先覆盖 Viewer/Analyst/Editor/Admin 对通知列表、标记已读和管理入口的直接 API 越权，再同步前端五态。映射 `AC-004-007`。
5. `TASK-004-S01-T05`：先证明当前 `localStorage.readThroughID` 不是服务端事实，再用两个设备、缓存丢失、重复/倒退/越权游标 Fixture 建立追加式 Notification Read Receipt 和幂等标记已读 API；当前状态取用户最新合法 `read_through_id`，前端登录后以服务端收敛。映射 `AC-004-001`、`AC-004-007`。
6. `TASK-004-S01-T06`：先在 Claim 领取后、租约过期、Worker B 抢占、Worker A 迟到、Provider 已接收但 Attempt 提交前崩溃等停止点故障注入；再实现数据库时间校验、递增 Fencing Generation、稳定派发键、Provider Capability/回执对账和 `unknown` 停止状态。支持 Provider 幂等时证明恢复不重复派发；不支持时证明不盲目自动重发且不宣称 exactly-once。映射 `AC-004-006`。

#### S01 T06 实施证据（2026-08-28）

- `TestEmailDeliveryRepositoryClaimsWithCurrentPermissionBackoffAndTerminalAttempt` 直接读取 PostgreSQL `clock_timestamp()`、`claimed_at` 与 `lease_until`，证明租约由数据库时间生成；客户端不再提交领取或完成时间。
- `TestEmailDeliveryRepositoryReusesDispatchKeyWithHigherFenceForIdempotentProvider` 从既有 Generation 7 Attempt 建立 Worker A Generation 8、过期后 Worker B Generation 9 的抢占链；B 复用同一稳定派发键，A 的旧 Token/Generation 无法提交 Attempt，最终 Attempt 只绑定 Generation 9。
- `TestEmailDeliveryRepositoryFencesExpiredWorkersAndStopsUnknownSMTPReplay` 在发送开始、租约过期、权限撤销和 A 迟到的停止点注入故障；无幂等/回执能力的 SMTP Claim 被追加为 `unknown / provider_outcome_unconfirmed`，随后不再进入自动领取。
- `TestEmailDeliveryStartsFencedDispatchBeforeCallingProvider` 固定 `claim → start → send → complete` 顺序；`TestEmailDeliveryReconcilesProviderReceiptWithoutSendingAgain` 证明已有 Provider 回执时发送计数为零；`TestEmailDeliveryReusesStableDispatchKeyForIdempotentProvider` 证明仅以原派发键重试一次；`TestEmailDeliveryStopsWhenProviderReceiptCannotBeConfirmed` 证明无幂等保护且回执不可确认时追加 `unknown` 而不发送；`TestEmailDeliveryLeavesStartedFenceWhenProviderAcceptedButAttemptCommitFails` 保存 Provider 已接收而本地提交失败的恢复锚点。
- SMTP 适配器显式声明不支持 Provider 幂等与回执。该实现只承诺“有能力时按稳定键对账/幂等重试；无能力或结果不确定时停止并形成 `unknown`”，不承诺外部 exactly-once。旧版未带派发键的临时 Claim 在 Schema 升级时被保守标记为已开始，过期后同样进入 `unknown`，不会盲目重发。

#### S01 T01–T05 收敛证据（2026-08-28）

- `TestProductEventRefreshRiverReplaysMembersMetricsSnapshotsUpdatesAndNotifications` 以真实 PostgreSQL/River 固定同一 Product Event 的多次刷新：同一 Update 重放只命中既有 Evaluation/Job/Outbox/User Notification，新的 Update 在发布配置冻结的 `alert_cooldown_minutes` 内只追加 `cooldown_suppressed` Evaluation，不创建第二条通知；跨 Monitor 仍保持独立通知身份。
- `TestEmailDeliveryRepositoryRechecksCurrentChannelPreferenceBeforeClaim` 在通知事实已存在后读取当前发布配置，邮件关闭时 Claim 和 Delivery Attempt 都保持为零；因此通道偏好在交付时生效，不回滚或删除已提交的站内通知事实。
- `TestUserNotificationRepositoryReplaysByUserRechecksMonitorAccessAndSeparatesDeliveryAttempts` 覆盖 Outbox/River 重放幂等、`after_id` 单调列表、同用户 Read Receipt 重放、前进、倒退冲突、跨用户游标拒绝和 Monitor 撤权后不可见；数据库外键、单调触发器和 append-only 触发器阻止客户端缓存或越权请求改写服务端事实。
- `TestWebSocketAuthenticatesInTheFirstFrameReplaysAndRecordsDelivery` 固化认证后按 `after_id` 重放；`RealtimeNotifications.test.tsx` 固化连接在 N 后中断、REST 补拉、重连和重复帧抑制；`notificationStore.test.ts` 证明本地游标只缓存数字 ID，丢失/倒退可由服务端重放收敛，伪造的本地已读前进会被服务端精确游标替换；`dashboard-notifications-page.test.tsx` 证明展示后只通过生成客户端提交服务端 Read Receipt。
- `make notification-convergence-acceptance` 是 `AC-004-001` 的统一复测入口，串行执行上述真实数据库、River、WebSocket 和前端状态测试；它不以 WebSocket 或 `localStorage` 代替 PostgreSQL 通知事实。

### S02：日报与冻结 Revision

1. `TASK-004-S02-T01`：先用固定时间窗和 Event/Claim/Evidence Fixture 验证候选冻结、来源覆盖和重复运行幂等，再实现日报构建 Job。映射 `AC-004-002`。
2. `TASK-004-S02-T02`：先构造缺失、撤权、Hash 不符、不属于时间窗的 ClaimEvidence，以及只有 Event Evidence State 但无逐句引用的 Fixture，断言报告拒绝发布且稳定失败码可见。映射 `AC-004-002`。
3. `TASK-004-S02-T03`：先覆盖草稿、待审批、已批准、已拒绝、已冻结状态和资源版本冲突，再实现不可变 Revision/审批事务。映射 `AC-004-003`。
4. `TASK-004-S02-T04`：先证明重新生成只创建新 Revision、旧批准 Revision 内容和 Evidence 引用不变。映射 `AC-004-003`。
5. `TASK-004-S02-T05`：在报告标题、摘要、Claim 和链接字段注入脚本标签、事件属性、`javascript:` URL、恶意 SVG/HTML Fixture，先断言其在草稿持久化、预览/审批和浏览器渲染前被拒绝或按字段白名单清理，页面无脚本执行且净化审计不回显载荷，再实现统一报告内容安全边界。映射 `AC-004-002`。

#### S02 T01–T05 收敛证据（2026-08-29）

- `TestReportPublicationRequiresExactMicroEventSentenceCitations` 固定事实句逐句引用、编辑注释不引用、重复引用和 Event 汇总状态不可替代 ClaimEvidence；`TestReportPublicationRevalidatesCitationFactsInsteadOfAggregateEvidenceState` 以真实 PostgreSQL 覆盖缺失引用、Evidence Set Hash/原句不一致、时间窗外引用和当前 quote 权利撤销，审批前均返回同一稳定 Evidence 冲突且不发布。
- `TestReportRejectsUnsafeContentBeforeDraftPersistenceOrPublication` 对标题、摘要、正文、Item、事实句和链接协议执行脚本标签、事件属性、`javascript:`、Data HTML/SVG、Entity/URL 编码与 `srcdoc` 矩阵；`TestReportRepositoryRejectsUnsafeDraftBeforeAnyWrite` 证明草稿写入前零副作用，`TestMaliciousReportFixtureCannotCreateApprovedOrDownstreamFacts` 证明受污染旧行在重复审批时不能创建 Report Transition、Delivery、Knowledge 或 River 事实，并只追加不回显载荷的 `report.content_rejected` 审计。
- `dashboard-reports-page.test.tsx` 把恶意 SVG/事件属性作为普通 React 文本展示并断言无可执行 DOM；全局 `Content-Security-Policy` 保留 Next App Router 内联引导脚本，同时以 `script-src-attr 'none'`、`object-src 'none'`、`frame-src 'none'` 和 `frame-ancestors 'none'` 关闭事件属性与嵌入执行面。`content-security-policy.test.ts` 固定配置，Fresh-container CI 直接检查 `8010` 实际响应头。
- `TestReportRepositoryFreezesExactSentenceCitationsAndPublishedVersion` 固定提交后父子行不可变；`TestReportRevisionConcurrentApprovalHasOneWinner` 证明两个相同版本审批请求只有一个成功；`TestReportRevisionApprovalIsOptimisticAuditedAndRegenerationPreservesApprovedSnapshot` 同时固定乐观冲突、Transition 审计、批准行/逐句 Evidence 快照不变，以及同日期重新生成只创建 `version_no + 1` 的新草稿。
- `make report-publication-acceptance` 是 `AC-004-002` 与 `AC-004-003` 的统一复测入口，串行执行领域、Application、真实 PostgreSQL、HTTP 错误协议、前端 DOM 与 CSP 契约。

### S03：Vault 投影与人工区域

1. `TASK-004-S03-T01`：先用固定 Markdown Golden 覆盖 Frontmatter、稳定 ID、自动区域、人工区域和可重复生成，再实现投影渲染。映射 `AC-004-004`。
2. `TASK-004-S03-T02`：先模拟写入中断、磁盘满、文件改名、重复稳定 ID、人工修改和并发发布，再实现临时文件+fsync+原子替换和冲突 Inbox。映射 `AC-004-004`、`AC-004-006`。
3. `TASK-004-S03-T03`：先证明 PostgreSQL 重建只替换自动区域、保留人工区域；人工区域仅从现存 Vault、Knowledge Revision 或备份恢复，缺失时必须停止而非生成；Hash 冲突不静默覆盖，再实现 Proposal/Reconciliation。映射 `AC-004-004`、`AC-004-006`。
4. `TASK-004-S03-T04`：先建立受控相对路径、绝对路径、`..`/编码穿越、分隔符绕过、符号链接逃逸、恶意 Markdown、脚本 URL 与不可信 HTML Fixture，断言在文件 I/O/发布前以稳定错误码拒绝并生成不含宿主路径的审计，再实现统一路径解析与内容净化边界。映射 `AC-004-004`。

#### S03 T01 实施证据（2026-08-28）

- 新增确定性 Vault Markdown 渲染契约：固定 Frontmatter 键顺序、Knowledge Document/Source 稳定 ID、Revision、Generated Hash，以及互不重叠且唯一的系统自动区/人工区标记；标题只使用确定性 YAML 字符串引用，生成内容不得注入任何 Vault 区域标记。
- `TestRenderVaultDocumentIsDeterministicAndSeparatesOwnedRegions` 固定字节级 Golden 并证明相同 PostgreSQL 事实可重复生成相同文件；`TestRenderVaultDocumentRejectsUnstableIdentityAndMarkerInjection` 覆盖缺失/非法 ID、未知类型、多行 Frontmatter 注入和自动/人工区标记注入。
- 本阶段只完成 S03 T01 的渲染基线；原子替换故障注入、冲突 Inbox、人工区受保护重建与内容安全仍由 T02–T04 门禁管理，因此不提前勾选 `CHK-004-G4-004`。

#### S03 T02 实施证据（2026-08-28）

- Vault 更新只重建系统拥有的 Frontmatter/自动区，并逐字节携带现存人工区；Knowledge Document ID、Document Type、Source ID、Revision 和区域标记任一冲突都会停止发布，避免重复稳定 ID、跳版和人工编辑被静默覆盖。
- 人工可维护文件通过“已观察完整文件 Hash + 进程内单写者互斥”执行 Compare-and-Swap；临时文件写入后执行文件 `fsync`、同目录原子替换和目录 `fsync`。磁盘满与重命名中断故障注入均证明旧文件保持原样且 `.hotkey-*.tmp` 零残留，并发旧发布者只能得到冲突。
- 文件被改名、目标文件身份重复、人工修改或并发发布冲突时，Proposal 以版本围栏持久化为 `conflict`，不调用后续文档/Revision 事务；事务应用也严格要求 Approved Proposal 版本围栏恰好命中一行，否则整笔回滚。`TestUpdateVaultDocumentPreservesHumanRegionByteForByte`、`TestWriterCompareAndSwapKeepsOriginalOnAtomicWriteFailure`、`TestWriterCompareAndSwapRejectsStalePublisherWithoutChangingVault` 和 `TestProposalApplyStopsOnRenamedOrDuplicateStableDocument` 固化上述停止点。
- S03 T02 已完成，但 PostgreSQL 重建/崩溃窗口对账和路径/Markdown 内容安全仍由 T03–T04 管理，因此 `CHK-004-G4-004` 继续保持未勾选。

#### S03 T03 实施证据（2026-08-28）

- 重建契约按“现存 Vault → 当前 Knowledge Revision 全文件快照 → 显式备份”选择人工区来源；第一个存在的受保护来源必须同时通过完整文件 Hash、Document/Source 稳定身份、Revision 和区域结构校验，冲突时立即停止，禁止跳到较旧副本掩盖人工编辑。
- PostgreSQL 适配器只在当前 Knowledge Document、同 Revision 的不可变 Revision、`applied` Proposal、Revision New Hash、Generated Hash 和 Frontmatter 全部闭合时返回自动区重建事实；无法从事实重建正文时返回不可用/冲突，不根据 Hash 或 Vault 内容猜测正文。
- Revision 快照写入使用 `If-None-Match: *` 条件创建：同键同内容重试幂等，不同内容返回冲突，禁止并发 Proposal 覆盖受保护恢复来源。Vault 文件缺失时，恢复服务以 4 MiB 上限读取该快照；仅当快照不存在时才读取显式备份。恢复出的完整文件必须仍等于 PostgreSQL 当前 Content Hash，并通过“目标必须不存在”的 Compare-and-Swap 发布；当前文件存在但 Hash 冲突时不读取旧快照、不覆盖，交由 Proposal/Reconciliation 治理。
- `project_knowledge` Worker 已接入上述恢复服务，队列载荷只携带 Knowledge Document ID，每次执行重新读取 PostgreSQL、快照和 Vault。领域、Application、MinIO、PostgreSQL 集成及 Worker Fx 图测试覆盖人工区逐字保留、来源缺失停止、旧备份禁止掩盖冲突、快照字节上限和运行时注册。
- S03 T03 已完成；T04 的统一路径解析、稳定错误码、脱敏审计和 Markdown/URL/HTML 内容安全仍未完成，因此 `CHK-004-G4-004` 继续保持未勾选。

#### S03 T04 实施证据（2026-08-28）

- Vault 路径只接受 `events/topics/reports` 受控命名空间和 ASCII 稳定 Key；绝对路径、非规范化点段/重复分隔符、反斜杠、原始或多重编码的穿越及符号链接都在创建根目录、读取文件或发布前失败停止。`TestStablePathRejectsTraversal`、`TestDocumentPathPartsRejectsNonCanonicalAndEncodedTraversal` 与 `TestWriterRejectsPathAndContentBeforeCreatingVaultRoot` 覆盖该矩阵。
- 统一 Markdown 边界限制 256 KiB，对 HTML Entity 和 URL 编码反复解码后拒绝 NUL、原始 HTML/事件属性、脚本 URL、恶意 SVG/Data URL、`srcdoc` 和 CSS Expression；新文档、Proposal、Compare-and-Swap、恢复快照及“现有人工区+新自动区”的完整合并结果均在持久化前重检。
- 路径、符号链接和内容拒绝分别对应稳定 API 错误码 `81000`、`81002`、`81001`；审计只写入 `knowledge.projection_rejected`、Knowledge Document ID 和低基数 `reason_code`，不写入 Vault 路径、宿主根目录或恶意载荷。Application 负向测试证明拒绝时 Vault 读写、Snapshot 和 PostgreSQL 业务事实均不发生变化；PostgreSQL AuditWriter 集成与 Worker Fx 图回归已通过。
- S03 T01–T04 现已合并覆盖确定性 Golden、人工区逐字保留、原子发布/冲突围栏、受保护恢复、恶意 Fixture、稳定错误码与脱敏审计，`CHK-004-G4-004` 可按实际证据勾选。

### S04：PostgreSQL 全文检索

1. `TASK-004-S04-T01`：先建立中英混合、短词、精确标签、实体、时间、状态、拼写片段及标签/属性/脚本 URL 等高亮 XSS Fixture，断言上下文转义后载荷不执行，再实现 FTS/Trigram 查询和安全高亮。映射 `AC-004-005`。
2. `TASK-004-S04-T02`：先增加架构测试，证明 P0 Search 不读取 Embedding 表、不调用 Provider、不构造 Elasticsearch 请求；再切换默认搜索读路径。映射 `AC-004-005`。
3. `TASK-004-S04-T03`：先覆盖角色/对象权限、撤权、删除、Workspace 隔离和过期 Projection，断言查询与 DTO 组装均重检当前权限、结果立即隐藏，并记录投影同步延迟与可人工修复的差异。映射 `AC-004-005`、`AC-004-006`、`AC-004-007`。
4. `TASK-004-S04-T04`：用固定语料和查询集测量 API P95、结果稳定性和索引更新延迟，保存查询计划但不记录私密正文。映射 `AC-004-008`。

#### S04 T01–T03 实施证据（2026-08-28）

- 新增内容、Event 和 Knowledge 三个拥有模块的 PostgreSQL 词法读模型：使用 `websearch_to_tsquery('simple', ...)`、`similarity`和有界子串回退覆盖中英混合、短词和拼写片段，并支持类型、Monitor、来源、实体、时间、状态与排序输入。来源条件在内容拥有模块精确应用；Event/Knowledge 若无本模块拥有的来源系谱则失败关闭，不跨模块直读表或放宽结果。
- `TestP0SearchArchitectureUsesOnlyPostgresLexicalPaths` 扫描正式检索路径，禁止 Embedding 表、Intelligence Provider、Elasticsearch/OpenSearch、pgvector 和最近向量调用，同时断言新 `GET` 为持久化默认读路径且原外部即时搜索 `POST` 仍存在。
- 查询 Application 在执行前重读当前用户/角色与 Monitor/来源范围，并在每个候选 DTO 组装前调用拥有模块 `CanDisplay`。三个 PostgreSQL 集成测试分别证明内容权利撤销、Event 关闭、Knowledge 归档后原候选立即不可见；当前 V1 是单 Workspace 模型，因此不伪造尚未存在的 Workspace ID 或租户隔离证据。
- 高亮先对完整不可信字符串做 HTML 转义，再仅插入受控 `<mark>` 标记；前端将该协议解析为 React 文本/标记节点，不使用 `dangerouslySetInnerHTML`。`TestHighlightEscapesEveryUntrustedByteBeforeAddingControlledMarks` 和 `dashboard-search-page.test.tsx` 证明恶意 SVG/事件属性只显示为文本，不产生可执行 DOM。
- OpenAPI、生成 TypeScript Client 和搜索页已同步；页面覆盖正常、加载、空和权限不足状态。

#### S04 T04 实施证据（2026-08-28）

- `53ffaf4b19dc901aeda32474d821648b77a166f1` 增加只能在全新隔离 PostgreSQL 运行的固定语料生成器，以及通过真实 Gin、Bearer 鉴权、Search Application、三个拥有模块 PostgreSQL Reader、当前可见性重检、DTO 和 JSON 的端到端测量器。工具以 `O_EXCL` 拒绝覆盖证据，固定查询只在报告保留摘要，非 `ANALYZE` 查询计划只保留节点深度、类型、Relation 与 Index。
- `docs/acceptance/evidence/004/search-capacity-macos-arm64-53ffaf4b.json` 在 Apple M5、10 逻辑 CPU、24 GiB RAM、APFS、PostgreSQL 18.4 本地 loopback 和 warm cache 上，对 1,000 条 Content、1,000 个 Micro Event、1,000 个 Knowledge Document 执行并发 4、10 次预热和 120 次采样。五个固定查询各得到 24 个稳定有序结果样本，总体 P50/P95/P99 为 142.254/171.480/176.179 ms，零错误；内容索引更新在第 1 次查询可见，耗时 117.593 ms。
- 报告同时保存 Content/Event/Knowledge 三条生产 SQL 的脱敏计划摘要；隐私扫描未发现查询原文、标题、Snippet、正文、DSN、凭据或宿主路径。该报告低于 800 ms 候选 P95，但仍明确为 `status=measured`、`approval=required`，只完成 S04 T04 的 warm-cache 候选测量，不替代生产同构/冷缓存批准，也不提前勾选仍要求通知、日报、Vault、统一安全运行 ID 和日志哨兵的 `CHK-004-G5-002`。

### S05：重建、故障与端到端

1. `TASK-004-S05-T01`：保留 append-only Notification Outbox、User Notification、Notification Read Receipt、Delivery Attempt、稳定 ID/派发键/游标与 Vault 人工区域，只清理可丢弃的 Delivery Claim/租约、WebSocket 会话、客户端缓存、知识自动区域和搜索投影，再从 PostgreSQL 与受保护 Vault 重新排队、恢复并对账；外部交付按 Fencing、Provider 回执或 `unknown` 停止规则处理。映射 `AC-004-006`。
2. `TASK-004-S05-T02`：在 WebSocket、外部 Provider lost-ack、Vault、Redis 和搜索投影分别故障时执行端到端故事，验证事实已提交、状态可见、恢复后补齐；通用 Worker 崩溃测试必须覆盖 Claim 自然过期、B 抢占和 A 的 stale Token 迟到完成。映射 `AC-004-001`、`AC-004-006`。
3. `TASK-004-S05-T03`：使用真实浏览器覆盖通知→日报审批→Vault→全文检索和五态/移动端，记录可访问性与失败请求。映射 `AC-004-007`、`AC-004-008`。
4. `TASK-004-S05-T04`：为通知→报告→Vault→检索链路注入正文、邮件地址列表、宿主路径和凭据哨兵，断言各阶段只用安全运行 ID/受控业务 ID 关联且结构化日志、错误和审计输出均无哨兵泄露。映射 `AC-004-008`。

#### S05 T01 实施进展（2026-08-28）

- 新增 `maintenance recover-projections` 的只读预检与受控执行入口。执行必须同时满足隔离环境、生产出口关闭、非生产配置、SMTP 关闭、双人复核和恢复/备份/演练摘要；预检或事务内目录发生变化即阻断。
- 恢复目录对 Notification Outbox、User Notification、Notification Read Receipt、Delivery Attempt 的完整规范化行集、计数和最大游标计算统一指纹；执行事务只删除 `dispatch_started_at IS NULL` 的 Delivery Claim，已开始 Claim 必须 Provider 对账，既有 `unknown` Attempt 原样保留且不重发。
- Vault 预检复用受保护恢复链，只输出人工区域 SHA-256；缺失文件按稳定 Document ID 排入 `project_knowledge`。当前可读且拥有唯一 `document_source` Evidence、授权明文 Artifact 但缺少活动检索投影的版本按 Evidence Reference ID 排入 `generate_source_document`。任务不携带正文、邮件地址、对象键、Vault 路径或凭据。
- 执行事务将双人身份、恢复/备份/演练摘要、事实与人工区前后指纹、稳定游标及清理/排队/保留计数追加到 `projection_recovery_runs`；Schema 触发器拒绝更新或删除，重复运行摘要也不能覆盖既有证据。
- 应用、PostgreSQL/River、Vault Domain 和 CLI 单元/集成契约已经落地；`CHK-004-G5-001` 仍保持未完成，直到独立恢复副本实际清理可丢弃状态、Worker 跑完、二次预检归零并形成零未解释差异证据。
- `TestM4ProjectionRecoveryDrillRestoresIndependentCopyToZeroDifference` 已补齐同一恢复故事执行器：完整源 PostgreSQL、Vault 和搜索投影始终保持不变；Custom Dump 只排除可丢弃 Search 数据，随后恢复到独立数据库，受保护知识 Revision 复制到独立 MinIO Bucket，Vault 复制到独立目录后仅清除目标知识文件，Redis DB 15 仅清除会话/客户端缓存 Fixture。预检固定识别一个未开始 Claim、一个 `unknown` Attempt、一个缺失 Vault 和一个缺失 Search；受控恢复仅删除 Claim 并排入两个正式 Worker，二次预检必须全部归零。
- 执行器同时比较四类事实的完整指纹、精确 ID 集合、最大游标和 Vault 人工区聚合哈希，验证 `unknown` Attempt 原 ID 保留、Provider 调用与盲重发均为零；搜索重建后再追加当前权利拒绝，记录生产查询从撤权提交到不可见的微秒值，并保留物理行供后续受控 Tombstone 清理。证据报告使用 `O_EXCL`/`0600`，不保存正文、邮件、对象键、路径、连接串或凭据；固定 Commit 的远端独立副本报告已在下节登记。

#### S05 T01 独立恢复副本证据（2026-08-30）

- 提交 `62af91fd34cce491cdd2306cb7f60ba1bd6b83e6` 的 GitHub Actions `33262989448` 在 `ubuntu-latest`、4 逻辑 CPU、隔离 PostgreSQL/MinIO/Vault/Redis DB 15 和生产出口关闭环境生成 `docs/acceptance/evidence/004/m4-projection-recovery-github-ubuntu-62af91fd.json`；下载 Artifact SHA-256 为 `bc1026d630b3a54c180f40537edae925bc6b161ae41055051fa59ee5d9876fdc`，同轮 8 个执行 Job 与 `All acceptance gates` 全部通过。
- 报告证明源 PostgreSQL、源 Vault 和源 Search 未被清理，目标使用三个独立副本；受控恢复前后 Notification Outbox/User Notification/Read Receipt/Delivery Attempt 分别为 2/2/1/1，精确 ID 集合、最大游标、事实指纹 `f3abdea6...dc7704` 和人工区域指纹 `f7ec734b...d27105` 均不变。未开始 Claim 从 1 归零，Redis 会话/客户端缓存清除 2 个，Vault/Search 缺失从 1/1 归零，两个 River Job 完成且失败 Attempt 为 0。
- 既有 `unknown` Delivery Attempt ID 1 前后保留；演练没有调用 Provider，也没有盲重发，处置记录固定为任何重发前必须人工 Provider 对账。搜索重建后追加当前 `store_derived=deny`，生产 Search/CanDisplay 在 8,832 μs 内均不可见，物理行仍保留供受控 Tombstone 清理；报告 `differences=[]`，敏感面扫描未发现正文、邮件、凭据、连接串、对象键、Vault/宿主路径。该证据闭合 `CHK-004-G5-001`，不替代 `CHK-004-G5-002` 尚需的生产同构容量批准。

#### S05 T02 实施进展（2026-08-28）

- 新增 `make m4-fault-recovery-acceptance`，以一个可定位门禁运行 WebSocket 已提交事实重放、Provider lost-ack/回执与 `unknown`、Delivery Claim 过期/B 抢占/A stale Token、Vault 受保护恢复、Redis 真实断开/恢复、搜索投影失败/恢复、通用 Worker 过期 Claim 和投影恢复编排；CI 复用已有 Worker Recovery Job 的 PostgreSQL、Redis、Go 与 MinIO 环境，不另起重复服务 Job。
- `TestVerificationStoreReturnsUnavailableDuringRealDisconnectAndRecoversExistingCode` 通过真实 TCP 中断证明故障期稳定返回 `unavailable`，同一个 Client 在链路恢复后可消费故障前已提交到 Redis 的短期验证码状态，不伪造成功。
- `TestProjectionRecoverySchedulesMissingVaultAndWorkerConvergesToZero` 首次制造真实缺失 Vault 投影，执行受控恢复、由 River Worker 消费 `project_knowledge`，再做二次预检归零；它同时证明 PostgreSQL 通知事实快照和 Vault 人工区域指纹不变。该 Fixture 发现并修复了合法稳定键中 `r/n/x/0` 被误判以及事务内保持行游标时递归查询导致 `conn busy` 的两个真实恢复阻断。
- `TestSourceDocumentGenerationRetriesAfterSearchProjectionRecoveryAndCompletes` 证明检索投影故障时先返回诚实的 `unavailable`/`derived_available` 部分状态，恢复后以同一 Evidence 稳定身份补齐 Search、下游调度和 Markdown 可读状态。前端 `RealtimeNotifications.test.tsx` 继续证明 WebSocket 断开时按 `after_id` REST 补拉、重连携带游标且不重复。
- 上述门禁闭合了分故障自动化矩阵，但还不是同一真实浏览器 Fixture 的通知→日报→Vault→搜索故事，也没有替代独立恢复副本、实际搜索行重建与零未解释差异报告；T03 已进入可运行前置建设，T04 尚未开始，`CHK-004-G5-001`、`CHK-004-G5-002` 继续保持未完成。

#### S05 T03 实施进展（2026-08-28）

- 正式 API 启动图已注册日报查询/构建/提交/审批和 Knowledge Document/Proposal/对账路由；OpenAPI 精确路径契约从 93 条扩展到 108 条，并生成对应 TypeScript Client。日报读取允许四角色，创建/提交限 Analyst/Editor/Admin，审批限 Editor/Admin；知识发布限 Editor/Admin，对账仍限 Admin，负向 API 回归固定该边界。
- 日报进入 `pending_approval` 和 `published` 时，在同一 PostgreSQL 事务追加 Notification Outbox 与 User Notification；通知只携带固定标题、受限摘要、业务 ID/Version 和 `/dashboard/reports?report=<id>` 白名单深链。领域、Schema 和前端三层均拒绝跨资源或追加参数的深链。
- 新增日报与知识投影工作台，覆盖加载、空、错误、权限不足和正常态，使用资源版本执行审批/发布，页面只按 React 文本渲染不可信报告字段；定向前端测试同时验证 Evidence ID 可见、恶意 SVG 不生成可执行 DOM、Editor/Admin 菜单与服务端角色一致。
- 报告批准事务现通过组合根适配器创建稳定的 `reports/report-<id>.md` Planned Document 和待审批 Knowledge Proposal；Proposal 失败时报告、Document 与 Proposal 整体回滚，Vault 仍只在 Editor/Admin 明确批准并应用后写入。空报告在 PostgreSQL 往返前后的输入快照 Hash 已统一，避免 `nil` 与空切片造成虚假版本冲突。
- 新鲜 Compose Project 已用同一隔离 Fixture 和真实 API/浏览器完成登录→通知→安全深链→日报批准冻结→知识提案批准→Vault 原子发布→PostgreSQL FTS 命中；数据库同时断言冻结报告、活动 Document、Revision、Applied Proposal 和两条用户通知全部收敛，Vault 文件实际存在。
- 真实浏览器证据覆盖报告、知识、桌面搜索、390×844 移动搜索、通知空态和知识权限不足态共 6 个 WCAG 2A/2AA 页面，结果为 0 违规、0 页面错误、0 失败请求、无横向溢出且 Tab 后焦点离开 `body`；网络记录证明登录、通知读取、报告批准、提案批准/应用和搜索均由真实 2xx API 完成。Viewer/Analyst/Editor/Admin 服务端矩阵、四类页面五态单元回归与上述浏览器证据共同闭合 `CHK-004-G4-006`，S05 T03 完成。

#### S05 T04 实施进展（2026-08-28）

- 同一 Fixture 在报告正文注入专用正文标记、双邮件地址列表、宿主绝对路径，并以仅用于可丢弃测试用户的固定口令作为凭据哨兵；Fixture 只通过安全运行 ID 和受控 Report/Document/Proposal/Notification ID 串联，上传证据不包含原始网络记录、容器日志或授权头。
- PostgreSQL 闭环扫描 `audit_logs`、River Job/Attempt、Vault Sync、Report Delivery 和 Delivery Attempt 的结构化错误/审计面；独立脚本扫描全部容器日志。5 个哨兵在上述操作面均为 0 泄露，CI 只上传脱敏浏览器摘要、数据库业务 ID 摘要、哨兵计数和可访问性 JSON。
- S05 T04 的全链路关联与泄露哨兵门禁已完成；`CHK-004-G5-002` 仍保持未完成，因为现有 3,000 条 PostgreSQL FTS warm-cache 候选测量和本次功能闭环尚未给出通知、日报、Vault、全文检索四段统一固定环境 P95，不能用零泄露结果替代剩余容量验收。

#### S05 T04 容量候选执行器（2026-08-29）

- 新增 `hotkey-m4-business-flow-capacity-v1` 执行器；它在全新 Compose 固定 Fixture 上用真实 Bearer API 重复完成日报构建→提交后通知可见→批准冻结→Knowledge Proposal 批准/原子写 Vault→PostgreSQL 检索可见，不以直接 SQL 替代业务操作。
- 五段分别保存 nearest-rank-ceiling P50/P95/P99 与有界样本，同一安全运行 ID 派生的 `X-Request-ID` 贯穿所有请求；报告只保存关联数量，不保存 Token、邮件地址、口令、正文、查询原文、Vault 或宿主路径，并用哨兵扫描和独占 `0600` 输出防止泄露/覆盖。
- 提交 `a577fa1f7de64003c3057e9685fc282e03a7057d` 的 Fresh-container CI [run 33260393634](https://github.com/StephenQiu30/hotkey-server/actions/runs/33260393634) 在原有真实浏览器业务故事和数据库/Vault 闭环断言后完成 2 次预热和 12 次样本；[`m4-business-flow-capacity-github-ubuntu-a577fa1f.json`](../acceptance/evidence/004/m4-business-flow-capacity-github-ubuntu-a577fa1f.json) 固定 GitHub hosted `ubuntu-latest`、4 逻辑 CPU、16,373,452 KiB RAM、Docker loopback、warm cache 和并发 1，五段 P95 分别为 13.794/19.233/14.742/20.987/10.416 ms，零错误，127 个请求关联 ID 全部唯一。
- 同一 run 的[`哨兵扫描摘要`](../acceptance/evidence/004/m4-sentinel-scan-github-ubuntu-a577fa1f.json)证明容器日志、Audit、River Job/Attempt 和 Delivery 错误面 5 类哨兵为零泄露；[`出站 Secret 扫描摘要`](../acceptance/evidence/004/m4-secret-surface-scan-github-ubuntu-a577fa1f.json)证明 607 个文件、9,558,825 字节的 11 类合成 Secret 为零泄露，8 个必需执行 Job 和最终 `All acceptance gates` 全部成功。报告仍为 `status=measured`、`approval=required`，且明确排除冷缓存、生产同构硬件/流量；独立资源占用、投影积压报告和人工批准尚未完成，因此 `AC-004-008` 仍为部分通过，`CHK-004-G5-002` 保持未勾选。

## 实施规格（SPEC）

| SPEC ID | 规格条款 | 上游 AC |
|---|---|---|
| `SPEC-004-API-001` | 通知列表以单调游标和 `after_id` 补拉，WebSocket 只传已提交通知；标记已读 API 只接受当前用户可见的 `read_through_id`、幂等追加 Read Receipt，拒绝倒退、越权或不存在游标并返回服务端当前值。报告、知识和搜索接口使用统一 Result、生成 OpenAPI 与服务端权限。 | `AC-004-001`、`AC-004-007` |
| `SPEC-004-DATA-001` | Notification Outbox、User Notification、Notification Read Receipt、Delivery Attempt、日报窗口、Report Revision、ClaimEvidence Reference、Knowledge Revision 和 Projection Run 使用稳定 ID、版本与幂等约束；Read Receipt 对同一用户单调前进，通知事实/游标/交付尝试不可清空，通知事实保存最小间隔/去重窗口并在交付时重读用户偏好，批准 Revision 不可覆盖。Delivery Attempt 或等价不可变结果必须能区分 `succeeded / failed / permanent_failure / unknown`；Schema 只在 canonical `backend/db/schema.sql` 向前兼容演进。 | `AC-004-001`、`AC-004-002`、`AC-004-003`、`AC-004-006` |
| `SPEC-004-DATA-002` | Vault 文件包含稳定 ID、Schema/Revision/Hash；自动区域与人工区域标记不重叠，自动区域可从 PostgreSQL 重建，人工区域只从 Vault/Knowledge Revision/备份恢复，冲突或缺失生成 Proposal/Inbox 事实而非覆盖或生成。 | `AC-004-004`、`AC-004-006` |
| `SPEC-004-JOB-001` | 通知恢复、日报、知识发布与重建均使用 River；Job 只传事实 ID/Revision，处理时重读权限和当前版本。外部 Claim 包含 Token、数据库时间租约和递增 Fencing Generation；抢占后旧 Token/Generation 无法提交 Attempt 或删除新 Claim。适配器必须声明 Provider 幂等键/回执能力：有能力时用稳定派发键先对账再重试；请求可能已送达却无法确认时追加 `unknown` 或等价事实并停止自动重发。恢复不重建或删除 Notification Outbox/User Notification/Notification Read Receipt/既有 Delivery Attempt。 | `AC-004-001`、`AC-004-003`、`AC-004-006` |
| `SPEC-004-UI-001` | 通知、报告、知识和搜索页面覆盖正常、空、加载、错误、权限不足、移动端与键盘操作；WebSocket 失败明确显示 REST 补拉状态。 | `AC-004-001`、`AC-004-007` |
| `SPEC-004-SEC-001` | 报告标题/摘要/Claim/链接在草稿持久化、预览、审批与浏览器渲染前按字段白名单拒绝或清理脚本标签、事件属性、脚本 URL、恶意 SVG/HTML。Vault 只接受根目录下受控相对路径，并在 I/O 前拒绝绝对路径、穿越、分隔符绕过与符号链接逃逸；恶意 Markdown/不可信 HTML 被拒绝或净化并审计。检索高亮按上下文转义；查询与 DTO 组装重检角色、对象权限及删除/撤权状态。错误与审计不得暴露宿主路径、凭据、撤权正文或恶意载荷。 | `AC-004-002`、`AC-004-004`、`AC-004-005`、`AC-004-006`、`AC-004-007` |
| `SPEC-004-OBS-001` | 通知、报告、Vault 和检索以安全运行 ID 关联，记录可见/构建/审批/写入/投影/撤权同步延迟；指标与结构化日志不得包含查询原文、正文、邮件地址列表、宿主路径、凭据、标题或用户 ID 高基数标签。 | `AC-004-008` |
| `SPEC-004-OPS-001` | 搜索只使用 PostgreSQL FTS/Trigram；通知可从 append-only 通知事实恢复，恢复只清理临时 Claim/租约、会话和客户端缓存，既有 Delivery Attempt、Notification Outbox/User Notification/Notification Read Receipt/游标和 Vault 人工区域不可清空。`unknown` 外部交付必须进入告警、Provider 回执对账或经批准人工处置，不得普通重放；知识自动区域/搜索投影可从 PostgreSQL 重建并与受保护 Vault 对账，不引入 Kafka/Elasticsearch/向量/RAG。 | `AC-004-005`、`AC-004-006` |

## 执行检查清单（CHECKLIST）

- [x] `CHK-004-G4-001`（`AC-004-001`）：最小通知间隔、重复抑制和交付时用户偏好生效，断线期间可通过 `after_id` 无损补齐且重连不重复；两个设备以服务端 Read Receipt 收敛，本地缓存丢失、倒退或越权游标不能改写事实；证据：`TestProductEventRefreshRiverReplaysMembersMetricsSnapshotsUpdatesAndNotifications`、`TestEmailDeliveryRepositoryRechecksCurrentChannelPreferenceBeforeClaim`、`TestUserNotificationRepositoryReplaysByUserRechecksMonitorAccessAndSeparatesDeliveryAttempts`、WebSocket Handler 重放测试、`notificationStore.test.ts`、`RealtimeNotifications.test.tsx`、`dashboard-notifications-page.test.tsx` 与统一入口 `make notification-convergence-acceptance`。
- [x] `CHK-004-G4-002`（`AC-004-002`）：日报每条事实性 Claim 的 Evidence 覆盖率为 100%，无效引用阻断发布；标题、摘要、Claim、链接中的脚本标签、事件属性、脚本 URL、恶意 SVG/HTML 在草稿/预览/审批前拒绝或清理，浏览器无脚本执行；证据：逐句引用领域矩阵、`TestReportPublicationRevalidatesCitationFactsInsteadOfAggregateEvidenceState`、`TestReportRepositoryRejectsUnsafeDraftBeforeAnyWrite`、`TestMaliciousReportFixtureCannotCreateApprovedOrDownstreamFacts`、事实表指纹、脱敏 `report.content_rejected` 审计、`dashboard-reports-page.test.tsx` DOM 断言、`content-security-policy.test.ts`、Fresh-container 实际 CSP 响应头检查与 `make report-publication-acceptance`。
- [x] `CHK-004-G4-003`（`AC-004-003`）：批准 Revision 冻结，重新生成不覆盖；证据：`TestReportRepositoryFreezesExactSentenceCitationsAndPublishedVersion`、`TestReportRevisionConcurrentApprovalHasOneWinner`、`TestReportRevisionApprovalIsOptimisticAuditedAndRegenerationPreservesApprovedSnapshot` 的数据库父子行不可变、Transition 快照、单赢家审批和新 `version_no` 草稿断言，以及统一入口 `make report-publication-acceptance`。
- [x] `CHK-004-G4-004`（`AC-004-004`）：重复发布、人工编辑、写入中断和冲突均不丢人工区域；绝对/穿越/符号链接路径与恶意 Markdown/不可信 HTML 在 I/O/发布前拒绝并审计且不泄露宿主路径；证据：Vault 字节级 Golden、写入故障/CAS/受保护恢复测试、恶意路径与 Markdown Fixture 矩阵、`81000`–`81002` 稳定错误码、`knowledge.projection_rejected` 脱敏审计与 PostgreSQL AuditWriter/Worker Fx 图回归。
- [x] `CHK-004-G4-005`（`AC-004-005`）：全文检索不调用向量/Provider/Elasticsearch，高亮 XSS 载荷不执行，角色/对象权限及删除/撤权结果不可见；证据：`TestP0SearchArchitectureUsesOnlyPostgresLexicalPaths`、`TestHighlightEscapesEveryUntrustedByteBeforeAddingControlledMarks`、`TestServiceRechecksCurrentSubjectAndOwnerVisibilityBeforeDTOAssembly`、三个拥有模块 PostgreSQL 词法检索集成测试、`dashboard-search-page.test.tsx`恶意 DOM/五态断言、OpenAPI 生成契约与默认 `GET /api/v1/search` 路径。
- [x] `CHK-004-G4-006`（`AC-004-007`）：四角色服务端权限与四类页面五态一致；证据：Notification/Report/Knowledge/Search 服务端角色矩阵与负向 API、前端 45 文件 235 项单元回归、同一真实浏览器 Fixture 的正常/空/权限不足/移动端/键盘状态，以及 6 个页面 WCAG 2A/2AA 0 违规、0 页面错误、0 失败请求结果。
- [x] `CHK-004-G4-007`（`AC-004-006`）：Claim 自然过期后 B 以更高 Fencing Generation 抢占，A 的 stale Token 不能提交或删除 B 的 Claim；Provider lost-ack 在支持幂等/回执时不重复派发，不支持时进入 `unknown` 并停止自动重发；证据：`TestEmailDeliveryRepositoryClaimsWithCurrentPermissionBackoffAndTerminalAttempt`、`TestEmailDeliveryRepositoryReusesDispatchKeyWithHigherFenceForIdempotentProvider`、`TestEmailDeliveryRepositoryFencesExpiredWorkersAndStopsUnknownSMTPReplay`、`TestEmailDeliveryStartsFencedDispatchBeforeCallingProvider`、`TestEmailDeliveryReconcilesProviderReceiptWithoutSendingAgain`、`TestEmailDeliveryReusesStableDispatchKeyForIdempotentProvider`、`TestEmailDeliveryStopsWhenProviderReceiptCannotBeConfirmed`、`TestEmailDeliveryLeavesStartedFenceWhenProviderAcceptedButAttemptCommitFails`、Attempt/Claim 数据库快照与上述 exactly-once 非承诺说明。
- [x] `CHK-004-G5-001`（`AC-004-006`）：仅清理 Delivery Claim/租约、WebSocket 会话、客户端缓存、知识自动区域和搜索投影后可恢复并与 PostgreSQL/受保护 Vault 对账；Notification Outbox/User Notification/Notification Read Receipt/既有 Delivery Attempt、稳定游标和人工区域保持不变，`unknown` 外部交付没有盲重发，删除/撤权投影延迟可观测且差异可人工修复；证据：提交 `62af91fd`、GitHub Actions `33262989448`、`m4-projection-recovery-github-ubuntu-62af91fd.json` 的三个独立副本、事实/ID/游标/人工区前后一致、Claim/Redis/Vault/Search 收敛、Provider 零调用/零盲重发、8,832 μs 撤权可见延迟与 `differences=[]`。
- [ ] `CHK-004-G5-002`（`AC-004-008`）：通知、日报、Vault、全文检索候选延迟在固定环境可复测，全链路由安全运行 ID 关联且日志不泄露正文、邮件地址列表、宿主路径或凭据；预期证据：基准数据、命令、P95、查询计划、关联追踪和哨兵扫描报告。

## 验证命令

```bash
cd backend
make m4-fault-recovery-acceptance
go run ./test/runner test ./internal/modules/notification/... ./internal/modules/alert/... ./internal/modules/delivery/... ./internal/modules/report/... ./internal/modules/knowledge/... ./internal/modules/event/... ./internal/modules/ingestion/... -count=1
go run ./test/runner test -tags=integration ./internal/modules/notification/... ./internal/modules/report/... ./internal/modules/knowledge/... -count=1
go test ./test/architecture -count=1
sh test/tools/verify-schema.sh
go run ./test/runner vet ./...
go build ./...

cd ../frontend
npm run openapi:generate
npm run openapi:check
npm run typecheck
npm run test:unit
npm run build

cd ..
docker compose -f docker-compose.yml config --quiet
git diff --check
```

Vault 重建、真实 WebSocket 和浏览器验收使用隔离目录、独立 Compose Project 与可丢弃数据；不得覆盖真实 Vault 或生产卷。

## 数据与契约顺序

1. 先保存通知 Read Receipt、Claim/Fencing/lost-ack、Revision、Vault、搜索和重建的失败测试；
2. 数据变化只在 canonical `schema.sql` 向前兼容增加，先同步 Read Receipt/Delivery `unknown`、Repository/Model/Schema 检查；
3. 先提交 PostgreSQL 事实和 River Job，再推送 WebSocket、写 Vault 或刷新搜索投影；外部投影失败不回滚事实；
4. 报告先冻结时间窗和 Evidence，再生成草稿、审批、冻结 Revision，最后发布知识投影；
5. 搜索读模型先影子构建和对比，确认权限/召回后切换默认读取；
6. Transport 注解完成后生成 OpenAPI，再生成客户端和更新 UI；
7. 重建/对账和停用入口必须在生产灰度前完成。

## 灰度、迁移与回滚

- 通知先对内部用户启用服务端 Read Receipt 和 WebSocket，同时保留 REST 读取；迁移时将本地 `readThroughID` 仅作为一次性候选，服务端校验后追加 Receipt，此后服务端为准。实时故障时关闭 WebSocket 但不停止通知事实和补拉；
- 外部交付先用 Fake Provider 验证 Fencing、幂等/回执和 lost-ack，再按 Provider Capability 灰度；出现 `unknown` 时暂停该派发键自动重试，不删除 Claim/Attempt 证据；
- 日报先生成不可发布草稿，Evidence 覆盖和审批稳定后开放发布；回滚只停新生成/发布，不修改已冻结 Revision；
- Vault 先写隔离目录并做 Hash 对账，再按 Monitor/报告灰度；异常时暂停 Writer，保留 Proposal 和已冻结报告；
- PostgreSQL FTS 先影子查询并与当前搜索比较，再切换默认；质量或性能回退时切回旧读路径，不删除新投影；
- 现有 Embedding/向量表和旧搜索代码在替换验收与回滚窗口内保留，本计划不删除；
- 任何撤权和删除先在读取/搜索层阻断，再由 005 的保留工作流处理投影与对象；
- 邮件等既有通道保持兼容，不在本计划新增通道或删除历史投递事实。

## 完成定义（DoD）

- 站内通知重放、日报 Evidence、冻结 Revision、Vault 人工区域、无向量搜索、投影重建、权限五态和候选时延全部满足八条 AC；
- 通知→日报→审批→Vault→全文检索的真实浏览器故事通过；
- PostgreSQL 通知事实可恢复实时与外部交付，知识自动区域与搜索投影可重建并对账；Notification Outbox/User Notification/Notification Read Receipt/Delivery Attempt、游标和 Vault 人工区域不被清空；Claim 抢占由 Fencing 保护，Provider 支持幂等/回执时不重复派发，不可确认时进入 `unknown` 且无盲重发，不虚构 exactly-once；
- Schema、OpenAPI、生成客户端、架构检查、目标测试、Compose 和构建通过；
- 未让 Python Agent 成为报告/知识事实源，且未引入 Kafka、Elasticsearch、向量/RAG、其他业务服务或第二套事实源；
- 未覆盖人工 Vault 区域、批准报告或历史投递事实；
- 同编号 Acceptance 保存重放、重建、时延、浏览器、已知限制和回滚证据后，Plan 才可标为 `completed`。
