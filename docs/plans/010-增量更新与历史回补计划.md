---
layer: Plan
scope: shared
doc_no: "010"
title: 增量更新与历史回补计划
status: in_progress
version: v1.8
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/010-增量更新与历史回补计划.md
design: docs/design/010-增量更新与历史回补设计.md
prd: docs/prd/010-增量更新与历史回补.md
source_requirement: FR-001-008
backlog: BACKLOG.md
priority: P0
milestone: M2
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。Kafka 重试、消费位点与 Redis 失效场景沿用已通过的 009/031 先行切片；本计划产品验收仍为 0/6。

# 增量更新与历史回补执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

日期：2026-09-23。状态：`in_progress`，S00/S01 内部切片已完成，S02 时间标记与游标有限重扫内部子片已通过，真实处理器/通知待执行，产品 AC 0/6。来源：[010 PRD](../prd/010-增量更新与历史回补.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

## 1. 交付目标与当前差距

完成 FR-001-008 的实现和证据闭环，保持 P0。当前已有 API/Worker/CLI 与 Web 工程底座、依赖锁和基础 CI，S01 的来源无关范围状态与受控联验、S02 的时间标记及游标有限重扫内部子片已落地；真实多页来源处理器、实际失效游标识别、回补任务和完整验收仍未交付。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把内部样本当作产品已验收；尚无 Acceptance。

建议责任角色：**后端＋来源接入**，实际执行人于开工时登记；归属里程碑 **M2：关键词和账号监控闭环**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。它包含第 4 节规格、S01—S03 具体切片、S04 的原始 AC 证据，以及可执行的停止/回退说明。当前完成 S00/S01 内部切片与 S02 时间标记/游标有限重扫内部子片，PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

[010 Design](../design/010-增量更新与历史回补设计.md) v1.2 已实现来源无关的 S01 内部范围状态，S00 G0—G2 与 S01 G3/G4 内部门禁完成；真实来源的有界查询/终点证据和样本尚缺，不能据此实采或勾选产品 AC。`OPEN-010-001` 的 10 分钟重叠只保留为待校准建议，不成为全局默认。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| [007 作品资料与上下文](007-作品资料与上下文计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得：该 Plan 的 `CHK-007-G4-001` |
| [009 采集任务控制](009-采集任务控制计划.md) | S03 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得：该 Plan 的 `CHK-009-G4-003` |
| [031 可靠执行与幂等](031-可靠执行与幂等计划.md) | S02 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得：该 Plan 的 `CHK-031-G4-002` |
| [036 数据访问与生命周期](036-数据访问与生命周期计划.md) | S02 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得：该 Plan 的 `CHK-036-G4-002` |
| [037 费用与资源约束](037-费用与资源约束计划.md) | S02 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得：该 Plan 的 `CHK-037-G4-002` |

以上是**切片依赖**，并非各 Plan 的最终产品验收。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

`OPEN-010-001` 对内部 S01/S02 无必需默认值：受控测试显式给定 UTC 窗口、排序和规则版本，且不标记来源可用；首个真实处理器入场时以该来源样本再冻结重叠时长、刷新周期、访问条件及预算。已确定技术栈沿用 AGENTS；新增平台、付费依赖或框架等实质选择按项目规范处理，普通实现细节由执行者负责。

## 3. 预计变更文件与归属

以下均为**拟建/拟改路径**，不是现存实现。只在切片实际开始时创建必要文件；先登记新领域职责和结构检查，再加入代码。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| backend/app/jobs/models.py、schemas.py、services.py；backend/app/api/routers/incremental_windows.py | 本项业务模型、事务与协议；API 经依赖注入，不直接操作 ORM |
| backend/tests/unit/test_incremental_windows.py | 规则与状态的针对性失败/通过验证 |
| backend/tests/integration/test_incremental_windows.py | 适用的真实 PostgreSQL/Redis/Kafka、权限、幂等或边界验证；纯前端/人工项写明适用范围 |
| frontend/src/app/jobs/；frontend/tests/e2e/incremental_windows.spec.ts | 业务交互与真实浏览器结果 |
| backend/app/api/router.py、dependencies.py；运行时 /openapi.json（自动生成）；frontend/src/api/ | 若 HTTP 契约变化，注册路由及唯一生成链；无变化则记录不适用原因 |
| backend/database/schema.sql；根 Compose 和配置样例 | 只在确需数据/配置变化时修改；SQL 与 ORM 同批更新，禁止覆盖用户配置或删除持久卷 |
| docs/design/010-增量更新与历史回补设计.md；对应 Acceptance（真实验证后创建） | 开工前设计与交付后证据，使用同编号，不预填通过 |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-010-API-001 | POST /api/backfill-runs 与 GET /api/coverage-windows；范围必须显式指定；覆盖窗口与任务完成状态分别返回。 |
| SPEC-010-DATA-001 | 来源能力级窗口、水位、游标、重叠范围及删除约束；仅已保存结果可推动确认进度，保存缺口和补采理由。 |
| SPEC-010-JOB-001 | 增量优先于历史回补；游标失效有限重扫，旧帖刷新和新帖扫描分开；补采不制造新事件通知。 |
| SPEC-010-UI-001 | 在 frontend/src/app/jobs/ 实现本项已有 PRD 流程；桌面与窄屏同时覆盖正常、空、加载、部分、错误和无权限状态，调用生成客户端，不复制服务端业务规则。 |
| SPEC-010-SEC-001 | 先检查当前身份及资源权限；配置版本不能绕过撤权、删除、停用和更严格预算。秘密不可回读，正文与外部输入只作数据。关键变更留操作者、对象、时间与版本。 |
| SPEC-010-OBS-001 | 跟踪最早欠账和预算延期；回退保留窗口及当前有效水位，不对未确认尾段标完成。 |
| SPEC-010-OPS-001 | 在隔离依赖验证后渐进启用本能力；保持历史资料可读。配置缺失或外部条件不满足时明确拒绝/延期。变更前评估数据兼容与回退，停止新增操作优先于破坏性迁移。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-010-S00-T01 | 阅读 PRD 与关联设计，关闭本期必要未决项，补同编号 Design；明确未纳入本期的分支 | 需求/设计评审记录、边界与风险 |
| TASK-010-S00-T02 | 核查先行切片，登记文件职责与主责，冻结 SPEC、样本、接口及测试命令 | 可执行切片清单、隔离资源和验证清单 |

- [x] `CHK-010-G0-001` → AC-010-001—006；`EV-010-001`（2026-09-23）：PRD v1.2 与 Design v1.1 核对，接受单表范围状态、连续确认与现有 Job/内容分责；仅内部 S01/S02 可执行。`OPEN-010-001` 的真实来源参数未关闭，不设全局默认，真实平台和产品 AC 均不在本门禁内。
- [x] `CHK-010-G1-001` → AC-010-001—006；`EV-010-002`（2026-09-23）：046 S03 Acceptance、007 G4-001、009 G4-003、031 G4-002、036 G4-002、037 G4-002 均有已勾选先行输出；现有本机 PostgreSQL `pg_isready`、Redis `PONG`、Kafka TCP、MinIO health 200，browser/egress 与 Firecrawl 容器运行。`backend/.env` 已有本机测试配置，隔离测试仅用一次性空库；真实多页来源和授权样本尚无，Firecrawl `/health` 返回 404，未将其计作健康验证。
- [x] `CHK-010-G2-001` → AC-010-001—006；`EV-010-003`（2026-09-23）：Design 第 3/5 节冻结 jobs 范围状态、内容/Job 分责、相邻重叠窗口、半批预算失败、跨缺口连续确认、无边界证明空页及重投样本。S01 修改 jobs 模型/契约/服务、模型注册、唯一 DDL 与必要 content 服务；测试归 `backend/tests/unit|integration/test_incremental_windows.py`。显式 UTC 范围、排序、规则版本，无真实来源默认值；pytest 一次性 PostgreSQL、Ruff、mypy、全量回归及 DDL/ORM 对齐适用。无业务处理器前 API/UI/OpenAPI/浏览器与真实来源验收不适用，不创建空接口。

### S01 重复窗口与确认水位

前置：S00 完成。本切片覆盖 FR-010-001、FR-010-002；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-010-S01-T01 | Red：以 AC-010-002、AC-010-003 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-010-S01-T02 | Green：实现观察身份去重和窗口推进事务，覆盖半批失败与重投。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-010-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-010-G3-001` → AC-010-002、AC-010-003；`EV-010-004`（2026-09-23）：隔离空库运行相邻窗口半批失败/重投和无边界证明空页两项测试，均因缺少 `CoverageWindowInput`/终点证据契约而失败；证明当前无范围状态入口，不冒充已有业务行为错误或环境故障。输入为 Design v1.2 的受控分页样本。
- [x] `CHK-010-G4-001` → AC-010-002、AC-010-003；`EV-010-007`（2026-09-23）：唯一 DDL/ORM 增加 owner 隔离范围表，`jobs` 服务行锁与 epoch 检查、同事务 checkpoint、终点证据和连续水位；`content` 新增同事务作品写入入口。受控 PostgreSQL 联验得到 1 作品、2 发现、2 观察；A 部分/B 确认时水位停 A 起点，A 恢复后到 B 终点，重投未增加记录。另覆盖回滚、跨 owner、Job 删除保留范围、合法空页无证明不确认；专用 5 集成＋6 单元、后端全量 342 passed/5 skipped、Ruff/format/mypy 与空库 DDL 通过。此门禁只关闭 S01 内部切片：无真实多页处理器、API/UI 或平台终点证据，产品 AC 仍 0/6；新增 DDL 未重放到既有开发库。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。

### S02 历史时间与游标恢复

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 FR-010-003、FR-010-004；涉及的完整规则以对应 PRD 为准。

2026-09-23 先执行时间标记子片：范围 Job 的 `scan_kind` 显式固定，既有作品观察保留原发布时间/本次观测时间，详情发现依据投影类型，旧任务未标记时不假定追新。文件职责见 Design v1.3；不新增 DDL 或空路由。失败验证用同一旧作品在追新/回补 Job 中两次观察，要求原始发布时间不变、发现依据分别可辨。随后运行 PostgreSQL、全量后端、OpenAPI 生成/漂移、前端测试/构建与桌面/窄屏检查。游标有限重扫、实际通知抑制和真实处理器尚未交付，故本子片不勾选 S02 G3/G4 或产品 AC。

时间标记子片证据（2026-09-23）：隔离 PostgreSQL 用例先因 `ContentDiscoveryView` 无 `scan_kind` 字段失败；实现后专用 6 项及后端全量 343 passed/5 skipped、Ruff/mypy 通过。来源发布时间 `2026-09-22T07:00:00Z` 在次日回补后保持不变，追新/回补为同一作品的两条发现、两次观察；缺失类型的旧 Job 拒绝开启覆盖窗口且任务上下文保持 `null`。运行 API 的 OpenAPI 生成 `ContentDiscoveryView.scan_kind` 可空必填字段，客户端与页面同步；前端 36 tests、lint、typecheck、format、build 通过。桌面 1280 和窄屏 390 的受控响应页面均显示“历史回补 · 非新发生”，390 无横向溢出，axe 0 violation/0 incomplete；此浏览器样本为网络拦截响应，不是获授权真实来源。现有本机依赖复用，未启动第二套；该子片尚未验证游标与通知，G3/G4 保持未勾选。

游标内部子片证据（2026-09-23）：先以受控 A→B→A 循环与失效/崩溃重扫用例得到缺少持久游标状态的红测；随后复用现有 Job checkpoint、租约 fencing 与 `CoverageWindowService` 同事务提交，页数上限 32、重扫上限 3，且每个窗口固定预算。checkpoint 仅存窗口摘要、页数、重扫数、已见/预期游标 SHA-256 摘要，不存原始游标；失去内存游标时从同一显式窗口重新读取，重复或失效游标留 `partial`，预算用尽不再发请求。受控 PostgreSQL 循环 3 页后水位停在窗口起点，重扫至有界终点证据后才确认，共记 5 页；后端 347 passed/5 skipped、Ruff/mypy、一次性空库 DDL，前端生成契约/36 tests/lint/typecheck/format/build 通过。本片没有真实多页 Worker、实际平台失效游标判定、有界时间查询或通知入口，因此 G3/G4、产品 AC 均不勾选；测试预算不是来源默认值。

002 S01a 已补共享 `SearchRequest` 的可选 UTC 时间窗与 X Recent Search 模拟请求，含近 7 天、未来时间及被非校验复制的无效窗口的费用授权前拒绝；后端 420 passed/5 skipped。它没有多页业务处理器、实际平台终点证明或通知入口，尤其不能把 Recent Search 用于旧于 7 天的历史回补。因此本计划 S02 G3/G4 与产品 AC 仍未勾选。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-010-S02-T01 | Red：以 AC-010-004、AC-010-005 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-010-S02-T02 | Green：区分原始发布时间、观察时间及回填类型，实现循环游标停止和有限重扫。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-010-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-010-G3-002` → AC-010-004、AC-010-005；任务 TASK-010-S02-T01：失败验证确实证明当前行为缺失；预期证据 `EV-010-005`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-010-G4-002` → AC-010-004、AC-010-005；任务 TASK-010-S02-T02、TASK-010-S02-T03：切片闭环与必要回归通过；预期证据 `EV-010-008`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。

### S03 旧讨论刷新与公平

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 FR-010-005、FR-010-006；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-010-S03-T01 | Red：以 AC-010-006 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-010-S03-T02 | Green：为活跃旧作品刷新指标/回复，遵守删除记录及实时任务资源保留。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-010-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-010-G3-003` → AC-010-006；任务 TASK-010-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-010-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-010-G4-003` → AC-010-006；任务 TASK-010-S03-T02、TASK-010-S03-T03：切片闭环与必要回归通过；预期证据 `EV-010-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-010-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-010-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-010-G5-001` → AC-010-001；**总纲场景继承**：Given 两轮结果部分重叠，旧帖有新回复，期间任务中断；When 恢复并补采；Then 不重复作品/评论/提醒，新增回复可见；未完成窗口保留，不越过缺口宣称完整。预期证据 `EV-010-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-010-G5-002` → AC-010-002；**重叠窗口**：Given 相邻两轮含相同作品与新指标；When 写入第二轮；Then 作品不重复但新增有效快照与发现关系；预期证据 `EV-010-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-010-G5-003` → AC-010-003；**截断水位**：Given 窗口采到一半即耗尽预算；When 任务结束；Then 保留缺口，不推进到未采样尾端；预期证据 `EV-010-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-010-G5-004` → AC-010-004；**历史补采**：Given 旧作品今天才入库；When 完成回填；Then 发布时间保持原值，通知明确历史而非新发生；预期证据 `EV-010-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-010-G5-005` → AC-010-005；**失效游标**：Given 恢复时游标已不可用；When 按窗口重扫；Then 有限重试且去重，不跳过未知页报完成；预期证据 `EV-010-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-010-G5-006` → AC-010-006；**旧回复与删除**：Given 旧帖新增回复，另一作品已有删除标记；When 增量与回补同时运行；Then 新回复可见，删除对象不复活，新任务预算不被回填占尽；预期证据 `EV-010-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-010-G6-001` → AC-010-001、AC-010-002、AC-010-003、AC-010-004、AC-010-005、AC-010-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-010-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-010-G6-002` → AC-010-001、AC-010-002、AC-010-003、AC-010-004、AC-010-005、AC-010-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-010-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| FR-010-001 | 业务去重 | TASK-010-S01-T02 | AC-010-002 | CHK-010-G4-001 |
| FR-010-002 | 窗口确认 | TASK-010-S01-T02 | AC-010-003 | CHK-010-G4-001 |
| FR-010-003 | 时间语义 | TASK-010-S02-T02 | AC-010-004 | CHK-010-G4-002 |
| FR-010-004 | 游标失效 | TASK-010-S02-T02 | AC-010-005 | CHK-010-G4-002 |
| FR-010-005 | 旧帖更新 | TASK-010-S03-T02 | AC-010-006 | CHK-010-G4-003 |
| FR-010-006 | 删除与公平 | TASK-010-S03-T02 | AC-010-006 | CHK-010-G4-003 |

父级综合场景 AC-010-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| backend/ | `uv run ruff check .`；`uv run ruff format --check .`；`uv run mypy` | 既定静态与边界要求，依赖须先由 042 锁定安装 |
| backend/ | `uv run pytest tests/unit/test_incremental_windows.py` | 本切片规则验证 |
| backend/ | `uv run pytest tests/integration/test_incremental_windows.py` | 隔离的真实依赖与边界验证 |
| backend/ | `uv run pytest tests/architecture` | 新模块必须受结构/依赖门禁覆盖 |
| frontend/ | 已运行同提交 API 时执行 `pnpm openapi:check` | HTTP 变化时核对运行时契约与生成客户端；不新增校验脚本 |
| frontend/ | 锁定包管理器执行 `typecheck`、`build`；`test:e2e` 选择 incremental_windows.spec.ts | 在 042 定义脚本；仅在适用 UI 范围执行，不冒充已存在 |
| 对应业务/验证环境 | 跟踪最早欠账和预算延期；回退保留窗口及当前有效水位，不对未确认尾段标完成。 | 真实来源、B0/Q0、故障、恢复或用户证据按本项范围执行 |

通用静态检查和基础测试入口已存在；本项专用测试、契约漂移及 E2E 入口须按实际切片补齐。本文件列出验证要求，不宣称命令已运行。若某项纯人工或无对应界面，G2 写出不适用理由和替代证据；不能删掉必要质量要求来让结果通过。涉及持久化/消息用真实 PostgreSQL/Redis/Kafka，涉及文件证据用获授权隔离 MinIO；不复用生产卷或在真实平台压测。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-23 | v1.4 | 建立 proposed 同编号 Design 并链接；范围状态与 Job/内容分责已准备，S00 门禁、真实来源默认值和产品 AC 均未通过。 |
| 2026-09-23 | v1.5 | 关闭 S00 G0—G2，冻结无真实来源默认值的内部样本、职责与门禁；S01—S04、真实来源和产品 AC 仍待执行。 |
| 2026-09-23 | v1.6 | S01 来源无关的持久窗口、半批缺口与重叠观察技术切片通过；S02—S04、真实来源/API/UI、完整回退和产品 AC 仍待执行。 |
| 2026-09-23 | v1.7 | 完成 S02 时间标记子片的失败验证、服务投影、生成客户端与受控页面；游标、通知和产品 AC 仍待执行。 |
| 2026-09-23 | v1.8 | 补记既有游标内部片及 002 显式时间窗离线接口；真实多页、终点、通知与产品 AC 仍待验证。 |

文档复核（2026-09-21）：同步现有工程基线、实际检查入口和 App Router 路径；需求 ID、优先级及必要 AC 不变，未补记产品通过证据。
