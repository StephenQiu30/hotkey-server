---
layer: Plan
scope: shared
doc_no: "037"
title: 费用与资源约束计划
status: in_progress
version: v1.15
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/037-费用与资源约束计划.md
design: docs/design/037-费用与资源约束设计.md
prd: docs/prd/037-费用与资源约束.md
source_requirement: NFR-001-011
backlog: BACKLOG.md
priority: P0
milestone: M5
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。本文状态与验收进度不变；Kafka 重试、消费位点与 Redis 失效场景需在实现前补齐设计。

# 费用与资源约束执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

日期：2026-09-17。状态：`in_progress`；S00—S02 完成，S03a 预算窗口快照已通过；X User Read 显式报价已实现并通过本地单元/静态验证，真实 PostgreSQL 集成回归待本次隔离 CI，产品 AC 仍为 0/6。X 付费准入和 S03/S04 完整能力仍未完成。来源：[037 PRD](../prd/037-费用与资源约束.md)；设计：[037 Design](../design/037-费用与资源约束设计.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

## 1. 交付目标与当前差距

完成 NFR-001-011 的实现和证据闭环，保持 P0。2026-09-23 用户把 X 改为官方按量 API；本计划新增唯一 X 付费例外门禁，必须在 002/005 真实请求前完成控制台账期硬上限、本地最坏费用预留与实际资源核算。当前免费策略与数据库约束仍拒绝 `paid`，不能因用户选型直接放行。当前已有 API/Worker/CLI 与 Web 工程底座、依赖锁和基础 CI；S00 已冻结 S01 的免费组件与全尝试计量契约，完整业务能力及验收仍未交付。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把工程存在当作本项已验收。本文未建立 Acceptance。

2026-09-23 离线接口进展：002 S01a 已在纯来源适配器去掉额外 User 展开，并要求每次请求前授权最大 Post 返回数、请求结束后报告校验通过的实际 Post 数或未知。037 的整数美元微单位、持久窗口及网络请求/金额双预算原子预留已通过隔离测试；唯一 paid 例外、账期硬上限核验、费率快照及付费尝试账本与双预算原子装配仍未实现，`CHK-037-G3/G4-003` 未通过，数据库拒绝 `paid` 的约束保持不变。正式装配前需按控制台当期费率和用户批准的账期上限冻结预算输入，不以此模拟接口替代真实费用门禁。

同日补充双预算幂等边界：相同预留 ID 与总金额可对应不同单价/页容量；X 金额预留要求显式匹配报价，并把报价纳入既有预留指纹。不同报价重放拒绝冲突，非 X 预算维持原指纹；仅修补离线账本，不扩大付费执行权限。

2026-09-25 X User Read 离线金额报价子片：官方定价页按返回 User 资源计费并提示以 Developer Console 当前费率为准；实现不采纳任何默认价格。`XApiUserReadCost` 显式接收一 User 单价，未知结果按最大金额保守结算，并复用网络请求/金额双预留和报价幂等指纹。该增量不解除 `paid` 拒绝、不创建请求尝试账本、不读取 X 凭据或发出网络请求。

建议责任角色：**后端＋来源接入＋运维**，实际执行人于开工时登记；从 M1 开始准备/基础接入，归属里程碑 **M5：首版质量验收与交付**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。它包含第 4 节规格、S01—S03 具体切片、S04 的原始 AC 证据，以及可执行的停止/回退说明。PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

同编号 [Design](../design/037-费用与资源约束设计.md) v1.6 已接受 S00—S02、X 离线账本子片及 S03a 预算窗口只读快照。已建立免费组件门禁、持久尝试账本和分层预算预留契约；不解释为已批准业务数值、实采或全部产品验收。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| [042 容量与部署可重复性](042-容量与部署可重复性计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已由 `09e0ca69` 提供依赖锁、本地拓扑和验证入口 |

以上是**切片依赖**。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

`OPEN-037-001` 已对 S01—S02 的机制部分关闭：一次实际网络/分析尝试计 1，核心只允许 `local`/`zero_price`；预算由调用方明示提供 limit/window/anchor，不猜测业务默认值。实际来源配额、分析时长和存储阈值仍待 S03 及适配器实验冻结。已确定技术栈沿用 AGENTS，不新增平台、付费依赖或框架。

## 3. 预计变更文件与归属

以下均为**拟建/拟改路径**，不是现存实现。只在切片实际开始时创建必要文件；先登记新领域职责和结构检查，再加入代码。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| backend/app/jobs/models.py、schemas.py、services.py、execution.py | S01 组件政策与尝试账本；S02 才加入预留，S03 处理资源公平 |
| backend/tests/unit/test_resource_budgets.py | 规则与状态的针对性失败/通过验证 |
| backend/tests/integration/test_resource_budgets.py | 适用的真实 PostgreSQL/Redis/Kafka、权限、幂等或边界验证；纯前端/人工项写明适用范围 |
| frontend/tests/e2e/resource_budgets.spec.ts（仅存在相关界面时） | 验证受影响业务页面；不为质量项额外建业务页面 |
| backend/app/api/router.py、dependencies.py；运行时 /openapi.json（自动生成）；frontend/src/api/ | 若 HTTP 契约变化，注册路由及唯一生成链；无变化则记录不适用原因 |
| backend/database/schema.sql；根 Compose 和配置样例 | 只在确需数据/配置变化时修改；SQL 与 ORM 同批更新，禁止覆盖用户配置或删除持久卷 |
| docs/design/037-费用与资源约束设计.md；对应 Acceptance（真实验证后创建） | 开工前设计与交付后证据，使用同编号，不预填通过 |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-037-API-001 | S01 不增加 HTTP；预算读改接口在 S02—S03 结合权限与实际界面再冻结，不预建空 API。 |
| SPEC-037-DATA-001 | S01 持久化组件费用类别和每次实际尝试，重启不清零；失败/过滤/空结果均保留计数。S02 增加窗口、预留和释放。 |
| SPEC-037-JOB-001 | S01 在请求或分析前验证免费策略并写入尝试账本，禁止付费兜底。S02 才实现最严格上限预留、并发防超发与耗尽延期。 |
| SPEC-037-UI-001 | 改进本质量要求影响的实际业务页面、状态或运维记录，不默认新建一个独立质量功能页面；无界面变化时以书面理由说明该 UI 分支不适用，不能跳过实际受影响界面。 |
| SPEC-037-SEC-001 | 先检查当前身份及资源权限；配置版本不能绕过撤权、删除、停用和更严格预算。秘密不可回读，正文与外部输入只作数据。关键变更留操作者、对象、时间与版本。 |
| SPEC-037-OBS-001 | 对比组件真实网络尝试与计量记录，不能用保存条数抵扣；显示自建资源成本与配置变更。 |
| SPEC-037-OPS-001 | 在隔离依赖验证后渐进启用本能力；保持历史资料可读。配置缺失或外部条件不满足时明确拒绝/延期。变更前评估数据兼容与回退，停止新增操作优先于破坏性迁移。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-037-S00-T01 | 阅读 PRD 与关联设计，关闭本期必要未决项，补同编号 Design；明确未纳入本期的分支 | 需求/设计评审记录、边界与风险 |
| TASK-037-S00-T02 | 核查先行切片，登记文件职责与主责，冻结 SPEC、样本、接口及测试命令 | 可执行切片清单、隔离资源和验证清单 |

- [x] `CHK-037-G0-001` → AC-037-001、AC-037-002、AC-037-003、AC-037-004、AC-037-005、AC-037-006；`EV-037-001`：PRD v1.3 与 Design v1.0 冻结 S00—S01；数值配额、并发与存储问题保留给后续切片。
- [x] `CHK-037-G1-001` → AC-037-001、AC-037-002、AC-037-003、AC-037-004、AC-037-005、AC-037-006；`EV-037-002`：042 S01 证据为 `09e0ca69`；复用现有本地服务和 `hotkey_dev`，不新建环境；尚无真实来源/分析适配器，不宣称实采。
- [x] `CHK-037-G2-001` → AC-037-001、AC-037-002、AC-037-003、AC-037-004、AC-037-005、AC-037-006；`EV-037-003`：文件限定为 `jobs` 的 Model/Schema/Service/execution helper、`schema.sql`、单元和真实 PostgreSQL 集成测试；HTTP/UI/Worker/Redis/Kafka/MinIO 不属于 S01。

### S01 免费组件与全消耗计量

前置：S00 完成。本切片覆盖 NFR-037-001、NFR-037-002；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-037-S01-T01 | Red：以 AC-037-002、AC-037-003 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-037-S01-T02 | Green：先关闭付费配置，建立包含 SDK 内部请求的计量钩子及冻结样本。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-037-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-037-G3-001` → AC-037-002、AC-037-003；`EV-037-004`：冻结单元/集成输入后执行 `uv run pytest tests/unit/test_resource_budgets.py -q`，收集期因 `jobs.execution.resource_attempt_id` 不存在失败（exit 2），证明契约缺失，不是环境未启动。
- [x] `CHK-037-G4-001` → AC-037-002、AC-037-003；`EV-037-007`：在现有本地 PostgreSQL 18.4 的空 `hotkey_dev` 上用完整 `schema.sql` 建立 13 表；目标 12 tests、后端全量 75 tests、Ruff format/check 与 mypy 全通过；`/api/ready` 200，回归后数据库 0 行，Redis `hotkey:test:*`、Kafka `hotkey.tests.*`、MinIO `tests/lifecycle/` 残留均为 0。S01 无 HTTP/UI/Worker 变更，不适用浏览器/OpenAPI 验证。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S02 并发预留与耗尽延期

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 NFR-037-003、NFR-037-004；涉及的完整规则以对应 PRD 为准。

2026-09-21 冻结记录：Design v1.1 接受 S02，采用 PostgreSQL 持久窗口、固定顺序行锁、全局默认拒绝、累计额度与可回收并发槽位；不设业务默认值，不增 HTTP/UI/Worker 或 Redis 权威账本。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-037-S02-T01 | Red：以 AC-037-004、AC-037-005 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-037-S02-T02 | Green：测试最后一次额度竞争、窗口跨越和重启；为采集提供有界预算接口。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-037-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [x] `CHK-037-G3-002` → AC-037-004、AC-037-005；`EV-037-005`：冻结分层窗口、预留/结算、延期与重启输入后执行 `uv run pytest tests/unit/test_resource_budgets.py -q`，收集期因 `jobs.schemas.BudgetContext` 不存在失败（exit 2），证明预算契约缺失，不是环境未启动。
- [x] `CHK-037-G4-002` → AC-037-004、AC-037-005；`EV-037-008`：Design v1.1 下完成 PostgreSQL 分层窗口、固定顺序行锁、原子预留、幂等结算/释放、缺全局配额默认拒绝及 `next_window|capacity_release` 恢复条件；目标 23 tests、后端全量 86 tests、Ruff format/check 与 mypy 全通过。复用现有本地 PostgreSQL 18.4，用完整 `schema.sql` 建立 16 表；两事务竞争最后额度最多一个成功，重启不恢复同窗口额度，跨窗口恢复。`/api/ready` 返回 200；回归后数据库 0 行，Redis `hotkey:test:*`、Kafka `hotkey.tests.*`、MinIO `tests/lifecycle/` 残留均为 0。S02 无 HTTP/OpenAPI/UI/Worker 变更，浏览器与客户端生成检查 N/A。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S03 读取保留及成本可见

X 官方 API 离线金额账本先行子片：复用 S02 的窗口与预留服务，仅增加 `x_api_usd_micros` 度量和显式单价×本页最大/实际 Post 数的整数计算；来源必须为 `x`。在一次性空 PostgreSQL QA 库按完整 Schema 验证，没有全局金额预算默认拒绝、最后额度竞争、正常/未知结算及调低额度。此子片不放宽 `paid` 核心组件、不设置业务默认金额、不启用 X 请求；控制台账期上限、费率快照和请求/费用原子装配仍待后续 037/002 S01b 完成。

- [x] `CHK-037-G3-X01`：首次执行预算单元/集成测试因 `jobs.schemas.XApiPostReadCost` 不存在而收集失败；随后在旧 QA Schema 上新增来源约束断言先失败，证明必须使用更新后的完整 Schema。显式价格、未知结果与来源边界均有失败优先样本。
- [x] `CHK-037-G4-X01`：`x_api_usd_micros` 与整数费用计算进入 Schema/ORM/服务；现有预算服务在隔离 PostgreSQL 中验证缺全局政策拒绝、并发最后额度仅一个成功、实际数释放、未知数全额结算、调低额度及数据库来源约束。受控 X 两页回调联验通过；预算单元 14 项、集成 19 项、后端全量 401 passed/5 skipped，Ruff check/format 与 mypy 通过。5 项跳过均需显式启用隔离浏览器服务器；本片无 HTTP/UI/Worker 变更，相关验证不适用。QA 库及既有测试库验后均为 0 行，一次性 QA 库已删除并确认不存在。不勾产品 AC，不启用真实 X。

X 离线双预算预留子片：同一 `jobs` 事务先锁定请求次数，再预留本页最坏金额；金额不足或政策缺失时回滚请求预算和本次窗口。成对重放保持幂等，单边旧预留及已结算 ID 拒绝重新授权；真实 X 请求和 `paid` 尝试仍禁止。

- [x] `CHK-037-G3-X02`：在一次性空 PostgreSQL QA 库先运行双预算场景，因 `reserve_x_api_request_budgets_in_transaction` 缺失而两项失败；固定合成价格、请求/金额政策、同一操作 ID 与预留 ID。
- [x] `CHK-037-G4-X02`：金额不足、缺政策、并发争抢最后金额、单边旧预留拒绝、幂等重放和结算后拒绝均通过；受控 X 两页回调同时预留/结算请求与金额。预算集成 23 项、后端全量 405 passed/5 skipped，Ruff check/format 与 mypy 通过；5 项跳过需要显式启用隔离浏览器服务器。无 HTTP/UI/Worker/DDL 变化，相关门禁不适用；QA 库验证后删除。不勾产品 AC，不发真实 X 请求。

X 报价幂等离线子片：不增加默认价格或表结构；相同总金额但不同单价/页容量不能借用旧预留。付费门禁、账期上限和真实调用仍保留在 S03/S04 未完成范围。

- [x] `CHK-037-G3-X03`：在现有 PostgreSQL 的独立空 QA 库，固定 10×5,000 与 5×10,000 微美元两份同额报价，以同一操作/预留 ID 重放；现有服务未抛 `BudgetReservationConflictError`，用例因 `DID NOT RAISE` 失败。此前旧测试库缺少现行 Schema 的 `coverage_windows`，该环境失败不计为 Red 证据。
- [x] `CHK-037-G4-X03`：要求 X 金额预留携带与请求金额一致的报价，报价参与既有幂等指纹；不同报价重放冲突，非 X 预算不接受报价且指纹不变。隔离 PostgreSQL 预算目标测试 41 passed、后端全量 426 passed/5 skipped，Ruff check/format 与 mypy 通过；5 项跳过需要显式启用隔离浏览器服务器。本片未改 DDL/HTTP/UI/Worker，未访问 X；QA 库验后删除。不勾产品 AC。

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 NFR-037-005、NFR-037-006；涉及的完整规则以对应 PRD 为准。

#### X User Read 单资源费用报价（离线子片）

2026-09-25 先新增回归用例并确认 `XApiUserReadCost` 尚不存在，目标文件因 import error 收集失败；固定一请求、最多一 User、显式单价 12,345 微美元（合成值）、成功/零结果/未知结算及同额跨资源重放输入。实现复用已有 X 双预算 API，不新增默认费率/DDL/运行入口。数据库集成验证交由仓库隔离 PostgreSQL CI；本机预算 fixture 会 TRUNCATE 整库表，因此不得指向用户的 `hotkey-server` 开发数据库。

- [x] `CHK-037-G3-X04`：新单测因 `jobs.schemas.XApiUserReadCost` 不存在而在收集期失败；失败反映单 User 资源报价契约缺失。官方定价资料另证 User Read 按返回资源计费且 Developer Console 为实际费率来源，冻结样本使用合成费率，不把页面价当作测试默认值。
- [ ] `CHK-037-G4-X04`：新增显式单 User 报价 DTO，验证 0/1/未知结果结算、非正/缺失费率拒绝、X 来源/金额吻合、保留网络+金额原子预留及与 Post 报价同额重放冲突。本地单测与静态门禁通过；隔离 PostgreSQL 集成回归在 `backend` CI 中验证后再关闭此项。不包含付费尝试账本、控制台硬上限、真实费率采集或 X 请求，故 `CHK-037-G3/G4-003` 和产品 AC 仍未关闭。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-037-S03-T01 | Red：以 AC-037-006 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-037-S03-T02 | Green：限制回填和昂贵分析，降低上限即时停止新增超额工作且不删已有资料。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-037-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

#### S03a 预算窗口只读快照（来源无关内部子片）

仅将已持久化的预算政策与当前窗口计数投影为 owner 过滤的服务读模型；读取不得新建窗口、预留或尝试。关闭政策仍可见但不准入；未来生效政策显示 `next_window_at` 且不显示为当前可运行额度；缺少政策返回空集合，不伪造零消耗。累计额度的剩余量按 `max(0, limit-used-reserved)` 计算，并发槽位按 `max(0, limit-reserved)` 计算。当前 `identity_users` singleton 约束只允许创建一个持久身份，本子片验证不同 owner ID 不得读取数据，不构造第二 owner 行，也不声称完成双 owner 行夹具隔离。该子片仅证明预算单位快照，不代表实耗成本、物理磁盘/CPU 容量或产品 AC。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-037-S03a-T01 | 冻结只读 DTO、窗口推导、owner 过滤及不读写物理容量的边界。 | Design v1.6 与本子片 SPEC；复用现有 ORM/服务，不增表、依赖、API 或 UI。 |
| TASK-037-S03a-T02 | Red/Green：覆盖累计/并发剩余量、未来锚点、禁用政策、owner 过滤及读取无副作用。 | 纯规则与真实 PostgreSQL 证据；缩额行为使用已消费窗口夹具；仍保留完整 AC-037-006 未通过。 |

- [x] `CHK-037-G2-003`：`EV-037-018` 已冻结 owner 过滤、只读窗口快照、累计/并发公式与物理容量边界。
- [x] `CHK-037-G3-004`：`EV-037-019` 在一次性空 PostgreSQL QA 库执行新集成测试；业务夹具成功写入后，测试因 `ResourceBudgetService.budget_usage_snapshot` 不存在而失败（`AttributeError`），证明缺少服务能力而非测试环境失效。
- [x] `CHK-037-G4-004`：`EV-037-020` 增加冻结 DTO 与只读服务投影，并与预留共用窗口边界算法；预算单元/集成目标 42 passed，后端全量 501 passed/5 skipped，Ruff check/format 与全局 mypy 通过。5 项 skip 均需显式启用隔离浏览器服务，与本切片无关；2 项上游依赖弃用警告。快照测试验证累计/并发剩余量、缩额、未来与禁用政策、owner 过滤及读取不增预算窗口/预留。无 DDL/API/UI/Worker/依赖变化；一次性 QA 库已删除并确认不存在。仅为技术子片证据，AC-037-006 与产品 0/6 未通过。

- [ ] `CHK-037-G3-003` → AC-037-006；任务 TASK-037-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-037-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-037-G4-003` → AC-037-006；任务 TASK-037-S03-T02、TASK-037-S03-T03：切片闭环与必要回归通过；预期证据 `EV-037-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-037-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-037-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-037-G5-001` → AC-037-001；**总纲场景继承**：Given 预算接近耗尽且有分页、重试和最终被过滤的结果；When 继续执行；Then 被过滤内容产生的请求与分析调用仍计入预算；停止原因明确，不自动购买或切收费服务。预期证据 `EV-037-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-037-G5-002` → AC-037-002；**免费路径可运行**：Given 全部付费配置关闭且非 X 免费来源访问条件满足；When 完成采集、查看与基础分析并检查 X 状态；Then 不调用付费服务，X 明确未就绪而非暗中兜底；X 付费路径另需控制台与本地预算证据；预期证据 `EV-037-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-037-G5-003` → AC-037-003；**无输出仍计量**：Given 初始化及多页结果均被过滤，且发生失败重试；When 查看预算使用记录；Then 每次实际尝试都有计数，使用量不因结果为零而减少；预期证据 `EV-037-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-037-G5-004` → AC-037-004；**并发最后额度**：Given 两个任务同时争取仅剩的一次请求预算；When 并发启动；Then 最多一个获得额度，另一个延期，无预算超发；预期证据 `EV-037-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-037-G5-005` → AC-037-005；**重启与窗口**：Given 预算耗尽但任务未完成；When 重启、重复提交或切换主题；Then 额度不被刷新，继续延期，显示真实重置条件与进度；预期证据 `EV-037-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-037-G5-006` → AC-037-006；**存储紧张与调低上限**：Given 媒体回填消耗较大且维护者降低预算；When 继续浏览和调度；Then 核心读取与必要清理保留资源，低优先工作延期；配置变化和成本边界可见；预期证据 `EV-037-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-037-G6-001` → AC-037-001、AC-037-002、AC-037-003、AC-037-004、AC-037-005、AC-037-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-037-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-037-G6-002` → AC-037-001、AC-037-002、AC-037-003、AC-037-004、AC-037-005、AC-037-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-037-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| NFR-037-001 | 零付费核心路径 | TASK-037-S01-T02 | AC-037-002 | CHK-037-G4-001 |
| NFR-037-002 | 全消耗计数 | TASK-037-S01-T02 | AC-037-003 | CHK-037-G4-001 |
| NFR-037-003 | 分层预留与并发 | TASK-037-S02-T02 | AC-037-004 | CHK-037-G4-002 |
| NFR-037-004 | 超限有界延期 | TASK-037-S02-T02 | AC-037-005 | CHK-037-G4-002 |
| NFR-037-005 | 资源与公平 | TASK-037-S03-T02 | AC-037-006 | CHK-037-G4-003 |
| NFR-037-006 | 成本与变更透明 | TASK-037-S03-T02 | AC-037-006 | CHK-037-G4-003 |

父级综合场景 AC-037-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| backend/ | `uv run ruff check .`；`uv run ruff format --check .`；`uv run mypy` | 既定静态与边界要求，依赖须先由 042 锁定安装 |
| backend/ | `uv run pytest tests/unit/test_resource_budgets.py` | 本切片规则验证 |
| backend/ | `uv run pytest tests/integration/test_resource_budgets.py` | 隔离的真实依赖与边界验证 |
| backend/ | `uv run pytest tests/architecture` | 新模块必须受结构/依赖门禁覆盖 |
| 根目录 | `pnpm openapi:check` | 仅当 HTTP 变化时检查运行契约与生成客户端；S01/S02 无 HTTP 变化，N/A |
| frontend/ | `pnpm typecheck`、`pnpm build`与适用 E2E | 仅在 UI 范围执行；S01/S02 无 UI 变化，N/A |
| 对应业务/验证环境 | 对比组件真实网络尝试与计量记录，不能用保存条数抵扣；显示自建资源成本与配置变更。 | 真实来源、B0/Q0、故障、恢复或用户证据按本项范围执行 |

通用静态检查和基础测试入口已存在；本项专用测试、契约漂移及 E2E 入口须按实际切片补齐。本文件列出验证要求，不宣称命令已运行。若某项纯人工或无对应界面，G2 写出不适用理由和替代证据；不能删掉必要质量要求来让结果通过。涉及持久化/消息用真实 PostgreSQL/Redis/Kafka，涉及文件证据用获授权隔离 MinIO；不复用生产卷或在真实平台压测。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-21 | v1.4 | 完成 S00：接受 Design v1.0，冻结 S01 的免费组件门禁、全尝试账本、目录与验证边界。 |
| 2026-09-21 | v1.5 | 完成 S01：实现免费组件政策、副作用前计量、幂等重放、终态与汇总；产品 AC 仍为 0/6。 |
| 2026-09-21 | v1.6 | 进入 S02：Design v1.1 冻结分层窗口、原子预留、结算/释放与耗尽决策，实现和证据待 Red/Green 闭环。 |
| 2026-09-21 | v1.7 | 完成 S02：在现有 PostgreSQL 环境验证分层窗口、原子预留、结算/释放、最后额度竞争、重启与跨窗口语义；产品 AC 仍为 0/6。 |
| 2026-09-23 | v1.9 | 登记 002 S01a 的离线最坏/实际 Post 数回调；唯一 paid 例外、持久美元预算与控制台上限尚未实施。 |
| 2026-09-23 | v1.10 | 完成 X 离线整数美元金额账本子片的红绿验证；真实 paid 门禁、控制台账期及 S03/S04 产品验收仍未完成。 |
| 2026-09-23 | v1.11 | 完成 X 网络请求/金额双预算原子预留的离线红绿验证；付费尝试账本与双预算尚未同事务，S03/S04 及产品验收仍未完成。 |
| 2026-09-23 | v1.12 | 完成 X 金额报价进入幂等指纹的离线子片；未放行付费调用。 |
| 2026-09-23 | v1.13 | 接受 037 Design v1.6 的 S03a 只读预算窗口快照；测试与实现结果待回填，产品 AC 仍为 0/6。 |
| 2026-09-23 | v1.14 | S03a 快照实现并通过真实 PostgreSQL 与后端全量回归；5 个隔离浏览器用例仍跳过，产品 AC 仍为 0/6。 |
| 2026-09-25 | v1.15 | 接受 Design v1.7 并实现 X User Read 单资源报价与双预算离线子片；360 项 unit/architecture、Ruff、format、mypy 通过；隔离 PostgreSQL 回归待 CI。不固化价格、不解锁 paid、不发真实 X 请求；产品 0/6 AC 仍开放。 |

文档复核（2026-09-21）：同步现有工程基线、实际检查入口和 App Router 路径；需求 ID、优先级及必要 AC 不变，未补记产品通过证据。
