---
layer: Plan
doc_no: "09"
audience:
  - Tech-Lead
  - Dev
  - QA
feature_area: "area:report"
purpose: "实现 Qwen 中文总结、时间线、影响分析和日报生成。"
canonical_path: "docs/plans/09-AI总结与中文日报实现计划.md"
status: draft
version: "1.0.0"
owner: "StephenQiu30"
inputs:
  - "docs/product/prd/09-AI总结与中文日报PRD.md"
outputs:
  - "AI总结与中文日报实现任务"
triggers:
  - "日报内容或 AI provider 变化"
downstream:
  - "由 WORKFLOW.md 指定的 Symphony / Linear 流程接管"
---

# 09-AI总结与中文日报实现计划

## 1. 目标

基于热点和来源证据生成中文摘要、时间线、影响分析和 daily_reports。

## 2. 文件清单

- 创建：`migrations/000009_reports_ai_summaries.up.sql`
- 创建：`internal/service/report/`
- 创建：`internal/platform/dashscope/qwen.go`
- 创建：`internal/repository/postgres/reportrepo/`
- 创建：`internal/worker/handlers/report/`
- 创建：`internal/transport/http/handlers/report/`

## 3. 任务拆解

1. 创建 `ai_summaries`、`daily_reports`。
2. 实现 Qwen client interface 和 mock。
3. 实现 prompt 模板，要求中文和来源引用。
4. 实现频道日报和用户个性化日报生成。
5. 证据不足时降级为摘要和来源列表。

## 4. TDD 与验证

- mock Qwen 生成中文日报。
- 证据不足不输出影响分析。
- DashScope 未配置返回 `failed_config`。

## 5. 执行顺序

1. `test:` report service 和 Qwen mock 失败测试。
2. `impl:` migration、repo、service、worker、handler。
3. `refactor:` prompt version 管理。

## 6. 回滚策略

回滚 reports migration，停用 generate_report worker。

## 7. 验收命令

```bash
go test ./...
python3 -m unittest discover -s tests
```

## 8. Symphony / Linear 要求

任务状态、标签和流转规则完全以本仓库 `WORKFLOW.md` 和本地 Symphony 实现为准。Plan 不定义额外状态、不发明额外标签、不覆盖 Symphony 的状态机。

Linear issue 只承载任务内容：PRD 路径、Plan 路径、任务范围、禁止范围、TDD 验收命令和回写要求。Symphony 负责监听 active states、创建 workspace、运行 Codex，并按 `WORKFLOW.md` prompt 驱动执行。

## 9. 变更记录

| 日期 | 作者 | 版本 | 变更说明 |
| --- | --- | --- | --- |
| 2026-05-31 | StephenQiu30 | 1.0.0 | 初版 |
