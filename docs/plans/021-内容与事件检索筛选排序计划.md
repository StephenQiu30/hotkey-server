---
layer: Plan
scope: shared
doc_no: "021"
title: 内容与事件检索筛选排序计划
status: planned
version: v1.0
owner: HotKey Team
phase: P0
canonical_path: docs/plans/021-内容与事件检索筛选排序计划.md
design: docs/design/021-内容与事件检索筛选排序设计.md
prd: docs/prd/021-内容与事件检索筛选排序.md
---

# 内容与事件检索筛选排序计划

## 前置门禁

- Design 已 accepted，PRD 已 approved，依赖编号达到所需状态。
- 先建立当前实现清单和可复现差距；行为变化保存失败测试。
- UI 开发前确认真实 OpenAPI 能力，禁止用 Mock 掩盖缺失后端。

## 预计变更范围

- backend/internal/modules/ingestion/
- backend/internal/modules/event/
- backend/db/schema.sql
- frontend/src/app/dashboard/contents/page.tsx
- frontend/src/app/dashboard/events/page.tsx
- frontend/src/components/dashboard/CursorPagination.tsx

## 执行步骤

1. 将 AC-021-* 转为领域、应用、Transport、前端组件和浏览器场景测试。
2. 在唯一 Schema 中做最小、向前兼容的数据变化并完成仓储实现。
3. 按模块依赖实现用例、持久任务、权限、审计、指标和错误映射。
4. 更新 Swaggo 注解并生成 OpenAPI，再生成前端 Client 和验证漂移。
5. 以 shadcn/ui 官方组件组合完成页面，不重复实现基础控件。
6. 验证并发、幂等、降级、配额、移动端、可访问性和完整用户路径。
7. 新增同编号 Acceptance；部署或人工流程需要长期执行时新增 Operations。

## 验证

- `cd backend && make ci`
- `cd frontend && npm run openapi:check && npm run typecheck && npm run test:unit && npm run build`
- 启动完整环境，以浏览器验证登录→监控→采集→事件→告警→报告主故事。
- 根目录运行开发/生产 Compose 配置检查及 `git diff --check`。

## 迁移与回滚

新行为通过配置或 capability 逐步启用。回滚先关闭新入口和任务生产者，等待/取消可重试任务，再回退应用；保留事实、审计、投递和模型运行记录。

## 依赖与功能切片

- 前置编号：018、020。
- 依赖未验收时，本条只允许完成不改变对外行为的基础工作。

- [ ] Slice-021-1：实现「全局关键词搜索」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-021-* 回归。
- [ ] Slice-021-2：实现「内容/事件多条件筛选」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-021-* 回归。
- [ ] Slice-021-3：实现「热度、时间、相关性、重要性排序」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-021-* 回归。
- [ ] Slice-021-4：实现「游标分页、URL 状态和空结果建议」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-021-* 回归。

## 完成定义

- 跨页无重复或遗漏
- 刷新页面保留查询状态
- 权限过滤在数据库查询前生效而非仅前端隐藏
