---
layer: Plan
scope: shared
doc_no: "007"
title: 热点事件与评论知识库计划
status: in_progress
version: v1.2
owner: HotKey Team
canonical_path: docs/plans/007-热点事件与评论知识库计划.md
design: docs/design/007-热点事件与评论知识库设计.md
prd: docs/prd/007-热点事件与评论知识库.md
---

# 热点事件与评论知识库计划

2026-09-16｜用户已授权持续分片实施。S00工程门禁、S01首轮有界探测、S02配置/页事务/采集账本/预算/调度/运行可见性、S03有界两页/两父级评论链与取消/重试/原子结算、S04人工事件/趋势/提醒/冻结样本、S05精确与语义检索/受控问答，以及S06删除清单、数据库备份恢复POC和旧应用隔离快照回滚演练均已有合成或隔离环境证据。现有MinIO已在主机和产品后端镜像的Docker bridge中完成真实隔离对象的幂等写入、完整读回和全版本删除；当前bucket是未版本化/无对象锁/无生命周期规则，服务端自动过期与未完multipart中止未就绪，公开端点TLS握手与远程生产网络也未验收。B站个人非商业学习用途已确认，四操作单次公开探测均成功；真实产品canary使用临时MinIO完成搜索/正文入库与hash读回，评论随后返回`access_denied`并停止，现有MinIO当时未运行，所以生产准入、真实评论链、真实内容模型盲测和七日观察仍未完成。X/微博需要授权，小红书/抖音未验证；完整G2、EV-007-003至EV-007-011与P0验收保持未通过。没有采购付费内容。

## 1. 当前差距与承接

保留当前身份/草稿/诊断任务/Outbox/生成契约/Compose 基础与未提交来源切片；不重建仓库、不清理用户改动。下一轮先审阅这些工作区变化，再增加007代码，不将其混同为本计划已交付。

| 006事项 | 007承接 | 实施判断 |
|---|---|---|
| 来源实测、Bluesky有界适配器 | S01 | 按用户新选择优先X免费发现、B站/微博/小红书/抖音评论；Bluesky保留作补充，不以其成功替代X |
| Python/身份/草稿/诊断基础 | S02复用 | 不重写；诊断任务kind/result仍不足以接采集 |
| 页持久化、内容版本、评论和恢复 | S02–S03 | 第一条真实路径比一次铺满所有平台更优先 |
| 事件/分析/报告/提醒 | S04–S05 | 先人工事件、可复现样本，后混合检索与问答 |
| 删除、迁移、恢复、连续观察 | 每片约束 + S06综合验证 | 不等到最后才加入权限、幂等和删除设计 |

## 2. 门禁与交付顺序

- **G0 范围/事实/目录**：007三文档追踪一致；每片先列文件位置、模块依赖、技术选择和待确认项。单owner；X等信息平台发现、四类国内评论来源、内容零付费、复用现有MinIO为用户已确定范围，不默认具备平台账号授权。
- **G1 数据/契约**：明确每片表约束、DTO、任务白名单、预算、权限及迁移影响；生产者与消费者兼容。
- **G2 来源**：目标来源逐操作真实小样本，保存成功、失败、分页/用途/成本证据。可以先开发通用代码，但没有G2不得声称完成真实采集链。
- **G3 测试先行**：本片关键行为先有失败测试，再实现与重构；模型/性能阈值先定义评估口径。
- **G4 功能**：真实数据库/消息、API生成契约和浏览器主链通过。
- **G5 可靠性/分析**：崩溃、权限/删除、预算、样本、引用与恢复实验通过。
- **G6 完整交付**：国内外真实覆盖、七日观察、备份恢复、人工使用验收；记录限制后建立007 Acceptance。

### 垂直切片

| 阶段 | 任务 | 可交付结果与停止条件 |
|---|---|---|
| S00 目录与选型门禁（已完成） | TASK-007-S00-T01：登记文件迁移/归属和切片选型；TASK-007-S00-T02：全源码目录扫描、依赖负向测试、前端分层检查；TASK-007-S00-T03：FastAPI自动Swagger/OpenAPI与UmiOpenAPI客户端门禁 | 本地隔离验收通过：全源码和负向边界检查覆盖新目录；运行时契约、发布快照和生成客户端一致；目录迁移后的原业务主流程通过浏览器复验 |
| S01 来源可行性 | TASK-007-S01-T01：B站/微博/小红书/抖音操作清单与评论候选探测；TASK-007-S01-T02：X免费/自建发现入口验证 | 信息发现与评论的职责分别验证；网页摘要不是完整X帖子；若免费X入口不成立保留缺口，不能切付费或悄悄以Bluesky代替 |
| S02 发现收件箱 | TASK-007-S02-T01：配置快照、启用/暂停和按slot调度；TASK-007-S02-T02：页执行/MinIO证据/内容/命中持久化；TASK-007-S02-T03：实际运行与收件箱UI | 先跑通已准入的信息来源，再扩展；复用现有MinIO，保存/观察/重试可复核，模型可缺席 |
| S03 评论追踪 | TASK-007-S03-T01：根评/回复游标、关系和增量；TASK-007-S03-T02：编辑/不可见/预算/取消/故障恢复 | 从重点帖到评论与父级上下文；恢复无重复、部分结果可解释；完成数据质量闭环 |
| S04 事件与观点 | TASK-007-S04-T01：人工事件、时间线、趋势和站内提醒；TASK-007-S04-T02：标注集、冻结样本、模型标签与引用 | 用户可组织事件、理解有证据的观点；模型不达标保留待分析，不发布无依据结论 |
| S05 知识与问答 | TASK-007-S05-T01：知识条目/词法/pgvector；TASK-007-S05-T02：受控统计、引用问答、更新/删除传播 | 可回答“发生了什么/评论如何变化/依据是什么”，有范围与版本，不把top-k当全量 |
| S06 运行验收 | TASK-007-S06-T01：升级/回滚/备份恢复与安全；TASK-007-S06-T02：七日真实观察、负载/成本校准、桌面/窄屏UAT | 全部P0验收有证据；缺国内入口、模型质量或恢复证据时如实保留未通过项 |

S00完成后，S01证据清单和无外部依赖的S02失败测试可并行推进。一个来源先通过可先交付局部能力，但X等信息发现与国内评论的完整门槛不变。暂不承诺日历工期：来源准入是外部依赖，每片完成后按实际吞吐与人员投入估算下一片。若需要排期，先将开放的账号/预算条件落定。

不实施项：整栈重写、复制鱼皮数据库/定时器、一次性支持所有来源、接入多个向量数据库、先建传播图谱、自动购买API、在本轮部署平台爬虫。

## 3. 预计改动位置与职责

完整目标树和依赖方向见[Design第13节](../design/007-热点事件与评论知识库设计.md#13-实现前目录规划与文件归属)。以下新文件是实施目标，不表示已存在。

| 路径 | 变化 |
|---|---|
| backend/src/monitors/{models,schemas,services}.py | 状态、不可变配置版本、主题命中关联、启停、版本冲突与历史草稿转换 |
| backend/src/sources/{schemas,services}.py、sources/adapters/*.py | 从Bluesky专属DTO演进到有版本的通用结果、能力/查询语义；适配器仍无ORM |
| backend/src/collection/{models,schemas,services,execution}.py | 新增运行/检查点、预算预留、页事务和后续任务；统一用例边界 |
| backend/src/contents/{models,schemas,services}.py | 新增内容身份、正文版本、指标观察和评论关系；主题命中表归monitors |
| backend/src/evidence/{models,schemas,contracts,services}.py、evidence/adapters/minio.py | raw_pages、对象键/hash/保留和删除；MinIO SDK只在适配器内，连接现有实例 |
| backend/src/ai/{schemas,contracts}.py、ai/adapters/ | 后续模型调用/embedding外部协议，无ORM和业务prompt；费用范围确认后才实现相应供应商 |
| backend/src/jobs/{models,contracts,execution}.py、worker/{app,messaging}.py | 新kind白名单、负载引用、超时策略；复用现有Outbox/fencing |
| backend/src/cli/commands.py、cli/sources.py | 保留显式探测，增加调度到期采集/恢复子命令；不得在预览中联网 |
| backend/src/events/、analysis/、knowledge/、notifications/ | 按S04/S05增加各模块models/schemas/services，不提前创建空壳服务 |
| backend/src/api/routers/、api/dependencies.py、db/metadata.py | 路由注入、元数据注册；API路由不操作ORM |
| backend/src/migrations/versions/ | 新revision；不改已应用0001–0003；升级/回填可对账 |
| frontend/src/app/、features/、api/、request.ts | 功能页放features，app负责装配，api为生成目录，request.ts为唯一Axios封装；不设置shared层 |
| docs/openapi/openapi.json、frontend/src/api/* | 前者从 FastAPI 应用自动导出，后者由 `@umijs/openapi` 从前者生成；两者均禁止手写 |
| backend/tests/{unit,integration,architecture}/、frontend/tests/ | 分别覆盖契约、页事务/消息故障、依赖边界、真实浏览器流程 |
| docker-compose.yml、docker-compose-prod.yml、backend/src/core/config.py | 接现有MinIO所需HOTKEY_S3_配置；不新建MinIO服务/持久卷；后续按需要加分析队列/pgvector |

### 3.1 现有文件迁移表

| 当前路径 | 目标路径/动作 | 执行时机与验证 |
|---|---|---|
| backend/src/sources/bluesky.py | backend/src/sources/adapters/bluesky.py | S00目录切片，更新sources/services、CLI、测试和静态扫描；探测行为/契约保持 |
| frontend/src/App.tsx | frontend/src/app/App.tsx | S00目录切片；main导入更新，原有会话/草稿/诊断流程浏览器复验 |
| frontend/src/Login.tsx | frontend/src/features/identity/Login.tsx | 同上，登录字段与交互保持 |
| frontend/src/MonitorEditor.tsx | frontend/src/features/monitors/MonitorEditor.tsx | 同上，来源标签/Monitor类型归功能目录 |
| frontend/src/client.ts | frontend/src/request.ts | 改为唯一 Axios 传输封装；不定义任何业务 URL、方法或 DTO |
| frontend/src/style.css | 保留为frontend/src/style.css | main直接导入，不创建shared样式目录 |
| frontend/src/api.generated.ts | frontend/src/api/ | 改由 `@umijs/openapi` 按 tag 生成端点函数和类型；业务代码仅调用生成方法，逐文件漂移检查禁止手写 |
| backend/src/migrations/versions/、docs/operations/evidence/006相关文件 | 保留 | 禁止因目录美观重写迁移或挪动既有证据 |

S00 已按本表移动来源适配器和前端文件，原路径不再保留副本。后续每次迁移继续先记录工作区差异并一次完成导入修正；目录变更的测试关注行为回归与依赖违规拦截。

### 3.2 S00 实施登记

| 类别 | 路径与职责 |
|---|---|
| 后端修改 | `backend/src/main.py` 配置自动Swagger/OpenAPI；`api/routers/*.py` 固定operationId；`tools/export_openapi.py` 从应用工厂确定性导出，不维护手写Schema |
| 后端移动/测试 | `sources/bluesky.py` 移到 `sources/adapters/bluesky.py`；architecture测试递归扫描全部Python并验证未知模块、路由/适配器违规依赖 |
| 前端移动 | `app/App.tsx` 负责装配；`features/identity`、`features/monitors` 放领域交互；`style.css` 保留根级全局样式；无shared目录 |
| 前端生成/传输 | `openapi2ts.config.ts` 配置 `@umijs/openapi`；`api/*` 为唯一端点客户端；根级 `request.ts` 封装Axios会话/XSRF/序列化/错误 |
| 前端门禁 | `scripts/check-contract.mjs` 在临时目录重新生成并逐文件比较；`check-boundaries.mjs` 与负向测试阻止未知层级和反向依赖 |
| 选型 | 保持React/Vite；采用Axios 1.20.0、@umijs/openapi 1.14.1。生成器只作为开发依赖并只读取仓库内自动导出的可信契约 |

`@umijs/openapi` 1.14.1 的返回类型解析只识别default/200/201。Job和collection_run在响应前已经持久化，因此资源创建契约统一使用HTTP 201，后续异步进度由资源状态表达；生成配置不改写响应码或Schema，不保留202兼容hook。

### 3.3 每片开始前的登记项

在该TASK下记录：目标与AC；新增/修改/移动/生成文件；表与事务所有者；允许依赖；所需组件/版本和部署影响；OPEN答复/评测结论；验证命令。若片内需要的新库尚未选定或用户费用/部署偏好未答复，先给出明确选项询问，只继续独立工作。

MinIO已确定复用现有实例。实施前取得非敏感endpoint/网络方式、bucket命名和私有凭据注入方式；连通性测试只使用隔离测试bucket/prefix，不修改现有实例配置、不清理既有对象。AI推理费用规则尚待明确，不能把内容采购零付费擅自解释为已经授权付费AI。

### 3.4 S01 来源可行性实施登记

本片对应 `TASK-007-S01-T01`、`TASK-007-S01-T02`、`AC-007-001` 和 `EV-007-001`。只交付来源能力清单、有界探测器及真实小样本结论，不创建采集任务、数据库来源状态或内容入库，不把技术可访问等同于允许长期保存/分析。

| 类别 | 文件与职责 |
|---|---|
| 来源契约 | 修改 `backend/src/sources/schemas.py`：定义平台、操作、技术支持、用途权限、流水线与探测结果；不含ORM或HTTP客户端 |
| 来源目录 | 修改 `backend/src/sources/services.py`：提供当前经过审阅的逐操作能力卡和零网络查询预览 |
| B站适配器 | 新增 `backend/src/sources/adapters/bilibili.py`：无Cookie、无重试、限制响应大小的一次搜索/详情/根评论/回复探测；只返回标准DTO |
| API与CLI | 修改 `backend/src/api/routers/sources.py`、`api/routers/monitors.py`、`cli/sources.py`：来源API归sources路由；CLI必须显式指定平台和操作，不保留隐式Bluesky入口 |
| 前端 | 新增 `frontend/src/features/sources/SourceCapabilities.tsx` 并修改App装配；只调用 `frontend/src/api/` 生成方法，生成文件由OpenAPI刷新 |
| 测试与证据 | 修改来源/API/浏览器测试；新增 `docs/operations/evidence/007/EV-007-001-source-poc.json`，只保存状态码、结构计数、响应hash/大小和停止原因，不提交社交正文或身份 |

选型沿用HTTPX/Pydantic和现有React客户端，不增加通用爬虫框架、浏览器执行器或新服务。首轮记录时B站公开Web搜索页与公开JSON详情/评论接口只作为技术POC，用途权限保持unknown、流水线保持not_connected；2026-09-16用户既定的个人非商业学习范围已使B站用途结论更新为allowed，但真实持久canary在评论步骤被拒绝，因此pipeline仍保持not_connected。X公开搜索页本次只得到登录/挑战Web壳；微博跳转登录；小红书和抖音未证明评论结构，均不标记supported或connected。Firecrawl CLI当前未认证，该工具状态不作为任何平台能力结论。

### 3.5 S02-T01 监控配置与查询预览实施登记

本片对应 `TASK-007-S02-T01`，已完成并记录于 `docs/operations/evidence/007/EV-007-002-monitor-configuration-poc.json`。目标是在不联网、不启动采集的前提下，将监控配置从可覆盖草稿升级为可审计的不可变版本，并提供通用查询预览和严格的启用/暂停状态机。验收口径为：配置更新只新增版本；预览逐来源说明规则由平台原生执行、本地过滤或不支持；当前无来源通过用途权限和流水线双门禁，因此启用必须稳定失败，不能制造运行中假象。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0004_monitor_configuration_versions.py`；`monitors` 仅保存身份、状态和当前版本指针，`monitor_versions` 保存查询、来源、周期与零内容采购预算的不可变快照；从0003升级只回填一个已知版本，不伪造历史 |
| 监控领域 | 修改 `backend/src/monitors/{models,schemas,services}.py`：定义 `draft → active ↔ paused` 状态、乐观锁、不可变版本创建与启停审计；编辑非draft监控返回稳定冲突 |
| 来源领域 | 修改 `backend/src/sources/{schemas,services}.py`：查询规格、逐来源编译结果、规则执行位置和请求估算；预览为纯计算，禁止调用适配器或网络 |
| API | 修改 `backend/src/api/routers/{monitors,sources}.py`：增加通用 `/sources/query-preview`、监控启用与暂停；移除Bluesky专属预览，不保留第二套兼容接口 |
| 前端 | 修改 `frontend/src/features/monitors/MonitorEditor.tsx` 与 `frontend/src/app/App.tsx`：编辑查询条件、周期和预算，保存前显示生成的逐来源查询预览；只调用 `src/api` 生成函数 |
| 生成产物 | 从FastAPI重新导出 `docs/openapi/openapi.json`，再由 `@umijs/openapi` 更新 `frontend/src/api/*`；不手写生成文件 |
| 测试 | 更新后端来源单元、API与PostgreSQL集成测试及前端Playwright流程；验证旧配置迁移、不可变版本、版本冲突、零网络预览、未准入来源启用失败和窄屏显示 |

本片继续使用现有 FastAPI/Pydantic/SQLAlchemy/Alembic/React/Axios/UmiOpenAPI，不增加依赖、定时器、采集任务或对象存储代码。`schedule.interval_minutes` 只形成配置快照，真正按slot派发属于S02-T02/T03；MinIO连接信息与来源用途授权未确定前，不实现持久采集，不把技术可读误写为已准入。

### 3.6 S02-T02A 页提交与收件箱事务基础实施登记

本片是 `TASK-007-S02-T02` 的内部基础片，不单独宣称完成该任务。目标是在不猜测现有 MinIO 参数、不访问外部平台的条件下，先固定“一页证据 → 规范内容/版本/观察 → 主题命中 → 检查点”的原子提交边界，并提供数据库只读收件箱。真实 MinIO 适配器、来源任务和 connected 状态仍需后续真实连通与用途准入证据。

| 类别 | 文件与职责 |
|---|---|
| 采集领域 | 新增 `backend/src/collection/{models,schemas,services}.py`：拥有运行、页检查点和单页提交编排；对象上传在数据库事务前完成，数据库提交时重验 fencing，重复页返回既有结果 |
| 证据领域 | 新增 `backend/src/evidence/{contracts,models,services}.py`：定义对象存储协议、确定性 gzip 与对象键、raw page 元数据写入；本片不实现或伪造 MinIO 适配器 |
| 内容领域 | 新增 `backend/src/contents/{models,schemas,services}.py`：内容身份、不可变正文版本、指标观察及收件箱读取；`null` 与真实 `0` 分开 |
| 主题命中 | 修改 `backend/src/monitors/{models,services}.py`：`monitor_matches` 归监控领域，按 monitor version/content 唯一并保存命中词与审核状态 |
| 数据与API | 新增 `0005_collection_page_foundation.py`；修改元数据、依赖与路由，增加认证只读 `GET /api/contents`，不增加可伪装成采集成功的HTTP写入口 |
| 前端与生成 | 新增 `frontend/src/features/contents/Inbox.tsx` 并装配工作台；OpenAPI与 `frontend/src/api/*` 继续自动生成 |
| 容器代理 | 修改 `frontend/nginx.conf`：使用Docker内置DNS动态解析backend upstream；新增 `backend/scripts/verify_proxy_replacement.py` 并接入CI，在Web不重启时替换backend，确认代理不再连接旧IP |
| 测试 | 使用真实 PostgreSQL 验证同页重投、跨主题单内容、多正文版本、0/null指标、旧 fencing 拒绝及上传后拒绝提交留下的孤儿对象事实；内存 store 只验证业务协议，不作为 MinIO 连通证据 |

本片不新增第三方依赖。对象存储协议只暴露私有 bucket/key/hash/bytes；正式 `evidence/adapters/minio.py` 必须在取得 endpoint、TLS、bucket 和凭据注入引用后，选定并锁定 MinIO SDK，再对现有实例执行隔离 prefix POC。

实施结果（2026-09-15）：本登记内的运行/页检查点、确定性压缩与证据元数据、含真实provider namespace的内容身份/版本/观察、关系约束、监控命中、基于 `(first_seen_at,id)` 游标的认证只读收件箱、`0005_collection_page` 迁移、Swagger/UmiOpenAPI生成客户端和空状态页面已实现。页提交在上传前预检稳定约束，上传后以锁和fencing复验，竞态失败留下显式孤儿供后续对账。容器回归发现并修复Nginx缓存旧backend IP造成的502；Web不重启、只替换backend后，动态Docker DNS路径返回预期401。真实PostgreSQL/RabbitMQ环境中76项测试通过；隔离Compose迁移至0005，真实prefork诊断成功且attempts=1；运行时OpenAPI与发布快照相等，Chromium的Swagger和owner工作台2项通过。合成事务证据见 [collection-page-foundation-poc.json](../operations/evidence/collection-page-foundation-poc.json)。它明确不是EV-007-003：未连接MinIO、未发出外部来源请求，也未实现采集Worker、预算预留或来源准入。

### 3.7 S02-T02B MinIO适配器与隔离连通POC实施登记

本片继续 `TASK-007-S02-T02`，只交付正式对象存储适配器、配置门禁和隔离S3连通验证；不新增真实来源任务，不把临时MinIO容器替代用户选择的现有实例。官方GitHub Release、PyPI与Firecrawl Developer Index在2026-09-15共同核对 `minio-py 7.2.20`，锁定该版本；SDK支持Python 3.9+，满足当前3.12基线。

| 类别 | 文件与职责 |
|---|---|
| 依赖 | 修改 `backend/pyproject.toml` 与 `backend/uv.lock`：精确锁定 `minio==7.2.20`，不引入另一套S3 SDK |
| 配置 | 修改 `backend/src/core/config.py`、`.env.example`、`.env.prod.example`、`docker-compose.yml`：增加endpoint、TLS、access key、secret key、bucket配置；四项连接值全有或全无，密钥保持SecretStr，API不返回 |
| 适配器 | 新增 `backend/src/evidence/adapters/{__init__,minio}.py`：实现现有EvidenceStore协议；对象存在时读回校验，冲突拒绝覆盖；新上传后以stat大小和完整读回SHA-256验收，不把ETag当正文hash |
| 验证 | 新增对象适配器单元测试和 `backend/scripts/verify_minio.py`；本地临时容器只用于SDK/协议POC且测试后删除，不进入根Compose；真实实例后续只用隔离prefix，不创建/修改bucket策略 |
| 证据 | 新增 `docs/operations/evidence/minio-adapter-poc.json` 并更新Operations；分别标明临时实例验证和现有实例未验收，不使用EV-007-003编号 |

本片不修改数据库迁移、HTTP路由、OpenAPI或前端生成文件。现有实例的非敏感endpoint/网络方式、bucket和凭据注入仍是完成S02-T02的外部输入；未提供时配置保持缺席，来源状态保持not_connected。

实施结果（2026-09-15）：已锁定 `minio-py 7.2.20`，增加全有或全无且隐藏密钥的 `HOTKEY_S3_*` 配置，并在 `evidence/adapters/minio.py` 实现冲突拒绝、上传后stat大小检查和完整读回SHA-256校验。独立脚本要求bucket预先存在，只操作本次生成的 `raw/poc/` 对象；一次性MinIO容器上首次上传与幂等重投成功，65字节对象hash一致，精确删除后对象数为0，容器已移除。真实PostgreSQL 16/RabbitMQ 4.1环境中82项后端测试通过；静态、依赖审计、OpenAPI/UmiOpenAPI漂移、前端构建、隔离Compose/prefork和Chromium 2项通过，正常停机无SIGKILL/OOM。证据见 [minio-adapter-poc.json](../operations/evidence/minio-adapter-poc.json)。该结果只关闭SDK和对象协议风险：现有MinIO的endpoint、TLS、权限、生命周期和服务端版本未验收，故不构成EV-007-003或TASK-007-S02-T02完成。

### 3.8 S02-T02C 采集任务消费者与运行查询实施登记

本片继续 `TASK-007-S02-T02`，目标是把现有页事务接到持久Job/Outbox/Celery消费者，并暴露运行提交和查询契约。执行顺序遵循先消费者、再producer：先让Worker识别 `collect_page`，再开放HTTP创建。来源用途权限与现有MinIO连接仍是运行门禁；本片测试使用注入的合成页，不会把任一真实平台改为eligible或发送外部平台请求。

| 类别 | 文件与职责 |
|---|---|
| 迁移 | 新增 `backend/src/migrations/versions/0006_collection_jobs.py`：扩展Job kind为`collect_page`，为collection_runs增加唯一非空job_id；历史未暴露的排队/运行记录安全终止并回填对应Job，不修改0001–0005 |
| 任务账本 | 修改 `jobs/{models,contracts,execution,services}.py` 与 `worker/{messaging,app}.py`：消息仍只含job_id/epoch/version；Lease携带白名单kind，诊断结果仍只写JobResult，采集业务结果留在collection_run |
| 采集编排 | 新增 `collection/execution.py`，修改 `collection/{models,schemas,services}.py`：创建run与collect_page Job/Outbox同事务；以Job fencing启动一页执行；无证据响应也形成明确失败结果；不持长数据库事务调用网络 |
| 来源契约 | 新增 `sources/contracts.py` 并调整已存在适配器：页结果显式包含脱敏请求指纹、原始响应bytes和media type；旧CLI复用同一页方法，不建立第二套采集实现 |
| HTTP/生成 | 新增 `api/routers/collection.py` 并修改依赖/路由；FastAPI自动生成 `POST /monitors/{id}/runs` 与 `GET /collection-runs/{id}`，由OpenAPI快照和UmiOpenAPI生成到 `frontend/src/api/`，不手写请求 |
| 测试/证据 | 增加Job kind、迁移、fencing、无权限/无MinIO拒绝、注入页执行和HTTP契约测试；更新Operations并新增脱敏POC JSON。真实PostgreSQL/RabbitMQ与隔离Compose验证消费者；真实来源和现有MinIO另行验收 |

本片不增加依赖、浏览器执行器、预算表、评论子任务或新前端页面。`collect_page` 先只执行 `search_posts` 单页；后续分页、详情展开和评论树按S03增加独立检查点，不能把搜索引用冒充已入库帖子。未获准入的来源仍在创建run前返回 `source_not_eligible`，MinIO缺失时返回 `evidence_store_not_configured`，两者都不得产生Job/Outbox。

实施结果（2026-09-15）：`0006_collection_jobs` 已将 `collect_page` 加入Job白名单，并为每个collection_run建立唯一非空Job关联；带数据的0005→0006验证中，历史queued运行以`migration_boundary`失败关闭，completed运行保持ok并回填succeeded Job。run、Job与Outbox同事务创建；Celery消息仍只有job_id/epoch，Worker领取后以fencing执行一个原始搜索页，执行前再次检查来源准入。页已提交但Job尚未完成时，重领任务只收口Job而不重复抓取；幂等重放在后续撤权或存储配置缺失时仍返回既有运行，并发同键创建通过事务级锁收敛到同一run/Job/Outbox。任务硬超时60秒，租约配置下限严格大于硬超时，默认90秒。HTTP使用201表达持久资源创建，异步进度由运行状态表达，UmiOpenAPI配置不再改写响应码。真实PostgreSQL 16/RabbitMQ 4.1下92项测试通过；运行时OpenAPI、生成客户端、前端构建、真实prefork、代理替换和Chromium 2项通过。证据见 [collection-job-foundation-poc.json](../operations/evidence/collection-job-foundation-poc.json)。该结果不包含现有MinIO、来源网络或真实内容，不能标记EV-007-003与TASK-007-S02-T02完成。

### 3.9 S02-T02D 预算预留、周期槽与运行列表实施登记

本片继续 `TASK-007-S02-T02` 并为 `TASK-007-S02-T03` 提供运行可见性，只实现不依赖真实平台账号和现有MinIO参数的确定性部分。目标是让每个单页运行在创建Job前原子占用一次日请求预算，让独立scheduler只生成当前已结束周期槽的运行，并让工作台读取真实运行状态。所有生产来源仍未准入，未配置现有MinIO时scheduler必须零写入；测试只注入已准入来源，不发送外部请求。

| 类别 | 文件与职责 |
|---|---|
| 迁移 | 新增 `backend/src/migrations/versions/0007_collection_schedule_budget.py`：增加按monitor_version/UTC日期唯一的日请求预算账本；collection_runs记录manual/scheduled触发方式、周期槽、预算日期与预留请求数；历史运行按created_at回填并对账，不修改0001–0006 |
| 预算与调度 | 修改 `collection/{models,schemas,services}.py`，新增 `collection/scheduling.py`：create_run在同一事务锁定并预留一次请求，超限稳定返回`request_budget_exhausted`；scheduler按UTC周期边界生成当前最近一个已结束槽，使用确定性幂等键，不追补无限历史 |
| 监控快照 | 修改 `monitors/{schemas,services}.py`：只读导出当前active配置DTO供调度使用，不让collection直接读取monitor ORM；暂停或版本变化后不再生成旧配置运行 |
| 运行装配/API | 修改 `cli/commands.py`在现有scheduler循环中先生成到期意图、再发布Outbox；修改collection路由与依赖，增加认证分页 `GET /collection-runs`，不在GET中触发采集 |
| 前端 | 新增 `frontend/src/features/collection/Runs.tsx` 并修改 `app/App.tsx`：通过UmiOpenAPI生成的collection函数展示触发方式、业务状态、来源、计数、预算日期和停止原因；不手写URL/DTO，不增加全局store或图表库 |
| 契约与验证 | 更新FastAPI自动OpenAPI与 `frontend/src/api/*`；增加预算原子性、同槽幂等、无MinIO/撤权零写入、暂停停止、运行keyset分页、scheduler真实进程和浏览器空状态测试；新增脱敏POC证据 |

本片沿用Python/FastAPI/SQLAlchemy/Alembic/Celery/RabbitMQ、React/Axios/UmiOpenAPI，不增加依赖或服务。日预算按UTC日期统计一次来源请求意图；MVP单页运行固定预留1次且不回退，避免失败重试绕过上限。周期槽以`floor(now/interval)-interval → floor(now/interval)`形成完整窗口，只生成最近槽；scheduler重启不自动补齐更早窗口。运行列表按`(created_at,id)`游标分页。现有MinIO连通、真实来源准入、评论跟踪和真实收件箱仍需后续验收，本片不能关闭EV-007-003。

实施结果（2026-09-15）：`0007_collection_budget` 已增加不可超领的 UTC 日请求账本，run、Job 与 Outbox 只有在额度预留成功后同事务创建；8个不同幂等键并发争抢1次额度时只有1个成功。scheduler从当前active配置DTO生成最近一个已结束周期槽，重复扫描、暂停、来源不可用或MinIO未配置均不新增运行；不回补无限历史。历史0006数据升级时回填manual触发、预算日、每run一次预留和monitor保留天数；若历史请求数超过旧预算快照，则账本以既有数量为下限，确保升级不丢事实。认证只读运行列表使用 `(created_at,id)` 游标，工作台经UmiOpenAPI生成函数展示空态和真实状态字段。真实PostgreSQL 16/RabbitMQ 4.1下97项测试通过；Ruff、严格mypy（84个源文件）、运行时OpenAPI、生成客户端、前端边界/格式/构建、生产Compose解析和依赖审计通过。隔离Compose完成0007迁移、真实prefork诊断和后端替换；未配置S3时运行数为0。Chromium 2项通过，正常停机worker/scheduler=0、backend=143且无SIGKILL/OOM。证据见 [collection-schedule-budget-poc.json](../operations/evidence/collection-schedule-budget-poc.json)。本片没有连接现有MinIO或任何真实平台，仍不能标记EV-007-003与TASK-007-S02-T02完成。

### 3.10 S02-T02E 精确来源操作准入实施登记

本片继续 `TASK-007-S02-T02`，只解决生产配置无法表达“某平台的某个操作已获用途授权且持久流水线已连通”的架构缺口。准入必须按 `source.operation` 精确配置，默认全部关闭；技术支持、用途权限、持久流水线和本次运行结果保持独立。未提供现有MinIO参数或未获得用户用途确认时不执行真实采集，也不把配置机制本身作为connected证据。

| 类别 | 文件与职责 |
|---|---|
| 配置 | 修改 `backend/src/core/config.py`、`.env.example`、`.env.prod.example` 与根Compose：增加JSON数组 `HOTKEY_SOURCE_RIGHTS_ALLOWED` 和 `HOTKEY_SOURCE_PIPELINES_CONNECTED`；生产覆盖继续显式透传现有MinIO配置，所有开关默认空数组 |
| 来源契约 | 修改 `sources/{schemas,services}.py`：将pipeline与eligible移动到每个operation能力卡；只允许代码中已有持久执行器的operation被声明connected；`activation_issues`按指定operation判断，不用整个平台布尔值放行 |
| 运行装配 | 修改 `main.py`、`api/dependencies.py`、`cli/commands.py`、`worker/app.py`：API在lifespan启动时构造并共享SourceService，Monitor、Collection、scheduler和Worker从同一Settings读取准入；非法操作配置必须让服务启动失败，避免各进程看到不同准入状态 |
| 前端 | 修改 `features/sources/SourceCapabilities.tsx` 与 `app/App.tsx`：逐操作显示授权/连接/可采集状态；监控启用只读取search_posts准入；继续只使用UmiOpenAPI生成DTO |
| 契约与验证 | 更新FastAPI OpenAPI及 `frontend/src/api/*`；修改配置、来源、API/工作流和浏览器测试，先验证旧平台级字段与未注入配置不能满足目标，再验证精确操作准入、未知/未实现operation拒绝、生产Compose透传和默认零采集 |

本片沿用现有Pydantic Settings、FastAPI、React、Axios与UmiOpenAPI，不增加依赖、迁移、服务、来源账号或平台网络请求。rights allowlist是部署者对用途范围的显式确认，pipeline allowlist只表示已经由该版本持久执行器验证的操作；两者同时满足且技术support为supported时才eligible。当前代码只登记 `bilibili.search_posts` 为可声明的持久操作，详情、评论、回复以及其他平台在对应消费者与真实POC完成前即使写入配置也必须启动失败。

实施结果（2026-09-15）：来源目录已删除平台级pipeline/eligible字段，24张能力卡分别返回support、rights、pipeline与eligible；监控启用固定检查search_posts操作。API在lifespan构造并共享SourceService，配置未知operation或把未实现的 `bilibili.list_comments` 声明connected时启动退出3；scheduler与Worker使用同一Settings。生产Compose已向migrate/backend/worker/scheduler显式透传现有MinIO与两个准入数组，默认均为空。查询预览的pipeline_connected也由同一search能力计算，不再固定false。FastAPI自动OpenAPI与运行时一致，UmiOpenAPI只在 `frontend/src/api` 更新DTO，页面按操作展示用途、连接与可采集状态，业务请求仍全部经 `src/request.ts`。

真实PostgreSQL 16/RabbitMQ 4.1下102项测试通过；Ruff、严格mypy（84个源文件）、生成契约、前端边界/格式/构建、生产Compose解析和依赖审计通过。隔离Compose完成迁移、prefork诊断（attempts=1）、后端原地替换和2项Chromium测试；默认目录为6个平台、24个操作、0项eligible。正常停机worker=0、backend=143、scheduler=0且无SIGKILL/OOM，隔离资源已删除。证据见 [source-operation-admission-poc.json](../operations/evidence/source-operation-admission-poc.json)。本片未访问外部平台、未连接用户现有MinIO，也没有获得真实用途确认；因此所有生产操作仍默认not_connected，EV-007-003与TASK-007-S02-T02保持未完成。

### 3.11 S02-T02F 搜索引用到正文的父子任务实施登记

本片继续 `TASK-007-S02-T02`，解决搜索页只保存引用而不能形成收件箱正文的主链断点。MVP每个搜索请求最多展开1个帖子详情；搜索与详情各自占用1次请求预算、独立保存原始证据并由独立Job/租约执行。详情任务由搜索页数据库提交事务创建，消息仍只含job_id/epoch/version；重投同页不得重复派生详情任务。未连接现有MinIO或未确认用途时默认路径仍不执行。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0008_search_reference_expansion.py`；将collection run的 `query_variant` 根字段改名为通用 `request_value`，增加自引用 `parent_run_id`，并用操作约束保证search为根、fetch_post为子；不保留旧字段兼容 |
| 来源契约 | 修改 `sources/{schemas,contracts,services,execution}.py` 与 `sources/adapters/bilibili.py`：用operation + request_value的统一页输入取代search专属输入；B站search声明依赖fetch_post；详情返回原始JSON页及规范post；只有两项分别准入时search才eligible |
| 采集与预算 | 修改 `collection/{models,schemas,services,execution,scheduling}.py` 与 `monitors/{schemas,services}.py`：搜索页提交与最多1个详情run/Job/Outbox/预算预留同事务，并在页边界复核当前monitor version仍active；预算不足、暂停或详情撤权时以partial停止原因结束父run，不调用详情；详情页沿用证据先上传、数据库后提交与fencing；新监控默认日预算调整为可覆盖每小时1个关键词的48次，旧快照不改写 |
| API与前端 | 保持现有端点路径，FastAPI契约将run字段切换为 `request_value` 并增加parent_run_id；UmiOpenAPI只生成 `frontend/src/api`，`features/collection/Runs.tsx` 按搜索词/详情目标显示，`features/monitors/MonitorEditor.tsx` 同步新配置默认值，不手写请求 |
| 测试与证据 | 先以失败测试证明搜索引用不会入箱且无详情任务，再验证真实PostgreSQL中父页原子派生、预算不足、重投、撤权和详情入箱；增加适配器原始页、迁移、Worker、Swagger/生成客户端、浏览器与Compose回归证据 |

本片沿用Python/FastAPI/SQLAlchemy/Alembic/Celery/RabbitMQ、MinIO协议、React/Axios/UmiOpenAPI，不增加依赖或服务。上限1是可测的MVP资源边界，不声称覆盖搜索页所有结果；查询预览与启用预算按每个关键词最多2次请求计算。旧监控预算快照不篡改，预算不足的旧配置需由用户建立新版本后再启用。评论与回复仍属于S03，不在本片把详情成功扩写成评论链完成。

实施结果（2026-09-15）：`0008_reference_expansion` 已把运行目标统一为 `request_value` 并增加父子关系和数据库约束；从0007升级的既有search运行保留原值且parent为空，不保留 `query_variant`。周期槽唯一索引只约束parent为空的根搜索，同周期两个关键词发现同一帖子时可分别创建详情子任务。B站search能力显式依赖fetch_post，两项分别准入后才可启用。搜索页提交时最多为一个合法bvid同事务预留预算并创建详情run、Job与Outbox；同页重投不重复派生。预算耗尽、monitor暂停或详情撤权均停止在页边界并记录partial原因。合成搜索页与正文页经真实PostgreSQL任务账本后形成一个真实数据库收件箱条目，两页证据分别记录；该验证使用内存EvidenceStore，不代表现有MinIO已验收。

真实PostgreSQL 16/RabbitMQ 4.1下107项测试通过；Ruff、严格mypy（85个源文件）、FastAPI OpenAPI导出、UmiOpenAPI漂移、前端目录/负向边界、Prettier、TypeScript/Vite构建、生产Compose解析和依赖审计通过。隔离Compose迁移到0008并确认根任务部分唯一索引，真实prefork诊断attempts=1，后端替换后Web代理返回401；Chromium 2项通过并验证新监控默认48次日预算。默认运行仍为6个平台、24个操作、0项eligible、0个collection run；正常停机无SIGKILL/OOM且一次性资源已删除。证据见 [search-reference-expansion-poc.json](../operations/evidence/search-reference-expansion-poc.json)。本片没有访问外部平台、没有连接用户现有MinIO或确认真实用途，因此EV-007-003与TASK-007-S02-T02仍未完成。

### 3.12 S03-T01A 正文到首屏根评论实施登记

本片开始 `TASK-007-S03-T01`，只打通已发现B站正文到一页根评论的持久链。正文详情仅在平台明确返回评论数大于0时派生一个 `list_comments` 子任务；每页最多20条，若平台仍有下一页则本次运行标记partial/page_limit，不在首片自动遍历全部分页或回复。评论无需重复关键词，但必须能解析到同来源根帖并继承该monitor version的上下文命中。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0009_root_comment_page.py`；扩展collection run操作约束到list_comments，继续使用parent关系、每run一次预算和现有证据/检查点，不增加评论专属重复账本 |
| 来源与执行 | 修改 `sources/{schemas,services,execution}.py` 和B站适配器：fetch_post声明依赖list_comments；准入依赖按传递关系计算；详情结果用 `aid:<id>` 引用首屏评论；list_comments使用统一CollectionPageInput并返回原始JSON页，固定cursor=0、limit=20 |
| 内容与匹配 | 修改 `contents/services.py` 与 `collection/services.py`：按同来源root/parent外部身份解析relation_status；评论只在根帖已经属于当前monitor version时继承上下文命中，不按评论正文关键词过滤，也不跨来源关联 |
| 预算与界面 | 修改monitor默认预算与查询估算为每小时单关键词最多search+post+comments三次，即72次/日；运行列表支持list_comments标签，收件箱沿用现有post/comment类型，不新增页面或状态库 |
| 契约与验证 | FastAPI自动更新OpenAPI，UmiOpenAPI只生成 `frontend/src/api`；先以失败测试证明详情不会创建评论任务和无关键词评论不会入箱，再验证父子原子性、0评论不派生、上下文命中、关系解析、页上限、撤权/预算/暂停继承及Compose/浏览器回归 |

本片不增加依赖、服务、HTTP写端点、评论回复、全量分页或其他平台适配。B站search、fetch_post、list_comments三项必须分别满足support、rights和pipeline，任一缺失都不得启用监控。合成响应只用于工程验证；没有现有MinIO与真实用途确认时生产仍为零准入。

实施结果（2026-09-15）：`0009_root_comments` 已把运行约束扩展到list_comments，同时保留search根任务和fetch/comments子任务的数据库父子约束。B站详情只在reply_count大于0时输出一个aid引用；正文页提交原子派生一页根评论任务，评论请求固定cursor=0、limit=20，有下一页时明确记录partial/page_limit而不继续遍历。评论关系只解析到同来源post；无关键词评论在根帖已命中同一monitor version时以root_context进入收件箱。搜索、详情和根评论形成传递准入链，查询估算与新监控默认预算调整为每小时单关键词最多72次/日。运行列表新增明确的根评论标签。

真实PostgreSQL 16/RabbitMQ 4.1下111项测试通过；Ruff、严格mypy（86个源文件）、FastAPI OpenAPI导出、UmiOpenAPI漂移、前端目录/负向边界、Prettier、TypeScript/Vite构建、生产Compose解析和依赖审计均通过。0008→0009迁移POC保留既有搜索/正文父子运行且模型无漂移。隔离Compose完成0009迁移、真实prefork诊断attempts=1、运行时契约比较、Backend替换代理及2项Chromium测试；正常停机无SIGKILL/OOM并删除全部一次性资源。证据见 [root-comment-page-poc.json](../operations/evidence/root-comment-page-poc.json)。本片使用合成来源响应和内存EvidenceStore，没有外部平台请求、现有MinIO或真实用途确认，因此EV-007-003与TASK-007-S03-T01仍未完成。

### 3.13 S03-T01B 根评论到首屏回复实施登记

本片继续 `TASK-007-S03-T01`，只从已提交的B站根评论页中选择最多1条reply_count大于0的根评论，派生一页 `list_replies` 任务。回复请求固定page=1、limit=20；仍有下一页时记录partial/page_limit，不自动遍历。没有回复的根评论不派生任务，父评论缺失或身份歧义时关系保持unresolved且不继承监控命中。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0010_reply_page.py`；扩展collection run操作约束到list_replies，继续复用parent_run_id、每run一次预算、Job/Outbox、raw page与checkpoint，不增加回复专属表 |
| 来源与执行 | 修改 `sources/{schemas,services,execution}.py` 与B站适配器：list_comments声明依赖list_replies；根评论以 `aid:<aid>/root:<rpid>` 输出回复引用；list_replies走统一CollectionPageInput并返回原始JSON页，固定page=1、limit=20 |
| 内容与匹配 | 修改 `contents/services.py` 与采集提交测试：回复必须唯一解析到同来源post和父评论才标记resolved并继承root_context；缺失或歧义父节点保持unresolved，不做猜测关联 |
| 预算与界面 | 传递依赖按每个关键词最多search+post+comments+replies四次估算，新监控每小时单关键词默认96次/日；运行列表增加list_replies明确标签 |
| 契约与验证 | FastAPI自动更新OpenAPI，UmiOpenAPI只生成 `frontend/src/api`；Red先证明list_replies不能声明connected，再验证一页回复父子链、0回复、非法引用、页上限、关系/上下文、迁移、Compose与浏览器回归 |

本片不增加依赖、服务、HTTP端点、第二页遍历、多个根评论回复展开或其他平台。B站四项操作必须分别满足support、rights和pipeline；默认生产配置仍为零准入。合成响应只证明工程链路，不计作真实平台或现有MinIO验收。

实施结果（2026-09-15）：`0010_reply_page` 已扩展运行约束到list_replies。B站根评论页仅为reply_count大于0的评论输出 `aid:<aid>/root:<rpid>` 引用，采集服务最多选择1条并原子派生回复run、Job、Outbox和预算；回复固定page=1、limit=20，存在下一页时以partial/page_limit结束。回复必须唯一解析到同来源post与父评论才标记resolved并继承root_context；父评论缺失或根身份歧义时保持unresolved且不继承。四项传递准入的最大请求估算使新监控每小时单关键词默认预算调整为96次/日，运行列表新增回复标签。

真实PostgreSQL 16/RabbitMQ 4.1下114项测试通过；Ruff、严格mypy（91个应用与验证源文件）、FastAPI OpenAPI导出、UmiOpenAPI漂移、前端目录/负向边界、Prettier与TypeScript/Vite构建通过。0009→0010迁移POC保留既有搜索/正文/根评论三层运行且模型无漂移。隔离Compose、依赖审计、浏览器、代理替换与停机结果记录在 [reply-page-poc.json](../operations/evidence/reply-page-poc.json)。本片没有外部平台请求、现有MinIO或真实用途确认，不完成EV-007-003或TASK-007-S03-T01。

### 3.14 S04-T01A 人工事件档案实施登记

本片启动 `TASK-007-S04-T01` 的最小人工整理闭环：创建事件、把已入库内容加入或移出事件、读取事件与不可变修订。每条内容同一时刻最多归属一个事件；重复加入同一事件幂等，跨事件重复归属明确冲突。合并、拆分、趋势和提醒留在后续切片，不在本片预建空模块。

| 边界 | 本片文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/events/{models,schemas,services}.py` 与 `0011_event_dossiers.py`；events保存当前档案，event_members保存当前归属，event_revisions保存每次变化后的完整轻量快照；更新db metadata、revision和测试清理顺序 |
| HTTP契约 | 新增 `api/routers/events.py` 与依赖注入：创建/列表/详情、加入/移出成员、读取修订；路由只处理协议与认证，事务和并发锁在EventService |
| 前端 | FastAPI自动导出契约，UmiOpenAPI只生成 `frontend/src/api/events.ts` 和类型；新增 `features/events/EventDossiers.tsx`，工作台可创建事件并查看成员；收件箱通过生成端点把内容加入已有事件 |
| 约束与验证 | Red先证明events模块尚不存在；Green验证唯一归属、幂等、移出、修订快照、认证/CSRF、迁移、OpenAPI生成、浏览器与真实PostgreSQL；不增加新依赖、对象存储、AI或消息kind |

SMART验收：本片完成后，单owner能在一个工作台会话内创建事件，将一条已命中内容加入事件并看到成员数，移出后成员数恢复为0；数据库依次保留create/add_member/remove_member三条修订，冲突不会改写状态。验收只证明人工事件档案基础，不声明完成合并拆分、趋势提醒或 `TASK-007-S04-T01`。

实施结果（2026-09-15）：新增events、event_members和event_revisions，内容行锁与数据库唯一约束保证当前单一事件归属；同事件重放不增加修订，跨事件归属返回稳定冲突。FastAPI发布6个认证事件端点，UmiOpenAPI生成 `frontend/src/api/events.ts`；工作台支持创建事件、从收件箱归档内容和移出成员，未手写请求。

真实PostgreSQL 16/RabbitMQ 4.1下116项测试通过，Ruff、严格mypy（97个应用与验证源文件）、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建通过。0010→0011迁移POC保留既有内容且模型无漂移；隔离Compose、浏览器、代理替换与停机证据见 [event-dossier-poc.json](../operations/evidence/event-dossier-poc.json)。本片未连接外部平台或现有MinIO，不包含合并、拆分、趋势和提醒，因此不完成EV-007-006或TASK-007-S04-T01。

### 3.15 S04-T01B 事件合并与拆分实施登记

本片继续 `TASK-007-S04-T01`，实现两个活跃事件的人工合并，以及从一个活跃事件选择成员创建新事件。请求必须携带涉及事件的期望修订号；服务按UUID固定顺序加行锁避免死锁，任一修订变化即整体拒绝。合并后源事件归档且成员移动到目标；拆分后被选成员移动到新事件。每个受影响事件同事务各写一条带关联事件ID的完整快照，当前成员唯一约束保持不变。

| 边界 | 本片文件与职责 |
|---|---|
| 数据与迁移 | 新增 `0012_event_merge_split.py`；event_revisions增加related_event_id并扩展change_type，现有修订保持原值；更新events模型、schema、服务与数据库revision |
| HTTP与生成客户端 | events路由新增merge/split两个端点；FastAPI自动导出OpenAPI，UmiOpenAPI只更新 `frontend/src/api`，请求继续只经根级Axios |
| 前端 | EventDossiers对活跃事件提供目标选择合并；有成员时提供复选成员和新事件标题拆分；App只调用生成的mergeEvent/splitEvent并刷新数据库状态 |
| 约束与验证 | Red先证明合并/拆分输入和服务不存在；Green验证成员移动、源归档、双边修订、幂等冲突、旧期望修订拒绝、迁移保留、Swagger、浏览器和真实PostgreSQL/RabbitMQ；不引入分析、提醒或新依赖 |

SMART验收：两个不同事件合并后只保留一个活跃成员集合且源事件归档；随后选择至少一条但非全部成员拆分，新事件与原事件成员互斥且总数不变。所有变化可由revision、related_event_id和snapshot还原，过期revision请求返回409且数据库零变化。

实施结果（2026-09-16）：`0012_event_merge_split` 为修订增加related_event_id并扩展merge/split变化类型，历史create/add/remove修订保持原值且关联事件为空。合并按UUID固定顺序锁定两个事件，原子移动成员并将源事件归档；拆分要求选择当前成员的真子集，原子创建事件并移动成员。目标、源和新事件分别保留变化后快照，旧期望修订稳定返回409。

FastAPI新增mergeEvent/splitEvent，UmiOpenAPI自动更新events.ts和类型；工作台对活跃事件提供合并目标选择，对至少两个成员的事件提供成员复选拆分。远程CI在真实PostgreSQL/RabbitMQ下117项测试通过，并完成Compose、代理替换、2项Playwright和无SIGKILL关停验证；完整结果见 [GitHub Actions #34991217129](https://github.com/StephenQiu30/hotkey-server/actions/runs/34991217129) 与 [event-merge-split-poc.json](../operations/evidence/event-merge-split-poc.json)。趋势、提醒与分析失效尚未实现，不完成完整TASK-007-S04-T01。

### 3.16 S04-T01C 站内事件变化提醒实施登记

本片继续 `TASK-007-S04-T01` 的最小提醒闭环。事件加入/移出成员、合并和拆分已有不可变修订；本片在同一数据库事务内为每个非create修订生成一条站内提醒，并使用`(rule_version,event_id,change_id)`唯一约束阻止重放。本片不发送邮件/企业协作消息，不引入新队列，不用用户刚手工变更的提醒冒充趋势突发。

| 边界 | 本片文件与职责 |
|---|---|
| 数据与迁移 | 新增 `notifications/{models,schemas,services}.py` 与 `0013_event_notifications.py`；表只保存站内提醒、变化ID、规则版本和已读时间 |
| 原子性 | 修改 `events/services.py`，修订与提醒使用同一Session/事务；notifications不导入events ORM，events只传入原语值 |
| HTTP与生成客户端 | 新增有界分页列表与幂等已读端点；cursor使用URL安全不透明令牌；FastAPI自动导出OpenAPI，UmiOpenAPI只生成 `frontend/src/api` |
| 前端 | 新增 `features/notifications/NotificationInbox.tsx`；App只组装生成的list/mark-read函数，Axios继续只在根级`request.ts` |
| 约束与验证 | Red先证明notifications模块/服务不存在；Green在真实PostgreSQL验证原子写入、唯一去重、不透明cursor、已读持久化、迁移、Swagger、生成客户端和浏览器 |

SMART验收：一次事件成员变更提交后，对应修订和一条未读提醒同时可见；重放同一幂等业务请求不增加修订或提醒；标记已读后刷新仍为已读；伪造cursor稳定返回422。趋势时间桶、覆盖中断与趋势规则提醒留在后续片，本片不单独完成AC-007-010或EV-007-006。

实施结果：Red阶段因`notifications`模块不存在而在测试收集失败。Green阶段将事件修订和提醒写入同一事务，唯一约束保证同一规则版本、事件和变化只生成一条；FastAPI自动增加`listNotifications`和`markNotificationRead`，UmiOpenAPI只在`frontend/src/api`生成端点和类型。真实PostgreSQL/RabbitMQ全量127项测试通过；0012→0013迁移保留既有事件且metadata无差异；浏览器验证合并生成2条未读提醒、标记后刷新保持1条未读。远端CI复现全量门禁，见 [GitHub Actions #34995710640](https://github.com/StephenQiu30/hotkey-server/actions/runs/34995710640) 与 [event-notification-poc.json](../operations/evidence/event-notification-poc.json)。本片仍不实现趋势可比性、回填识别或突发规则，因此不完成AC-007-010。

### 3.17 S04-T01D 分平台趋势与覆盖中断实施登记

本片继续 `TASK-007-S04-T01`，只实现可解释的分平台时间桶。采集运行新增必需业务语义 `ingestion_mode=live|backfill`，搜索运行明确设置，后续详情、评论和回复运行继承父运行模式；历史迁移前不存在回填入口，因此既有运行一次性标记为live。趋势按事件当前成员、第一版证据和互动观察按需聚合，不增加趋势表、缓存、图表库或评分公式。

| 边界 | 本片文件与职责 |
|---|---|
| 采集事实与迁移 | 新增 `0014_collection_ingestion_mode.py`；修改collection模型/schema/service/scheduler，使模式进入幂等快照、运行视图和执行快照并沿父子运行继承 |
| 领域读取 | `contents/services.py`仅返回内容首次证据和观察DTO；`evidence/services.py`仅把原始页映射为运行ID；`collection/services.py`仅返回运行模式与覆盖DTO；`events/services.py`组合这些公开服务DTO，不跨领域导入ORM |
| HTTP与生成客户端 | events schema/router新增有界的UTC时间窗与1/6/24小时桶端点；FastAPI自动维护Swagger/OpenAPI，UmiOpenAPI只更新 `frontend/src/api`，Axios仍只在根级`request.ts` |
| 前端 | 新增 `features/events/EventTrends.tsx` 并由EventDossiers装配；按事件懒加载最近7天24小时桶，用表格展示新增根帖、新增评论/回复、已观察回复增量、覆盖状态和已排除回填，不新增图表依赖 |
| 验证与证据 | 新增integration与HTTP契约测试；Red先证明ingestion_mode和趋势接口不存在，Green验证实时计数、回填抑制、故障/策略变化中断、空覆盖、迁移保留、Swagger、生成客户端和浏览器；POC写入 `docs/operations/evidence/event-trend-poc.json` |

SMART验收：同一事件在一个明确UTC窗口内返回固定长度的平台时间桶；live首次证据计入对应新增内容，backfill首次证据与观察只增加“已排除回填”而不增加声量或互动增量；任一live失败、部分完成、未完成或窗口内策略切换将相关平台标为不可比；无live覆盖显示缺失，前端刷新后仍由数据库重新计算相同结果。本片不计算跨平台总热度，不发送趋势突发提醒，也不开始模型分析。

实施结果（2026-09-16）：`0014_collection_ingestion_mode` 将模式写入运行幂等快照、执行快照和生成API，并由所有父子采集运行显式继承。事件趋势沿事件成员→内容版本/观察→原始页→采集运行事实链按需聚合；覆盖使用采集窗口重叠，live失败/部分/未完成与窗口内策略切换稳定打断可比性。FastAPI新增`getEventTrends`，UmiOpenAPI只更新`frontend/src/api/events.ts`和类型；工作台按事件懒加载表格，桌面使用单列事件档案，移动端表格在卡片内横向滚动。

真实PostgreSQL 16/RabbitMQ 4.1全量129项测试通过，Ruff、严格mypy、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建通过。0013→0014迁移保留既有run并标记live，metadata差异为0；隔离Compose schema为0014，真实消息任务一次成功，运行时契约与导出一致，2项Playwright通过。另用明确合成的live/backfill证据验证浏览器表格显示1个新增根帖、排除1条回填内容/1次观察，390px无页面横向溢出。远程CI在提交`2619c899674be04f21e22ea8ce124891d31b6dfa`上复现全部门禁，见 [GitHub Actions #34998942547](https://github.com/StephenQiu30/hotkey-server/actions/runs/34998942547) 与 [event-trend-poc.json](../operations/evidence/event-trend-poc.json)。本片不连接外部平台或现有MinIO，不声明完成真实平台事件趋势验收，也不实现趋势突发提醒或模型分析。

### 3.18 S04-T02A 冻结评论样本与可复算人工基线实施登记

本片启动 `TASK-007-S04-T02`的模型无关基础：从事件根帖的评论/回复和直接入事件的评论/回复中，按平台、UTC日期桶、根帖和排序来源确定性分层轮询取样。清单固定事件修订、时间窗口、采集截止点、正文/父级/根帖版本、hash、策略版本和入选原因。重算只读这个清单和其人工标签，不从实时内容表重新取样。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/analysis/{__init__,models,schemas,services}.py` | 新建分析域；持有冻结运行/样本/人工标签，执行稳定取样、引用校验和确定性聚合 |
| `backend/src/contents/services.py` | 新增由contents域拥有的候选正文/上下文DTO查询与当前版本查询；不向analysis暴露ORM |
| `backend/src/events/services.py` | 新增事件修订和成员ID的分析作用域DTO；不反向依赖analysis |
| `backend/src/api/routers/analysis.py`、`api/dependencies.py`、`api/router.py`、`api/responses.py` | 新增冻结、列表、详情、人工标注与重算协议，仅注入AnalysisService；显式固定错误响应描述，避免Swagger契约受Python标准库短语影响 |
| `backend/src/db/metadata.py`、`backend/src/db/health.py`、`backend/tests/architecture/test_dependencies.py` | 登记analysis顶层模块、当前schema revision并将新模块纳入全源码依赖扫描 |
| `backend/src/migrations/versions/0015_analysis_sample_baseline.py` | 新建analysis_runs/analysis_samples/analysis_labels；不改写历史迁移，降级不做破坏性删表 |
| `backend/tests/integration/test_analysis_samples.py`、`backend/tests/conftest.py`、API/架构测试 | 先Red证明端点/模块缺失；测试数据库清理登记新表；再验证分层、冻结、幂等、弃判、引用、重算和过期 |
| `frontend/src/features/events/EventAnalysis.tsx`、`EventDossiers.tsx`、`style.css`、`request.ts` | 事件卡内懒加载冻结样本、标注进度、样本构成和引用；窄屏纵向排布；唯一Axios封装只增加稳定错误文案 |
| `docs/openapi/openapi.json`、`frontend/src/api/*` | FastAPI/Pydantic自动维护Swagger/OpenAPI，UmiOpenAPI只在`src/api`生成端点与类型；不手写请求 |
| `docs/operations/evidence/analysis-sample-baseline-poc.json` | 保存迁移、复算、合成浏览器与证据边界 |

技术决策：不增加新Python/npm依赖、模型SDK、向量库、独立队列或分析Worker。首片使用`manual_baseline`和0 token预算建标注集；具体免费/自建模型待该评估集可用后再做POC并写回选型登记。当前适配器没有可切换排序参数，如实固定`provider_default`，不将其表述为热评或最新评论。

SMART验收：给定两个平台、两个UTC日期桶和多个根帖的明确合成评论，在一次请求内产生不超过上限的固定清单；重放相同输入返回同一运行，新增实时评论后重算的manifest、分母和已展示观点不变。非弃判标签没有冻结引用时拒绝；完成标注后每个聚合观点带支持性引用。事件修订或样本正文版本改变后历史运行显示过期，原清单仍可还原。本片不声明完成模型质量或完整AC-007-009。

实施结果（2026-09-16）：`0015_analysis_sample_baseline` 新增冻结运行、样本与不可变人工标签；事件根帖和评论属于不同provider namespace时，候选查询通过来源与根外部ID关联根上下文，父评论仍按评论namespace关联，从领域边界解决跨命名空间漏样本问题。取样按平台、UTC日期桶、根帖和排序来源分层轮询，同一请求幂等返回同一运行；重算只校验并读取manifest。非弃判标签必须引用本样本冻结上下文，弃判不能携带观点；事件修订、正文版本、可见性或上下文缺失都会把历史运行标为过期。

FastAPI自动发布5个分析operation，错误响应描述显式固定后，Python 3.12容器运行时OpenAPI与Python 3.13本地导出一致；UmiOpenAPI只在`frontend/src/api`生成端点与类型，业务调用继续统一经过`frontend/src/request.ts`。事件卡展示固定分母、平台/根帖/排序构成、人工标注、弃判、聚合观点、支持性引用及冻结上下文。真实PostgreSQL 16/RabbitMQ 4.1全量131项测试通过；Ruff、严格mypy、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建及依赖审计通过。0014→0015带数据升级保留事件且metadata差异为0；隔离Compose schema为0015，真实Celery任务一次完成，后端替换后代理返回预期401，关停无SIGKILL/OOM。浏览器以明确合成的两平台、两根帖、四评论验证2条跨平台引用、2条弃判；桌面卡片完整可读，390px视口无页面横向溢出。远端CI在提交`8e1886f92e85468f4b124425e4ed22a11f7da918`复现全部门禁，见 [GitHub Actions #35003487831](https://github.com/StephenQiu30/hotkey-server/actions/runs/35003487831) 与 [analysis-sample-baseline-poc.json](../operations/evidence/analysis-sample-baseline-poc.json)。本片未访问外部平台、现有MinIO或模型，不完成模型质量评估和完整AC-007-009。

### 3.19 S04-T02B 本地免费模型结构化标注 POC 登记

本片只关闭 `TASK-007-S04-T02` 的模型选型风险，不接入生产分析。当前机器已存在 Ollama 0.33.2、`qwen3.5:latest`、`qwen3.8:latest`，无需下载模型、增加SDK或调用付费/外部API。依据 Ollama 官方 structured output 建议，POC 将 Pydantic JSON Schema 同时传入 `format` 和提示，关闭流式输出与thinking并使用 temperature 0；请求仅允许回环地址。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/analysis/evaluation.py`、`backend/scripts/evaluate_analysis_models.py` | analysis持有合成fixture契约、提示与评分门槛；脚本只编排回环Ollama、按模型卸载并输出脱敏报告 |
| `backend/tests/fixtures/analysis/model-evaluation-v1.json` | 保存8条明确合成的分类样本和人工期望；不含真实社交正文、URL、账户或凭据 |
| `backend/tests/unit/test_analysis_model_evaluation.py` | 离线验证fixture、引用/字段评分、门槛和非回环拒绝；不调用模型 |
| `docs/operations/evidence/analysis-model-evaluation-poc.json` | 保存本机、Ollama、模型版本、字段质量、耗时、token和常驻内存结果及限制 |

Red标准：评估脚本/fixture尚不存在，离线测试收集失败。Green标准：两个本地候选模型都完整跑8条；结构化输出、引用白名单、提示注入和关键弃判必须100%，四个核心字段准确率各不低于85%，P95≤30秒且常驻≤20GiB。至少一个模型通过才可在下一片登记 `ai/` 生产适配器；本片不增加迁移、API、前端、Worker或生产配置，不把合成分类质量表述为真实平台质量。

实施结果（2026-09-16）：Red阶段在基线提交上运行目标测试，因文件不存在以pytest退出码4失败。评估器只允许明文HTTP回环origin，并用`httpx.Client(trust_env=False)`从根源阻止本机Ollama请求继承环境代理；第一次受代理影响且被中止的调用明确排除，不计模型结果。初版“unknown枚举+abstained布尔值”无法在JSON Schema中表达跨字段约束，`qwen3.5`有2条输出在Pydantic校验时暴露矛盾；改成`decision=label|abstain`判别联合后，结构化与引用均达到100%。继续强化提示仍不能稳定执行无上下文/多目标弃判，因此没有降低阈值或堆叠提示，而是在模型前增加只负责输入可判定性的确定性范围门。

最终混合管线在8条合成样本中以范围门处理2条、`qwen3.5:latest`推理6条，主题、目标、情绪、引用、注入防护和关键弃判均为100%，立场87.5%，P95 9.51秒，最大常驻约5.50GB，全部通过冻结门槛。`qwen3.8:latest`初轮8条全部超过120秒请求超时，已确定不能满足30秒P95门槛，未在结构调整后重复运行。选定`qwen3.5:latest`作为下一片生产适配器候选；当前没有新增`ai/`、迁移、API、Swagger、生成客户端、前端或Worker，也没有访问真实平台、现有MinIO或付费服务。完整迭代、模型digest、指标和限制见[analysis-model-evaluation-poc.json](../operations/evidence/analysis-model-evaluation-poc.json)。远端CI在提交`967a30113ad7d290b4160c299f211941ed13c95d`复现全量门禁并于2分50秒成功完成，见[GitHub Actions #35008691411](https://github.com/StephenQiu30/hotkey-server/actions/runs/35008691411)。

### 3.20 S05-T01A 分析知识条目与中文精确检索实施登记

本片启动`TASK-007-S05-T01`的最小可用知识闭环：把一个已完成、未过期且带支持性引用的分析运行确定性保存为不可变知识版本，并按事件或归一化中文子串检索。它不接入pgvector、embedding、模型摘要、任意手写正文或问答，不改变S04模型分类口径，也不访问外部平台、现有MinIO或付费服务。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/knowledge/__init__.py`、`models.py`、`schemas.py`、`services.py` | knowledge拥有条目、不可变版本、版本引用、确定性正文和精确检索；不直接导入其他领域ORM或调用模型 |
| `backend/src/analysis/services.py`、`backend/src/events/services.py` | 分别提供同一Session可读的分析快照DTO与事件标题，不向knowledge暴露ORM模型 |
| `backend/src/api/routers/knowledge.py`、`api/dependencies.py`、`api/router.py` | 新增发布、列表/检索、详情三个同步协议，只注入KnowledgeService；FastAPI/Pydantic自动维护Swagger |
| `backend/src/migrations/versions/0016_analysis_knowledge.py`、`db/metadata.py`、`db/health.py` | 新增knowledge_entries/versions/citations并登记唯一schema head；不修改0015及更早迁移 |
| `backend/tests/integration/test_knowledge.py`、`backend/tests/conftest.py`、架构/API测试 | 先Red证明模块和operation缺失，再验证发布幂等、引用、字面检索、通配符转义、过期与依赖边界 |
| `frontend/src/features/knowledge/KnowledgeSearch.tsx`、`app/App.tsx`、`features/events/EventAnalysis.tsx`、`style.css` | 提供保存分析、精确检索、空态、范围说明、过期状态和可用引用；390px纵向排布 |
| `docs/openapi/openapi.json`、`frontend/src/api/*` | 只由FastAPI导出和UmiOpenAPI生成；生成目录固定`src/api`，业务请求继续只经根级Axios `request.ts` |
| `docs/operations/evidence/knowledge-exact-search-poc.json` | 保存迁移、合成数据、浏览器、检索与限制；不保存真实平台正文或凭据 |

技术决策：不增加Python/npm依赖或第二套检索服务。正文用Unicode NFKC、casefold和折叠空白生成`search_text`，PostgreSQL以转义后的`contains`执行小规模字面子串查找；结果按创建时间和ID稳定排序。发布来源固定为`succeeded`且非`stale`的分析运行，每个运行至多一个知识条目；正文按固定模板从观点统计生成，引用固定到content_version及hash，读取时重新检查可见性、当前版本和分析状态。

SMART验收：给定一个明确合成、已完成且包含两个观点和两条引用的分析运行，首次发布返回HTTP 201及一个版本，重复请求返回同一条目且不增加版本；查询一个正文中的中文词组命中，查询包含`%`或`_`只按字面量匹配，按其他事件过滤为空。给任一被引用内容增加新正文版本后，知识详情和检索结果均显示`stale`，旧hash与版本ID仍可追溯但失效正文不作为可信引用展示。该片不声明语义召回、自动摘要、问答、删除传播或完整AC-007-011。

实施结果（2026-09-16）：`analysis_snapshot`发布、幂等重放、事件过滤、中文连续词组与`%_`字面检索均通过；引用正文新增版本后，详情与列表立即标记过期并隐藏失效正文。独立迁移POC从0015升级到`0016_analysis_knowledge`，既有合成分析行保留、三张知识表为空建立，SQLAlchemy模型比较无差异。FastAPI自动生成3个新operation，`@umijs/openapi`只生成`frontend/src/api/knowledge.ts`、索引和DTO，业务界面继续只经根级Axios请求层调用。

本地门禁为真实PostgreSQL 16与RabbitMQ 4.1下138项pytest通过，保留2条上游弃用提示；Ruff、严格mypy（120个源文件）、OpenAPI/UmiOpenAPI漂移、前端目录与负向边界、Prettier、TypeScript/Vite和生产依赖审计通过。隔离Compose的prefork诊断以attempts=1完成；干净完整拓扑上Swagger与owner工作台2项Chromium场景通过。额外合成业务POC实际展示知识快照，命中中文和字面通配符查询，并在引用变化后显示过期状态；390px无横向溢出。功能提交`40b31b789a3a32382cbaf9eb2eb52220aa94cfed`的远端CI在2分37秒内复现完整门禁并成功。该结果不包含真实平台、现有MinIO、付费服务或模型调用，结构化证据见[knowledge-exact-search-poc.json](../operations/evidence/knowledge-exact-search-poc.json)。

### 3.21 S05-T01B 中文语义召回与pgvector选型POC登记

本片只回答“现有免费本地embedding是否值得进入生产迁移”。它不改数据库镜像、迁移、API、Swagger、生成客户端、前端或Worker，也不把合成召回结果表述为真实平台质量。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/knowledge/retrieval_evaluation.py` | 定义冻结fixture、字面基线、向量评分和预设门槛；不含生产检索或数据库访问 |
| `backend/tests/fixtures/knowledge/retrieval-evaluation-v1.json` | 保存8条合成事件知识和16条中英文别名查询；不含真实正文、URL或账户 |
| `backend/scripts/evaluate_knowledge_retrieval.py` | 仅编排回环Ollama `/api/embed`、资源观测和脱敏JSON输出 |
| `backend/tests/unit/test_knowledge_retrieval_evaluation.py` | 离线验证fixture、字面评分、余弦排序、门槛与非回环拒绝 |
| `docs/operations/evidence/knowledge-retrieval-evaluation-poc.json` | 保存模型digest、质量/延迟、pgvector 0.8.6协议POC、限制和选型结论 |

Red标准：目标离线测试因评估模块不存在而收集失败。Green门槛在模型运行前固定：16条查询Top-1≥75%、Recall@3≥90%、MRR≥0.80，Recall@3较字面基线至少提高40个百分点；向量必须为1024维且L2范数误差≤0.001，查询P95≤2秒、模型常驻≤12GiB。pgvector必须完成扩展创建、1024维余弦排序和事件过滤；任一门槛失败则维持S05-T01A精确检索，不加入生产依赖。

实施结果（2026-09-16）：Red阶段目标测试因文件不存在未运行。Green阶段4项离线测试通过；本机`qwen3-embedding:latest`对8条合成知识、16条中英文别名查询的Top-1、Recall@3和MRR均为100%，字面基线均为0%，1024维单位向量最大范数误差约`3.95e-7`，查询P95约0.124秒，常驻约6.60GB，全部通过冻结门槛。可丢弃`pgvector/pgvector:0.8.6-pg16`成功建立扩展、1024维列和HNSW余弦索引，未过滤及事件过滤查询都返回预期条目，容器随后删除。

选型结论：允许下一片设计`qwen3-embedding` 1024维 + pgvector 0.8.6生产迁移；个人规模先用带关系过滤的精确余弦查询，HNSW只保留已验证的演进路径，不因8条数据创建近似索引。当前提交仍不改Compose、迁移、API、Swagger、生成客户端、前端或Worker。完美分数只适用于合成集，生产启用前必须用真实已授权内容扩充盲测集。提交`0067fe80c1c93375f1cd5f3122d6a551ba22a54a`的远端CI在2分35秒内复现完整门禁并成功，完整证据见[knowledge-retrieval-evaluation-poc.json](../operations/evidence/knowledge-retrieval-evaluation-poc.json)。

### 3.22 S05-T01C 可恢复语义索引与检索实施登记

本片把已通过POC的自建embedding和pgvector接入知识库，只完成“一条知识版本建立索引 → 语义查询 → 返回带原引用结果”的最小闭环。Ollama作为用户配置的外部自建HTTP服务，不纳入根Compose，也不下载模型；未配置时精确检索可用、语义操作明确不可用。本片不增加分析模型、问答、自动后台重试、ANN、中文分词或第二套检索服务。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/ai/__init__.py`、`contracts.py`、`adapters/{__init__,ollama}.py` | 新登记无ORM的ai模块；定义embedding协议/结果/稳定错误并封装Ollama `/api/embed`，不含业务prompt或索引事务 |
| `backend/src/core/config.py`、`.env.example`、`.env.prod.example`、`docker-compose.yml`、`docker-compose-prod.yml` | 增加可选自建Ollama origin、固定模型和超时配置；不创建Ollama服务或写入凭据 |
| `backend/src/knowledge/{models,schemas,services}.py` | 每个知识版本持有一个chunk与索引状态；负责显式建索引、版本/hash防竞态和精确余弦查询 |
| `backend/src/migrations/versions/0017_knowledge_semantic_index.py`、`db/{metadata,health}.py` | 原子创建vector扩展和1024维chunk表，并为历史版本补pending记录；schema head前移 |
| `docker-compose.yml`、`.github/workflows/ci.yml`、`backend/pyproject.toml`、`backend/uv.lock` | 开发、CI与应用依赖统一锁定pgvector 0.8.6协议；不保留无扩展PostgreSQL兼容路径 |
| `backend/src/api/routers/knowledge.py`、`api/dependencies.py` | 增加显式建索引操作和检索mode，只做认证/验证/服务注入 |
| `backend/tests/{unit,integration,architecture}/` | 先Red验证ai未登记、chunk/迁移/语义操作缺失，再覆盖协议校验、失败状态、重试、过滤、排序和依赖边界 |
| `docs/openapi/openapi.json`、`frontend/src/api/*` | 只由FastAPI与UmiOpenAPI生成；不手写端点或类型，不创建shared/shard文件 |
| `frontend/src/features/knowledge/KnowledgeSearch.tsx`、`frontend/src/style.css`、`frontend/tests/knowledge.spec.ts` | 切换精确/语义模式、触发pending/failed索引、显示相似度与明确不可用状态，并验证390px |
| `docs/operations/evidence/knowledge-semantic-search-poc.json` | 记录迁移、真实pgvector、回环Ollama、合成闭环、浏览器和验证边界，不含平台正文或凭据 |

技术决策：新增`pgvector` Python类型适配依赖，数据库统一使用`pgvector/pgvector:0.8.6-pg16`；向量固定1024维、模型固定`qwen3-embedding:latest`。Ollama origin只接受无凭据HTTP(S) origin，必须与64位模型digest同时配置；客户端`trust_env=false`且有界超时，digest随向量持久化并参与查询过滤。发布事务只创建pending chunk，显式索引在外部调用前后分别读取并二次核验版本/hash；失败记录稳定code，重试覆盖同一chunk。语义查询只查ready且模型、digest、维度一致的当前版本，先应用event过滤，再按cosine distance与条目ID稳定排序，limit最大20。

SMART验收：迁移在带0016数据的PostgreSQL 16 + pgvector 0.8.6上升级到0017后保留旧知识并补一个pending chunk；用明确合成知识和本机`qwen3-embedding`建立索引后，英文别名查询命中目标条目且Top-1正确，事件过滤不越界，返回`query_mode=semantic`与`similarity`。模拟超时必须保留知识条目并将chunk置failed，随后重试可恢复ready；模型返回非1024维或非有限数值必须拒绝。Swagger快照、UmiOpenAPI逐文件漂移、Axios边界、完整后端门禁、Compose与Chromium流程全部通过。

实施结果（2026-09-16）：Red阶段因`ai`与embedding契约不存在在测试收集退出码2。Green阶段统一切换pgvector 0.8.6镜像，0016→0017迁移保留1条知识版本和2条引用并补1个pending chunk；真实pgvector测试覆盖失败持久化、同chunk重试、事件过滤和精确余弦排序。本机Ollama从宿主回环和Compose backend均可达，合成中文知识从pending建立为ready，英文`release outage`返回预期Top-1、相似度约55.7%及2条可用引用，另一事件过滤为空。首次Compose验证发现环境变量字符串不能由`Literal[1024]`解析，已改为整数解析后显式拒绝非1024值，重新构建后迁移和全部角色启动通过。

本地验收：148项后端测试通过并保留2条上游弃用提示；Ruff、严格mypy（127个源/脚本文件）、pip-audit、FastAPI OpenAPI快照、UmiOpenAPI逐文件漂移、前端边界/负向样例、Prettier、TypeScript/Vite和生产依赖审计均通过。干净Compose完成迁移、prefork诊断、代理替换和无SIGKILL停机，Chromium 3项通过。功能提交`64ff570342c4cec4a43703779fd26ea53e114a35`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35016406522)在2分46秒内复现完整门禁并成功；结构化边界见[knowledge-semantic-search-poc.json](../operations/evidence/knowledge-semantic-search-poc.json)。真实平台正文、现有MinIO、外部托管Ollama、扩展权限和七日运行尚未验收。

### 3.23 S05-T02A 受控统计、证据问答与撤权屏障实施登记

本片完成`TASK-007-S05-T02`的在线最小闭环：用户明确选择评论数量或证据问题，服务以确定性规则返回范围、版本、答案状态和可用引用；内容删除/用途撤销后，同一事务建立可见性与知识索引屏障。本片不增加迁移、依赖、后台清理任务、MinIO物理删除、备份恢复或生产分类模型。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/analysis/{schemas,services}.py` | 定义并计算事件评论统计DTO；复用现有事件范围和评论候选规则，不接收SQL或生成文本 |
| `backend/src/contents/{schemas,services}.py` | 定义撤权输入/结果及同Session可见性写入；收件箱继续只展示available内容 |
| `backend/src/knowledge/{schemas,services}.py` | 严格判别问题、编排受控统计/检索、生成确定性带引用回答；传播关联chunk的stale/deleted屏障 |
| `backend/src/api/routers/{knowledge,contents}.py`、`api/dependencies.py` | 暴露唯一operation_id与服务注入，只处理认证、验证和协议 |
| `backend/tests/{integration,unit,architecture}/` | Red覆盖统计/问答/未知事实/撤权传播及HTTP契约，验证跨领域依赖仍通过公开服务函数 |
| `docs/openapi/openapi.json`、`frontend/src/api/*` | 只由FastAPI和UmiOpenAPI生成，不手写端点、类型、shared或shard文件 |
| `frontend/src/features/knowledge/KnowledgeSearch.tsx`、`features/contents/Inbox.tsx`、`app/App.tsx`、`request.ts`、`style.css`、`frontend/tests/knowledge.spec.ts` | 增加受控问答表单、范围/统计/引用/未知状态、撤权动作及稳定错误文案；复用生成客户端与根Axios，验证390px |
| `docs/operations/evidence/knowledge-controlled-query-poc.json` | 记录明确合成统计、证据、未知、撤权前后和验证边界，不含真实平台正文 |
| `README.md`、`README.en.md` | 更新当前可用范围和仍未完成的真实来源、MinIO物理删除与自动分析边界 |

技术决策：`POST /api/knowledge/query`只接收`comment_count|evidence`判别输入；统计时间窗为UTC半开区间，按内容ID去重并返回`event-comment-count-v1`。证据回答调用现有精确/语义检索，仅拼装当前有效知识正文与引用，方法固定`deterministic_retrieval_v1`；零命中返回`unknown`。`POST /api/contents/{id}/withdraw`只允许`deleted|purpose_revoked`，内容领域负责可见性，知识领域在同一事务清空关联embedding并设置chunk状态；详情保留审计，所有在线列表/检索/问答排除失效内容。

SMART验收：给定一个明确合成事件、两个平台和三条可见评论，固定UTC时间窗回答总数3且平台/类型分项相加一致，窗口外与根帖不计入；同一数据的证据问题返回`answered`及可打开的固定正文版本引用，陌生问题返回`unknown`且引用为空。对一条被引用评论执行用途撤销或删除后，收件箱、统计、精确/语义检索和问答立即不再暴露它，知识详情显示过期、引用正文隐藏且关联向量为空；重复撤权幂等。Swagger、UmiOpenAPI逐文件漂移、Axios边界、后端门禁、Compose和Chromium流程全部通过。MinIO对象/历史版本清理与恢复重放明确保留未完成。

实施结果：Red阶段的知识集成与HTTP操作测试在收集期以退出码2失败，原因是受控问题和内容撤权Schema尚不存在；实现后完整后端测试为150项通过并保留2条上游弃用提示。Ruff格式/检查、严格mypy（127个源/脚本文件）、pip-audit、FastAPI OpenAPI快照、UmiOpenAPI逐文件漂移、前端边界/负向样例、Prettier、TypeScript/Vite和生产依赖审计均通过；`npm ci`仍报告`@umijs/openapi`既有开发期传递依赖的2个high告警，生产依赖为0。可丢弃Compose环境完成pgvector迁移、prefork诊断、后端替换代理及干净全拓扑，Chromium 3项通过；Worker、Backend、Scheduler无SIGKILL停止，临时容器与卷已移除。功能提交`63fc5feba90a5c2f3040a843a1795b870c796354`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35036895106)在2分45秒内复现完整门禁并成功。结构化结果见[knowledge-controlled-query-poc.json](../operations/evidence/knowledge-controlled-query-poc.json)。这些证据仅使用明确合成数据，不代表真实平台、现有MinIO、物理删除、恢复重放或七日运行已验收。

### 3.24 S06-T01A 删除清单、证据清理与恢复重放实施登记

本片只实现`TASK-007-S06-T01`可离线验收的生命周期核心，不增加HTTP接口、前端页面、依赖、Celery kind或常驻服务。在线撤权仍由现有接口完成；对象删除与恢复重放由显式CLI在请求外执行。真实现有MinIO、bucket策略和完整备份恢复留待获得私有运行配置后验收。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/migrations/versions/0018_content_withdrawal_lifecycle.py` | additive增加内容tombstone及原始页对象清理状态；保留0017历史不变 |
| `backend/src/contents/{models,schemas,services}.py` | 保存稳定来源身份的撤权清单、导出严格manifest、按manifest查找并重放内容屏障；返回关联raw_page ID，不导入证据ORM |
| `backend/src/evidence/{models,contracts,services}.py` | 拥有原始页`available/delete_pending/deleted/failed`状态、调度与逐个对账；不导入内容ORM |
| `backend/src/evidence/adapters/minio.py` | 校验不可变对象后删除精确键及其全部版本/删除标记；不存在即幂等成功，不调用SDK私有接口 |
| `backend/src/knowledge/services.py` | 在唯一外层事务编排内容屏障、知识索引失效、tombstone和证据待删状态；提供manifest重放入口 |
| `backend/src/collection/services.py` | 提交时按稳定内容身份加事务锁并复查tombstone；上传后拒绝时补偿删除，失败对象登记为可重试清理记录 |
| `backend/src/cli/deletions.py`、`cli/commands.py` | 导出私有manifest、重放屏障、显式执行有界对象对账；输出计数和hash，不输出正文、凭据或对象键 |
| `backend/src/db/health.py`、`backend/scripts/verify_minio.py` | 就绪版本推进到0018；MinIO协议脚本改用正式删除方法并核对全部对象版本不存在 |
| `backend/tests/{conftest.py,integration/test_collection_page.py,integration/test_deletion_lifecycle.py,unit/test_minio.py,unit/test_deletions.py}` | Red覆盖版本删除、共享页整页撤回、采集/撤权竞态、补偿失败对账、用途撤销、manifest恢复重放、正文最小化及私有文件权限 |
| `docs/operations/evidence/deletion-recovery-poc.json`、`README.md`、`README.en.md` | 记录结构化证据与未验收的现有MinIO、multipart生命周期、真实备份和七日运行边界 |

技术决策：`purpose_revoked`只建立`unavailable`屏障，不删除仍获准保存的证据；`deleted`建立`deleted`屏障并把内容所有版本/观察关联的原始页置为`delete_pending`。共享页不做来源无关的JSON/HTML字符串替换；整页对象删除后，raw_page最小元数据保留用于对账。manifest使用稳定来源身份而不是数据库UUID，恢复后按当前数据库重新发现原始页。CLI每次有界处理并支持失败后重试，不假设PostgreSQL和MinIO分布式提交。

SMART验收：给定一页包含两个规范对象的明确合成证据，删除其中一个后在线查询立即屏蔽该对象，tombstone与整页待删状态同事务存在；对账确认对象不存在后标记页`deleted`并清除被删对象规范正文，另一个对象仍可见但旧页不可作为有效证据。用途撤销不进入对象删除。给对象存储一次失败，状态保留可重试且第二次成功；导出manifest、模拟恢复到撤权前状态并重放后，内容、知识向量与原始页删除状态重新生效，重复重放和清理不增加记录。迁移、后端门禁、Compose与结构化POC通过；未连接现有MinIO时不完成完整EV-007-010。

实施结果（2026-09-16）：Red阶段目标测试以退出码2在收集期失败，原因是撤权记录模型尚不存在。Green/Refactor后161项后端测试通过并保留2条上游弃用提示；Ruff覆盖156个文件，严格mypy覆盖129个源/脚本文件，pip-audit、FastAPI OpenAPI快照、UmiOpenAPI逐文件漂移、前端边界/负向样例、Prettier、TypeScript/Vite与生产依赖审计均通过。0017数据库中的既有raw_page升级到0018后得到`available/0`安全默认值，迁移头正确且ORM元数据差异为0。显式CLI导出1条清单到0600文件，模拟恢复为`available`后重放得到`deleted/delete_pending`，再次建立唯一tombstone；采集期间写入tombstone的竞态和一次补偿删除失败也由集成测试验证为可重试。

可丢弃Compose环境完成0018迁移、Backend/Worker/Scheduler/Web全拓扑和Chromium 3项流程；功能提交`6ce9d90bca469583a22994d0852e4257f6c169d4`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35039356595)在2分42秒内复现完整门禁并成功。结构化结果见[deletion-recovery-poc.json](../operations/evidence/deletion-recovery-poc.json)。本片证明应用协议、数据库迁移和合成恢复重放，不证明用户现有MinIO的TLS/权限/版本历史/对象锁/multipart生命周期，也没有执行真实`pg_dump/pg_restore`、旧应用回滚或七日真实来源运行，因此EV-007-010与`TASK-007-S06-T01`保持部分完成。

### 3.25 S06-T01B PostgreSQL备份恢复POC实施登记

本片只补齐可自动复现的数据库备份恢复证据，不增加API、页面、依赖、迁移、队列kind或常驻服务。脚本只接受显式`HOTKEY_BACKUP_POC_DATABASE_URL`，要求源库名为`hotkey`、Compose项目名以`hotkey-`开头且不能是默认`hotkey`，并核对目标postgres容器项目标签；不提供生产兼容旁路。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/scripts/verify_backup_restore.py` | 在可丢弃Compose PostgreSQL中构造合成内容、执行custom-format dump、恢复到隔离库、重放删除清单两次并核对屏障；finally清理临时库/文件 |
| `.github/workflows/ci.yml` | 在浏览器流程后、停机前运行真实备份恢复POC，并显式传入隔离项目和本机数据库URL |
| `docs/operations/evidence/postgres-backup-restore-poc.json` | 记录dump/restore版本、恢复前后状态、幂等结果、清理和未覆盖边界 |
| `README.md`、`README.en.md` | 公开数据库恢复POC的运行边界，并继续区分现有MinIO与生产灾备验收 |
| `docs/design/007-热点事件与评论知识库设计.md`、`docs/plans/007-热点事件与评论知识库计划.md` | 固化恢复顺序、破坏边界、SMART验收与证据结论 |

SMART验收：给定0018头的可丢弃`hotkey`库和一条`available`合成内容/raw_page，在删除前执行真实`pg_dump -Fc`，随后在源库删除该内容并导出一条严格manifest；把旧dump恢复到随机隔离库后，恢复库在重放前仍为`available/available/0 tombstone`，连续重放两次后为`deleted/delete_pending/1 tombstone`且第二次不新增记录。脚本输出只包含计数、状态和工具版本，不输出正文、连接串、对象键或凭据；成功/失败均删除恢复库、dump和manifest。CI、完整门禁与结构化证据通过；现有MinIO、真实生产备份保留和旧应用回滚仍不因此验收。

实施结果（2026-09-16）：Red阶段按登记命令运行时以退出码2失败，因为验证脚本尚不存在。实现后使用PostgreSQL 16.15的`pg_dump -Fc`生成非空custom dump；恢复库在清单重放前为`available/available/0`，两次重放后均为`deleted/delete_pending/1`，每次`applied=1,missing=0`。默认Compose项目和非`hotkey`数据库均在任何数据库操作前被拒绝；成功后复核恢复库、dump、0600 manifest、源业务行与关联审计均不存在。完整后端161项、Ruff 157文件、严格mypy 130文件、pip-audit、OpenAPI、UmiOpenAPI漂移、前端边界/格式/构建与生产依赖审计通过；按远端顺序执行prefork、Backend替换、Chromium 3项、恢复脚本和正常停机均通过。功能提交`0aa7b5068eb3f4d71371f986206f02455af49dcb`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35040730933)首次执行新增步骤即成功，耗时2分35秒，远端dump为90422字节且重放/清理结果与本地一致。结构化证据见[postgres-backup-restore-poc.json](../operations/evidence/postgres-backup-restore-poc.json)。

### 3.26 S03-T02A 来源失败延期、预算与任务结算实施登记

本片继续 `TASK-007-S03-T02`，先修复评论多页执行依赖的失败恢复语义。当前执行器在来源失败时只结束 CollectionRun，随后 Worker 仍会完成 Job，可能形成“采集失败、任务成功”；429 的 Retry-After 虽由适配器解析，但尚未进入持久任务调度。目标是在不连接真实来源或现有MinIO的条件下，让一次来源调用只有成功提交、原子延期或原子失败三种可恢复结果。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0019_collection_retry_budget.py`，将每个run固定一次预留改为至少一次，使每次真实重试都能累计请求预算；同步 `backend/src/db/health.py` 的唯一迁移头，并让 `backend/scripts/verify_backup_restore.py` 复用该事实源，不修改既有迁移 |
| 任务状态机 | 修改 `backend/src/jobs/execution.py`：增加同Session的租约失败与延期操作，严格校验epoch/fencing/租约/截止时间，延期递增epoch并写Outbox |
| 采集编排 | 修改 `backend/src/collection/{execution,services}.py`：永久失败同时结算run/job；429遵循Retry-After，暂时网络失败有界退避；延期前重新锁定并占用日预算，预算或尝试耗尽时原子失败 |
| 模型与契约 | 修改 `backend/src/collection/models.py` 的约束镜像；HTTP DTO、Swagger和前端API不增加字段或手写请求，现有reserved_requests字段表达累计预留次数 |
| 验证与证据 | 先在真实PostgreSQL测试中复现失败run对应成功Job，再验证429延期时间、epoch/Outbox、第二次成功、预算耗尽、三次失败和旧租约拒绝；新增 `docs/operations/evidence/collection-retry-poc.json` |
| 运行说明 | 修改 `README.md`、`README.en.md`，公开Retry-After、预算和失败账本语义，同时保留真实来源未准入边界 |

本片沿用SQLAlchemy、PostgreSQL、Celery/RabbitMQ和现有适配器，不增加库、队列、HTTP端点或兼容分支。合成来源只验证状态机；真实429、平台用途、现有MinIO、多页评论和七日观察继续保持未通过。

SMART验收：给一个返回Retry-After=12秒的合成429页，首次领取后run与Job回到queued、epoch增至2、新Outbox的due_at/available_at按12秒设置且run/日账本预留均增加1；到期后的第二次执行只保存一页并成功。连续三次暂时失败后run/job都为failed且无第四个Outbox；预算已满时不创建重试Outbox。永久失败一次后run/job同时failed，Worker不能再把Job完成为succeeded。迁移、真实数据库、真实RabbitMQ、静态检查和现有浏览器主链通过。

实施结果（2026-09-16）：Red阶段4个真实PostgreSQL用例同时失败，分别复现永久失败后Job仍成功、429不延期、暂时失败无尝试上限和预算满时无明确重试结算。Green/Refactor后，429的12秒Retry-After原子建立epoch 2与第二条Outbox，run和日账本预留从1增加到2；第二次执行只保存1页并让run/job完成。连续3次network_error后run/job共同失败且只有3条Outbox，预算4/4时不新增重试消息；旧epoch不能失败或延期替代租约。永久失败和执行前撤权也同时结束run/job，不再由Worker覆盖为成功。

完整166项后端测试、Ruff 158文件、严格mypy 131文件、Python/前端生产依赖审计、OpenAPI/UmiOpenAPI、前端边界/格式/构建均通过。隔离Compose升级到0019，真实prefork诊断attempts=1、Backend替换代理401、Chromium 3项、PostgreSQL 16.15备份恢复与正常停机通过；备份脚本改为读取唯一迁移事实源。功能提交`17c64f20f0c305c10610395a49e034709b037801`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35042332305)在2分46秒内复现全部门禁并成功，远端0019 dump为90347字节。证据见[collection-retry-poc.json](../operations/evidence/collection-retry-poc.json)。本片仍未执行真实平台429、现有MinIO、多页评论或七日观察，因此EV-007-005与`TASK-007-S03-T02`保持部分完成。

### 3.27 S03-T02B 用户取消与页提交屏障实施登记

本片继续 `TASK-007-S03-T02`，修复采集 Job 被用户取消后 CollectionRun 仍可能保持 queued/running，以及取消发生在来源请求或对象上传期间时旧 Worker 仍可能提交页面的问题。沿用现有 SQLAlchemy、Job/CollectionRun 状态机和证据删除账本，不增加依赖、迁移、HTTP 端点、DTO、OpenAPI字段、队列kind或兼容分支。

| 类别 | 文件与职责 |
|---|---|
| 任务分派 | 修改 `backend/src/jobs/services.py`：按不可变 Job kind 调用注入的取消处理器，诊断任务继续使用通用取消；修改 `backend/src/api/dependencies.py`：仅在组合根为 `collect_page` 注入采集取消处理器 |
| 原子状态机 | 修改 `backend/src/jobs/execution.py`：提供同Session取消原语；修改 `backend/src/collection/services.py`：按 CollectionRun→Job 顺序锁定并原子取消两者，上传后的状态/租约拒绝执行精确对象补偿，补偿失败进入既有 `failed` 清理账本 |
| 执行边界 | 修改 `backend/src/collection/execution.py`：来源返回后遇到取消或陈旧租约时停止当前执行，不让 Worker继续完成任务；其他 AppError 保持可见 |
| 失败测试 | 修改 `backend/tests/integration/test_collection_page.py` 与 `backend/tests/integration/test_user_workflow.py`：覆盖排队取消、请求期间取消、上传期间取消、补偿失败和旧lease隔离；现有 `backend/tests/integration/test_jobs.py` 继续证明诊断任务取消不回归 |
| 证据与说明 | 新增 `docs/operations/evidence/collection-cancellation-poc.json`，修改 `README.md`、`README.en.md` 及本Design/Plan，记录状态、对象与未覆盖边界 |

SMART验收：给一个 queued 或 running 的 `collect_page` Job，经现有取消接口后对应 Job/CollectionRun在同一事务中均为 cancelled，run记录`outcome=partial`、`stop_reason=user_cancelled`和完成时间；旧Dispatch/Lease不能领取、完成、失败或延期。给一个在来源请求返回前被取消的合成页，执行器返回停止且 RawPage、Content、Checkpoint、后续run和对象均为0。给一个在对象put完成后触发取消的合成页，数据库提交被第二道屏障拒绝且该对象被删除；注入一次删除失败时只登记一条 `failed` RawPage，既有清理器重试后删除。完整真实PostgreSQL/RabbitMQ、静态检查、OpenAPI/UmiOpenAPI漂移和浏览器主链通过；真实平台请求、用户现有MinIO和多页评论仍不因此验收。

实施结果（2026-09-16）：Red阶段聚焦运行出现5项失败，其中3项证明采集取消处理器不存在、1项证明陈旧fencing上传后遗留对象，另1项是新增HTTP fixture预算不足；先把fixture改为明确充足预算，再进入实现。Green/Refactor后，现有取消接口按Job kind分派，`collect_page`按CollectionRun→Job顺序在同一事务完成取消；请求中取消不写入任何页面/内容/检查点，上传中取消删除精确对象，补偿删除失败只登记1条`failed` RawPage并由既有对账器删除。旧lease不能完成、失败或延期；诊断任务原有取消行为保持通过。

完整170项后端测试、Ruff 158文件、严格mypy 131文件、Python/前端生产依赖审计、OpenAPI/UmiOpenAPI、前端边界/格式/构建均通过。隔离Compose保持0019，真实prefork诊断attempts=1、Backend替换代理401、Chromium 3项、PostgreSQL 16.15备份恢复与正常停机通过；本地dump为90426字节。功能提交`ff514541ce3204eac8091eff1dc92806590313c6`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35044168135)在3分05秒内复现全部门禁并成功，远端0019 dump为90413字节。证据见[collection-cancellation-poc.json](../operations/evidence/collection-cancellation-poc.json)。本片未修改HTTP契约、迁移或生成API，也未执行真实来源、现有MinIO、多页评论或七日观察，因此EV-007-005与`TASK-007-S03-T02`仍保持部分完成。

### 3.28 S03-T02C 页面与Job终态原子结算实施登记

本片继续 `TASK-007-S03-T02`，修复最后一页事务已把CollectionRun结算为completed/failed后，Worker仍需第二个事务完成Job的崩溃窗口；同时禁止调用方只凭fencing token领取run。该切片是多页续采的直接前置，不修改HTTP路由、DTO、OpenAPI快照、UmiOpenAPI生成文件、迁移、依赖或队列kind。

| 类别 | 文件与职责 |
|---|---|
| Job状态机 | 修改 `backend/src/jobs/execution.py`：增加同Session的Lease校验与成功结算原语，复用现有失败/取消规则，不放宽epoch、fencing、租约或deadline |
| 采集事务 | 修改 `backend/src/collection/services.py`：领取run必须接收完整Lease；最后一页在保存证据、内容、Checkpoint和run终态的同一事务结算Job；同一Lease的重复页只返回已有Checkpoint |
| Worker边界 | 修改 `backend/src/collection/execution.py`、`backend/src/worker/app.py`：collect_page由采集服务拥有终态，Worker不再事务外二次complete；诊断Job仍走通用complete |
| 失败测试 | 修改 `backend/tests/integration/test_collection_page.py`：覆盖成功/带证据失败原子终态、提交后重投、暂停、旧Lease和无第二次complete |
| 证据与说明 | 新增 `docs/operations/evidence/collection-atomic-settlement-poc.json`，修改 `README.md`、`README.en.md`、Design/Plan与Operations，记录本地/远端门禁和未覆盖边界 |

SMART验收：给一个有效collect_page Lease和最后一页，单次服务事务后run=completed且Job=succeeded，RawPage/Checkpoint各1，Worker不调用第二次complete；给一个带允许证据但结果failed的页面，两者同事务failed。模拟页面事务提交后消息重投，同epoch消息不能再次领取Job，页面、内容、对象和Checkpoint不增加。给一个错误epoch/fencing、过期Lease或在领取后暂停的运行，不能调用来源或写页，run/job按原事实保持陈旧拒绝或原子取消。170项以上后端、真实PostgreSQL/RabbitMQ、静态检查、OpenAPI/UmiOpenAPI漂移和Chromium主链通过；真实平台、现有MinIO与多页预算仍不因此验收。

本地实施结果：Red阶段3项断言失败；Green/Refactor后30项聚焦采集测试和170项全量后端测试通过。Ruff、严格mypy、Python/前端生产依赖审计、FastAPI OpenAPI快照、UmiOpenAPI生成客户端、前端边界/格式/构建均通过。全新隔离Compose在PostgreSQL 16.15、RabbitMQ 4.1.8与0019 schema上完成prefork诊断attempts=1、backend替换代理401、3项Chromium和无SIGKILL关停；一次空态失败由复用合成数据导致，清理一次性卷后原样通过。功能提交`134d8facc5cf8e1018cdeb70f785a9d0a900c498`的[远程CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35047820313)在4分09秒内复现全部门禁并成功，远端0019 dump为90351字节。证据见[collection-atomic-settlement-poc.json](../operations/evidence/collection-atomic-settlement-poc.json)。本片未访问现有MinIO或外部平台，未实现多页/多根评论，因此不独立完成EV-007-005或`TASK-007-S03-T02`。

### 3.29 S02-T03A 可见页面活动状态轮询实施登记

本片继续 `TASK-007-S02-T03` 的运行可见性闭环：工作台首次加载后，只有存在 `queued|running` 的 Job 或 CollectionRun 且页面处于可见状态时，才以固定有界间隔读取第一页活动状态；页面隐藏时取消在途请求并停止计时，所有活动项进入终态后再执行一次完整刷新，使新收件箱、提醒和事件状态从数据库回显，随后停止轮询。它不创建采集任务、不修改来源准入、预算、OpenAPI、生成客户端或 Axios 封装。

| 类别 | 文件与职责 |
|---|---|
| 前端装配 | 修改 `frontend/src/app/App.tsx`：复用 UmiOpenAPI 生成的 `listJobs`、`listCollectionRuns`，管理页面可见性、在途请求取消、活动状态合并和终态后的单次完整刷新；不手写 URL、HTTP 方法或 DTO |
| 浏览器验收 | 修改 `frontend/tests/workspace.spec.ts`：诊断任务提交后不再点击手工刷新，验证页面自动从排队/运行进入终态；增加浏览器路由计数，证明终态后不会继续轮询 |
| 证据与说明 | 新增 `docs/operations/evidence/activity-polling-poc.json`，修改本 Plan、`README.md`、`README.en.md` 与 Operations，记录轮询边界、验证结果和不包含真实来源的限制 |

技术选择：沿用 React `useEffect`、浏览器 `visibilitychange` 和 Axios `AbortSignal`，不增加数据请求库、WebSocket、SSE、Service Worker 或新依赖。轮询第一页足以观察当前单 owner MVP 的活动任务；终态后完整刷新一次，避免把收件箱等业务资源也放进周期请求。固定间隔为2秒，单次请求未完成时不叠加下一次；卸载、退出登录、页面隐藏或新一轮刷新都会取消旧请求。

SMART验收：给一个真实 RabbitMQ/Celery 诊断任务，用户点击“运行诊断”后不进行任何手工刷新，20秒内界面显示终态；任务终态后等待至少两个轮询周期，Job与CollectionRun列表请求计数不再增长。给页面隐藏事件，在恢复可见前不开始新轮询且在途请求被取消；恢复后若仍有活动项只建立一个轮询链。TypeScript/Vite、Prettier、边界检查、完整Chromium主流程以及既有后端门禁通过。本片只证明数据库任务状态能自动回显，不代表真实平台采集、现有MinIO或七日运行完成。

实施结果：Red阶段诊断行在20秒内持续显示“排队中”。Green/Refactor后，工作台只在存在活动Job或CollectionRun且页面可见时每2秒读取一次生成接口；隐藏时取消计时与在途请求，恢复后仍保持单链，终态后完整刷新一次并停止。受控浏览器状态序列证明隐藏4.5秒请求数不增长、Job完成而CollectionRun仍运行时继续轮询、两者终态后连续两个4.5秒窗口请求数保持不变。完整170项后端测试、Ruff 158文件、严格mypy 131文件、Python/前端生产依赖审计、OpenAPI/UmiOpenAPI、前端边界/格式/构建均通过。隔离Compose保持0019，真实prefork诊断attempts=1、backend替换代理401、Chromium 3项、PostgreSQL 16.15备份恢复与正常停机通过；本地dump为90427字节。功能提交`9ebd91e87108cd5a632a3eefcd6ea7f7e5d7f254`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35049798922)在3分19秒内复现全部门禁并成功，远端dump为90351字节。证据见[activity-polling-poc.json](../operations/evidence/activity-polling-poc.json)。本片未改变HTTP契约、生成客户端、预算或来源准入，也未访问现有MinIO或外部平台，因此不完成EV-007-003与完整`TASK-007-S02-T03`。

### 3.30 S00-T04 去数字版本API根路径实施登记

用户明确要求当前设计不出现数字版本接口路径。本片在产品尚未对外发布时一次性将全部带数字版本段的业务契约收敛为`/api/*`，不提供旧路径重定向、别名或双路由。版本变更由FastAPI OpenAPI、UmiOpenAPI生成客户端和前后端同步发布管理。

| 类别 | 文件与唯一职责 |
|---|---|
| 路由组合 | 修改`backend/src/api/router.py`：集中声明唯一`/api`业务前缀，健康路由仍独立位于`/health/*` |
| 资源路由 | 修改`backend/src/api/routers/*.py`：删除模块内全局业务前缀并只保留资源路径，不增加兼容处理 |
| 架构与业务验证 | 修改`backend/tests/{architecture,unit,integration}`中的HTTP契约断言；新增架构断言拒绝`/api/v数字`并要求所有非健康操作位于`/api/*` |
| 自动生成 | 由FastAPI重新导出`docs/openapi/openapi.json`，由`@umijs/openapi`重新生成`frontend/src/api/*`；不手写任何生成文件 |
| 前端与文档 | 修改`frontend/tests/*.spec.ts`、当前Design/Plan/Operations和README中的契约路径；`frontend/src/request.ts`不变，Nginx继续代理`/api/` |

SMART验收：FastAPI生成的全部非健康操作仅位于`/api/*`，无任何`/api/v数字`路径；`/api/monitors`未认证返回401，任一带数字版本段的旧路径返回404且无兼容跳转。OpenAPI快照和UmiOpenAPI生成文件逐文件无漂移，工作台仅通过生成函数访问新路径，完整后端、Chromium和Compose门禁通过。

实施结果：Red阶段架构测试因现有Swagger仍包含数字版本路径失败。Green/Refactor后`api/router.py`持有唯一`/api`前缀，资源路由只声明资源路径；旧格式路径直接404且没有重定向。FastAPI OpenAPI和UmiOpenAPI客户端重新生成并通过逐文件漂移检查。171项后端测试、Ruff 158文件、严格mypy 131文件、Python/前端生产依赖审计、前端边界/格式/构建、Chromium 4项、prefork/代理/备份恢复/停机门禁全部通过。功能提交`586b77610341802c9b8efb7b043772ac5eed618b`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35051558868)在3分钟内复现全部结果，远端dump为90457字节。证据见[api-root-poc.json](../operations/evidence/api-root-poc.json)。

### 3.31 S02-T03B 采集运行取消入口实施登记

本片继续 `TASK-007-S02-T03` 的用户可操作闭环：已有后端取消接口会将 `collect_page` Job 与 CollectionRun 原子结算，但采集运行列表没有取消入口。目标是仅对 `queued|running` 运行展示“取消采集”，通过运行关联的 `job_id` 调用 UmiOpenAPI 已生成的 `cancelJob`，成功后立即重读工作台事实；不在前端重写状态机或伪造终态。

| 类别 | 文件与唯一职责 |
|---|---|
| 前端组件 | 修改 `frontend/src/features/collection/Runs.tsx`：只在活动运行卡片显示可访问的取消按钮，终态不显示操作 |
| 前端装配 | 修改 `frontend/src/app/App.tsx`：复用 `src/api/jobs.ts` 生成的 `cancelJob`，沿用统一 action 错误与 busy 管理，成功后调用现有 `refresh` |
| 浏览器验收 | 新增 `frontend/tests/collection-run-cancel.spec.ts`：在真实登录/工作台上受控提供合成活动运行，断言按钮发出一次正确 POST，重读后显示“已取消”并移除按钮 |
| 证据与说明 | 新增 `docs/operations/evidence/collection-run-cancel-ui-poc.json`，修改本 Plan、`README.md`、`README.en.md` 与 Operations，分开记录浏览器接线证据与既有后端真实数据库原子取消证据 |

技术选择：不新增依赖、HTTP端点、DTO、OpenAPI快照、生成API文件或目录；Axios仍只位于 `frontend/src/request.ts`。浏览器路由只合成一个未准入来源时无法自然产生的活动 CollectionRun，后端 `S03-T02B` 的真实 PostgreSQL 测试继续负责原子状态与对象补偿语义，两类证据不互相替代。

SMART验收：给定一个关联固定 Job ID 的 `queued` 合成 CollectionRun，浏览器显示一个带查询值的“取消采集”按钮；点击后只向 `/api/jobs/{job_id}/cancel` 发送一次 POST，完成后重读运行为 `cancelled`，页面显示“已取消”且不再显示按钮。生成客户端漂移、前端边界/Prettier/TypeScript/Vite、Chromium全流程和既有170项后端测试通过。本片不证明真实平台采集、现有MinIO或多页评论完成。

实施结果：在正确Origin的Red用例中，合成queued运行已显示但“取消采集”按钮不存在。Green/Refactor后只有queued/running卡片显示按钮；页面通过生成的`cancelJob`向关联Job发送一次POST，完整重读后显示cancelled并移除入口。Chromium用例仅证明UI接线；原子取消仍由S03-T02B的真实PostgreSQL测试证明。与S00-T04共用的功能提交及[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35051558868)已通过；证据见[collection-run-cancel-ui-poc.json](../operations/evidence/collection-run-cancel-ui-poc.json)。真实来源、现有MinIO、多页评论和七日观察仍未验收。

### 3.32 S02-T03C 手动运行统一编排实施登记

本片继续 `TASK-007-S02-T03`，为活动监控提供“立即采集”入口。手动与周期触发必须共用后端批次编排：后端从活动快照派生来源、查询、时间窗、策略和保留期，一次事务完成整批预算预留、CollectionRun、Job与Outbox。前端不循环组装低层运行，不推断运行窗口。

| 类别 | 文件与唯一职责 |
|---|---|
| 契约 | 修改 `backend/src/collection/schemas.py`、`backend/src/api/routers/collection.py`：`POST /api/monitors/{id}/runs` 仅接收 `expected_version + idempotency_key`，返回批次运行列表与replayed标记；幂等键放在严格OpenAPI请求体中，使UmiOpenAPI能完整生成调用参数 |
| 服务 | 修改 `backend/src/collection/services.py`：增加原子批次编排，确定性派生子运行幂等键，整批预算不足时不写入任何任务 |
| 调度 | 修改 `backend/src/collection/scheduling.py`：每个活动监控/时间槽只调用一次同一批次用例，不在调度器内重复预览和逐条建任务 |
| 前端 | 修改 `frontend/src/app/App.tsx`、`frontend/src/request.ts`：活动监控通过 `frontend/src/api/collection.ts` 生成的 `createCollectionRun` 提交一次请求，失败重试复用幂等键，成功后重读服务端事实 |
| 验证 | 修改 `backend/tests/integration/test_collection_scheduling.py`、`backend/tests/integration/test_user_workflow.py`、OpenAPI契约测试，新增 `frontend/tests/monitor-manual-run.spec.ts`；然后重新生成 `docs/openapi/openapi.json` 与 `frontend/src/api/` |

SMART验收：给定一个包含两个实际查询的活动监控，单次请求在同一事务创建2组CollectionRun/Job/Outbox并预留2次请求；相同幂等键重放返回原ID且预算不增加。若剩余预算为1，返回429且三类记录和预算使用均不增加。调度同样通过该用例产生整批运行并在重放时返回0个新运行。Chromium中点击一次只发出一次由生成API组装的POST，成功后页面显示手动排队任务。完整后端、真实PostgreSQL/RabbitMQ、OpenAPI/UmiOpenAPI漂移、前端边界/构建与Chromium门禁通过；这不代表真实平台、现有MinIO或多页评论已验收。

实施结果：Red阶段聚焦测试因批次输入与统一编排用例不存在而失败。Green/Refactor后，两个查询的手动批次一次创建2组CollectionRun/Job/Outbox，重放返回原ID且预算仍为2；剩余1次预算时整批返回429，无新运行、Job或Outbox。周期调度改为每个监控/时间槽调用一次同一用例。FastAPI生成的请求体只有`expected_version`与`idempotency_key`，UmiOpenAPI重生后前端仅调用`createCollectionRun`。本地174项后端测试、Ruff、严格mypy、Python/前端生产依赖审计、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建和Chromium 5项通过。隔离Compose上prefork诊断attempts=1、替换Backend后代理401、PostgreSQL 16.15备份恢复与正常停机通过，dump为87933字节。功能提交`d17ed696425d82e9114b4ad74ad9e934010a6eb2`及格式修正`d94f4f502374289555c1eff80a6cfe4aa2a3d667`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35053536106)在3分06秒内复现全部门禁并成功，远端dump为90562字节。证据见[manual-collection-batch-poc.json](../operations/evidence/manual-collection-batch-poc.json)。真实来源、现有MinIO、多页/多根评论和七日观察仍未验收。

### 3.33 S03-T01C 根评与回复两页续采实施登记

本片继续 `TASK-007-S03-T01`与`TASK-007-S03-T02`，采用MVP有界口径：每个根评运行最多2页，该运行的全部页合计最多派生1个回复运行，每个回复运行最多2页。下一页必须复用原CollectionRun/Job，在上一页事务内提交Checkpoint、预留预算、增加epoch并写Outbox；不创建新的分页运行，不把游标放入消息。

| 类别 | 文件与唯一职责 |
|---|---|
| 页契约 | 修改 `backend/src/sources/schemas.py`、`backend/src/sources/execution.py`、`backend/src/sources/adapters/bilibili.py`：在内部页请求中表达已验证的数字游标，B站根评映射cursor，回复映射page，搜索/正文禁止游标 |
| 任务状态 | 修改 `backend/src/jobs/execution.py`：增加“已提交页后继续”原语，结束当前Attempt、增加epoch并写立即可用的Outbox |
| 采集事务 | 修改 `backend/src/collection/schemas.py`、`backend/src/collection/services.py`、`backend/src/collection/execution.py`：领取时从最新Checkpoint恢复游标；页提交同事务预留下一页预算并续新epoch；超额、页上限和游标停滞都结算明确partial原因 |
| 验证 | 修改 `backend/tests/integration/test_collection_page.py`与来源执行测试；新增 `docs/operations/evidence/collection-two-page-poc.json`，不修改迁移、HTTP契约、OpenAPI、生成API、前端目录或依赖 |

SMART验收：给根评第1页返回下一游标，单次页事务后run保留1个Checkpoint/RawPage、预算和reserved_requests各增1、Job由epoch1进入queued epoch2且Outbox唯一；旧epoch不能再领取。epoch2使用上一Checkpoint的游标请求第2页；无游标时run/job同事务成功，仍有游标时两者以partial/page_limit结束且不产生epoch3。回复运行同样完成2页，两个根评页合计只产生1个回复运行。若第1页后无剩余预算，已提交页保留，run/job同事务结算partial/page_budget_exhausted，不写下一页Outbox。完整后端、真实PostgreSQL/RabbitMQ、静态、OpenAPI/UmiOpenAPI漂移、Compose和Chromium门禁通过；合成页不替代真实平台、现有MinIO或七日观察。

实施结果：Red阶段内部页契约因不接受cursor失败。Green/Refactor后，根评与回复各在同一CollectionRun/Job中完成2页，第2页从最新Checkpoint恢复游标，旧epoch无法领取，两页根评合计仅派生1个回复运行。无剩余预算时已提交页与Checkpoint保留，Job与运行原子结算为`partial/page_budget_exhausted`。本地177项后端测试、Ruff 158文件、严格mypy 131个源/验证文件、依赖审计、OpenAPI/UmiOpenAPI零漂移、前端门禁与Chromium 5项通过；隔离Compose上prefork attempts=1、代理401、PostgreSQL 16.15备份恢复和0/143/0正常停机通过，dump为90570字节。功能提交`84effd002d41965accc3fe1359eb3d228d3c5353`的[远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35055542112)在2分29秒内复现全部门禁并成功，远端dump为90556字节。证据见[collection-two-page-poc.json](../operations/evidence/collection-two-page-poc.json)。本片不改HTTP/OpenAPI、数据库迁移、生成API或前端业务代码。

### 3.34 S04-T01E 可比较趋势阈值提醒实施登记

本片补齐 `TASK-007-S04-T01` 的趋势提醒闭环。MVP只支持单事件、单平台、单指标、单时间桶的正整数绝对阈值；调度器只评估结束且经过5分钟结算期的最近时间桶。只有 `comparable` 桶可触发，回填量、缺失覆盖和覆盖中断不得生成提醒。周期调度与人工检查共用同一业务用例；人工入口不接收时间参数，不能成为任意历史回放接口。

| 类别 | 文件与唯一职责 |
|---|---|
| 持久化 | 新增 `backend/src/migrations/versions/0020_trend_alerts.py`；修改 `backend/src/notifications/models.py`：保存规则、版本化触发记录，并让站内提醒恰好关联事件修订或趋势触发之一 |
| 领域契约 | 修改 `backend/src/notifications/schemas.py`、`backend/src/notifications/services.py`：定义规则创建/更新/读取、乐观锁、活动规则查询、触发幂等与同事务提醒；修改 `backend/src/events/services.py`：复用趋势事实链计算最近合格桶并协调规则触发，不跨领域导入ORM |
| HTTP与调度 | 修改 `backend/src/api/routers/events.py`：增加规则列表、创建、更新和立即检查端点，路径保持 `/api/events/...`；修改 `backend/src/cli/commands.py`：调度进程每分钟调用同一到期检查用例，不增加队列或第二套调度器 |
| 前端与生成物 | 修改 `frontend/src/features/events/EventTrends.tsx`、`frontend/src/features/events/EventDossiers.tsx`、`frontend/src/app/App.tsx`：在事件趋势区域配置、停用和检查规则，触发后重读收件箱；由FastAPI自动重生 `docs/openapi/openapi.json`，再由 `@umijs/openapi` 只生成 `frontend/src/api/`，Axios仍只位于 `frontend/src/request.ts` |
| 验证与证据 | 新增 `backend/tests/integration/test_event_trend_alerts.py` 并修改HTTP/契约/迁移测试；新增 `frontend/tests/event-trend-alert.spec.ts`；输出 `docs/operations/evidence/event-trend-alert-poc.json`，先Red再Green/Refactor |

规则在同一事件、平台、指标和时间桶上唯一。创建重复规则返回409；更新携带 `expected_version`，只修改阈值与启用状态，真实变化才增加版本。对每条活动规则，系统选取按UTC对齐、已结束并超过5分钟结算期的最近时间桶；所选指标达到阈值时，在一个事务中保存触发记录和未读站内提醒，唯一键 `(rule_id, rule_version, bucket_start)` 阻止调度重试和多进程竞争产生重复提醒。规则版本变化后允许按新规则重新判断最近桶，这是用户显式改变判断标准的结果。

SMART验收：合成事件的可比较live桶达到阈值后，一次检查产生1条触发记录和1条未读提醒；重复手动检查、调度器重试和并发插入均不增加记录。相同计数来自backfill，或桶为missing/interrupted时均为0条。错误期望版本返回409且不改规则；停用规则后调度不触发。Swagger自动出现四个稳定 operationId，UmiOpenAPI生成对应函数，工作台不手写URL即可创建规则、检查并看到新提醒。真实PostgreSQL/RabbitMQ、全量静态与测试、OpenAPI/生成客户端漂移、Compose和Chromium通过；本片不连接真实平台、现有MinIO或模型服务，也不声明七日持续观察完成。

实施结果（2026-09-16）：Red阶段因 `TrendAlertRuleInput` 尚不存在而在测试收集失败。Green/Refactor后，规则按事件/平台/指标/时间桶唯一，5分钟结算期后的最近可比较桶达到阈值时，触发记录与站内提醒同事务提交；重复手动检查与周期调度由 `(rule_id,rule_version,bucket_start)` 抑制。回填、失败覆盖和停用规则均不触发，乐观锁阻止过期更新。FastAPI自动增加4个无数字版本段的操作，UmiOpenAPI只在 `frontend/src/api` 生成请求函数和类型，工作台完成创建、检查、收件箱刷新和停用闭环。

本地真实PostgreSQL/RabbitMQ下180项测试、Ruff、严格mypy、Python与前端生产依赖审计、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建和Chromium 6项通过；0019→0020迁移保留既有事件与提醒且模型无漂移。隔离Compose上prefork attempts=1、代理401、PostgreSQL 16.15备份恢复和0/143/0无SIGKILL停机通过。功能提交 `d4bc207a53c4c32ff64421ce8731ba0b84c77031` 的[远端CI #35057702156](https://github.com/StephenQiu30/hotkey-server/actions/runs/35057702156)在3分22秒内复现全部门禁并成功，远端dump为99040字节；完整证据见 [event-trend-alert-poc.json](../operations/evidence/event-trend-alert-poc.json)。本片使用合成数据，不连接真实平台、现有MinIO或模型服务，不声明七日观察完成。

### 3.35 S06-T01A 旧应用隔离快照回滚演练实施登记

本片继续 `TASK-007-S06-T01`，只验证数据库版本已前进后的旧应用紧急只读回退。旧应用对Alembic revision做精确校验，不得通过放宽就绪检查、添加旧路由或让旧代码猜测新Schema来伪造回滚。演练使用发布前快照在隔离数据库中恢复旧revision，用仅SELECT账号启动固定旧应用；当前新库、新表和证据卷全部保留。

| 类别 | 文件与职责 |
|---|---|
| 回滚脚本 | 新增 `backend/scripts/verify_old_application_rollback.py`：只允许一次性Compose项目和回环PostgreSQL；从固定Git提交导出旧后端，在隔离库重放旧迁移、创建测试会话，然后以数据库强制只读账号启动旧应用并验证就绪/认证/旧读接口 |
| CI | 修改 `.github/workflows/ci.yml`：`actions/checkout` 取得固定基线历史，在当前Compose与备份恢复POC健康后执行回滚脚本 |
| 证据与运维 | 新增 `docs/operations/evidence/old-application-rollback-poc.json`，修改Operations、README和本Plan；只记录提交/revision/计数/结果，不记密码、Cookie或数据库URL |

技术选择：固定旧应用基线为趋势提醒上线前的 `13545f223ea9257464c6d4f8c91c16c2543eb8d0` / schema `0019_collection_retry_budget`；当前schema为 `0020_trend_alerts`。演练只使用Git、现有Python虚拟环境、PostgreSQL和HTTP标准库，不增加运行时依赖、兼容路由、Schema降级或第二套永久服务栈。未来更换回滚基线时必须显式更新运维记录和证据，禁止按相对 `HEAD^` 猜测版本。

SMART验收：先证明旧应用直连当前库稳定返回 `schema_mismatch`。给定同一次性Compose项目，脚本建立旧revision隔离库、会话和数据库强制只读角色后，固定旧应用在30秒内进入ready，持有既存会话可读取owner与事件列表；连接会话报告 `transaction_read_only=on`，HTTP读取前后所有旧库用户表计数不变。当前库revision和表计数不变；脚本结束删除隔离库、只读角色和临时源码。完整后端、静态、OpenAPI/UmiOpenAPI、前端构建、Compose、Chromium、备份恢复与停机门禁同时通过。该结果不代表现有MinIO物理对象恢复、生产流量切换或七日观察已验收。

实施结果（2026-09-16）：Red阶段的固定0019应用直连0020库返回503/`schema_mismatch`。Green/Refactor后，脚本在PostgreSQL 16.15上重放30张表的0019隔离库，创建会话后换用`transaction_read_only=on`的仅SELECT角色；旧应用ready、会话与事件列表读取均通过，新旧库计数无变化，临时库/角色/源码均已清理。本地180项后端测试通过并保留2条上游弃用提示；Ruff检查161个文件，严格mypy检查133个源/验证文件；Python与前端生产依赖审计为0已知漏洞，OpenAPI/UmiOpenAPI、前端边界/负向样例/Prettier/构建通过。一次性Compose上prefork诊断attempts=1、backend替换后代理401、Chromium 6项、PostgreSQL备份恢复与删除重放通过，dump为99106字节；停机worker/backend/scheduler为0/143/0且无SIGKILL，最后只删除本次隔离项目和卷。首次浏览器执行分别因使用未登记的28010 Origin和未覆盖默认8867 Swagger入口失败；清理一次性卷后以登记的8010 Origin与实际28867 API空库原样重跑，6项一次通过。功能提交`1fe2955759f8de99d5f6d868c21f6ddc4e765f95`的[远程CI #35059802024](https://github.com/StephenQiu30/hotkey-server/actions/runs/35059802024)在3分20秒内复现全部门禁并成功。证据见 [old-application-rollback-poc.json](../operations/evidence/old-application-rollback-poc.json)。

### 3.36 S03-T01D 两根评独立回复链实施登记

本片继续 `TASK-007-S03-T01`，补齐 `AC-007-005` 的“每个父级独立回复页”。MVP口径为：每个重点帖的根评运行仍最多2页，从所有已提交页按页序和平台返回顺序最多选择2个有回复的根评，每个根评拥有独立CollectionRun/Job/Checkpoint并最多续采2页。搜索候选、正文和根评任务的派生上限仍为1，不扩展为全量抓取。

| 类别 | 文件与职责 |
|---|---|
| 来源成本 | 修改 `backend/src/sources/services.py`：用按下游operation的派生上限代替全局单值，仅`list_replies`为2；请求预估必须计入两个父级 |
| 采集编排 | 修改 `backend/src/collection/services.py`：按operation扣减已派生数量，跨两个根评页总计最多建立2个回复运行；预算不足时保留已建立运行并记录稳定停止原因 |
| 默认预算 | 修改 `backend/src/monitors/schemas.py` 与 `frontend/src/features/monitors/MonitorEditor.tsx`：单关键词/单B站/每小时运行的日默认从96调整为120；既有不可变快照不回填 |
| 契约与生成 | 从FastAPI重新导出 `docs/openapi/openapi.json`，再由 `@umijs/openapi` 只生成 `frontend/src/api/`；不手写请求、DTO或兼容分支 |
| 验证与证据 | 修改来源成本、HTTP/浏览器和 `backend/tests/integration/test_collection_page.py`；新增 `docs/operations/evidence/collection-multi-root-replies-poc.json` |

技术选择：不增加依赖、表、迁移、HTTP端点或队列。两个根评是能证明父级独立性的最小多根样本；顺序完全来自已保留的平台页和引用，不用本地热度分数重排。日预算估算只是最大请求意图，真实调用仍按每页原子预留；旧监控需要用户创建新配置版本才能采用新预算，不篡改历史快照。

SMART验收：给两页根评返回3个有回复的父级，按首次出现顺序只建立2个`list_replies`运行，第3个不占预算或建任务。第1个父级回复完成2页并以`partial/page_limit`结算，第2个父级独立完成1页并成功结算；两者的父子关系、epoch、Attempt、Checkpoint、RawPage和预算计数可对账，重投不增加运行。单关键词每小时预估为120次/日，119拒绝启用、120通过。完整后端、真实PostgreSQL/RabbitMQ、静态、OpenAPI/UmiOpenAPI、前端构建、Compose、Chromium、备份/回滚与停机门禁通过。合成页仍不代表真实平台、现有MinIO或七日观察已验收。

实施结果（2026-09-16）：Red阶段来源预估用例按新口径期望15次而实现仍返回12次。Green/Refactor后，按operation派生上限成为请求估算和运行时编排的共同事实源；两页根评中的3个候选只为前2个建立独立回复运行，第1个以2页`partial/page_limit`结束，第2个单页成功，第3个没有run、Job或预算占用。单关键词/单B站/每小时启用边界为119拒绝、120通过；FastAPI生成契约只把默认预算更新为120，UmiOpenAPI重生后没有手写API或DTO。真实PostgreSQL 16.15/RabbitMQ 4.1.8下180项后端测试通过并保留2条上游弃用提示；Ruff检查161个文件，严格mypy检查133个源/验证文件，Python与前端生产依赖审计为0已知漏洞，OpenAPI/UmiOpenAPI、前端边界/负向样例、Prettier和构建通过。一次性Compose完成prefork任务`attempts=1`、backend替换代理401、Chromium 6项、99114字节数据库备份恢复、固定旧应用只读快照回退和0/143/0无SIGKILL停机，资源与卷已删除。首轮全量回归暴露测试/备份夹具仍使用旧预算，统一到五请求链后原样全绿；并行执行Compose smoke与backend替换造成一次137竞争，改按CI顺序串行复跑通过。功能提交`0433fa8d287b55e13eec2646d476547943de8f23`的[远端CI #35061534325](https://github.com/StephenQiu30/hotkey-server/actions/runs/35061534325)在3分48秒内复现180项后端、6项Chromium、prefork一次执行、代理替换、99015字节备份恢复、旧应用回退与无SIGKILL停机门禁并成功。证据见[collection-multi-root-replies-poc.json](../operations/evidence/collection-multi-root-replies-poc.json)。

### 3.37 S02-T03D 主题匹配审核与收件箱筛选实施登记

本片继续 `TASK-007-S02-T03`，补齐收件箱中“命中哪个主题、当前审核状态、跟进、忽略和恢复”的最小业务闭环。审核对象是已有 `monitor_match`，忽略只改变这一条主题匹配；同一内容命中其他主题时仍可在默认收件箱看到。默认列表只展示 `new|following` 匹配，`ignored` 由用户显式筛选后恢复。

| 类别 | 文件与职责 |
|---|---|
| 主题匹配领域 | 修改 `backend/src/monitors/{schemas,services}.py`：定义只读匹配DTO、按内容/筛选读取契约和幂等审核状态写入；沿用现有表与审计，不新增迁移 |
| 收件箱读取 | 修改 `backend/src/contents/{schemas,services}.py`：按主题、来源、发现时间和审核状态组合内容查询，并返回匹配原因、相关性与审核状态；不直接导入monitors ORM |
| HTTP与生成物 | 修改 `backend/src/api/routers/{contents,monitors}.py`：GET列表增加有界筛选，PATCH `/api/monitor-matches/{id}` 更新审核状态；FastAPI自动导出 `docs/openapi/openapi.json`，UmiOpenAPI只生成 `frontend/src/api/`，`request.ts`不变 |
| 前端 | 修改 `frontend/src/features/contents/Inbox.tsx` 与 `frontend/src/app/App.tsx`：使用生成函数筛选、跟进、忽略和恢复，筛选变化从第一页重读，不在浏览器推断领域状态 |
| 验证与证据 | 修改真实PostgreSQL集成测试、OpenAPI契约测试和Swagger浏览器断言；新增 `frontend/tests/inbox-review.spec.ts` 与 `docs/operations/evidence/inbox-match-review-poc.json` |

技术选择：不增加依赖、表、迁移、队列或兼容路由。写操作归属monitors，contents通过公开DTO/查询函数只读组合；列表继续使用既有 `(first_seen_at,id)` 游标。时间筛选采用发现时间下界 `discovered_since`，满足24小时/7天MVP预设，避免引入日期组件。审核采用最后一次owner操作生效的幂等状态写入，不增加无业务冲突来源的版本字段。

SMART验收：给同一内容命中主题A和B，忽略A后默认列表仍返回该内容且只显示B；按A筛选默认列表为空，显式筛选ignored后返回A并可恢复new；将B设为following后刷新仍保持。主题、来源、24小时/7天和审核状态参数由FastAPI契约生成到UmiOpenAPI，前端不手写URL或DTO。真实PostgreSQL下验证状态与审计原子持久化，完整后端、静态、OpenAPI/UmiOpenAPI、前端边界/格式/构建、Compose与Chromium门禁通过。本片使用合成入库事实，不代表真实平台、现有MinIO或七日运行已验收。

实施结果（2026-09-16）：Red阶段稳定操作ID测试因`reviewMonitorMatch`不存在而失败。Green/Refactor后，同一内容的两个主题匹配可独立跟进、忽略和恢复；默认收件箱只返回有效匹配，ignored显式筛选可恢复，相同状态重放不新增审计。列表的主题、来源、审核状态和发现时间参数均由FastAPI生成到UmiOpenAPI；工作台只调用生成函数。真实PostgreSQL/RabbitMQ下181项后端测试通过并保留2条上游弃用提示；Ruff检查161个文件，严格mypy检查133个源/验证文件，依赖审计、OpenAPI/UmiOpenAPI、前端边界/负向样例/Prettier/构建通过。一次性Compose完成prefork attempts=1、代理401、Chromium 7项、100117字节备份恢复、固定旧应用回退与0/143/0无SIGKILL停机，隔离资源已删除。功能提交`952d501e5551122c4f51ffcafa4188386d447cef`的[远端CI #35063725148](https://github.com/StephenQiu30/hotkey-server/actions/runs/35063725148)在3分31秒内复现181项后端、7项Chromium、prefork一次执行、代理替换、99152字节备份恢复、旧应用回退与无SIGKILL停机门禁并成功。证据见[inbox-match-review-poc.json](../operations/evidence/inbox-match-review-poc.json)。真实来源、现有MinIO、真实收件箱与七日观察仍未验收。

### 3.38 S03-T01E 内容详情与评论上下文实施登记

本片继续 `TASK-007-S03-T01`，把已经持久化的根帖、根评论和回复关系以只读内容详情提供给工作台。读取任意内容时返回该内容、可解析的根帖与父评论，以及同一已解析根帖下的有界评论列表；页面读取不得启动采集、创建Job或访问外部来源。

| 类别 | 文件与职责 |
|---|---|
| 内容领域 | 修改 `backend/src/contents/{schemas,services}.py`：定义内容详情DTO，按既有来源、根外部ID和`relation_status`解析上下文，批量读取最新正文与指标，使用稳定 `(first_seen_at,id)` 游标；不跨域读取采集ORM |
| HTTP与生成物 | 修改 `backend/src/api/routers/contents.py`：增加 `GET /api/contents/{identity}` 与人工操作ID `getContentDetail`；FastAPI自动导出 `docs/openapi/openapi.json`，再由 `@umijs/openapi` 只生成 `frontend/src/api/`，不手写接口、DTO或数字版本路径 |
| 前端 | 新增 `frontend/src/features/contents/ContentDiscussion.tsx` 并修改 `Inbox.tsx`：按用户操作懒加载详情，展示根帖、父级和有界评论列表，继续只调用生成客户端；不修改根级Axios传输封装 |
| 测试 | 新增 `backend/tests/integration/test_content_detail.py` 与 `frontend/tests/content-discussion.spec.ts`，修改稳定操作ID测试和Swagger断言；真实PostgreSQL验证已解析/歧义/撤权边界，Chromium验证懒加载和生成路径 |
| 文档与证据 | 修改 `README.md`、`docs/operations/006-Python运行与验证.md`，新增 `docs/operations/evidence/content-discussion-poc.json`；仅记录脱敏合成事实和可复现门禁结果 |

技术选择：沿用FastAPI/Pydantic、SQLAlchemy 2、现有内容关系和React，不增加依赖、表、迁移、队列或来源调用。详情读取会重新校验`source + root_external_id`及父外部ID当前仍只有一个候选，只有`relation_status=resolved`且根身份仍唯一时才返回评论树；后续出现命名空间碰撞也不会混合上下文。歧义或缺失关系显式返回空上下文，不做兼容猜测。评论列表默认50、最大100并分页，不把当前采集上限当成永久数据上限。

SMART验收：给一个根帖、根评论、回复、歧义评论和已撤权内容，读取回复可得到正确根帖和父评论，同一根帖列表只含可用且已解析的评论/回复；歧义评论不串入评论树，撤权内容返回404。两页读取无重复且顺序稳定，读取前后Job和CollectionRun数量不变。Chromium只在点击“查看评论上下文”后调用生成的`getContentDetail`，展示层级并能加载下一页。完整后端、真实PostgreSQL/RabbitMQ、静态、OpenAPI/UmiOpenAPI、前端边界/格式/构建、Compose与Chromium门禁通过；合成数据不代表真实来源、现有MinIO或完整平台评论树已验收。

本地实施结果（2026-09-16）：Red阶段稳定操作ID测试确认`getContentDetail`不存在。Green/Refactor后，回复详情返回唯一根帖和父评论，评论列表用`(first_seen_at,id)`稳定分页；读取时再次验证根身份唯一且只返回available内容，歧义、撤权与未知内容均不会旁路正文屏障。FastAPI运行时与41组路径的发布快照一致，UmiOpenAPI只生成`frontend/src/api`，业务组件只调用`getContentDetail`。真实PostgreSQL/RabbitMQ下184项后端测试通过并保留2条上游弃用提示；Ruff检查162个文件，严格mypy检查133个源/验证文件，Python和前端生产依赖审计为0已知漏洞。前端契约、边界/负向样例、Prettier和构建通过；Chromium 8项通过，证明点击前无详情请求、两页上下文由生成路径读取。一次性Compose完成prefork attempts=1、代理401、99488字节备份恢复、固定旧应用回退和0/143/0无SIGKILL停机，隔离资源已删除。结构化证据见[content-discussion-poc.json](../operations/evidence/content-discussion-poc.json)。功能提交`79bb4af263e94d1bcf9a7dd0130b8130b5545a6f`的[远端CI #35066046632](https://github.com/StephenQiu30/hotkey-server/actions/runs/35066046632)在3分34秒内复现184项后端、8项Chromium、prefork一次执行、代理401、99342字节备份恢复、旧应用回退和无SIGKILL停机门禁并成功。真实平台、现有MinIO和完整评论树仍未验收。

### 3.39 S04-T01D 事件档案评论上下文实施登记

本片继续 `TASK-007-S04-T01` 与 `CHK-007-G4-002`，让事件成员能够直接打开已持久化的根帖、父评论和评论列表。事件档案只复用S03已经验收的内容详情读取，不复制评论查询、推断新关系或触发来源采集。

| 类别 | 文件与职责 |
|---|---|
| 事件界面 | 修改 `frontend/src/features/events/EventDossiers.tsx`：在每个事件成员下复用 `features/contents/ContentDiscussion.tsx`，内容详情继续由生成的`getContentDetail`懒加载；移出、合并、拆分与分析行为不变 |
| 浏览器验证 | 新增 `frontend/tests/event-content-context.spec.ts`：受控事件成员在点击前不读取详情，点击后展示根帖、父评论与回复，并验证请求仍是Swagger生成路径 |
| 文档与证据 | 修改 `README.md`、`docs/operations/006-Python运行与验证.md`，新增 `docs/operations/evidence/event-content-context-poc.json`；记录合成浏览器边界和完整门禁，不把它写成真实来源验收 |

技术选择：不增加后端端点、DTO、OpenAPI字段、迁移、依赖、状态库或兼容路径。评论关系、撤权和分页仍由`contents`领域负责，事件功能只传入已拥有的`content_id/external_id/kind`。组件放既有业务目录，不新建shared层；API继续只由`frontend/src/api`生成，`request.ts`不变。

SMART验收：给事件E加入一个已持久化回复R，初次渲染事件档案时详情请求数为0；用户在E内点击“查看评论上下文”后只请求一次`GET /api/contents/{R}`，展示根帖、父评论和已保存回复。移出按钮仍可用，事件趋势和分析入口保持。前端契约、边界/负向样例、Prettier、TypeScript/Vite与全部Chromium用例通过；后端、Compose、备份/回退和停机门禁保持绿色。本片不访问外部平台或现有MinIO，不独立完成EV-007-004/006。

本地实施结果（2026-09-16）：Red阶段的Chromium用例稳定等待事件成员内“查看评论上下文”并超时，证明入口不存在且点击前详情请求数为0。Green/Refactor后，事件成员复用`ContentDiscussion`，只在用户展开时调用UmiOpenAPI生成的`getContentDetail`；受控回复展示唯一根帖、父评论和回复，移出按钮继续可用。没有修改后端、OpenAPI、生成客户端、根级Axios封装、迁移或依赖。真实PostgreSQL/RabbitMQ下184项后端测试通过并保留2条上游弃用提示；Ruff检查162个文件，严格mypy检查133个源/验证文件，Python与前端生产依赖审计均无已知漏洞；OpenAPI/UmiOpenAPI、边界/负向样例、Prettier和构建通过。按CI顺序的一次性Compose完成prefork `attempts=1`、backend替换代理401、Chromium 9项、99345字节备份恢复、固定0019旧应用只读快照回退和0/143/0无SIGKILL停机。完整浏览器首次复跑复用了前一轮工作台创建的事件，空态断言按设计失败；删除本次一次性卷后从空库原样运行9项通过。首次远端CI又暴露既有工作台轮询用例的速度竞争：真实prefork在浏览器观察受控排队态前已经完成；测试路由现在把POST返回固定为排队态，再由后续生成列表读取驱动既定终态序列，真实POST与Worker仍照常执行。修复提交`92d8a849d17a8ac120a33155968014f58de11b7e`的[远端CI #35068391771](https://github.com/StephenQiu30/hotkey-server/actions/runs/35068391771)在3分30秒内复现184项后端、9项Chromium、prefork一次执行、代理401、99360字节备份恢复、旧应用回退和无SIGKILL停机门禁并成功。结构化证据见[event-content-context-poc.json](../operations/evidence/event-content-context-poc.json)。本片仍只证明合成事件对既有内容事实的只读接线，真实来源、现有MinIO与七日观察未验收。

### 3.40 S03-T01F 用户选择内容并启动评论追踪实施登记

本片继续 `TASK-007-S03-T01` 与 `FR-007-005`，关闭“收件箱中的跟进状态只是审核标签、用户不能选择具体内容启动评论任务”的产品缺口。用户从某条主题命中启动评论追踪后，后端必须在同一事务中锁定该命中、解析当前唯一根帖、找到属于同一不可变监控版本的已提交来源页，并创建或重放正确的下一步页任务；前端状态不能冒充持久任务。

| 类别 | 文件与职责 |
|---|---|
| 内容边界 | 修改 `backend/src/contents/services.py`：只读解析选中内容的唯一可用根帖及其证据页ID，不导入采集ORM；撤权、歧义或缺根稳定拒绝 |
| 主题边界 | 修改 `backend/src/monitors/{schemas,services}.py`：审核输入只允许待处理/忽略，新增锁定命中并原子标记评论追踪的方法；`following`只能由成功创建持久任务的评论追踪事务产生 |
| 采集编排 | 修改 `backend/src/collection/{schemas,services}.py`：按命中的监控版本筛选根帖证据对应的父运行，优先从已完成详情页创建`list_comments`，否则从搜索页创建`fetch_post`；复用现有Job/Outbox、预算、准入与唯一约束，重放返回同一运行 |
| HTTP与生成物 | 修改 `backend/src/api/routers/monitors.py`：增加 `POST /api/monitor-matches/{identity}/comment-tracking` 与人工操作ID `startCommentTracking`；FastAPI自动更新 `docs/openapi/openapi.json`，UmiOpenAPI只生成 `frontend/src/api/`，不手写URL、DTO或数字版本路径 |
| 前端 | 修改 `frontend/src/features/contents/Inbox.tsx` 与 `frontend/src/app/App.tsx`：把原“跟进”改为真实“追踪评论”，只调用生成函数；成功后从后端重读，`following`显示为评论追踪中，不在浏览器推断任务成功 |
| 验证与证据 | 新增 `backend/tests/integration/test_comment_tracking.py` 与 `frontend/tests/comment-tracking.spec.ts`，修改HTTP/OpenAPI与既有收件箱测试；后续新增 `docs/operations/evidence/comment-tracking-poc.json` 并更新Operations/README |

技术选择：沿用现有内容、命中、RawPage、CollectionRun、Job与日预算事实，不新增表、迁移、依赖、队列种类或兼容端点。追踪目标由既有`monitor_match.review_state=following`持久表达，但普通审核接口不再能直接写入该状态；只有下一步采集运行与Outbox成功入账后才在同一事务标记。来源操作未准入、现有MinIO未配置、监控版本已失活、根关系不唯一、证据页不属于该命中版本或预算不足时，返回稳定错误且命中状态和任务账本零变化。

SMART验收：给同一监控版本的搜索页写入两个帖子，自动链已选择帖子A；用户对帖子B的主题命中启动追踪，系统绕过自动采样数量上限但不绕过操作准入和日预算，创建唯一`fetch_post`子运行与Job/Outbox，并把该命中标为`following`。相同请求重放返回同一运行且不重复扣预算；从已有`fetch_post`页启动时直接创建`list_comments`。预算不足、未配置证据存储、失活版本、歧义根或跨版本证据均零写入。Chromium验证点击前无追踪请求、点击后只调用生成端点并重读“评论追踪中”。完整后端、真实PostgreSQL/RabbitMQ、静态、OpenAPI/UmiOpenAPI、前端边界/格式/构建、Compose、浏览器、备份/回退与停机门禁通过；合成页仍不代表真实平台、现有MinIO或七日运行已验收。

本地实施结果（2026-09-16）：普通审核输入已收窄为`new|ignored`；评论追踪端点锁定命中并只接受当前活动监控版本，按可用RawPage反查同版本来源运行，优先从详情页派生`list_comments`，否则派生`fetch_post`。创建下一运行、Job/Outbox、预算预留和`following`状态在同一事务提交；重复操作重放同一运行。真实PostgreSQL集成用例验证已有一个抽样子运行时仍能为用户选择的第二内容创建独立任务、详情证据直接进入评论任务，以及缺对象存储配置、预算耗尽、监控失活、根身份歧义和跨版本证据的零写入边界。完整后端为189项通过并保留2条上游弃用提示；严格mypy检查133个源/验证文件，Python与前端生产依赖审计无已知漏洞。FastAPI运行时契约与42组路径快照相同，UmiOpenAPI只更新`frontend/src/api`；前端边界、负向样例、Prettier与TypeScript/Vite构建通过。全新一次性Compose完成真实prefork诊断`attempts=1`、backend替换代理401、空库Chromium 10项、99472字节备份恢复与撤权重放、固定0019旧应用隔离只读回退；正常停机worker=0、backend=143、scheduler=0且无SIGKILL。结构化证据见[comment-tracking-poc.json](../operations/evidence/comment-tracking-poc.json)。功能提交`4c1695b42c40ee95259eff8a0fa2956fe0c8aab7`的[远端CI #35079689852](https://github.com/StephenQiu30/hotkey-server/actions/runs/35079689852)在3分48秒内复现189项后端、10项Chromium、prefork一次执行、代理401、99480字节备份恢复、旧应用回退和无SIGKILL停机门禁并成功。这些验证使用合成内容和受控浏览器响应，没有请求外部平台或连接现有MinIO；真实评论、用途准入、内容盲测和七日运行仍未验收。

### 3.41 S02-T02G 现有 MinIO 实例接入验证实施登记

本片继续 `TASK-007-S02-T02` 与 `SPEC-007-DATA-003`，只验证用户已选定的现有 MinIO 实例是否能承载已实现的证据协议。使用已有私有运行配置注入子进程，不输出、复制或提交 endpoint、bucket 或凭据；不创建 bucket、不改服务端配置，不遍历或删除非本次生成的对象。

| 类别 | 文件与职责 |
|---|---|
| 对象协议 | 优先不修改 `backend/src/evidence/adapters/minio.py`；只在真实实例暴露协议根因时修复适配器，不增加兼容分支 |
| 验证入口 | 修改 `backend/scripts/verify_minio.py` 及其必要的单元测试：仅对本次唯一 `raw/poc/` 对象执行两次幂等上传、完整读回 SHA-256 校验、精确删除与所有版本/删除标记余留检查 |
| 私有运行配置 | 仅从现有本地 `.env` 按键名映射为 `HOTKEY_S3_*`；本仓库不新增或改写密钥文件，证据 JSON 不记录连接标识 |
| 证据与说明 | 新增 `docs/operations/evidence/existing-minio-poc.json`，更新 `docs/operations/006-Python运行与验证.md`、`docs/operations/README.md` 和本计划；明确它只关闭现有实例连通与对象协议风险，不写成真实来源验收 |

技术选择：沿用已锁定的 `minio-py 7.2.20`、`MinioEvidenceStore` 与全有或全无 `HOTKEY_S3_*` 配置，不增加依赖、服务、HTTP 端点、数字版本路径、数据库迁移或前端生成物。验证必须使用预先存在的 bucket；若网络、TLS、权限、版本化或对象锁限制使协议不成立，保留稳定失败证据并从配置/权限根因处理。

SMART 验收：给定现有 MinIO 的私有配置和预先存在的 bucket，验证进程在 60 秒内对一个随机唯一对象完成首次上传、幂等重投、大小与 SHA-256 读回、精确删除，再通过带版本列举和 `stat` 证明该键无任何余留。无论成功或失败，本次对象不得留存，输出不包含 endpoint、bucket、access key、secret key 或既有对象名。完成后运行相关后端测试和静态门禁；本片不访问任何社交平台，不独立完成 EV-007-003。

实施结果（2026-09-16）：从已有项目的私有生产配置在子进程内映射 `HOTKEY_S3_*`，没有打印或写入任何连接标识和凭据。公开端点在bucket访问和证书校验前以`UNEXPECTED_EOF_WHILE_READING`结束TLS握手，不放宽TLS或证书校验；配置明确的主机回环端点在1秒内通过。预先存在的bucket中，一个65字节确定性gzip对象首次上传、幂等重投和完整读回SHA-256均通过；精确删除后，带版本列举和`stat`均证明该键无余留。未创建bucket、未改服务端配置、未访问社交平台；结构化证据见[existing-minio-poc.json](../operations/evidence/existing-minio-poc.json)。该结果关闭本机开发接入与对象协议风险，生产容器可达端点、公开TLS协议配置、生命周期/对象锁和真实来源入库仍未验收，不独立完成EV-007-003。

### 3.42 S02-T02H Compose 容器到现有 MinIO 验证实施登记

本片继续 `TASK-007-S02-T02` 与 `SPEC-007-DATA-003`，只验证与产品 Compose 相同的 Linux/arm64 后端镜像能否从容器网络访问主机已有 MinIO 转发端点。验证使用 Docker 官方 `host.docker.internal`/主机网关语义和一个仓库外的0600临时env文件；运行结束无论成败都删除该文件，不在命令行、镜像、仓库或证据中嵌入私有值。

| 类别 | 文件与职责 |
|---|---|
| 运行方式 | 复用当前 `backend/Dockerfile` 产出的 `hotkey-backend:local`，只读挂载 `backend/scripts/verify_minio.py`；不更改应用Compose、镜像或运行时依赖 |
| 私有配置 | 从已有私有配置取主机回环端口，容器内仅将host换为`host.docker.internal`；临时env设为0600并在`finally`删除 |
| 证据 | 新增 `docs/operations/evidence/existing-minio-container-poc.json`，更新 `docs/operations/006-Python运行与验证.md`、`docs/operations/README.md`和本计划；只记录镜像摘要、协议结果和验收边界 |

技术选择：不将主机回环地址直接传给容器，不使用host network，不关闭TLS校验，也不在业务代码增加环境感知的地址重写。若主机网关路径可用，运维配置显式给容器传入`host.docker.internal:<existing-port>`；生产远程部署仍必须使用可从该网络到达且证书合法的端点。

SMART验收：给定当前生产后端镜像、已有MinIO私有配置与主机网关映射，临时容器在60秒内完成一个唯一对象的幂等上传、完整SHA-256读回、全版本删除和`stat`不存在校验；容器退出为0、本次对象无余留、临时env文件已删除，命令/输出/证据不包含任何私有值。本片不访问社交平台，不验收公开TLS或远程生产网络，不独立完成EV-007-003。

实施结果（2026-09-16）：Linux/arm64 `hotkey-backend:local`在默认Docker bridge中通过主机网关访问已有MinIO；容器启用只读根文件系统、`no-new-privileges`并去除全部capabilities。首次运行因挂载脚本缺少镜像源码路径在网络访问前退出；设置镜像内`PYTHONPATH=/app/src`后原样重试成功。65字节对象的幂等上传、完整SHA-256读回、全版本删除与`stat`不存在均通过，容器退出0；0600临时env文件已在`finally`删除，未输出私有值。证据见[existing-minio-container-poc.json](../operations/evidence/existing-minio-container-poc.json)。本机Compose可显式配置`host.docker.internal:<existing-port>`，无需host network或代码重写；公开TLS握手配置、远程生产网络、对象锁/生命周期和真实来源入库仍未验收，不独立完成EV-007-003。

### 3.43 S02-T02I 现有 MinIO 保留策略只读审计实施登记

本片继续 `TASK-007-S02-T02`、`SPEC-007-DATA-003` 与 `TASK-007-S06-T01`，关闭现有 MinIO 的版本化、对象锁、过期和未完成 multipart 清理策略一直未核对的运维缺口。审计只调用 bucket HEAD/版本化/对象锁/生命周期读取操作，不写对象、不列对象名、不修改策略，也不把权限拒绝写成“策略不存在”。

| 类别 | 文件与职责 |
|---|---|
| 审计领域与脚本 | 新增 `backend/src/evidence/audit.py`：定义只读client协议并产生脱敏策略摘要；新增 `backend/scripts/audit_minio_bucket.py`：只从全有或全无`HOTKEY_S3_*`配置构造已锁定SDK客户端并输出JSON。不将可测业务逻辑放入非模块化`scripts` 目录 |
| 单元验证 | 新增 `backend/tests/unit/test_minio_bucket_audit.py`：通过`evidence.audit`的fake client覆盖完整策略、缺席、权限拒绝和未知S3错误传播；断言输出不含bucket或endpoint |
| 现有实例POC | 使用现有私有配置在主机回环端点运行审计；仅保存脱敏状态和数值摘要，不保存rule ID、prefix、bucket、endpoint或凭据 |
| 证据与运维 | 新增 `docs/operations/evidence/existing-minio-retention-audit.json`，更新 `docs/operations/006-Python运行与验证.md`、`docs/operations/README.md`与本计划；策略缺口保持failed/not-ready，不自动修复外部实例 |

技术选择：沿用`minio-py 7.2.20`的公开读取API，不调用MinIO admin API、不增加依赖、HTTP端点、数字版本路径、数据库迁移或前端生成物。未完成multipart的验收依据是启用规则的`AbortIncompleteMultipartUpload.days_after_initiation`，不为了盘点而扫描实际upload ID。

SMART验收：给定预先存在的现有bucket，审计在10秒内返回bucket可达性、版本化状态、对象锁状态，以及启用生命周期规则数、当前版本过期天数、非当前版本过期天数和multipart中止天数；缺席与AccessDenied明确区分，其他S3错误导致非0退出。脚本不执行任何写请求，输出/证据不含私有标识；聚焦测试、Ruff、官方mypy和远端完整CI通过。审计结果只证明当前策略事实，不独立完成EV-007-003/010。

实施结果（2026-09-16）：新增无ORM的`evidence.audit`只读协议与薄脚本入口；Red阶段因目标模块不存在在收集期失败，同时暴露`scripts`不应持有可测逻辑，文件责任先修正后再Green。fake client覆盖完整策略、缺席、AccessDenied和未知S3错误传播；聚焦的16项pytest、Ruff与135个源/脚本文件的严格mypy通过。真实现有bucket可达且策略可读，当前是`unversioned`、对象锁`absent`、生命周期`absent`；审计未执行写操作或输出私有标识。功能提交`79e16970`的[完整远端CI](https://github.com/StephenQiu30/hotkey-server/actions/runs/35091475304)通过。证据见[existing-minio-retention-audit.json](../operations/evidence/existing-minio-retention-audit.json)。该状态不阻断当前精确删除，但服务端自动过期和未完multipart清理未就绪，因此保留治理与EV-007-003/010仍未完成。

### 3.44 S02-T02J B站公开真实持久链 canary 实施登记

本片继续 `TASK-007-S02-T02`、`TASK-007-S03-T01` 与 `EV-007-003/004`，目标是把已经分别验证的现有MinIO和B站公开四操作接入同一个产品运行，证明一次真实关键词搜索可以经PostgreSQL Outbox、RabbitMQ、Celery prefork和现有MinIO形成正文、根评论与回复。项目用途沿用已确认的个人非商业学习范围；只访问无需登录即可读取的公开内容，不使用Cookie、账号、代理注入或付费接口。

| 职责 | 本片文件与动作 |
|---|---|
| 计划 | 修改本文件：先固定数据范围、文件归属、资源边界、清理步骤和SMART验收 |
| 运行证据 | 修改 `docs/operations/006-Python运行与验证.md`，新增 `docs/operations/evidence/007/EV-007-003-bilibili-live-canary.json`；只记录操作状态、数量、hash、时间、运行结论和限制，不记录关键词、正文、作者、平台对象ID、私有endpoint/bucket或凭据 |
| 部署与测试 | 修改 `docker-compose.yml` 与 `backend/tests/architecture/test_layout.py`：真实Red证明应用容器缺少已验证的`host.docker.internal:host-gateway`映射，再在共享应用锚点为backend/worker/scheduler固定同一主机网关；不修改来源、存储或业务代码 |
| 运行资源 | 使用独立Compose project、独立PostgreSQL/RabbitMQ卷和既有私有MinIO配置；临时owner、监控和数据库随Compose卷销毁，本次对象按精确键全版本删除并复核，私有env文件在`finally`删除 |

技术选择：固定 `bilibili.search_posts/fetch_post/list_comments/list_replies` 四项精确rights与pipeline准入，不增加依赖、HTTP端点、数字版本路径、手写请求或浏览器执行器。关键词只请求搜索第一页且最多展开1条正文；根评论与两个父级回复继续受现有“最多2页/2父级”约束。先用无持久化探测确认四项当前均为HTTP 200，再启动产品链；探测与产品运行都关闭环境代理并使用无会话HTTP client。所有仓库证据只保存脱敏聚合，社会内容仅进入本次临时数据库和私有对象存储，验证结束立即清理。若既有MinIO端点在执行时未运行，允许先以一次性同协议MinIO fixture证明真实来源产品链，但必须把既有实例步骤保持未通过并在fixture卷删除后另行原样复验，不能用fixture替代用户选定的实例。

SMART验收：给定四项精确准入、现有MinIO和一个公开关键词，10分钟内由真实prefork消费者完成或以稳定原因终止整个有界链；至少产生1个正文、1个根评论和1个回复，搜索/正文/评论/回复RawPage都能从现有MinIO按hash完整读回，数据库中的run、Job、Attempt、Checkpoint、内容关系和主题命中可对账。重复消息不新增业务结果；本次对象全版本删除、数据库/队列卷和私有env清理可验证。完整本地门禁与远端CI通过。单次canary不代表七日稳定性、全量评论、其他国内平台、X发现、公开TLS端点或生产保留治理已经验收。

实施结果（2026-09-16）：无持久化低频探测以9次总请求中的前4次分别验证search/post/comments/replies均为HTTP 200，其中正文1条、根评论3条、首个父级回复19条；未输出或提交正文、作者与对象ID。真实产品首轮暴露正式Compose缺少已验证的主机网关映射，Red架构测试先失败后在共享`x-app`一次修复backend/worker/scheduler；进一步核对发现现有MinIO宿主机端口已停止监听，不能用新建实例冒充用户指定实例。一次性MinIO fixture上的真实prefork链保存搜索、正文和评论拒绝页共3个RawPage，全部按hash读回；搜索`partial/page_limit`、正文`ok`，评论以稳定`access_denied`失败且没有继续请求回复。3个对象随后经应用删除账本全版本删除，0失败/0余留；临时数据库、队列、MinIO卷、容器和私有env均已删除。功能提交`c8d2c320301a9bd489ccacfaad99b7f772449a38`的[远端CI #35095350599](https://github.com/StephenQiu30/hotkey-server/actions/runs/35095350599)完整通过。证据见[EV-007-003-bilibili-live-canary.json](../operations/evidence/007/EV-007-003-bilibili-live-canary.json)。本片只使EV-007-003部分成立；EV-007-004、现有MinIO端到端和生产来源准入仍未通过，不对评论拒绝进行高频重试。

## 4. 实施规格：API

### SPEC-007-API-001 监控与来源

保持 `/api` 和 owner会话。已有创建/编辑接口继续保存草稿；增加内容前明确DTO兼容。目标端点：

| 方法/路径 | 输入与输出 |
|---|---|
| GET /sources | 当前操作能力、用途、验证时间、连接状态；不返回凭据 |
| POST /sources/query-preview | query_spec + source_ids + 时间范围 → 每源实际查询、native/local/unsupported规则、请求估计；零网络调用 |
| POST /monitors/{id}/activate | expected_version + 已明确预算 → active配置快照；不支持关键操作返回冲突及稳定原因 |
| POST /monitors/{id}/pause | expected_version → paused；页边界停止，已采证据保留 |
| POST /monitors/{id}/runs | expected_version + idempotency_key → 201 + items/replayed；服务端从活动快照派生整批首页运行并原子入账，不在HTTP等待采集 |
| GET /collection-runs | 认证只读运行列表；按 `(created_at,id)` 游标分页，GET不触发采集 |
| GET /collection-runs/{id} | 返回业务状态、collection_outcome、计数、检查点摘要、coverage/stop_reason、预算与时间 |

现有Bluesky查询预览端点的真实路径以生成契约为准；新通用预览若另设路由，实施时同步调用方并移除已无用的内部旧入口，不维护第二套长期契约。错误约定：401未登录、403 CSRF/用途拒绝、409版本冲突/来源不可启用/幂等键载荷冲突、422输入不合法、429本服务限额。上游429记录为异步运行结果，不伪装成HTTP提交失败。公开错误稳定code+request_id，敏感内部原因仅审计。

同一个owner/操作/幂等键及相同载荷返回同一运行；不同载荷复用键返回409。查询分页使用稳定排序 `(first_seen_at,id)` 或 `(published_at,id)` 的游标，不使用易漂移的offset。

### SPEC-007-API-002 内容、事件、分析与知识

目标端点分期增加：GET /contents、GET /contents/{id}、POST /contents/{id}/track-comments；POST/PATCH /events、POST /events/{id}/members、POST /events/{id}/merge、POST /events/{id}/split；POST /events/{id}/analyses、GET /analyses/{id}；GET /knowledge、POST /knowledge/query；GET /alerts、PATCH /alerts/{id}。

内容DTO必须有kind/identity/version/source_url/root/parent/关系状态、published_at/observed_at、coverage和rights摘要。合并/拆分提供expected_revision和显式成员映射；重试幂等。分析和问答异步返回ID，结果有sample_manifest、model_version、statistics_scope、citations、unknowns。引用内容超出权限不返回原文。

所有写操作延续CSRF与幂等/乐观锁；页面GET不触发隐式采集/模型费用。端点与字段在本切片Schema定稿后生成OpenAPI，不据本表提前修改发布契约。

## 5. 实施规格：DATA

### SPEC-007-DATA-001 身份、版本与迁移

使用Design第5节逻辑模型。身份唯一键包含真实namespace，外部ID和内部UUID分开；monitor_match唯一键 `(monitor_version_id,content_id)`。评论parent/root只引用同平台相容身份；未知关系允许暂存外部ID，已解析关联禁止自环/循环。

内容版本按正文hash去重；指标不变也可记录有价值的观察时间，但不得制造正文新版本。首次发现时间只赋值一次；发布/源更新时间为空不以当前时间伪造。未提供计数null，0为有效值。事件成员关联到内容，分析样本必须进一步固定到版本。

旧monitor.version快照回填只形成一个已知版本，不伪造此前修订；旧monitor默认为draft。jobs新增采集kind要变更数据库CheckConstraint和消息白名单。新的collection_run承载结构化产出，现有JobResult保持诊断语义，不能塞长JSON进String(80)。

### SPEC-007-DATA-002 样本、知识与删除

冻结样本清单包含成员版本/入选策略、时间桶、平台、根帖、排序、去重策略、采集截止点、内容hash；分析结果记录prompt/model/schema版本及预算。复算使用同一清单，不再次从实时表取“前100条”。

知识chunk绑定knowledge_version与证据版本；索引状态pending/ready/stale/deleted。词法和向量过滤使用相同权限/时间/事件条件。未被允许存储的页面不落原文，只保存可被允许的元数据；每数据类型配置有效保留期。删除先立屏障使在线查询立即排除，再异步清理原始页、版本、embedding、缓存及包含其引用的派生条目。报告可重算或撤回，不保留含已删原文的旧摘要旁路。

原始页可能含多个对象：单对象删除时对共享原始页进行可验证清理/重写；无法精确清理则撤回整个页证据，其他对象不得继续引用旧页。审计仅保留允许的最少非正文记录。备份按保留策略过期，恢复先重放删除/撤权清单再开放检索。

### SPEC-007-DATA-003 MinIO证据存储

连接现有MinIO。配置拟为HOTKEY_S3_ENDPOINT、HOTKEY_S3_ACCESS_KEY、HOTKEY_S3_SECRET_KEY、HOTKEY_S3_BUCKET、HOTKEY_S3_SECURE；密钥用SecretStr和私有运行配置，不写入任务载荷或API响应。由evidence适配器解释endpoint/TLS设置，业务只使用对象键和证据ID。

推荐环境独立私有bucket，例如hotkey-evidence-dev/test/prod，最终名称先检查现有命名；对象键为raw/{platform}/{yyyy}/{mm}/{dd}/{run_id}/{page_id}/{hash}.json.gz，不含Cookie/用户名等凭据信息。上传校验成功后才允许DB引用，重试幂等；ETag不一概等于正文hash，正文SHA256独立保存。

原文通过受认证后端读取并再次检查可见性；首片不默认发可绕过删除屏障的长期签名URL。不擅自变更现有bucket版本/锁定/生命周期设置。若已启用版本保留，删除需涵盖历史对象版本和未完成multipart上传，否则只创建删除标记不能算完成清除。服务端对象TTL作为后备，应用负责先撤销可见性、传播删除、清理孤儿对象与对账。

## 6. 实施规格：JOB

### SPEC-007-JOB-001 调度、提交与恢复

扩展kind建议为collect_page、normalize_page（只有确需分步时）、analyze_sample、index_knowledge、deliver_alert。消息仍只带内部job_id/epoch/contract_version，参数存业务表并做版本校验，不允许传任意可执行函数或外部认证数据。

定时运行唯一键：monitor_version/source_operation/schedule_slot；页唯一键：run_id/operation/query_variant/root/parent/sort/window/cursor_digest。持久页数据、checkpoint推进和后续Outbox同事务；发现重复页返回原提交结果。网络调用不持有长事务，提交时重新验证fencing和删除/授权屏障。

新任务默认页粒度；HTTP连接/读取时限、总页时限、软/硬任务时限、租约续期、重试上限由操作配置，实测后固定，不能套用诊断20秒总期限。暂时网络/5xx指数退避加抖动并设上限，429遵循Retry-After；403/永久解析契约错误停止该操作并暴露原因。cursor无变化或重复页面触发cursor_stalled并结束partial，不无限循环。

暂停/取消每页前后检查；deadline到达停止派发。恢复扫描只处理过期租约/未发送Outbox，旧epoch不回写。停机期间计划按有限回看窗口合并补采，不逐slot无界追赶；backfill单独标记，不触发实时突发提醒。保存页失败不推进水位，证据文件已写而事务失败产生的孤儿由清理任务按引用表核对。

### SPEC-007-JOB-002 预算、分析和通知

预算在数据库原子预留，维度至少监控/来源/日+每run。请求数、页数、节点、字节、模型输入/输出tokens分别限制。调用失败也记录实际或未知消耗；无法确认费用时不直接退还全部额度以触发重试风暴。刷新热度不重复分析未变化正文。

内容采购预算强制0；只有已验证免费或自建的来源provider可启用。云抓取/搜索服务免费额度独立记账，不能自动充值或转收费端点；自建的机器/带宽成本单独展示。X付费API和收费数据商不在本轮执行路径。

模型输出严格Schema校验，超时/格式错/引用无效记录失败并保留候选。模型只读数据，不执行来源内容里的指令。analysis幂等键包含sample_hash/model/prompt/schema版本。

站内提醒unique(rule_version,event_id,change_id)；业务变化、提醒记录与投递Outbox同事务。外部提醒P1增加provider receipt、retry与对账，不因内容已保存就跳过未送达通知。相同内容在重试或回填时不得重复提醒。

## 7. 实施规格：UI / SEC / OBS / OPS

### SPEC-007-UI-001 主流程

沿用React工作台并按阶段增加收件箱→内容评论→事件→知识页面。主题编辑显示真实查询与作用方式；收件箱区分pending/rejected/analysis_failed；来源可用性按操作呈现。任务轮询仅在页面可见且存在运行任务时进行，结束后停止，退避且取消过期请求；刷新页面从数据库恢复，不靠浏览器内存保存状态。

390px/1440px验证正常、初始空、合法空、加载、部分结果、权限不足、错误、预算耗尽和过期引用。确认关键动作可键盘操作；原链接安全打开。模拟fixture标识明确，最终验收必须运行真实来源路径。

### SPEC-007-UI-002 文件和依赖门禁

页面/业务状态在features/{domain}，装配在app，生成端点在api，Axios传输封装在根级request.ts；任何新功能先选目录再写文件。S00增加frontend/scripts/check-boundaries.mjs并接入npm/CI，检查app/features/api/request方向、反向依赖及已迁移文件不再留副本。后端architecture测试从src递归枚举全部Python文件，对未登记顶层模块显式失败；固定PACKAGES不能成为排除新目录的扫描入口。分别构造未知目录、路由导入ORM、适配器导入数据库、request导入feature等负向样例，先验证失败再实现检查。

### SPEC-007-SEC-001 用途与隔离

每次采集/分析/展示/导出按allowed/denied/unknown校验相应权限与保留期。凭据仅私有配置/引用；CSRF、owner检查继续存在。HTTP适配器限制主机、协议、重定向、私网和响应大小；需要浏览器时单独进程/资源/出站边界，取消回收进程树。对来源正文的提示注入建立负向测试，模型不获得写权限或凭据。

### SPEC-007-OBS-001 指标与证据

记录run/job/adapter/policy版本、实际查询指纹、窗口、每页计数/bytes/时延、停止原因、重试、预算、最后成功时间。分别计算作业成功率和来源采集结果，不用单一绿色状态掩盖partial。检测来源schema漂移、游标停滞、陈旧数据、积压与无效引用。日志不记录Cookie/token/认证头/完整评论；诊断面板只显示允许的信息。

### SPEC-007-OPS-001 发布、负载与恢复

开发与生产沿用根Compose及prod覆盖。修改顺序：失败测试 → additive迁移/后端Schema/Worker消费者 → OpenAPI导出 → 生成客户端/页面 → 配置/producer → 真实小样本 → 回滚实验。变更任务kind时先部署可识别新契约的消费者，再启新调度。

先单owner/单来源/单主题/一页放量，随后国内海外各一来源及多主题；每次放量以预算和积压证据决定。记录机器CPU/RAM、数据库版本/数据规模、内容/评论数、并发、页大小、缓存冷热、测试窗口、外部平台延迟/限制和成本。SLO在这些条件具备后制定，不预设无条件QPS或延迟。

回滚先暂停调度/分析并停止producer，排空或隔离新任务，旧应用只读旧能力；保留新表和证据卷。数据库恢复在隔离环境演练，核对内容/版本/页/checkpoint/Outbox/删除清单，恢复后新任务必须重新校验授权。不得执行删除用户持久卷的命令。

## 8. 任务 → SPEC → AC 与证据清单

表中EV是未来验收证据编号，尚未产生。

| 任务 | 主要SPEC | AC | 预期证据 |
|---|---|---|---|
| TASK-007-S00-T01、TASK-007-S00-T02、TASK-007-S00-T03 | UI-002、OPS-001 | AC-007-016 | EV-007-012：文件清单、依赖图、选型登记、Swagger/OpenAPI/客户端漂移、违规负向测试和迁移复验 |
| TASK-007-S01-T01、TASK-007-S01-T02 | API-001、SEC-001、OBS-001 | AC-007-001 | EV-007-001：逐操作探测、用途/分页/限额清单 |
| TASK-007-S02-T01 | API-001、DATA-001、JOB-001 | AC-007-002 | EV-007-002：预览零网络、版本/调度测试与UI |
| TASK-007-S02-T02、TASK-007-S02-T03 | DATA-001、DATA-003、JOB-001、UI-001 | AC-007-003、AC-007-004 | EV-007-003：真实收件箱、多主题、MinIO上传/读取/hash/孤儿对账、正文/指标快照 |
| TASK-007-S03-T01 | API-002、DATA-001、JOB-001 | AC-007-005 | EV-007-004：评论树、独立分页与恢复对账 |
| TASK-007-S03-T02 | JOB-001、JOB-002、OBS-001 | AC-007-006、AC-007-007 | EV-007-005：崩溃/重投/fencing/预算/429实验 |
| TASK-007-S04-T01 | API-002、DATA-001、JOB-002、UI-001 | AC-007-008、AC-007-010 | EV-007-006：合并拆分、覆盖变化和提醒去重 |
| TASK-007-S04-T02 | DATA-002、JOB-002、SEC-001 | AC-007-009 | EV-007-007：冻结清单、标注集、模型评估/引用复核 |
| TASK-007-S05-T01 | DATA-002、API-002、UI-001 | AC-007-011 | EV-007-008：中文检索评估、过滤召回和版本引用 |
| TASK-007-S05-T02 | API-002、DATA-002、SEC-001 | AC-007-012、AC-007-013 | EV-007-009：统计/问答样例、撤权删除传播 |
| TASK-007-S06-T01 | DATA-001、DATA-003、SEC-001、OPS-001 | AC-007-013、AC-007-014 | EV-007-010：迁移、回滚、MinIO对象/版本清理、备份恢复与删除重放 |
| TASK-007-S06-T02 | UI-001、OBS-001、OPS-001 | AC-007-015 | EV-007-011：七日观察、负载/费用、真实浏览器UAT |

上表SPEC简写统一前缀 `SPEC-007-`，全部定义在本文件。

### CHECKLIST

- [ ] CHK-007-G0-001 → AC-007-001、AC-007-014：范围、既有改动、来源缺口和007承接表核对；预期为评审记录。
- [x] CHK-007-G0-002 → AC-007-016：S00 文件职责与选型已登记；MinIO/模型未决项不影响本片，生成链选型和部署影响已固定；证据见006 Operations的S00记录。
- [x] CHK-007-G3-002 → AC-007-016：未知目录、反向依赖和遗留副本负向检查通过；64项后端测试、生成/构建、隔离Compose与2项Chromium回归通过。
- [ ] CHK-007-G1-001 → AC-007-002、AC-007-006、AC-007-014：DTO/唯一约束/消息兼容/迁移和权限边界定稿；预期为Schema与迁移审阅记录。
- [ ] CHK-007-G2-001 → AC-007-001：已有首轮EV-007-001；B站用途为allowed且四操作单次探测通过，但真实持久评论步骤`access_denied`，pipeline仍未连接；X/微博需要授权，小红书/抖音未验证，完整国内/海外准入仍未通过。
- [ ] CHK-007-G3-001 → AC-007-002至AC-007-014：每片业务失败测试和模型评估口径先于实现；预期测试名称、失败摘要与评估清单。
- [ ] CHK-007-G4-001 → AC-007-002、AC-007-003、AC-007-004：收件箱真实主链；预期EV-007-002、EV-007-003及OpenAPI/客户端一致性。
- [ ] CHK-007-G4-002 → AC-007-005、AC-007-008：评论树、事件修订与UI；预期EV-007-004、EV-007-006。
- [ ] CHK-007-G5-001 → AC-007-006、AC-007-007、AC-007-010：真实DB/RabbitMQ故障和预算/告警对账；预期EV-007-005、EV-007-006。
- [ ] CHK-007-G5-002 → AC-007-009、AC-007-011、AC-007-012：可复算样本、中文检索和引用支持性；预期EV-007-007至EV-007-009。
- [ ] CHK-007-G5-003 → AC-007-013、AC-007-014：用途/删除/升级/回滚/恢复；预期EV-007-009、EV-007-010。
- [ ] CHK-007-G6-001 → AC-007-015：完整真实P0七日观察与人工UAT；预期EV-007-011，全部AC逐项判定后建立Acceptance。

## 9. 验证命令与完成定义

以下是实施阶段命令，不是本次规划执行记录；需真实数据库与RabbitMQ的检查不得以skip或Celery eager替代。

```sh
uv sync --project backend --locked
uv run --project backend ruff format --check backend
uv run --project backend ruff check backend
uv run --directory backend mypy
uv run --project backend pytest backend/tests
uv run --directory backend/src python -m tools.export_openapi --check
npm ci --prefix frontend
npm run check:contract --prefix frontend
npm run build --prefix frontend
npm run test:e2e --prefix frontend
docker compose config --quiet
```

数据库/RabbitMQ测试环境、现有容器验证脚本和生产覆盖按 [006 Operations](../operations/006-Python运行与验证.md) 准备；实施采集后补充新的运行/恢复步骤并演练。新增迁移需在一次性数据库验证空库重放和当前版本升级；浏览器、故障注入、来源小样本及模型评估各自保留独立证据，不由pytest数量替代。

完成定义：AC-007-001–016逐项有环境/版本/命令/结果/限制；开放问题有结论或明确未达项；运行手册经演练；源数据与知识引用可以回查；完整P0缺口不能隐藏在“后续优化”。届时才创建007 Acceptance并按证据标passed/failed。

当前完成边界包含S00工程门禁、S01来源能力展示/有界探测、S02至S06中不依赖真实来源的合成与隔离环境切片，以及可见页面活动状态轮询；合成来源下已验证“2页根评+2个父级各自最多2页回复”的MVP采样口径，固定旧应用已通过隔离旧Schema快照完成数据库强制只读回退演练。B站真实产品canary已形成搜索与正文持久事实并正确保留评论拒绝页，但评论`access_denied`后没有回复事实，且对象存储使用临时fixture。现有MinIO曾通过主机回环与本机产品容器的隔离对象协议验证并确认未版本化/无对象锁/无生命周期规则，本次执行时实例未运行；公开TLS握手配置、远程生产网络和服务端自动保留治理尚未验收。生产已准入来源、完整真实评论链、超过有界口径的评论树、生产模型分类和七日观察仍无验收证据。各切片通过不等于对应EV或AC整体通过，完整P0未完成。
