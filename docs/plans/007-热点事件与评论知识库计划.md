---
layer: Plan
scope: shared
doc_no: "007"
title: 热点事件与评论知识库计划
status: in_progress
version: v1.2
owner: HotKey Team
canonical_path: docs/plans/007-热点事件与评论知识库计划.md
design: docs/design/007-热点事件与评论知识库设计.md
prd: docs/prd/007-热点事件与评论知识库.md
---

# 热点事件与评论知识库计划

2026-09-15｜用户已授权开始实施。S00 工程门禁、S01 首轮有界实测、S02-T01 不可变配置/查询预览/启停门禁，以及 S02-T02 的页事务、MinIO适配器、采集消费者、日预算/周期槽/运行列表基础已完成。B站四操作技术结构可读但用途权限仍为unknown，X/微博需要授权，小红书/抖音操作未验证，现有MinIO也未连通，因此完整G2、EV-007-003与真实采集保持未通过。尚未调用模型或支付内容费用。

## 1. 当前差距与承接

保留当前身份/草稿/诊断任务/Outbox/生成契约/Compose 基础与未提交来源切片；不重建仓库、不清理用户改动。下一轮先审阅这些工作区变化，再增加007代码，不将其混同为本计划已交付。

| 006事项 | 007承接 | 实施判断 |
|---|---|---|
| 来源实测、Bluesky有界适配器 | S01 | 按用户新选择优先X免费发现、B站/微博/小红书/抖音评论；Bluesky保留作补充，不以其成功替代X |
| Python/身份/草稿/诊断基础 | S02复用 | 不重写；诊断任务kind/result仍不足以接采集 |
| 页持久化、内容版本、评论和恢复 | S02–S03 | 第一条真实路径比一次铺满所有平台更优先 |
| 事件/分析/报告/提醒 | S04–S05 | 先人工事件、可复现样本，后混合检索与问答 |
| 删除、迁移、恢复、连续观察 | 每片约束 + S06综合验证 | 不等到最后才加入权限、幂等和删除设计 |

## 2. 门禁与交付顺序

- **G0 范围/事实/目录**：007三文档追踪一致；每片先列文件位置、模块依赖、技术选择和待确认项。单owner；X等信息平台发现、四类国内评论来源、内容零付费、复用现有MinIO为用户已确定范围，不默认具备平台账号授权。
- **G1 数据/契约**：明确每片表约束、DTO、任务白名单、预算、权限及迁移影响；生产者与消费者兼容。
- **G2 来源**：目标来源逐操作真实小样本，保存成功、失败、分页/用途/成本证据。可以先开发通用代码，但没有G2不得声称完成真实采集链。
- **G3 测试先行**：本片关键行为先有失败测试，再实现与重构；模型/性能阈值先定义评估口径。
- **G4 功能**：真实数据库/消息、API生成契约和浏览器主链通过。
- **G5 可靠性/分析**：崩溃、权限/删除、预算、样本、引用与恢复实验通过。
- **G6 完整交付**：国内外真实覆盖、七日观察、备份恢复、人工使用验收；记录限制后建立007 Acceptance。

### 垂直切片

| 阶段 | 任务 | 可交付结果与停止条件 |
|---|---|---|
| S00 目录与选型门禁（已完成） | TASK-007-S00-T01：登记文件迁移/归属和切片选型；TASK-007-S00-T02：全源码目录扫描、依赖负向测试、前端分层检查；TASK-007-S00-T03：FastAPI自动Swagger/OpenAPI与UmiOpenAPI客户端门禁 | 本地隔离验收通过：全源码和负向边界检查覆盖新目录；运行时契约、发布快照和生成客户端一致；目录迁移后的原业务主流程通过浏览器复验 |
| S01 来源可行性 | TASK-007-S01-T01：B站/微博/小红书/抖音操作清单与评论候选探测；TASK-007-S01-T02：X免费/自建发现入口验证 | 信息发现与评论的职责分别验证；网页摘要不是完整X帖子；若免费X入口不成立保留缺口，不能切付费或悄悄以Bluesky代替 |
| S02 发现收件箱 | TASK-007-S02-T01：配置快照、启用/暂停和按slot调度；TASK-007-S02-T02：页执行/MinIO证据/内容/命中持久化；TASK-007-S02-T03：实际运行与收件箱UI | 先跑通已准入的信息来源，再扩展；复用现有MinIO，保存/观察/重试可复核，模型可缺席 |
| S03 评论追踪 | TASK-007-S03-T01：根评/回复游标、关系和增量；TASK-007-S03-T02：编辑/不可见/预算/取消/故障恢复 | 从重点帖到评论与父级上下文；恢复无重复、部分结果可解释；完成数据质量闭环 |
| S04 事件与观点 | TASK-007-S04-T01：人工事件、时间线、趋势和站内提醒；TASK-007-S04-T02：标注集、冻结样本、模型标签与引用 | 用户可组织事件、理解有证据的观点；模型不达标保留待分析，不发布无依据结论 |
| S05 知识与问答 | TASK-007-S05-T01：知识条目/词法/pgvector；TASK-007-S05-T02：受控统计、引用问答、更新/删除传播 | 可回答“发生了什么/评论如何变化/依据是什么”，有范围与版本，不把top-k当全量 |
| S06 运行验收 | TASK-007-S06-T01：升级/回滚/备份恢复与安全；TASK-007-S06-T02：七日真实观察、负载/成本校准、桌面/窄屏UAT | 全部P0验收有证据；缺国内入口、模型质量或恢复证据时如实保留未通过项 |

S00完成后，S01证据清单和无外部依赖的S02失败测试可并行推进。一个来源先通过可先交付局部能力，但X等信息发现与国内评论的完整门槛不变。暂不承诺日历工期：来源准入是外部依赖，每片完成后按实际吞吐与人员投入估算下一片。若需要排期，先将开放的账号/预算条件落定。

不实施项：整栈重写、复制鱼皮数据库/定时器、一次性支持所有来源、接入多个向量数据库、先建传播图谱、自动购买API、在本轮部署平台爬虫。

## 3. 预计改动位置与职责

完整目标树和依赖方向见[Design第13节](../design/007-热点事件与评论知识库设计.md#13-实现前目录规划与文件归属)。以下新文件是实施目标，不表示已存在。

| 路径 | 变化 |
|---|---|
| backend/src/monitors/{models,schemas,services}.py | 状态、不可变配置版本、主题命中关联、启停、版本冲突与历史草稿转换 |
| backend/src/sources/{schemas,services}.py、sources/adapters/*.py | 从Bluesky专属DTO演进到有版本的通用结果、能力/查询语义；适配器仍无ORM |
| backend/src/collection/{models,schemas,services,execution}.py | 新增运行/检查点、预算预留、页事务和后续任务；统一用例边界 |
| backend/src/contents/{models,schemas,services}.py | 新增内容身份、正文版本、指标观察和评论关系；主题命中表归monitors |
| backend/src/evidence/{models,schemas,contracts,services}.py、evidence/adapters/minio.py | raw_pages、对象键/hash/保留和删除；MinIO SDK只在适配器内，连接现有实例 |
| backend/src/ai/{schemas,contracts}.py、ai/adapters/ | 后续模型调用/embedding外部协议，无ORM和业务prompt；费用范围确认后才实现相应供应商 |
| backend/src/jobs/{models,contracts,execution}.py、worker/{app,messaging}.py | 新kind白名单、负载引用、超时策略；复用现有Outbox/fencing |
| backend/src/cli/commands.py、cli/sources.py | 保留显式探测，增加调度到期采集/恢复子命令；不得在预览中联网 |
| backend/src/events/、analysis/、knowledge/、notifications/ | 按S04/S05增加各模块models/schemas/services，不提前创建空壳服务 |
| backend/src/api/routers/、api/dependencies.py、db/metadata.py | 路由注入、元数据注册；API路由不操作ORM |
| backend/src/migrations/versions/ | 新revision；不改已应用0001–0003；升级/回填可对账 |
| frontend/src/app/、features/、api/、request.ts | 功能页放features，app负责装配，api为生成目录，request.ts为唯一Axios封装；不设置shared层 |
| docs/openapi/openapi.json、frontend/src/api/* | 前者从 FastAPI 应用自动导出，后者由 `@umijs/openapi` 从前者生成；两者均禁止手写 |
| backend/tests/{unit,integration,architecture}/、frontend/tests/ | 分别覆盖契约、页事务/消息故障、依赖边界、真实浏览器流程 |
| docker-compose.yml、docker-compose-prod.yml、backend/src/core/config.py | 接现有MinIO所需HOTKEY_S3_配置；不新建MinIO服务/持久卷；后续按需要加分析队列/pgvector |

### 3.1 现有文件迁移表

| 当前路径 | 目标路径/动作 | 执行时机与验证 |
|---|---|---|
| backend/src/sources/bluesky.py | backend/src/sources/adapters/bluesky.py | S00目录切片，更新sources/services、CLI、测试和静态扫描；探测行为/契约保持 |
| frontend/src/App.tsx | frontend/src/app/App.tsx | S00目录切片；main导入更新，原有会话/草稿/诊断流程浏览器复验 |
| frontend/src/Login.tsx | frontend/src/features/identity/Login.tsx | 同上，登录字段与交互保持 |
| frontend/src/MonitorEditor.tsx | frontend/src/features/monitors/MonitorEditor.tsx | 同上，来源标签/Monitor类型归功能目录 |
| frontend/src/client.ts | frontend/src/request.ts | 改为唯一 Axios 传输封装；不定义任何业务 URL、方法或 DTO |
| frontend/src/style.css | 保留为frontend/src/style.css | main直接导入，不创建shared样式目录 |
| frontend/src/api.generated.ts | frontend/src/api/ | 改由 `@umijs/openapi` 按 tag 生成端点函数和类型；业务代码仅调用生成方法，逐文件漂移检查禁止手写 |
| backend/src/migrations/versions/、docs/operations/evidence/006相关文件 | 保留 | 禁止因目录美观重写迁移或挪动既有证据 |

S00 已按本表移动来源适配器和前端文件，原路径不再保留副本。后续每次迁移继续先记录工作区差异并一次完成导入修正；目录变更的测试关注行为回归与依赖违规拦截。

### 3.2 S00 实施登记

| 类别 | 路径与职责 |
|---|---|
| 后端修改 | `backend/src/main.py` 配置自动Swagger/OpenAPI；`api/routers/*.py` 固定operationId；`tools/export_openapi.py` 从应用工厂确定性导出，不维护手写Schema |
| 后端移动/测试 | `sources/bluesky.py` 移到 `sources/adapters/bluesky.py`；architecture测试递归扫描全部Python并验证未知模块、路由/适配器违规依赖 |
| 前端移动 | `app/App.tsx` 负责装配；`features/identity`、`features/monitors` 放领域交互；`style.css` 保留根级全局样式；无shared目录 |
| 前端生成/传输 | `openapi2ts.config.ts` 配置 `@umijs/openapi`；`api/*` 为唯一端点客户端；根级 `request.ts` 封装Axios会话/XSRF/序列化/错误 |
| 前端门禁 | `scripts/check-contract.mjs` 在临时目录重新生成并逐文件比较；`check-boundaries.mjs` 与负向测试阻止未知层级和反向依赖 |
| 选型 | 保持React/Vite；采用Axios 1.20.0、@umijs/openapi 1.14.1。生成器只作为开发依赖并只读取仓库内自动导出的可信契约 |

`@umijs/openapi` 1.14.1 的返回类型解析只识别default/200/201。Job和collection_run在响应前已经持久化，因此资源创建契约统一使用HTTP 201，后续异步进度由资源状态表达；生成配置不改写响应码或Schema，不保留202兼容hook。

### 3.3 每片开始前的登记项

在该TASK下记录：目标与AC；新增/修改/移动/生成文件；表与事务所有者；允许依赖；所需组件/版本和部署影响；OPEN答复/评测结论；验证命令。若片内需要的新库尚未选定或用户费用/部署偏好未答复，先给出明确选项询问，只继续独立工作。

MinIO已确定复用现有实例。实施前取得非敏感endpoint/网络方式、bucket命名和私有凭据注入方式；连通性测试只使用隔离测试bucket/prefix，不修改现有实例配置、不清理既有对象。AI推理费用规则尚待明确，不能把内容采购零付费擅自解释为已经授权付费AI。

### 3.4 S01 来源可行性实施登记

本片对应 `TASK-007-S01-T01`、`TASK-007-S01-T02`、`AC-007-001` 和 `EV-007-001`。只交付来源能力清单、有界探测器及真实小样本结论，不创建采集任务、数据库来源状态或内容入库，不把技术可访问等同于允许长期保存/分析。

| 类别 | 文件与职责 |
|---|---|
| 来源契约 | 修改 `backend/src/sources/schemas.py`：定义平台、操作、技术支持、用途权限、流水线与探测结果；不含ORM或HTTP客户端 |
| 来源目录 | 修改 `backend/src/sources/services.py`：提供当前经过审阅的逐操作能力卡和零网络查询预览 |
| B站适配器 | 新增 `backend/src/sources/adapters/bilibili.py`：无Cookie、无重试、限制响应大小的一次搜索/详情/根评论/回复探测；只返回标准DTO |
| API与CLI | 修改 `backend/src/api/routers/sources.py`、`api/routers/monitors.py`、`cli/sources.py`：来源API归sources路由；CLI必须显式指定平台和操作，不保留隐式Bluesky入口 |
| 前端 | 新增 `frontend/src/features/sources/SourceCapabilities.tsx` 并修改App装配；只调用 `frontend/src/api/` 生成方法，生成文件由OpenAPI刷新 |
| 测试与证据 | 修改来源/API/浏览器测试；新增 `docs/operations/evidence/007/EV-007-001-source-poc.json`，只保存状态码、结构计数、响应hash/大小和停止原因，不提交社交正文或身份 |

选型沿用HTTPX/Pydantic和现有React客户端，不增加通用爬虫框架、浏览器执行器或新服务。B站公开Web搜索页与公开JSON详情/评论接口只作为技术POC；接口未有目标用途授权证据，因此用途权限保持unknown、流水线保持not_connected。X公开搜索页本次只得到登录/挑战Web壳；微博跳转登录；小红书和抖音未证明评论结构，均不标记supported或connected。Firecrawl CLI当前未认证，该工具状态不作为任何平台能力结论。

### 3.5 S02-T01 监控配置与查询预览实施登记

本片对应 `TASK-007-S02-T01`，已完成并记录于 `docs/operations/evidence/007/EV-007-002-monitor-configuration-poc.json`。目标是在不联网、不启动采集的前提下，将监控配置从可覆盖草稿升级为可审计的不可变版本，并提供通用查询预览和严格的启用/暂停状态机。验收口径为：配置更新只新增版本；预览逐来源说明规则由平台原生执行、本地过滤或不支持；当前无来源通过用途权限和流水线双门禁，因此启用必须稳定失败，不能制造运行中假象。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0004_monitor_configuration_versions.py`；`monitors` 仅保存身份、状态和当前版本指针，`monitor_versions` 保存查询、来源、周期与零内容采购预算的不可变快照；从0003升级只回填一个已知版本，不伪造历史 |
| 监控领域 | 修改 `backend/src/monitors/{models,schemas,services}.py`：定义 `draft → active ↔ paused` 状态、乐观锁、不可变版本创建与启停审计；编辑非draft监控返回稳定冲突 |
| 来源领域 | 修改 `backend/src/sources/{schemas,services}.py`：查询规格、逐来源编译结果、规则执行位置和请求估算；预览为纯计算，禁止调用适配器或网络 |
| API | 修改 `backend/src/api/routers/{monitors,sources}.py`：增加通用 `/sources/query-preview`、监控启用与暂停；移除Bluesky专属预览，不保留第二套兼容接口 |
| 前端 | 修改 `frontend/src/features/monitors/MonitorEditor.tsx` 与 `frontend/src/app/App.tsx`：编辑查询条件、周期和预算，保存前显示生成的逐来源查询预览；只调用 `src/api` 生成函数 |
| 生成产物 | 从FastAPI重新导出 `docs/openapi/openapi.json`，再由 `@umijs/openapi` 更新 `frontend/src/api/*`；不手写生成文件 |
| 测试 | 更新后端来源单元、API与PostgreSQL集成测试及前端Playwright流程；验证旧配置迁移、不可变版本、版本冲突、零网络预览、未准入来源启用失败和窄屏显示 |

本片继续使用现有 FastAPI/Pydantic/SQLAlchemy/Alembic/React/Axios/UmiOpenAPI，不增加依赖、定时器、采集任务或对象存储代码。`schedule.interval_minutes` 只形成配置快照，真正按slot派发属于S02-T02/T03；MinIO连接信息与来源用途授权未确定前，不实现持久采集，不把技术可读误写为已准入。

### 3.6 S02-T02A 页提交与收件箱事务基础实施登记

本片是 `TASK-007-S02-T02` 的内部基础片，不单独宣称完成该任务。目标是在不猜测现有 MinIO 参数、不访问外部平台的条件下，先固定“一页证据 → 规范内容/版本/观察 → 主题命中 → 检查点”的原子提交边界，并提供数据库只读收件箱。真实 MinIO 适配器、来源任务和 connected 状态仍需后续真实连通与用途准入证据。

| 类别 | 文件与职责 |
|---|---|
| 采集领域 | 新增 `backend/src/collection/{models,schemas,services}.py`：拥有运行、页检查点和单页提交编排；对象上传在数据库事务前完成，数据库提交时重验 fencing，重复页返回既有结果 |
| 证据领域 | 新增 `backend/src/evidence/{contracts,models,services}.py`：定义对象存储协议、确定性 gzip 与对象键、raw page 元数据写入；本片不实现或伪造 MinIO 适配器 |
| 内容领域 | 新增 `backend/src/contents/{models,schemas,services}.py`：内容身份、不可变正文版本、指标观察及收件箱读取；`null` 与真实 `0` 分开 |
| 主题命中 | 修改 `backend/src/monitors/{models,services}.py`：`monitor_matches` 归监控领域，按 monitor version/content 唯一并保存命中词与审核状态 |
| 数据与API | 新增 `0005_collection_page_foundation.py`；修改元数据、依赖与路由，增加认证只读 `GET /api/v1/contents`，不增加可伪装成采集成功的HTTP写入口 |
| 前端与生成 | 新增 `frontend/src/features/contents/Inbox.tsx` 并装配工作台；OpenAPI与 `frontend/src/api/*` 继续自动生成 |
| 容器代理 | 修改 `frontend/nginx.conf`：使用Docker内置DNS动态解析backend upstream；新增 `backend/scripts/verify_proxy_replacement.py` 并接入CI，在Web不重启时替换backend，确认代理不再连接旧IP |
| 测试 | 使用真实 PostgreSQL 验证同页重投、跨主题单内容、多正文版本、0/null指标、旧 fencing 拒绝及上传后拒绝提交留下的孤儿对象事实；内存 store 只验证业务协议，不作为 MinIO 连通证据 |

本片不新增第三方依赖。对象存储协议只暴露私有 bucket/key/hash/bytes；正式 `evidence/adapters/minio.py` 必须在取得 endpoint、TLS、bucket 和凭据注入引用后，选定并锁定 MinIO SDK，再对现有实例执行隔离 prefix POC。

实施结果（2026-09-15）：本登记内的运行/页检查点、确定性压缩与证据元数据、含真实provider namespace的内容身份/版本/观察、关系约束、监控命中、基于 `(first_seen_at,id)` 游标的认证只读收件箱、`0005_collection_page` 迁移、Swagger/UmiOpenAPI生成客户端和空状态页面已实现。页提交在上传前预检稳定约束，上传后以锁和fencing复验，竞态失败留下显式孤儿供后续对账。容器回归发现并修复Nginx缓存旧backend IP造成的502；Web不重启、只替换backend后，动态Docker DNS路径返回预期401。真实PostgreSQL/RabbitMQ环境中76项测试通过；隔离Compose迁移至0005，真实prefork诊断成功且attempts=1；运行时OpenAPI与发布快照相等，Chromium的Swagger和owner工作台2项通过。合成事务证据见 [collection-page-foundation-poc.json](../operations/evidence/collection-page-foundation-poc.json)。它明确不是EV-007-003：未连接MinIO、未发出外部来源请求，也未实现采集Worker、预算预留或来源准入。

### 3.7 S02-T02B MinIO适配器与隔离连通POC实施登记

本片继续 `TASK-007-S02-T02`，只交付正式对象存储适配器、配置门禁和隔离S3连通验证；不新增真实来源任务，不把临时MinIO容器替代用户选择的现有实例。官方GitHub Release、PyPI与Firecrawl Developer Index在2026-09-15共同核对 `minio-py 7.2.20`，锁定该版本；SDK支持Python 3.9+，满足当前3.12基线。

| 类别 | 文件与职责 |
|---|---|
| 依赖 | 修改 `backend/pyproject.toml` 与 `backend/uv.lock`：精确锁定 `minio==7.2.20`，不引入另一套S3 SDK |
| 配置 | 修改 `backend/src/core/config.py`、`.env.example`、`.env.prod.example`、`docker-compose.yml`：增加endpoint、TLS、access key、secret key、bucket配置；四项连接值全有或全无，密钥保持SecretStr，API不返回 |
| 适配器 | 新增 `backend/src/evidence/adapters/{__init__,minio}.py`：实现现有EvidenceStore协议；对象存在时读回校验，冲突拒绝覆盖；新上传后以stat大小和完整读回SHA-256验收，不把ETag当正文hash |
| 验证 | 新增对象适配器单元测试和 `backend/scripts/verify_minio.py`；本地临时容器只用于SDK/协议POC且测试后删除，不进入根Compose；真实实例后续只用隔离prefix，不创建/修改bucket策略 |
| 证据 | 新增 `docs/operations/evidence/minio-adapter-poc.json` 并更新Operations；分别标明临时实例验证和现有实例未验收，不使用EV-007-003编号 |

本片不修改数据库迁移、HTTP路由、OpenAPI或前端生成文件。现有实例的非敏感endpoint/网络方式、bucket和凭据注入仍是完成S02-T02的外部输入；未提供时配置保持缺席，来源状态保持not_connected。

实施结果（2026-09-15）：已锁定 `minio-py 7.2.20`，增加全有或全无且隐藏密钥的 `HOTKEY_S3_*` 配置，并在 `evidence/adapters/minio.py` 实现冲突拒绝、上传后stat大小检查和完整读回SHA-256校验。独立脚本要求bucket预先存在，只操作本次生成的 `raw/poc/` 对象；一次性MinIO容器上首次上传与幂等重投成功，65字节对象hash一致，精确删除后对象数为0，容器已移除。真实PostgreSQL 16/RabbitMQ 4.1环境中82项后端测试通过；静态、依赖审计、OpenAPI/UmiOpenAPI漂移、前端构建、隔离Compose/prefork和Chromium 2项通过，正常停机无SIGKILL/OOM。证据见 [minio-adapter-poc.json](../operations/evidence/minio-adapter-poc.json)。该结果只关闭SDK和对象协议风险：现有MinIO的endpoint、TLS、权限、生命周期和服务端版本未验收，故不构成EV-007-003或TASK-007-S02-T02完成。

### 3.8 S02-T02C 采集任务消费者与运行查询实施登记

本片继续 `TASK-007-S02-T02`，目标是把现有页事务接到持久Job/Outbox/Celery消费者，并暴露运行提交和查询契约。执行顺序遵循先消费者、再producer：先让Worker识别 `collect_page`，再开放HTTP创建。来源用途权限与现有MinIO连接仍是运行门禁；本片测试使用注入的合成页，不会把任一真实平台改为eligible或发送外部平台请求。

| 类别 | 文件与职责 |
|---|---|
| 迁移 | 新增 `backend/src/migrations/versions/0006_collection_jobs.py`：扩展Job kind为`collect_page`，为collection_runs增加唯一非空job_id；历史未暴露的排队/运行记录安全终止并回填对应Job，不修改0001–0005 |
| 任务账本 | 修改 `jobs/{models,contracts,execution,services}.py` 与 `worker/{messaging,app}.py`：消息仍只含job_id/epoch/version；Lease携带白名单kind，诊断结果仍只写JobResult，采集业务结果留在collection_run |
| 采集编排 | 新增 `collection/execution.py`，修改 `collection/{models,schemas,services}.py`：创建run与collect_page Job/Outbox同事务；以Job fencing启动一页执行；无证据响应也形成明确失败结果；不持长数据库事务调用网络 |
| 来源契约 | 新增 `sources/contracts.py` 并调整已存在适配器：页结果显式包含脱敏请求指纹、原始响应bytes和media type；旧CLI复用同一页方法，不建立第二套采集实现 |
| HTTP/生成 | 新增 `api/routers/collection.py` 并修改依赖/路由；FastAPI自动生成 `POST /monitors/{id}/runs` 与 `GET /collection-runs/{id}`，由OpenAPI快照和UmiOpenAPI生成到 `frontend/src/api/`，不手写请求 |
| 测试/证据 | 增加Job kind、迁移、fencing、无权限/无MinIO拒绝、注入页执行和HTTP契约测试；更新Operations并新增脱敏POC JSON。真实PostgreSQL/RabbitMQ与隔离Compose验证消费者；真实来源和现有MinIO另行验收 |

本片不增加依赖、浏览器执行器、预算表、评论子任务或新前端页面。`collect_page` 先只执行 `search_posts` 单页；后续分页、详情展开和评论树按S03增加独立检查点，不能把搜索引用冒充已入库帖子。未获准入的来源仍在创建run前返回 `source_not_eligible`，MinIO缺失时返回 `evidence_store_not_configured`，两者都不得产生Job/Outbox。

实施结果（2026-09-15）：`0006_collection_jobs` 已将 `collect_page` 加入Job白名单，并为每个collection_run建立唯一非空Job关联；带数据的0005→0006验证中，历史queued运行以`migration_boundary`失败关闭，completed运行保持ok并回填succeeded Job。run、Job与Outbox同事务创建；Celery消息仍只有job_id/epoch，Worker领取后以fencing执行一个原始搜索页，执行前再次检查来源准入。页已提交但Job尚未完成时，重领任务只收口Job而不重复抓取；幂等重放在后续撤权或存储配置缺失时仍返回既有运行，并发同键创建通过事务级锁收敛到同一run/Job/Outbox。任务硬超时60秒，租约配置下限严格大于硬超时，默认90秒。HTTP使用201表达持久资源创建，异步进度由运行状态表达，UmiOpenAPI配置不再改写响应码。真实PostgreSQL 16/RabbitMQ 4.1下92项测试通过；运行时OpenAPI、生成客户端、前端构建、真实prefork、代理替换和Chromium 2项通过。证据见 [collection-job-foundation-poc.json](../operations/evidence/collection-job-foundation-poc.json)。该结果不包含现有MinIO、来源网络或真实内容，不能标记EV-007-003与TASK-007-S02-T02完成。

### 3.9 S02-T02D 预算预留、周期槽与运行列表实施登记

本片继续 `TASK-007-S02-T02` 并为 `TASK-007-S02-T03` 提供运行可见性，只实现不依赖真实平台账号和现有MinIO参数的确定性部分。目标是让每个单页运行在创建Job前原子占用一次日请求预算，让独立scheduler只生成当前已结束周期槽的运行，并让工作台读取真实运行状态。所有生产来源仍未准入，未配置现有MinIO时scheduler必须零写入；测试只注入已准入来源，不发送外部请求。

| 类别 | 文件与职责 |
|---|---|
| 迁移 | 新增 `backend/src/migrations/versions/0007_collection_schedule_budget.py`：增加按monitor_version/UTC日期唯一的日请求预算账本；collection_runs记录manual/scheduled触发方式、周期槽、预算日期与预留请求数；历史运行按created_at回填并对账，不修改0001–0006 |
| 预算与调度 | 修改 `collection/{models,schemas,services}.py`，新增 `collection/scheduling.py`：create_run在同一事务锁定并预留一次请求，超限稳定返回`request_budget_exhausted`；scheduler按UTC周期边界生成当前最近一个已结束槽，使用确定性幂等键，不追补无限历史 |
| 监控快照 | 修改 `monitors/{schemas,services}.py`：只读导出当前active配置DTO供调度使用，不让collection直接读取monitor ORM；暂停或版本变化后不再生成旧配置运行 |
| 运行装配/API | 修改 `cli/commands.py`在现有scheduler循环中先生成到期意图、再发布Outbox；修改collection路由与依赖，增加认证分页 `GET /collection-runs`，不在GET中触发采集 |
| 前端 | 新增 `frontend/src/features/collection/Runs.tsx` 并修改 `app/App.tsx`：通过UmiOpenAPI生成的collection函数展示触发方式、业务状态、来源、计数、预算日期和停止原因；不手写URL/DTO，不增加全局store或图表库 |
| 契约与验证 | 更新FastAPI自动OpenAPI与 `frontend/src/api/*`；增加预算原子性、同槽幂等、无MinIO/撤权零写入、暂停停止、运行keyset分页、scheduler真实进程和浏览器空状态测试；新增脱敏POC证据 |

本片沿用Python/FastAPI/SQLAlchemy/Alembic/Celery/RabbitMQ、React/Axios/UmiOpenAPI，不增加依赖或服务。日预算按UTC日期统计一次来源请求意图；MVP单页运行固定预留1次且不回退，避免失败重试绕过上限。周期槽以`floor(now/interval)-interval → floor(now/interval)`形成完整窗口，只生成最近槽；scheduler重启不自动补齐更早窗口。运行列表按`(created_at,id)`游标分页。现有MinIO连通、真实来源准入、评论跟踪和真实收件箱仍需后续验收，本片不能关闭EV-007-003。

实施结果（2026-09-15）：`0007_collection_budget` 已增加不可超领的 UTC 日请求账本，run、Job 与 Outbox 只有在额度预留成功后同事务创建；8个不同幂等键并发争抢1次额度时只有1个成功。scheduler从当前active配置DTO生成最近一个已结束周期槽，重复扫描、暂停、来源不可用或MinIO未配置均不新增运行；不回补无限历史。历史0006数据升级时回填manual触发、预算日、每run一次预留和monitor保留天数；若历史请求数超过旧预算快照，则账本以既有数量为下限，确保升级不丢事实。认证只读运行列表使用 `(created_at,id)` 游标，工作台经UmiOpenAPI生成函数展示空态和真实状态字段。真实PostgreSQL 16/RabbitMQ 4.1下97项测试通过；Ruff、严格mypy（84个源文件）、运行时OpenAPI、生成客户端、前端边界/格式/构建、生产Compose解析和依赖审计通过。隔离Compose完成0007迁移、真实prefork诊断和后端替换；未配置S3时运行数为0。Chromium 2项通过，正常停机worker/scheduler=0、backend=143且无SIGKILL/OOM。证据见 [collection-schedule-budget-poc.json](../operations/evidence/collection-schedule-budget-poc.json)。本片没有连接现有MinIO或任何真实平台，仍不能标记EV-007-003与TASK-007-S02-T02完成。

### 3.10 S02-T02E 精确来源操作准入实施登记

本片继续 `TASK-007-S02-T02`，只解决生产配置无法表达“某平台的某个操作已获用途授权且持久流水线已连通”的架构缺口。准入必须按 `source.operation` 精确配置，默认全部关闭；技术支持、用途权限、持久流水线和本次运行结果保持独立。未提供现有MinIO参数或未获得用户用途确认时不执行真实采集，也不把配置机制本身作为connected证据。

| 类别 | 文件与职责 |
|---|---|
| 配置 | 修改 `backend/src/core/config.py`、`.env.example`、`.env.prod.example` 与根Compose：增加JSON数组 `HOTKEY_SOURCE_RIGHTS_ALLOWED` 和 `HOTKEY_SOURCE_PIPELINES_CONNECTED`；生产覆盖继续显式透传现有MinIO配置，所有开关默认空数组 |
| 来源契约 | 修改 `sources/{schemas,services}.py`：将pipeline与eligible移动到每个operation能力卡；只允许代码中已有持久执行器的operation被声明connected；`activation_issues`按指定operation判断，不用整个平台布尔值放行 |
| 运行装配 | 修改 `main.py`、`api/dependencies.py`、`cli/commands.py`、`worker/app.py`：API在lifespan启动时构造并共享SourceService，Monitor、Collection、scheduler和Worker从同一Settings读取准入；非法操作配置必须让服务启动失败，避免各进程看到不同准入状态 |
| 前端 | 修改 `features/sources/SourceCapabilities.tsx` 与 `app/App.tsx`：逐操作显示授权/连接/可采集状态；监控启用只读取search_posts准入；继续只使用UmiOpenAPI生成DTO |
| 契约与验证 | 更新FastAPI OpenAPI及 `frontend/src/api/*`；修改配置、来源、API/工作流和浏览器测试，先验证旧平台级字段与未注入配置不能满足目标，再验证精确操作准入、未知/未实现operation拒绝、生产Compose透传和默认零采集 |

本片沿用现有Pydantic Settings、FastAPI、React、Axios与UmiOpenAPI，不增加依赖、迁移、服务、来源账号或平台网络请求。rights allowlist是部署者对用途范围的显式确认，pipeline allowlist只表示已经由该版本持久执行器验证的操作；两者同时满足且技术support为supported时才eligible。当前代码只登记 `bilibili.search_posts` 为可声明的持久操作，详情、评论、回复以及其他平台在对应消费者与真实POC完成前即使写入配置也必须启动失败。

实施结果（2026-09-15）：来源目录已删除平台级pipeline/eligible字段，24张能力卡分别返回support、rights、pipeline与eligible；监控启用固定检查search_posts操作。API在lifespan构造并共享SourceService，配置未知operation或把未实现的 `bilibili.list_comments` 声明connected时启动退出3；scheduler与Worker使用同一Settings。生产Compose已向migrate/backend/worker/scheduler显式透传现有MinIO与两个准入数组，默认均为空。查询预览的pipeline_connected也由同一search能力计算，不再固定false。FastAPI自动OpenAPI与运行时一致，UmiOpenAPI只在 `frontend/src/api` 更新DTO，页面按操作展示用途、连接与可采集状态，业务请求仍全部经 `src/request.ts`。

真实PostgreSQL 16/RabbitMQ 4.1下102项测试通过；Ruff、严格mypy（84个源文件）、生成契约、前端边界/格式/构建、生产Compose解析和依赖审计通过。隔离Compose完成迁移、prefork诊断（attempts=1）、后端原地替换和2项Chromium测试；默认目录为6个平台、24个操作、0项eligible。正常停机worker=0、backend=143、scheduler=0且无SIGKILL/OOM，隔离资源已删除。证据见 [source-operation-admission-poc.json](../operations/evidence/source-operation-admission-poc.json)。本片未访问外部平台、未连接用户现有MinIO，也没有获得真实用途确认；因此所有生产操作仍默认not_connected，EV-007-003与TASK-007-S02-T02保持未完成。

### 3.11 S02-T02F 搜索引用到正文的父子任务实施登记

本片继续 `TASK-007-S02-T02`，解决搜索页只保存引用而不能形成收件箱正文的主链断点。MVP每个搜索请求最多展开1个帖子详情；搜索与详情各自占用1次请求预算、独立保存原始证据并由独立Job/租约执行。详情任务由搜索页数据库提交事务创建，消息仍只含job_id/epoch/version；重投同页不得重复派生详情任务。未连接现有MinIO或未确认用途时默认路径仍不执行。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0008_search_reference_expansion.py`；将collection run的 `query_variant` 根字段改名为通用 `request_value`，增加自引用 `parent_run_id`，并用操作约束保证search为根、fetch_post为子；不保留旧字段兼容 |
| 来源契约 | 修改 `sources/{schemas,contracts,services,execution}.py` 与 `sources/adapters/bilibili.py`：用operation + request_value的统一页输入取代search专属输入；B站search声明依赖fetch_post；详情返回原始JSON页及规范post；只有两项分别准入时search才eligible |
| 采集与预算 | 修改 `collection/{models,schemas,services,execution,scheduling}.py` 与 `monitors/{schemas,services}.py`：搜索页提交与最多1个详情run/Job/Outbox/预算预留同事务，并在页边界复核当前monitor version仍active；预算不足、暂停或详情撤权时以partial停止原因结束父run，不调用详情；详情页沿用证据先上传、数据库后提交与fencing；新监控默认日预算调整为可覆盖每小时1个关键词的48次，旧快照不改写 |
| API与前端 | 保持现有端点路径，FastAPI契约将run字段切换为 `request_value` 并增加parent_run_id；UmiOpenAPI只生成 `frontend/src/api`，`features/collection/Runs.tsx` 按搜索词/详情目标显示，`features/monitors/MonitorEditor.tsx` 同步新配置默认值，不手写请求 |
| 测试与证据 | 先以失败测试证明搜索引用不会入箱且无详情任务，再验证真实PostgreSQL中父页原子派生、预算不足、重投、撤权和详情入箱；增加适配器原始页、迁移、Worker、Swagger/生成客户端、浏览器与Compose回归证据 |

本片沿用Python/FastAPI/SQLAlchemy/Alembic/Celery/RabbitMQ、MinIO协议、React/Axios/UmiOpenAPI，不增加依赖或服务。上限1是可测的MVP资源边界，不声称覆盖搜索页所有结果；查询预览与启用预算按每个关键词最多2次请求计算。旧监控预算快照不篡改，预算不足的旧配置需由用户建立新版本后再启用。评论与回复仍属于S03，不在本片把详情成功扩写成评论链完成。

实施结果（2026-09-15）：`0008_reference_expansion` 已把运行目标统一为 `request_value` 并增加父子关系和数据库约束；从0007升级的既有search运行保留原值且parent为空，不保留 `query_variant`。周期槽唯一索引只约束parent为空的根搜索，同周期两个关键词发现同一帖子时可分别创建详情子任务。B站search能力显式依赖fetch_post，两项分别准入后才可启用。搜索页提交时最多为一个合法bvid同事务预留预算并创建详情run、Job与Outbox；同页重投不重复派生。预算耗尽、monitor暂停或详情撤权均停止在页边界并记录partial原因。合成搜索页与正文页经真实PostgreSQL任务账本后形成一个真实数据库收件箱条目，两页证据分别记录；该验证使用内存EvidenceStore，不代表现有MinIO已验收。

真实PostgreSQL 16/RabbitMQ 4.1下107项测试通过；Ruff、严格mypy（85个源文件）、FastAPI OpenAPI导出、UmiOpenAPI漂移、前端目录/负向边界、Prettier、TypeScript/Vite构建、生产Compose解析和依赖审计通过。隔离Compose迁移到0008并确认根任务部分唯一索引，真实prefork诊断attempts=1，后端替换后Web代理返回401；Chromium 2项通过并验证新监控默认48次日预算。默认运行仍为6个平台、24个操作、0项eligible、0个collection run；正常停机无SIGKILL/OOM且一次性资源已删除。证据见 [search-reference-expansion-poc.json](../operations/evidence/search-reference-expansion-poc.json)。本片没有访问外部平台、没有连接用户现有MinIO或确认真实用途，因此EV-007-003与TASK-007-S02-T02仍未完成。

### 3.12 S03-T01A 正文到首屏根评论实施登记

本片开始 `TASK-007-S03-T01`，只打通已发现B站正文到一页根评论的持久链。正文详情仅在平台明确返回评论数大于0时派生一个 `list_comments` 子任务；每页最多20条，若平台仍有下一页则本次运行标记partial/page_limit，不在首片自动遍历全部分页或回复。评论无需重复关键词，但必须能解析到同来源根帖并继承该monitor version的上下文命中。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0009_root_comment_page.py`；扩展collection run操作约束到list_comments，继续使用parent关系、每run一次预算和现有证据/检查点，不增加评论专属重复账本 |
| 来源与执行 | 修改 `sources/{schemas,services,execution}.py` 和B站适配器：fetch_post声明依赖list_comments；准入依赖按传递关系计算；详情结果用 `aid:<id>` 引用首屏评论；list_comments使用统一CollectionPageInput并返回原始JSON页，固定cursor=0、limit=20 |
| 内容与匹配 | 修改 `contents/services.py` 与 `collection/services.py`：按同来源root/parent外部身份解析relation_status；评论只在根帖已经属于当前monitor version时继承上下文命中，不按评论正文关键词过滤，也不跨来源关联 |
| 预算与界面 | 修改monitor默认预算与查询估算为每小时单关键词最多search+post+comments三次，即72次/日；运行列表支持list_comments标签，收件箱沿用现有post/comment类型，不新增页面或状态库 |
| 契约与验证 | FastAPI自动更新OpenAPI，UmiOpenAPI只生成 `frontend/src/api`；先以失败测试证明详情不会创建评论任务和无关键词评论不会入箱，再验证父子原子性、0评论不派生、上下文命中、关系解析、页上限、撤权/预算/暂停继承及Compose/浏览器回归 |

本片不增加依赖、服务、HTTP写端点、评论回复、全量分页或其他平台适配。B站search、fetch_post、list_comments三项必须分别满足support、rights和pipeline，任一缺失都不得启用监控。合成响应只用于工程验证；没有现有MinIO与真实用途确认时生产仍为零准入。

实施结果（2026-09-15）：`0009_root_comments` 已把运行约束扩展到list_comments，同时保留search根任务和fetch/comments子任务的数据库父子约束。B站详情只在reply_count大于0时输出一个aid引用；正文页提交原子派生一页根评论任务，评论请求固定cursor=0、limit=20，有下一页时明确记录partial/page_limit而不继续遍历。评论关系只解析到同来源post；无关键词评论在根帖已命中同一monitor version时以root_context进入收件箱。搜索、详情和根评论形成传递准入链，查询估算与新监控默认预算调整为每小时单关键词最多72次/日。运行列表新增明确的根评论标签。

真实PostgreSQL 16/RabbitMQ 4.1下111项测试通过；Ruff、严格mypy（86个源文件）、FastAPI OpenAPI导出、UmiOpenAPI漂移、前端目录/负向边界、Prettier、TypeScript/Vite构建、生产Compose解析和依赖审计均通过。0008→0009迁移POC保留既有搜索/正文父子运行且模型无漂移。隔离Compose完成0009迁移、真实prefork诊断attempts=1、运行时契约比较、Backend替换代理及2项Chromium测试；正常停机无SIGKILL/OOM并删除全部一次性资源。证据见 [root-comment-page-poc.json](../operations/evidence/root-comment-page-poc.json)。本片使用合成来源响应和内存EvidenceStore，没有外部平台请求、现有MinIO或真实用途确认，因此EV-007-003与TASK-007-S03-T01仍未完成。

### 3.13 S03-T01B 根评论到首屏回复实施登记

本片继续 `TASK-007-S03-T01`，只从已提交的B站根评论页中选择最多1条reply_count大于0的根评论，派生一页 `list_replies` 任务。回复请求固定page=1、limit=20；仍有下一页时记录partial/page_limit，不自动遍历。没有回复的根评论不派生任务，父评论缺失或身份歧义时关系保持unresolved且不继承监控命中。

| 类别 | 文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/migrations/versions/0010_reply_page.py`；扩展collection run操作约束到list_replies，继续复用parent_run_id、每run一次预算、Job/Outbox、raw page与checkpoint，不增加回复专属表 |
| 来源与执行 | 修改 `sources/{schemas,services,execution}.py` 与B站适配器：list_comments声明依赖list_replies；根评论以 `aid:<aid>/root:<rpid>` 输出回复引用；list_replies走统一CollectionPageInput并返回原始JSON页，固定page=1、limit=20 |
| 内容与匹配 | 修改 `contents/services.py` 与采集提交测试：回复必须唯一解析到同来源post和父评论才标记resolved并继承root_context；缺失或歧义父节点保持unresolved，不做猜测关联 |
| 预算与界面 | 传递依赖按每个关键词最多search+post+comments+replies四次估算，新监控每小时单关键词默认96次/日；运行列表增加list_replies明确标签 |
| 契约与验证 | FastAPI自动更新OpenAPI，UmiOpenAPI只生成 `frontend/src/api`；Red先证明list_replies不能声明connected，再验证一页回复父子链、0回复、非法引用、页上限、关系/上下文、迁移、Compose与浏览器回归 |

本片不增加依赖、服务、HTTP端点、第二页遍历、多个根评论回复展开或其他平台。B站四项操作必须分别满足support、rights和pipeline；默认生产配置仍为零准入。合成响应只证明工程链路，不计作真实平台或现有MinIO验收。

实施结果（2026-09-15）：`0010_reply_page` 已扩展运行约束到list_replies。B站根评论页仅为reply_count大于0的评论输出 `aid:<aid>/root:<rpid>` 引用，采集服务最多选择1条并原子派生回复run、Job、Outbox和预算；回复固定page=1、limit=20，存在下一页时以partial/page_limit结束。回复必须唯一解析到同来源post与父评论才标记resolved并继承root_context；父评论缺失或根身份歧义时保持unresolved且不继承。四项传递准入的最大请求估算使新监控每小时单关键词默认预算调整为96次/日，运行列表新增回复标签。

真实PostgreSQL 16/RabbitMQ 4.1下114项测试通过；Ruff、严格mypy（91个应用与验证源文件）、FastAPI OpenAPI导出、UmiOpenAPI漂移、前端目录/负向边界、Prettier与TypeScript/Vite构建通过。0009→0010迁移POC保留既有搜索/正文/根评论三层运行且模型无漂移。隔离Compose、依赖审计、浏览器、代理替换与停机结果记录在 [reply-page-poc.json](../operations/evidence/reply-page-poc.json)。本片没有外部平台请求、现有MinIO或真实用途确认，不完成EV-007-003或TASK-007-S03-T01。

### 3.14 S04-T01A 人工事件档案实施登记

本片启动 `TASK-007-S04-T01` 的最小人工整理闭环：创建事件、把已入库内容加入或移出事件、读取事件与不可变修订。每条内容同一时刻最多归属一个事件；重复加入同一事件幂等，跨事件重复归属明确冲突。合并、拆分、趋势和提醒留在后续切片，不在本片预建空模块。

| 边界 | 本片文件与职责 |
|---|---|
| 数据与迁移 | 新增 `backend/src/events/{models,schemas,services}.py` 与 `0011_event_dossiers.py`；events保存当前档案，event_members保存当前归属，event_revisions保存每次变化后的完整轻量快照；更新db metadata、revision和测试清理顺序 |
| HTTP契约 | 新增 `api/routers/events.py` 与依赖注入：创建/列表/详情、加入/移出成员、读取修订；路由只处理协议与认证，事务和并发锁在EventService |
| 前端 | FastAPI自动导出契约，UmiOpenAPI只生成 `frontend/src/api/events.ts` 和类型；新增 `features/events/EventDossiers.tsx`，工作台可创建事件并查看成员；收件箱通过生成端点把内容加入已有事件 |
| 约束与验证 | Red先证明events模块尚不存在；Green验证唯一归属、幂等、移出、修订快照、认证/CSRF、迁移、OpenAPI生成、浏览器与真实PostgreSQL；不增加新依赖、对象存储、AI或消息kind |

SMART验收：本片完成后，单owner能在一个工作台会话内创建事件，将一条已命中内容加入事件并看到成员数，移出后成员数恢复为0；数据库依次保留create/add_member/remove_member三条修订，冲突不会改写状态。验收只证明人工事件档案基础，不声明完成合并拆分、趋势提醒或 `TASK-007-S04-T01`。

实施结果（2026-09-15）：新增events、event_members和event_revisions，内容行锁与数据库唯一约束保证当前单一事件归属；同事件重放不增加修订，跨事件归属返回稳定冲突。FastAPI发布6个认证事件端点，UmiOpenAPI生成 `frontend/src/api/events.ts`；工作台支持创建事件、从收件箱归档内容和移出成员，未手写请求。

真实PostgreSQL 16/RabbitMQ 4.1下116项测试通过，Ruff、严格mypy（97个应用与验证源文件）、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建通过。0010→0011迁移POC保留既有内容且模型无漂移；隔离Compose、浏览器、代理替换与停机证据见 [event-dossier-poc.json](../operations/evidence/event-dossier-poc.json)。本片未连接外部平台或现有MinIO，不包含合并、拆分、趋势和提醒，因此不完成EV-007-006或TASK-007-S04-T01。

### 3.15 S04-T01B 事件合并与拆分实施登记

本片继续 `TASK-007-S04-T01`，实现两个活跃事件的人工合并，以及从一个活跃事件选择成员创建新事件。请求必须携带涉及事件的期望修订号；服务按UUID固定顺序加行锁避免死锁，任一修订变化即整体拒绝。合并后源事件归档且成员移动到目标；拆分后被选成员移动到新事件。每个受影响事件同事务各写一条带关联事件ID的完整快照，当前成员唯一约束保持不变。

| 边界 | 本片文件与职责 |
|---|---|
| 数据与迁移 | 新增 `0012_event_merge_split.py`；event_revisions增加related_event_id并扩展change_type，现有修订保持原值；更新events模型、schema、服务与数据库revision |
| HTTP与生成客户端 | events路由新增merge/split两个端点；FastAPI自动导出OpenAPI，UmiOpenAPI只更新 `frontend/src/api`，请求继续只经根级Axios |
| 前端 | EventDossiers对活跃事件提供目标选择合并；有成员时提供复选成员和新事件标题拆分；App只调用生成的mergeEvent/splitEvent并刷新数据库状态 |
| 约束与验证 | Red先证明合并/拆分输入和服务不存在；Green验证成员移动、源归档、双边修订、幂等冲突、旧期望修订拒绝、迁移保留、Swagger、浏览器和真实PostgreSQL/RabbitMQ；不引入分析、提醒或新依赖 |

SMART验收：两个不同事件合并后只保留一个活跃成员集合且源事件归档；随后选择至少一条但非全部成员拆分，新事件与原事件成员互斥且总数不变。所有变化可由revision、related_event_id和snapshot还原，过期revision请求返回409且数据库零变化。

实施结果（2026-09-16）：`0012_event_merge_split` 为修订增加related_event_id并扩展merge/split变化类型，历史create/add/remove修订保持原值且关联事件为空。合并按UUID固定顺序锁定两个事件，原子移动成员并将源事件归档；拆分要求选择当前成员的真子集，原子创建事件并移动成员。目标、源和新事件分别保留变化后快照，旧期望修订稳定返回409。

FastAPI新增mergeEvent/splitEvent，UmiOpenAPI自动更新events.ts和类型；工作台对活跃事件提供合并目标选择，对至少两个成员的事件提供成员复选拆分。远程CI在真实PostgreSQL/RabbitMQ下117项测试通过，并完成Compose、代理替换、2项Playwright和无SIGKILL关停验证；完整结果见 [GitHub Actions #34991217129](https://github.com/StephenQiu30/hotkey-server/actions/runs/34991217129) 与 [event-merge-split-poc.json](../operations/evidence/event-merge-split-poc.json)。趋势、提醒与分析失效尚未实现，不完成完整TASK-007-S04-T01。

### 3.16 S04-T01C 站内事件变化提醒实施登记

本片继续 `TASK-007-S04-T01` 的最小提醒闭环。事件加入/移出成员、合并和拆分已有不可变修订；本片在同一数据库事务内为每个非create修订生成一条站内提醒，并使用`(rule_version,event_id,change_id)`唯一约束阻止重放。本片不发送邮件/企业协作消息，不引入新队列，不用用户刚手工变更的提醒冒充趋势突发。

| 边界 | 本片文件与职责 |
|---|---|
| 数据与迁移 | 新增 `notifications/{models,schemas,services}.py` 与 `0013_event_notifications.py`；表只保存站内提醒、变化ID、规则版本和已读时间 |
| 原子性 | 修改 `events/services.py`，修订与提醒使用同一Session/事务；notifications不导入events ORM，events只传入原语值 |
| HTTP与生成客户端 | 新增有界分页列表与幂等已读端点；cursor使用URL安全不透明令牌；FastAPI自动导出OpenAPI，UmiOpenAPI只生成 `frontend/src/api` |
| 前端 | 新增 `features/notifications/NotificationInbox.tsx`；App只组装生成的list/mark-read函数，Axios继续只在根级`request.ts` |
| 约束与验证 | Red先证明notifications模块/服务不存在；Green在真实PostgreSQL验证原子写入、唯一去重、不透明cursor、已读持久化、迁移、Swagger、生成客户端和浏览器 |

SMART验收：一次事件成员变更提交后，对应修订和一条未读提醒同时可见；重放同一幂等业务请求不增加修订或提醒；标记已读后刷新仍为已读；伪造cursor稳定返回422。趋势时间桶、覆盖中断与趋势规则提醒留在后续片，本片不单独完成AC-007-010或EV-007-006。

实施结果：Red阶段因`notifications`模块不存在而在测试收集失败。Green阶段将事件修订和提醒写入同一事务，唯一约束保证同一规则版本、事件和变化只生成一条；FastAPI自动增加`listNotifications`和`markNotificationRead`，UmiOpenAPI只在`frontend/src/api`生成端点和类型。真实PostgreSQL/RabbitMQ全量127项测试通过；0012→0013迁移保留既有事件且metadata无差异；浏览器验证合并生成2条未读提醒、标记后刷新保持1条未读。远端CI复现全量门禁，见 [GitHub Actions #34995710640](https://github.com/StephenQiu30/hotkey-server/actions/runs/34995710640) 与 [event-notification-poc.json](../operations/evidence/event-notification-poc.json)。本片仍不实现趋势可比性、回填识别或突发规则，因此不完成AC-007-010。

### 3.17 S04-T01D 分平台趋势与覆盖中断实施登记

本片继续 `TASK-007-S04-T01`，只实现可解释的分平台时间桶。采集运行新增必需业务语义 `ingestion_mode=live|backfill`，搜索运行明确设置，后续详情、评论和回复运行继承父运行模式；历史迁移前不存在回填入口，因此既有运行一次性标记为live。趋势按事件当前成员、第一版证据和互动观察按需聚合，不增加趋势表、缓存、图表库或评分公式。

| 边界 | 本片文件与职责 |
|---|---|
| 采集事实与迁移 | 新增 `0014_collection_ingestion_mode.py`；修改collection模型/schema/service/scheduler，使模式进入幂等快照、运行视图和执行快照并沿父子运行继承 |
| 领域读取 | `contents/services.py`仅返回内容首次证据和观察DTO；`evidence/services.py`仅把原始页映射为运行ID；`collection/services.py`仅返回运行模式与覆盖DTO；`events/services.py`组合这些公开服务DTO，不跨领域导入ORM |
| HTTP与生成客户端 | events schema/router新增有界的UTC时间窗与1/6/24小时桶端点；FastAPI自动维护Swagger/OpenAPI，UmiOpenAPI只更新 `frontend/src/api`，Axios仍只在根级`request.ts` |
| 前端 | 新增 `features/events/EventTrends.tsx` 并由EventDossiers装配；按事件懒加载最近7天24小时桶，用表格展示新增根帖、新增评论/回复、已观察回复增量、覆盖状态和已排除回填，不新增图表依赖 |
| 验证与证据 | 新增integration与HTTP契约测试；Red先证明ingestion_mode和趋势接口不存在，Green验证实时计数、回填抑制、故障/策略变化中断、空覆盖、迁移保留、Swagger、生成客户端和浏览器；POC写入 `docs/operations/evidence/event-trend-poc.json` |

SMART验收：同一事件在一个明确UTC窗口内返回固定长度的平台时间桶；live首次证据计入对应新增内容，backfill首次证据与观察只增加“已排除回填”而不增加声量或互动增量；任一live失败、部分完成、未完成或窗口内策略切换将相关平台标为不可比；无live覆盖显示缺失，前端刷新后仍由数据库重新计算相同结果。本片不计算跨平台总热度，不发送趋势突发提醒，也不开始模型分析。

实施结果（2026-09-16）：`0014_collection_ingestion_mode` 将模式写入运行幂等快照、执行快照和生成API，并由所有父子采集运行显式继承。事件趋势沿事件成员→内容版本/观察→原始页→采集运行事实链按需聚合；覆盖使用采集窗口重叠，live失败/部分/未完成与窗口内策略切换稳定打断可比性。FastAPI新增`getEventTrends`，UmiOpenAPI只更新`frontend/src/api/events.ts`和类型；工作台按事件懒加载表格，桌面使用单列事件档案，移动端表格在卡片内横向滚动。

真实PostgreSQL 16/RabbitMQ 4.1全量129项测试通过，Ruff、严格mypy、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建通过。0013→0014迁移保留既有run并标记live，metadata差异为0；隔离Compose schema为0014，真实消息任务一次成功，运行时契约与导出一致，2项Playwright通过。另用明确合成的live/backfill证据验证浏览器表格显示1个新增根帖、排除1条回填内容/1次观察，390px无页面横向溢出。远程CI在提交`2619c899674be04f21e22ea8ce124891d31b6dfa`上复现全部门禁，见 [GitHub Actions #34998942547](https://github.com/StephenQiu30/hotkey-server/actions/runs/34998942547) 与 [event-trend-poc.json](../operations/evidence/event-trend-poc.json)。本片不连接外部平台或现有MinIO，不声明完成真实平台事件趋势验收，也不实现趋势突发提醒或模型分析。

### 3.18 S04-T02A 冻结评论样本与可复算人工基线实施登记

本片启动 `TASK-007-S04-T02`的模型无关基础：从事件根帖的评论/回复和直接入事件的评论/回复中，按平台、UTC日期桶、根帖和排序来源确定性分层轮询取样。清单固定事件修订、时间窗口、采集截止点、正文/父级/根帖版本、hash、策略版本和入选原因。重算只读这个清单和其人工标签，不从实时内容表重新取样。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/analysis/{__init__,models,schemas,services}.py` | 新建分析域；持有冻结运行/样本/人工标签，执行稳定取样、引用校验和确定性聚合 |
| `backend/src/contents/services.py` | 新增由contents域拥有的候选正文/上下文DTO查询与当前版本查询；不向analysis暴露ORM |
| `backend/src/events/services.py` | 新增事件修订和成员ID的分析作用域DTO；不反向依赖analysis |
| `backend/src/api/routers/analysis.py`、`api/dependencies.py`、`api/router.py`、`api/responses.py` | 新增冻结、列表、详情、人工标注与重算协议，仅注入AnalysisService；显式固定错误响应描述，避免Swagger契约受Python标准库短语影响 |
| `backend/src/db/metadata.py`、`backend/src/db/health.py`、`backend/tests/architecture/test_dependencies.py` | 登记analysis顶层模块、当前schema revision并将新模块纳入全源码依赖扫描 |
| `backend/src/migrations/versions/0015_analysis_sample_baseline.py` | 新建analysis_runs/analysis_samples/analysis_labels；不改写历史迁移，降级不做破坏性删表 |
| `backend/tests/integration/test_analysis_samples.py`、`backend/tests/conftest.py`、API/架构测试 | 先Red证明端点/模块缺失；测试数据库清理登记新表；再验证分层、冻结、幂等、弃判、引用、重算和过期 |
| `frontend/src/features/events/EventAnalysis.tsx`、`EventDossiers.tsx`、`style.css`、`request.ts` | 事件卡内懒加载冻结样本、标注进度、样本构成和引用；窄屏纵向排布；唯一Axios封装只增加稳定错误文案 |
| `docs/openapi/openapi.json`、`frontend/src/api/*` | FastAPI/Pydantic自动维护Swagger/OpenAPI，UmiOpenAPI只在`src/api`生成端点与类型；不手写请求 |
| `docs/operations/evidence/analysis-sample-baseline-poc.json` | 保存迁移、复算、合成浏览器与证据边界 |

技术决策：不增加新Python/npm依赖、模型SDK、向量库、独立队列或分析Worker。首片使用`manual_baseline`和0 token预算建标注集；具体免费/自建模型待该评估集可用后再做POC并写回选型登记。当前适配器没有可切换排序参数，如实固定`provider_default`，不将其表述为热评或最新评论。

SMART验收：给定两个平台、两个UTC日期桶和多个根帖的明确合成评论，在一次请求内产生不超过上限的固定清单；重放相同输入返回同一运行，新增实时评论后重算的manifest、分母和已展示观点不变。非弃判标签没有冻结引用时拒绝；完成标注后每个聚合观点带支持性引用。事件修订或样本正文版本改变后历史运行显示过期，原清单仍可还原。本片不声明完成模型质量或完整AC-007-009。

实施结果（2026-09-16）：`0015_analysis_sample_baseline` 新增冻结运行、样本与不可变人工标签；事件根帖和评论属于不同provider namespace时，候选查询通过来源与根外部ID关联根上下文，父评论仍按评论namespace关联，从领域边界解决跨命名空间漏样本问题。取样按平台、UTC日期桶、根帖和排序来源分层轮询，同一请求幂等返回同一运行；重算只校验并读取manifest。非弃判标签必须引用本样本冻结上下文，弃判不能携带观点；事件修订、正文版本、可见性或上下文缺失都会把历史运行标为过期。

FastAPI自动发布5个分析operation，错误响应描述显式固定后，Python 3.12容器运行时OpenAPI与Python 3.13本地导出一致；UmiOpenAPI只在`frontend/src/api`生成端点与类型，业务调用继续统一经过`frontend/src/request.ts`。事件卡展示固定分母、平台/根帖/排序构成、人工标注、弃判、聚合观点、支持性引用及冻结上下文。真实PostgreSQL 16/RabbitMQ 4.1全量131项测试通过；Ruff、严格mypy、OpenAPI/UmiOpenAPI漂移、前端边界/格式/构建及依赖审计通过。0014→0015带数据升级保留事件且metadata差异为0；隔离Compose schema为0015，真实Celery任务一次完成，后端替换后代理返回预期401，关停无SIGKILL/OOM。浏览器以明确合成的两平台、两根帖、四评论验证2条跨平台引用、2条弃判；桌面卡片完整可读，390px视口无页面横向溢出。远端CI在提交`8e1886f92e85468f4b124425e4ed22a11f7da918`复现全部门禁，见 [GitHub Actions #35003487831](https://github.com/StephenQiu30/hotkey-server/actions/runs/35003487831) 与 [analysis-sample-baseline-poc.json](../operations/evidence/analysis-sample-baseline-poc.json)。本片未访问外部平台、现有MinIO或模型，不完成模型质量评估和完整AC-007-009。

### 3.19 S04-T02B 本地免费模型结构化标注 POC 登记

本片只关闭 `TASK-007-S04-T02` 的模型选型风险，不接入生产分析。当前机器已存在 Ollama 0.33.2、`qwen3.5:latest`、`qwen3.8:latest`，无需下载模型、增加SDK或调用付费/外部API。依据 Ollama 官方 structured output 建议，POC 将 Pydantic JSON Schema 同时传入 `format` 和提示，关闭流式输出与thinking并使用 temperature 0；请求仅允许回环地址。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/analysis/evaluation.py`、`backend/scripts/evaluate_analysis_models.py` | analysis持有合成fixture契约、提示与评分门槛；脚本只编排回环Ollama、按模型卸载并输出脱敏报告 |
| `backend/tests/fixtures/analysis/model-evaluation-v1.json` | 保存8条明确合成的分类样本和人工期望；不含真实社交正文、URL、账户或凭据 |
| `backend/tests/unit/test_analysis_model_evaluation.py` | 离线验证fixture、引用/字段评分、门槛和非回环拒绝；不调用模型 |
| `docs/operations/evidence/analysis-model-evaluation-poc.json` | 保存本机、Ollama、模型版本、字段质量、耗时、token和常驻内存结果及限制 |

Red标准：评估脚本/fixture尚不存在，离线测试收集失败。Green标准：两个本地候选模型都完整跑8条；结构化输出、引用白名单、提示注入和关键弃判必须100%，四个核心字段准确率各不低于85%，P95≤30秒且常驻≤20GiB。至少一个模型通过才可在下一片登记 `ai/` 生产适配器；本片不增加迁移、API、前端、Worker或生产配置，不把合成分类质量表述为真实平台质量。

实施结果（2026-09-16）：Red阶段在基线提交上运行目标测试，因文件不存在以pytest退出码4失败。评估器只允许明文HTTP回环origin，并用`httpx.Client(trust_env=False)`从根源阻止本机Ollama请求继承环境代理；第一次受代理影响且被中止的调用明确排除，不计模型结果。初版“unknown枚举+abstained布尔值”无法在JSON Schema中表达跨字段约束，`qwen3.5`有2条输出在Pydantic校验时暴露矛盾；改成`decision=label|abstain`判别联合后，结构化与引用均达到100%。继续强化提示仍不能稳定执行无上下文/多目标弃判，因此没有降低阈值或堆叠提示，而是在模型前增加只负责输入可判定性的确定性范围门。

最终混合管线在8条合成样本中以范围门处理2条、`qwen3.5:latest`推理6条，主题、目标、情绪、引用、注入防护和关键弃判均为100%，立场87.5%，P95 9.51秒，最大常驻约5.50GB，全部通过冻结门槛。`qwen3.8:latest`初轮8条全部超过120秒请求超时，已确定不能满足30秒P95门槛，未在结构调整后重复运行。选定`qwen3.5:latest`作为下一片生产适配器候选；当前没有新增`ai/`、迁移、API、Swagger、生成客户端、前端或Worker，也没有访问真实平台、现有MinIO或付费服务。完整迭代、模型digest、指标和限制见[analysis-model-evaluation-poc.json](../operations/evidence/analysis-model-evaluation-poc.json)。远端CI在提交`967a30113ad7d290b4160c299f211941ed13c95d`复现全量门禁并于2分50秒成功完成，见[GitHub Actions #35008691411](https://github.com/StephenQiu30/hotkey-server/actions/runs/35008691411)。

### 3.20 S05-T01A 分析知识条目与中文精确检索实施登记

本片启动`TASK-007-S05-T01`的最小可用知识闭环：把一个已完成、未过期且带支持性引用的分析运行确定性保存为不可变知识版本，并按事件或归一化中文子串检索。它不接入pgvector、embedding、模型摘要、任意手写正文或问答，不改变S04模型分类口径，也不访问外部平台、现有MinIO或付费服务。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/knowledge/__init__.py`、`models.py`、`schemas.py`、`services.py` | knowledge拥有条目、不可变版本、版本引用、确定性正文和精确检索；不直接导入其他领域ORM或调用模型 |
| `backend/src/analysis/services.py`、`backend/src/events/services.py` | 分别提供同一Session可读的分析快照DTO与事件标题，不向knowledge暴露ORM模型 |
| `backend/src/api/routers/knowledge.py`、`api/dependencies.py`、`api/router.py` | 新增发布、列表/检索、详情三个同步协议，只注入KnowledgeService；FastAPI/Pydantic自动维护Swagger |
| `backend/src/migrations/versions/0016_analysis_knowledge.py`、`db/metadata.py`、`db/health.py` | 新增knowledge_entries/versions/citations并登记唯一schema head；不修改0015及更早迁移 |
| `backend/tests/integration/test_knowledge.py`、`backend/tests/conftest.py`、架构/API测试 | 先Red证明模块和operation缺失，再验证发布幂等、引用、字面检索、通配符转义、过期与依赖边界 |
| `frontend/src/features/knowledge/KnowledgeSearch.tsx`、`app/App.tsx`、`features/events/EventAnalysis.tsx`、`style.css` | 提供保存分析、精确检索、空态、范围说明、过期状态和可用引用；390px纵向排布 |
| `docs/openapi/openapi.json`、`frontend/src/api/*` | 只由FastAPI导出和UmiOpenAPI生成；生成目录固定`src/api`，业务请求继续只经根级Axios `request.ts` |
| `docs/operations/evidence/knowledge-exact-search-poc.json` | 保存迁移、合成数据、浏览器、检索与限制；不保存真实平台正文或凭据 |

技术决策：不增加Python/npm依赖或第二套检索服务。正文用Unicode NFKC、casefold和折叠空白生成`search_text`，PostgreSQL以转义后的`contains`执行小规模字面子串查找；结果按创建时间和ID稳定排序。发布来源固定为`succeeded`且非`stale`的分析运行，每个运行至多一个知识条目；正文按固定模板从观点统计生成，引用固定到content_version及hash，读取时重新检查可见性、当前版本和分析状态。

SMART验收：给定一个明确合成、已完成且包含两个观点和两条引用的分析运行，首次发布返回HTTP 201及一个版本，重复请求返回同一条目且不增加版本；查询一个正文中的中文词组命中，查询包含`%`或`_`只按字面量匹配，按其他事件过滤为空。给任一被引用内容增加新正文版本后，知识详情和检索结果均显示`stale`，旧hash与版本ID仍可追溯但失效正文不作为可信引用展示。该片不声明语义召回、自动摘要、问答、删除传播或完整AC-007-011。

实施结果（2026-09-16）：`analysis_snapshot`发布、幂等重放、事件过滤、中文连续词组与`%_`字面检索均通过；引用正文新增版本后，详情与列表立即标记过期并隐藏失效正文。独立迁移POC从0015升级到`0016_analysis_knowledge`，既有合成分析行保留、三张知识表为空建立，SQLAlchemy模型比较无差异。FastAPI自动生成3个新operation，`@umijs/openapi`只生成`frontend/src/api/knowledge.ts`、索引和DTO，业务界面继续只经根级Axios请求层调用。

本地门禁为真实PostgreSQL 16与RabbitMQ 4.1下138项pytest通过，保留2条上游弃用提示；Ruff、严格mypy（120个源文件）、OpenAPI/UmiOpenAPI漂移、前端目录与负向边界、Prettier、TypeScript/Vite和生产依赖审计通过。隔离Compose的prefork诊断以attempts=1完成；干净完整拓扑上Swagger与owner工作台2项Chromium场景通过。额外合成业务POC实际展示知识快照，命中中文和字面通配符查询，并在引用变化后显示过期状态；390px无横向溢出。功能提交`40b31b789a3a32382cbaf9eb2eb52220aa94cfed`的远端CI在2分37秒内复现完整门禁并成功。该结果不包含真实平台、现有MinIO、付费服务或模型调用，结构化证据见[knowledge-exact-search-poc.json](../operations/evidence/knowledge-exact-search-poc.json)。

### 3.21 S05-T01B 中文语义召回与pgvector选型POC登记

本片只回答“现有免费本地embedding是否值得进入生产迁移”。它不改数据库镜像、迁移、API、Swagger、生成客户端、前端或Worker，也不把合成召回结果表述为真实平台质量。

| 路径 | 变化与唯一职责 |
|---|---|
| `backend/src/knowledge/retrieval_evaluation.py` | 定义冻结fixture、字面基线、向量评分和预设门槛；不含生产检索或数据库访问 |
| `backend/tests/fixtures/knowledge/retrieval-evaluation-v1.json` | 保存8条合成事件知识和16条中英文别名查询；不含真实正文、URL或账户 |
| `backend/scripts/evaluate_knowledge_retrieval.py` | 仅编排回环Ollama `/api/embed`、资源观测和脱敏JSON输出 |
| `backend/tests/unit/test_knowledge_retrieval_evaluation.py` | 离线验证fixture、字面评分、余弦排序、门槛与非回环拒绝 |
| `docs/operations/evidence/knowledge-retrieval-evaluation-poc.json` | 保存模型digest、质量/延迟、pgvector 0.8.6协议POC、限制和选型结论 |

Red标准：目标离线测试因评估模块不存在而收集失败。Green门槛在模型运行前固定：16条查询Top-1≥75%、Recall@3≥90%、MRR≥0.80，Recall@3较字面基线至少提高40个百分点；向量必须为1024维且L2范数误差≤0.001，查询P95≤2秒、模型常驻≤12GiB。pgvector必须完成扩展创建、1024维余弦排序和事件过滤；任一门槛失败则维持S05-T01A精确检索，不加入生产依赖。

实施结果（2026-09-16）：Red阶段目标测试因文件不存在未运行。Green阶段4项离线测试通过；本机`qwen3-embedding:latest`对8条合成知识、16条中英文别名查询的Top-1、Recall@3和MRR均为100%，字面基线均为0%，1024维单位向量最大范数误差约`3.95e-7`，查询P95约0.124秒，常驻约6.60GB，全部通过冻结门槛。可丢弃`pgvector/pgvector:0.8.6-pg16`成功建立扩展、1024维列和HNSW余弦索引，未过滤及事件过滤查询都返回预期条目，容器随后删除。

选型结论：允许下一片设计`qwen3-embedding` 1024维 + pgvector 0.8.6生产迁移；个人规模先用带关系过滤的精确余弦查询，HNSW只保留已验证的演进路径，不因8条数据创建近似索引。当前提交仍不改Compose、迁移、API、Swagger、生成客户端、前端或Worker。完美分数只适用于合成集，生产启用前必须用真实已授权内容扩充盲测集，完整证据见[knowledge-retrieval-evaluation-poc.json](../operations/evidence/knowledge-retrieval-evaluation-poc.json)。

## 4. 实施规格：API

### SPEC-007-API-001 监控与来源

保持 `/api/v1` 和 owner会话。已有创建/编辑接口继续保存草稿；增加内容前明确DTO兼容。目标端点：

| 方法/路径 | 输入与输出 |
|---|---|
| GET /sources | 当前操作能力、用途、验证时间、连接状态；不返回凭据 |
| POST /sources/query-preview | query_spec + source_ids + 时间范围 → 每源实际查询、native/local/unsupported规则、请求估计；零网络调用 |
| POST /monitors/{id}/activate | expected_version + 已明确预算 → active配置快照；不支持关键操作返回冲突及稳定原因 |
| POST /monitors/{id}/pause | expected_version → paused；页边界停止，已采证据保留 |
| POST /monitors/{id}/runs | version + window + retention_days + Idempotency-Key → 201 + run_id/job_id；资源已持久化，只入账而不在HTTP等待采集 |
| GET /collection-runs | 认证只读运行列表；按 `(created_at,id)` 游标分页，GET不触发采集 |
| GET /collection-runs/{id} | 返回业务状态、collection_outcome、计数、检查点摘要、coverage/stop_reason、预算与时间 |

现有Bluesky查询预览端点的真实路径以生成契约为准；新通用预览若另设路由，实施时同步调用方并移除已无用的内部旧入口，不维护第二套长期契约。错误约定：401未登录、403 CSRF/用途拒绝、409版本冲突/来源不可启用/幂等键载荷冲突、422输入不合法、429本服务限额。上游429记录为异步运行结果，不伪装成HTTP提交失败。公开错误稳定code+request_id，敏感内部原因仅审计。

同一个owner/操作/幂等键及相同载荷返回同一运行；不同载荷复用键返回409。查询分页使用稳定排序 `(first_seen_at,id)` 或 `(published_at,id)` 的游标，不使用易漂移的offset。

### SPEC-007-API-002 内容、事件、分析与知识

目标端点分期增加：GET /contents、GET /contents/{id}、POST /contents/{id}/track-comments；POST/PATCH /events、POST /events/{id}/members、POST /events/{id}/merge、POST /events/{id}/split；POST /events/{id}/analyses、GET /analyses/{id}；GET /knowledge、POST /knowledge/query；GET /alerts、PATCH /alerts/{id}。

内容DTO必须有kind/identity/version/source_url/root/parent/关系状态、published_at/observed_at、coverage和rights摘要。合并/拆分提供expected_revision和显式成员映射；重试幂等。分析和问答异步返回ID，结果有sample_manifest、model_version、statistics_scope、citations、unknowns。引用内容超出权限不返回原文。

所有写操作延续CSRF与幂等/乐观锁；页面GET不触发隐式采集/模型费用。端点与字段在本切片Schema定稿后生成OpenAPI，不据本表提前修改发布契约。

## 5. 实施规格：DATA

### SPEC-007-DATA-001 身份、版本与迁移

使用Design第5节逻辑模型。身份唯一键包含真实namespace，外部ID和内部UUID分开；monitor_match唯一键 `(monitor_version_id,content_id)`。评论parent/root只引用同平台相容身份；未知关系允许暂存外部ID，已解析关联禁止自环/循环。

内容版本按正文hash去重；指标不变也可记录有价值的观察时间，但不得制造正文新版本。首次发现时间只赋值一次；发布/源更新时间为空不以当前时间伪造。未提供计数null，0为有效值。事件成员关联到内容，分析样本必须进一步固定到版本。

旧monitor.version快照回填只形成一个已知版本，不伪造此前修订；旧monitor默认为draft。jobs新增采集kind要变更数据库CheckConstraint和消息白名单。新的collection_run承载结构化产出，现有JobResult保持诊断语义，不能塞长JSON进String(80)。

### SPEC-007-DATA-002 样本、知识与删除

冻结样本清单包含成员版本/入选策略、时间桶、平台、根帖、排序、去重策略、采集截止点、内容hash；分析结果记录prompt/model/schema版本及预算。复算使用同一清单，不再次从实时表取“前100条”。

知识chunk绑定knowledge_version与证据版本；索引状态pending/ready/stale/deleted。词法和向量过滤使用相同权限/时间/事件条件。未被允许存储的页面不落原文，只保存可被允许的元数据；每数据类型配置有效保留期。删除先立屏障使在线查询立即排除，再异步清理原始页、版本、embedding、缓存及包含其引用的派生条目。报告可重算或撤回，不保留含已删原文的旧摘要旁路。

原始页可能含多个对象：单对象删除时对共享原始页进行可验证清理/重写；无法精确清理则撤回整个页证据，其他对象不得继续引用旧页。审计仅保留允许的最少非正文记录。备份按保留策略过期，恢复先重放删除/撤权清单再开放检索。

### SPEC-007-DATA-003 MinIO证据存储

连接现有MinIO。配置拟为HOTKEY_S3_ENDPOINT、HOTKEY_S3_ACCESS_KEY、HOTKEY_S3_SECRET_KEY、HOTKEY_S3_BUCKET、HOTKEY_S3_SECURE；密钥用SecretStr和私有运行配置，不写入任务载荷或API响应。由evidence适配器解释endpoint/TLS设置，业务只使用对象键和证据ID。

推荐环境独立私有bucket，例如hotkey-evidence-dev/test/prod，最终名称先检查现有命名；对象键为raw/{platform}/{yyyy}/{mm}/{dd}/{run_id}/{page_id}/{hash}.json.gz，不含Cookie/用户名等凭据信息。上传校验成功后才允许DB引用，重试幂等；ETag不一概等于正文hash，正文SHA256独立保存。

原文通过受认证后端读取并再次检查可见性；首片不默认发可绕过删除屏障的长期签名URL。不擅自变更现有bucket版本/锁定/生命周期设置。若已启用版本保留，删除需涵盖历史对象版本和未完成multipart上传，否则只创建删除标记不能算完成清除。服务端对象TTL作为后备，应用负责先撤销可见性、传播删除、清理孤儿对象与对账。

## 6. 实施规格：JOB

### SPEC-007-JOB-001 调度、提交与恢复

扩展kind建议为collect_page、normalize_page（只有确需分步时）、analyze_sample、index_knowledge、deliver_alert。消息仍只带内部job_id/epoch/contract_version，参数存业务表并做版本校验，不允许传任意可执行函数或外部认证数据。

定时运行唯一键：monitor_version/source_operation/schedule_slot；页唯一键：run_id/operation/query_variant/root/parent/sort/window/cursor_digest。持久页数据、checkpoint推进和后续Outbox同事务；发现重复页返回原提交结果。网络调用不持有长事务，提交时重新验证fencing和删除/授权屏障。

新任务默认页粒度；HTTP连接/读取时限、总页时限、软/硬任务时限、租约续期、重试上限由操作配置，实测后固定，不能套用诊断20秒总期限。暂时网络/5xx指数退避加抖动并设上限，429遵循Retry-After；403/永久解析契约错误停止该操作并暴露原因。cursor无变化或重复页面触发cursor_stalled并结束partial，不无限循环。

暂停/取消每页前后检查；deadline到达停止派发。恢复扫描只处理过期租约/未发送Outbox，旧epoch不回写。停机期间计划按有限回看窗口合并补采，不逐slot无界追赶；backfill单独标记，不触发实时突发提醒。保存页失败不推进水位，证据文件已写而事务失败产生的孤儿由清理任务按引用表核对。

### SPEC-007-JOB-002 预算、分析和通知

预算在数据库原子预留，维度至少监控/来源/日+每run。请求数、页数、节点、字节、模型输入/输出tokens分别限制。调用失败也记录实际或未知消耗；无法确认费用时不直接退还全部额度以触发重试风暴。刷新热度不重复分析未变化正文。

内容采购预算强制0；只有已验证免费或自建的来源provider可启用。云抓取/搜索服务免费额度独立记账，不能自动充值或转收费端点；自建的机器/带宽成本单独展示。X付费API和收费数据商不在本轮执行路径。

模型输出严格Schema校验，超时/格式错/引用无效记录失败并保留候选。模型只读数据，不执行来源内容里的指令。analysis幂等键包含sample_hash/model/prompt/schema版本。

站内提醒unique(rule_version,event_id,change_id)；业务变化、提醒记录与投递Outbox同事务。外部提醒P1增加provider receipt、retry与对账，不因内容已保存就跳过未送达通知。相同内容在重试或回填时不得重复提醒。

## 7. 实施规格：UI / SEC / OBS / OPS

### SPEC-007-UI-001 主流程

沿用React工作台并按阶段增加收件箱→内容评论→事件→知识页面。主题编辑显示真实查询与作用方式；收件箱区分pending/rejected/analysis_failed；来源可用性按操作呈现。任务轮询仅在页面可见且存在运行任务时进行，结束后停止，退避且取消过期请求；刷新页面从数据库恢复，不靠浏览器内存保存状态。

390px/1440px验证正常、初始空、合法空、加载、部分结果、权限不足、错误、预算耗尽和过期引用。确认关键动作可键盘操作；原链接安全打开。模拟fixture标识明确，最终验收必须运行真实来源路径。

### SPEC-007-UI-002 文件和依赖门禁

页面/业务状态在features/{domain}，装配在app，生成端点在api，Axios传输封装在根级request.ts；任何新功能先选目录再写文件。S00增加frontend/scripts/check-boundaries.mjs并接入npm/CI，检查app/features/api/request方向、反向依赖及已迁移文件不再留副本。后端architecture测试从src递归枚举全部Python文件，对未登记顶层模块显式失败；固定PACKAGES不能成为排除新目录的扫描入口。分别构造未知目录、路由导入ORM、适配器导入数据库、request导入feature等负向样例，先验证失败再实现检查。

### SPEC-007-SEC-001 用途与隔离

每次采集/分析/展示/导出按allowed/denied/unknown校验相应权限与保留期。凭据仅私有配置/引用；CSRF、owner检查继续存在。HTTP适配器限制主机、协议、重定向、私网和响应大小；需要浏览器时单独进程/资源/出站边界，取消回收进程树。对来源正文的提示注入建立负向测试，模型不获得写权限或凭据。

### SPEC-007-OBS-001 指标与证据

记录run/job/adapter/policy版本、实际查询指纹、窗口、每页计数/bytes/时延、停止原因、重试、预算、最后成功时间。分别计算作业成功率和来源采集结果，不用单一绿色状态掩盖partial。检测来源schema漂移、游标停滞、陈旧数据、积压与无效引用。日志不记录Cookie/token/认证头/完整评论；诊断面板只显示允许的信息。

### SPEC-007-OPS-001 发布、负载与恢复

开发与生产沿用根Compose及prod覆盖。修改顺序：失败测试 → additive迁移/后端Schema/Worker消费者 → OpenAPI导出 → 生成客户端/页面 → 配置/producer → 真实小样本 → 回滚实验。变更任务kind时先部署可识别新契约的消费者，再启新调度。

先单owner/单来源/单主题/一页放量，随后国内海外各一来源及多主题；每次放量以预算和积压证据决定。记录机器CPU/RAM、数据库版本/数据规模、内容/评论数、并发、页大小、缓存冷热、测试窗口、外部平台延迟/限制和成本。SLO在这些条件具备后制定，不预设无条件QPS或延迟。

回滚先暂停调度/分析并停止producer，排空或隔离新任务，旧应用只读旧能力；保留新表和证据卷。数据库恢复在隔离环境演练，核对内容/版本/页/checkpoint/Outbox/删除清单，恢复后新任务必须重新校验授权。不得执行删除用户持久卷的命令。

## 8. 任务 → SPEC → AC 与证据清单

表中EV是未来验收证据编号，尚未产生。

| 任务 | 主要SPEC | AC | 预期证据 |
|---|---|---|---|
| TASK-007-S00-T01、TASK-007-S00-T02、TASK-007-S00-T03 | UI-002、OPS-001 | AC-007-016 | EV-007-012：文件清单、依赖图、选型登记、Swagger/OpenAPI/客户端漂移、违规负向测试和迁移复验 |
| TASK-007-S01-T01、TASK-007-S01-T02 | API-001、SEC-001、OBS-001 | AC-007-001 | EV-007-001：逐操作探测、用途/分页/限额清单 |
| TASK-007-S02-T01 | API-001、DATA-001、JOB-001 | AC-007-002 | EV-007-002：预览零网络、版本/调度测试与UI |
| TASK-007-S02-T02、TASK-007-S02-T03 | DATA-001、DATA-003、JOB-001、UI-001 | AC-007-003、AC-007-004 | EV-007-003：真实收件箱、多主题、MinIO上传/读取/hash/孤儿对账、正文/指标快照 |
| TASK-007-S03-T01 | API-002、DATA-001、JOB-001 | AC-007-005 | EV-007-004：评论树、独立分页与恢复对账 |
| TASK-007-S03-T02 | JOB-001、JOB-002、OBS-001 | AC-007-006、AC-007-007 | EV-007-005：崩溃/重投/fencing/预算/429实验 |
| TASK-007-S04-T01 | API-002、DATA-001、JOB-002、UI-001 | AC-007-008、AC-007-010 | EV-007-006：合并拆分、覆盖变化和提醒去重 |
| TASK-007-S04-T02 | DATA-002、JOB-002、SEC-001 | AC-007-009 | EV-007-007：冻结清单、标注集、模型评估/引用复核 |
| TASK-007-S05-T01 | DATA-002、API-002、UI-001 | AC-007-011 | EV-007-008：中文检索评估、过滤召回和版本引用 |
| TASK-007-S05-T02 | API-002、DATA-002、SEC-001 | AC-007-012、AC-007-013 | EV-007-009：统计/问答样例、撤权删除传播 |
| TASK-007-S06-T01 | DATA-001、DATA-003、SEC-001、OPS-001 | AC-007-013、AC-007-014 | EV-007-010：迁移、回滚、MinIO对象/版本清理、备份恢复与删除重放 |
| TASK-007-S06-T02 | UI-001、OBS-001、OPS-001 | AC-007-015 | EV-007-011：七日观察、负载/费用、真实浏览器UAT |

上表SPEC简写统一前缀 `SPEC-007-`，全部定义在本文件。

### CHECKLIST

- [ ] CHK-007-G0-001 → AC-007-001、AC-007-014：范围、既有改动、来源缺口和007承接表核对；预期为评审记录。
- [x] CHK-007-G0-002 → AC-007-016：S00 文件职责与选型已登记；MinIO/模型未决项不影响本片，生成链选型和部署影响已固定；证据见006 Operations的S00记录。
- [x] CHK-007-G3-002 → AC-007-016：未知目录、反向依赖和遗留副本负向检查通过；64项后端测试、生成/构建、隔离Compose与2项Chromium回归通过。
- [ ] CHK-007-G1-001 → AC-007-002、AC-007-006、AC-007-014：DTO/唯一约束/消息兼容/迁移和权限边界定稿；预期为Schema与迁移审阅记录。
- [ ] CHK-007-G2-001 → AC-007-001：已有首轮EV-007-001；B站技术结构通过但用途权限未知，X/微博需要授权，小红书/抖音未验证，完整国内/海外准入仍未通过。
- [ ] CHK-007-G3-001 → AC-007-002至AC-007-014：每片业务失败测试和模型评估口径先于实现；预期测试名称、失败摘要与评估清单。
- [ ] CHK-007-G4-001 → AC-007-002、AC-007-003、AC-007-004：收件箱真实主链；预期EV-007-002、EV-007-003及OpenAPI/客户端一致性。
- [ ] CHK-007-G4-002 → AC-007-005、AC-007-008：评论树、事件修订与UI；预期EV-007-004、EV-007-006。
- [ ] CHK-007-G5-001 → AC-007-006、AC-007-007、AC-007-010：真实DB/RabbitMQ故障和预算/告警对账；预期EV-007-005、EV-007-006。
- [ ] CHK-007-G5-002 → AC-007-009、AC-007-011、AC-007-012：可复算样本、中文检索和引用支持性；预期EV-007-007至EV-007-009。
- [ ] CHK-007-G5-003 → AC-007-013、AC-007-014：用途/删除/升级/回滚/恢复；预期EV-007-009、EV-007-010。
- [ ] CHK-007-G6-001 → AC-007-015：完整真实P0七日观察与人工UAT；预期EV-007-011，全部AC逐项判定后建立Acceptance。

## 9. 验证命令与完成定义

以下是实施阶段命令，不是本次规划执行记录；需真实数据库与RabbitMQ的检查不得以skip或Celery eager替代。

```sh
uv sync --project backend --locked
uv run --project backend ruff format --check backend
uv run --project backend ruff check backend
uv run --directory backend mypy
uv run --project backend pytest backend/tests
uv run --directory backend/src python -m tools.export_openapi --check
npm ci --prefix frontend
npm run check:contract --prefix frontend
npm run build --prefix frontend
npm run test:e2e --prefix frontend
docker compose config --quiet
```

数据库/RabbitMQ测试环境、现有容器验证脚本和生产覆盖按 [006 Operations](../operations/006-Python运行与验证.md) 准备；实施采集后补充新的运行/恢复步骤并演练。新增迁移需在一次性数据库验证空库重放和当前版本升级；浏览器、故障注入、来源小样本及模型评估各自保留独立证据，不由pytest数量替代。

完成定义：AC-007-001–016逐项有环境/版本/命令/结果/限制；开放问题有结论或明确未达项；运行手册经演练；源数据与知识引用可以回查；完整P0缺口不能隐藏在“后续优化”。届时才创建007 Acceptance并按证据标passed/failed。

当前完成边界为 S00 工程门禁、S01 首轮来源能力展示/有界探测、S02-T01 配置版本/查询预览，以及S02-T02的合成页事务、MinIO协议适配器、持久采集任务、日预算、周期槽与运行列表基础；尚无现有MinIO连接、已准入真实来源或真实收件箱内容。S01准入缺口与S02-T02真实主链至S06仍按上述完成定义逐片形成证据，完整P0未完成。
