---
layer: Plan
doc_no: 023
audience: Dev, QA
feature_area: Event / Topic 知识模型与同步基线
purpose: 定义 Event/Topic 双层对象、知识同步基线、数据库演进和基础任务编排的落地任务
canonical_path: docs/plans/023-事件主题知识模型与同步基线计划.md
status: draft
version: v1.0
owner: Codex
inputs:
  - docs/prd/001-obsidian热点知识中台需求.md
  - docs/plans/022-Obsidian热点知识中台总体实施计划.md
  - docs/design/007-核心数据模型与数据库设计.md
  - db/schema.sql
outputs:
  - Event 主对象与关系模型
  - 知识同步主线基线
  - 基础 repository 与 worker 编排
triggers:
  - 需要将 Topic-only 日报导出升级为 Event/Topic 双层模型
  - 需要冻结同步任务前的核心数据边界
downstream:
  - docs/plans/024-Obsidian知识沉淀与导出计划.md
  - docs/plans/025-双向回写与治理计划.md
---

# Event Topic Knowledge Model Baseline Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为知识中台新增 Event 主对象、重构同步领域边界，并把当前日报导出能力升级为通用知识同步基线。

**Architecture:** 在现有 `platform_posts -> topic -> trend` 主线中插入 `event` 层，同时新增知识对象运行记录与同步边界。坚持 `repository + job + obsidian contract` 分层，不把 Markdown 目录直接当成主模型。

**Tech Stack:** Go, PostgreSQL, GORM, worker jobs, Markdown contract tests

---

### Task 1: 扩展 schema，增加 Event 与知识运行主对象

**Files:**
- Modify: `db/schema.sql`
- Create: `internal/database/eventrepo.go`
- Create: `internal/database/eventrepo_test.go`
- Modify: `internal/database/models.go`

- [ ] **Step 1: 写 Event / TopicEvent / KnowledgeRun 的失败测试用例**

```go
func TestEventRepo_CreateEvent(t *testing.T) {
    repo := NewEventRepo(newTestDB(t))
    eventID, err := repo.CreateEvent(context.Background(), CreateEventInput{
        MonitorID:      1,
        EventKey:       "evt:ai-regulation:2026-07-01",
        Title:          "AI 监管规则发布",
        Summary:        "监管机构发布新的 AI 监管规则。",
        FirstSeenAt:    time.Date(2026, 7, 1, 8, 0, 0, 0, time.UTC),
        LastActiveAt:   time.Date(2026, 7, 1, 12, 0, 0, 0, time.UTC),
        SourcePostID:   1001,
        MachineStatus:  "active",
    })
    if err != nil {
        t.Fatalf("create event: %v", err)
    }
    if eventID == 0 {
        t.Fatal("expected non-zero event id")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/database -run TestEventRepo_CreateEvent -v`
Expected: FAIL with `undefined: NewEventRepo` or missing `events` table

- [ ] **Step 3: 在 `db/schema.sql` 中新增核心表**

```sql
create table events (
  id bigserial primary key,
  monitor_id bigint not null references keyword_monitors(id),
  event_key text not null,
  title text not null,
  summary text not null default '',
  machine_status text not null default 'active',
  source_post_id bigint references platform_posts(id),
  first_seen_at timestamptz not null,
  last_active_at timestamptz not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (monitor_id, event_key)
);

create table topic_events (
  id bigserial primary key,
  topic_id bigint not null references topics(id),
  event_id bigint not null references events(id),
  relationship_type text not null default 'member',
  created_at timestamptz not null default now(),
  unique (topic_id, event_id)
);

create table knowledge_runs (
  id bigserial primary key,
  run_key text not null unique,
  run_type text not null,
  target_date date,
  status text not null default 'pending',
  error_message text not null default '',
  started_at timestamptz,
  finished_at timestamptz,
  created_at timestamptz not null default now()
);

create table themes (
  id bigserial primary key,
  monitor_id bigint not null references keyword_monitors(id),
  theme_key text not null,
  title text not null,
  summary text not null default '',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (monitor_id, theme_key)
);

create table export_bundles (
  id bigserial primary key,
  monitor_id bigint not null references keyword_monitors(id),
  bundle_key text not null,
  bundle_kind text not null,
  date_start date,
  date_end date,
  status text not null default 'pending',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (monitor_id, bundle_key)
);

create table event_annotations (
  id bigserial primary key,
  event_id bigint not null references events(id),
  manual_tags jsonb not null default '[]'::jsonb,
  analyst_conclusion text not null default '',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (event_id)
);

create table topic_annotations (
  id bigserial primary key,
  topic_id bigint not null references topics(id),
  material_status text not null default 'unreviewed',
  manual_summary text not null default '',
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique (topic_id)
);

create table theme_memberships (
  id bigserial primary key,
  theme_id bigint not null references themes(id),
  event_id bigint references events(id),
  topic_id bigint references topics(id),
  source_kind text not null,
  created_at timestamptz not null default now()
);

create table knowledge_object_revisions (
  id bigserial primary key,
  object_type text not null,
  object_id bigint not null,
  revision text not null,
  source_path text not null default '',
  updated_at timestamptz not null default now(),
  unique (object_type, object_id)
);
```

- [ ] **Step 4: 实现最小 EventRepo**

```go
type EventRepo struct {
    db *gorm.DB
}

func NewEventRepo(db *gorm.DB) *EventRepo {
    return &EventRepo{db: db}
}

func (r *EventRepo) CreateEvent(ctx context.Context, in CreateEventInput) (int64, error) {
    event := EventModel{
        MonitorID:     in.MonitorID,
        EventKey:      in.EventKey,
        Title:         in.Title,
        Summary:       in.Summary,
        MachineStatus: in.MachineStatus,
        SourcePostID:  in.SourcePostID,
        FirstSeenAt:   in.FirstSeenAt,
        LastActiveAt:  in.LastActiveAt,
    }
    if err := r.db.WithContext(ctx).Create(&event).Error; err != nil {
        return 0, err
    }
    return event.ID, nil
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `go test ./internal/database -run TestEventRepo_CreateEvent -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add db/schema.sql internal/database/eventrepo.go internal/database/eventrepo_test.go internal/database/models.go
git commit -m "feat: 新增事件主题与知识 sidecar 基础模型"
```

### Task 2: 新建 Event 领域服务并建立 Topic 关联边界

**Files:**
- Create: `internal/event/service.go`
- Create: `internal/event/service_test.go`
- Modify: `internal/topic/service.go`
- Create: `internal/topic/topic_event_linker.go`

- [ ] **Step 1: 写 Event 领域服务失败测试**

```go
func TestService_BuildEventFromPosts(t *testing.T) {
    svc := NewService(nil)
    event, err := svc.BuildEventFromPosts(BuildEventInput{
        MonitorID: 1,
        TopicID:   42,
        TopicTitle:"AI 监管",
        EventSeed: "监管机构发布新规并引发多方解读",
        Posts: []PostFact{
            {PostID: 1001, PublishedAt: time.Date(2026, 7, 1, 8, 0, 0, 0, time.UTC), Text: "监管规则发布"},
            {PostID: 1002, PublishedAt: time.Date(2026, 7, 1, 9, 0, 0, 0, time.UTC), Text: "多位从业者解读监管影响"},
        },
    })
    if err != nil {
        t.Fatalf("build event: %v", err)
    }
    if event.EventKey == "" {
        t.Fatal("expected event key")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/event -run TestService_BuildEventFromPosts -v`
Expected: FAIL with `undefined: NewService`

- [ ] **Step 3: 实现最小 EventService**

```go
func (s *Service) BuildEventFromPosts(in BuildEventInput) (Event, error) {
    if len(in.Posts) == 0 {
        return Event{}, ErrEmptyPosts
    }
    firstSeen := in.Posts[0].PublishedAt
    lastActive := in.Posts[0].PublishedAt
    for _, post := range in.Posts[1:] {
        if post.PublishedAt.Before(firstSeen) {
            firstSeen = post.PublishedAt
        }
        if post.PublishedAt.After(lastActive) {
            lastActive = post.PublishedAt
        }
    }
    return Event{
        MonitorID:    in.MonitorID,
        TopicID:      in.TopicID,
        EventKey:     NormalizeEventKey(in.EventSeed, firstSeen),
        Title:        SummarizeEventTitle(in.EventSeed, in.Posts),
        FirstSeenAt:  firstSeen,
        LastActiveAt: lastActive,
    }, nil
}
```

- [ ] **Step 4: 冻结 Event 与 Topic 的基数约束**

```go
func TestService_EventIsNotTopicAlias(t *testing.T) {
    svc := NewService(nil)
    event, err := svc.BuildEventFromPosts(BuildEventInput{
        MonitorID: 1,
        TopicID:   42,
        TopicTitle:"AI 监管",
        EventSeed: "监管新规发布",
        Posts: []PostFact{
            {PostID: 1001, PublishedAt: time.Date(2026, 7, 1, 8, 0, 0, 0, time.UTC), Text: "监管规则发布"},
        },
    })
    if err != nil {
        t.Fatalf("build event: %v", err)
    }
    if event.Title == "AI 监管" {
        t.Fatal("event title must not collapse to topic title by default")
    }
}
```

- [ ] **Step 5: 为 Topic 关联增加单独链接器**

```go
type TopicEventLinker interface {
    LinkEvent(ctx context.Context, topicID, eventID int64) error
}
```

- [ ] **Step 6: 运行测试确认通过**

Run: `go test ./internal/event ./internal/topic -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add internal/event internal/topic/service.go internal/topic/topic_event_linker.go
git commit -m "feat: 新增事件领域服务与主题事件关联边界"
```

### Task 3: 将 publish_daily_topics 升级为知识同步基线

**Files:**
- Modify: `internal/jobs/publish_daily_topics.go`
- Create: `internal/jobs/publish_knowledge_snapshot.go`
- Create: `internal/jobs/publish_knowledge_snapshot_test.go`
- Modify: `internal/app/worker_jobs.go`

- [ ] **Step 1: 写同步编排失败测试**

```go
func TestPublishKnowledgeSnapshotJob_Run(t *testing.T) {
    job := NewPublishKnowledgeSnapshotJob(
        &mockDigestService{},
        &mockEventAssembler{},
        &mockKnowledgeExporter{},
    )
    result, err := job.Run(context.Background(), time.Date(2026, 7, 2, 8, 0, 0, 0, time.UTC))
    if err != nil {
        t.Fatalf("run job: %v", err)
    }
    if result.EventsPublished == 0 {
        t.Fatal("expected events to be published")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/jobs -run TestPublishKnowledgeSnapshotJob_Run -v`
Expected: FAIL with `undefined: NewPublishKnowledgeSnapshotJob`

- [ ] **Step 3: 实现新的知识同步 job 骨架**

```go
type PublishKnowledgeSnapshotJob struct {
    digestSvc digestBuilder
    events    eventAssembler
    exporter  knowledgeExporter
}

func (j *PublishKnowledgeSnapshotJob) Run(ctx context.Context, now time.Time) (KnowledgeRunResult, error) {
    digestResult, err := j.digestSvc.BuildDigest(ctx, now)
    if err != nil {
        return KnowledgeRunResult{}, err
    }
    events, err := j.events.BuildEvents(ctx, digestResult.Topics)
    if err != nil {
        return KnowledgeRunResult{}, err
    }
    return j.exporter.Publish(ctx, digestResult, events)
}
```

- [ ] **Step 4: 增加 Theme / ExportBundle / Revision 的最小同步契约**

```go
type KnowledgeRevision struct {
    ObjectType string
    ObjectID   int64
    Revision   string
    SourcePath string
}

type ExportBundleSeed struct {
    BundleKind string
    ThemeIDs   []int64
    TopicIDs   []int64
    EventIDs   []int64
}
```

- [ ] **Step 5: 将旧日报 job 调整为兼容适配层**

```go
// publish_daily_topics 保留为历史兼容入口，内部委托给新的知识同步任务。
func NewPublishDailyTopicsJob(...) *PublishDailyTopicsJob {
    return &PublishDailyTopicsJob{delegate: newKnowledgeSnapshotDelegate(...)}
}
```

- [ ] **Step 6: 运行测试确认通过**

Run: `go test ./internal/jobs ./internal/app -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add internal/jobs internal/app/worker_jobs.go
git commit -m "refactor: 将日报导出升级为知识同步基线"
```

### Task 4: 增加同步契约测试，冻结 Event / Topic / Digest 的最小输出形态

**Files:**
- Create: `internal/obsidian/contracts.go`
- Create: `internal/obsidian/contracts_test.go`
- Modify: `internal/obsidian/render_test.go`

- [ ] **Step 1: 写契约失败测试**

```go
func TestKnowledgeContract_MinimumFields(t *testing.T) {
    contract := BuildEventContract(EventContractInput{
        EventID:   101,
        EventKey:  "evt:ai-regulation:2026-07-01",
        Title:     "AI 监管规则发布",
        TopicIDs:  []int64{42},
        Date:      "2026-07-01",
    })
    if contract.Frontmatter["event_id"] != int64(101) {
        t.Fatal("expected event_id frontmatter")
    }
}
```

- [ ] **Step 2: 运行测试确认失败**

Run: `go test ./internal/obsidian -run TestKnowledgeContract_MinimumFields -v`
Expected: FAIL with `undefined: BuildEventContract`

- [ ] **Step 3: 实现最小契约构造器**

```go
func BuildEventContract(in EventContractInput) Contract {
    return Contract{
        Frontmatter: map[string]any{
            "type":     "hotkey-event",
            "event_id": in.EventID,
            "event_key": in.EventKey,
            "topic_ids": in.TopicIDs,
            "date":     in.Date,
        },
    }
}
```

- [ ] **Step 4: 冻结 revision contract，供回写冲突检测复用**

```go
func BuildRevision(objectType string, objectID int64, content string) string {
    sum := sha256.Sum256([]byte(content))
    return fmt.Sprintf("%s:%d:%x", objectType, objectID, sum[:8])
}
```

- [ ] **Step 5: 运行测试确认通过**

Run: `go test ./internal/obsidian -run TestKnowledgeContract_MinimumFields -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add internal/obsidian/contracts.go internal/obsidian/contracts_test.go internal/obsidian/render_test.go
git commit -m "test: 冻结知识对象最小同步契约"
```
