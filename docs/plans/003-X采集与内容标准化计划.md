---
layer: Plan
doc_no: 003
audience: Dev, QA
feature_area: X采集与内容标准化
purpose: 定义 X connector、轮询任务、标准化入库与去重链路的详细实施步骤
canonical_path: docs/plans/003-X采集与内容标准化计划.md
status: draft
version: v1.0
owner: Codex
inputs:
  - docs/design/001-x热点监控平台设计.md
  - docs/plans/002-基础平台与监控任务计划.md
outputs:
  - X connector
  - 监控轮询任务
  - 内容标准化与命中关联
triggers:
  - 开始实现平台采集链路
downstream:
  - docs/plans/004-热点评分主题聚合趋势计划.md
  - docs/plans/005-提醒通知闭环计划.md
---

# X Ingestion And Normalization Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 从 X 拉取关键词相关内容，完成标准化、去重、监控命中关系建立和 run 审计落库。

**Architecture:** 通过 `internal/platform/x` 负责外部 API 交互，`internal/content` 负责平台内统一内容模型和入库，`internal/jobs/poll_monitor.go` 负责任务编排与 run 记录。

**Tech Stack:** Go, HTTP client, JSON, PostgreSQL, Redis, fixtures, table-driven tests

---

### Task 1: 定义 X connector 接口与 fixture 驱动测试

**Files:**
- Create: `internal/platform/x/client.go`
- Create: `internal/platform/x/types.go`
- Create: `internal/platform/x/client_test.go`
- Create: `internal/platform/x/testdata/search_success.json`

- [ ] **Step 1: 写搜索结果解析测试**

```go
func TestSearchPostsParsesFixtures(t *testing.T) {
    client := NewClient("token", "https://api.x.test")
    posts, meta, err := client.parseSearchResponse(bytes.NewBufferString(searchFixture))
    if err != nil {
        t.Fatalf("unexpected error: %v", err)
    }
    if len(posts) != 2 {
        t.Fatalf("expected 2 posts, got %d", len(posts))
    }
    if meta.NextCursor != "NEXT123" {
        t.Fatalf("expected next cursor NEXT123, got %q", meta.NextCursor)
    }
}
```

- [ ] **Step 2: 运行失败测试**

Run: `go test ./internal/platform/x -run TestSearchPostsParsesFixtures -v`
Expected: FAIL

- [ ] **Step 3: 写最小 connector 类型**

```go
type SearchPost struct {
    ID          string
    AuthorID    string
    AuthorName  string
    AuthorHandle string
    Text        string
    Language    string
    PublishedAt time.Time
    LikeCount   int
    ReplyCount  int
    RepostCount int
    QuoteCount  int
    ViewCount   int
}
```

- [ ] **Step 4: 验证 connector 解析通过**

Run: `go test ./internal/platform/x -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/platform/x
git commit -m "test: add x connector fixture parsing coverage"
```

### Task 2: 实现轮询任务和 monitor run 记录

**Files:**
- Create: `internal/jobs/poll_monitor.go`
- Create: `internal/jobs/poll_monitor_test.go`
- Modify: `db/schema.sql`

- [ ] **Step 1: 为 monitor run 写状态流测试**

```go
func TestPollMonitorCreatesSuccessfulRun(t *testing.T) {
    repo := &fakeRunRepo{}
    connector := &fakeConnector{}
    job := NewPollMonitorJob(repo, connector)
    err := job.Run(context.Background(), 42)
    if err != nil {
        t.Fatalf("unexpected error: %v", err)
    }
    if repo.lastStatus != "success" {
        t.Fatalf("expected success status, got %q", repo.lastStatus)
    }
}
```

- [ ] **Step 2: 运行失败测试**

Run: `go test ./internal/jobs -run TestPollMonitorCreatesSuccessfulRun -v`
Expected: FAIL

- [ ] **Step 3: 在 schema 中补充 `monitor_runs`**

```sql
create table monitor_runs (
  id bigserial primary key,
  monitor_id bigint not null references keyword_monitors(id),
  platform text not null,
  run_type text not null,
  status text not null,
  started_at timestamptz not null,
  finished_at timestamptz,
  fetched_count integer not null default 0,
  stored_count integer not null default 0,
  error_message text,
  cursor_snapshot jsonb not null default '{}'::jsonb
);
```

- [ ] **Step 4: 验证任务状态测试**

Run: `go test ./internal/jobs -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/jobs db/schema.sql
git commit -m "impl: add monitor polling job and run audit"
```

### Task 3: 实现内容标准化和去重

**Files:**
- Create: `internal/content/model.go`
- Create: `internal/content/normalizer.go`
- Create: `internal/content/repository.go`
- Create: `internal/content/normalizer_test.go`
- Modify: `db/schema.sql`

- [ ] **Step 1: 写标准化哈希去重测试**

```go
func TestNormalizeCreatesStableHash(t *testing.T) {
    post := platformx.SearchPost{
        ID: "123",
        Text: " OpenAI Agent launch ",
        AuthorHandle: "openai",
    }
    normalized := Normalize(post)
    if normalized.NormalizedHash == "" {
        t.Fatal("expected normalized hash")
    }
}
```

- [ ] **Step 2: 运行失败测试**

Run: `go test ./internal/content -run TestNormalizeCreatesStableHash -v`
Expected: FAIL

- [ ] **Step 3: 在 schema 中补充内容和作者表**

```sql
create table platform_authors (
  id bigserial primary key,
  platform text not null,
  platform_author_id text not null,
  handle text not null,
  display_name text not null,
  followers_count integer not null default 0,
  verified boolean not null default false,
  raw_payload jsonb not null default '{}'::jsonb,
  updated_at timestamptz not null default now(),
  unique(platform, platform_author_id)
);
```

```sql
create table platform_posts (
  id bigserial primary key,
  platform text not null,
  platform_post_id text not null,
  author_platform_id text not null,
  author_name text not null,
  author_handle text not null,
  content_text text not null,
  content_lang text not null,
  post_url text not null,
  published_at timestamptz not null,
  like_count integer not null default 0,
  reply_count integer not null default 0,
  repost_count integer not null default 0,
  quote_count integer not null default 0,
  view_count integer not null default 0,
  raw_payload jsonb not null default '{}'::jsonb,
  normalized_hash text not null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  unique(platform, platform_post_id)
);
```

- [ ] **Step 4: 验证标准化与去重**

Run: `go test ./internal/content -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/content db/schema.sql
git commit -m "impl: add normalized content persistence"
```

### Task 4: 建立监控命中关系

**Files:**
- Modify: `db/schema.sql`
- Create: `internal/content/hits.go`
- Create: `internal/content/hits_test.go`

- [ ] **Step 1: 写监控命中 upsert 测试**

```go
func TestUpsertMonitorHitRefreshesLastSeen(t *testing.T) {
    repo := &fakeHitRepo{}
    err := repo.UpsertHit(context.Background(), MonitorHit{
        MonitorID: 1,
        PostID: 2,
        RelevanceScore: 0.9,
    })
    if err != nil {
        t.Fatalf("unexpected error: %v", err)
    }
}
```

- [ ] **Step 2: 运行失败测试**

Run: `go test ./internal/content -run TestUpsertMonitorHitRefreshesLastSeen -v`
Expected: FAIL

- [ ] **Step 3: 在 schema 中补充 `monitor_post_hits`**

```sql
create table monitor_post_hits (
  id bigserial primary key,
  monitor_id bigint not null references keyword_monitors(id),
  post_id bigint not null references platform_posts(id),
  matched_keywords jsonb not null default '[]'::jsonb,
  relevance_score numeric(5,4) not null default 0,
  heat_score numeric(10,4) not null default 0,
  freshness_score numeric(10,4) not null default 0,
  author_influence_score numeric(10,4) not null default 0,
  final_score numeric(10,4) not null default 0,
  first_seen_at timestamptz not null default now(),
  last_seen_at timestamptz not null default now(),
  unique(monitor_id, post_id)
);
```

- [ ] **Step 4: 运行内容模块全量测试**

Run: `go test ./internal/content/... ./internal/jobs/...`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add internal/content internal/jobs db/schema.sql
git commit -m "feat: add monitor hit persistence for x posts"
```

## 关联文档

1. `docs/design/001-x热点监控平台设计.md`
2. `docs/plans/002-基础平台与监控任务计划.md`

## 验收门禁

1. 单次轮询能够生成 `monitor_runs`。
2. X 返回内容能标准化入库。
3. 重复内容不会重复插入 `platform_posts`。
4. 监控任务与内容命中关系可以查询和更新。

## 风险与边界

1. 首版允许使用录制 fixture 代替真实 X 联调。
2. 若 X API 不返回全部互动指标，空缺字段统一补零，不阻塞主链路。

## 变更记录

1. `v1.0`：生成 X 采集与标准化计划。
