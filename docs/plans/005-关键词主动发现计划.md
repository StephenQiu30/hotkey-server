---
layer: Plan
scope: shared
doc_no: "005"
title: 关键词主动发现计划
status: in_progress
version: v1.11
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/005-关键词主动发现计划.md
design: docs/design/005-关键词主动发现设计.md
prd: docs/prd/005-关键词主动发现.md
source_requirement: FR-001-003
backlog: BACKLOG.md
priority: P0
milestone: M2
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。本文状态与验收进度不变；Kafka 重试、消费位点与 Redis 失效场景需在实现前补齐设计。

# 关键词主动发现执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

更新：2026-09-23。状态：`in_progress`；S00 来源无关设计与环境门禁完成，S01 语言、查询冻结、页级持久、受控多页/逐请求计量、总时长/失败边界及无页失败缺口内部子片已通过，真实来源和 Worker/HTTP 处理器未就绪，产品 AC 0/6。来源：[005 PRD](../prd/005-关键词主动发现.md) · [005 Design](../design/005-关键词主动发现设计.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

2026-09-23 来源决策：X 改走官方 API；用户尚无开发者 App/Token，先做 002 S01a 离线适配器。005 的 X Worker/HTTP/真实查询仍等待用户确认账期费用上限及 002 S01b/037 付费门禁，不将旧 twscrape 受控测试算作官方能力。国内来源不受 X 费用例外覆盖。

同日 002 S01a 已补每页最大 Post 数授权/实际数或未知数回调，并去掉额外 User 展开；005 只能在后续持久费用预算与请求账本原子装配后调用，模拟回调不放行 Worker 或真实查询，既有 0/6 AC 不变。

## 1. 交付目标与当前差距

完成 FR-001-003 的实现和证据闭环，保持 P0。现有主题规则、连接能力、Job/Outbox、作品发现和范围状态可复用，S00 已冻结内部契约；Worker 只有网页处理器，关键词真实来源、结果入口和完整验收尚未交付。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把工程存在当作本项已验收。尚未建立 Acceptance。

建议责任角色：**来源接入＋后端＋前端**，实际执行人于开工时登记；归属里程碑 **M2：关键词和账号监控闭环**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。它包含第 4 节规格、S01—S03 具体切片、S04 的原始 AC 证据，以及可执行的停止/回退说明。PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

[005 Design](../design/005-关键词主动发现设计.md) v1.6 接受 S00 与 S01 来源无关契约；G0—G2 仅放行受控内部切片。真实平台查询语法、排序、时间/语言能力、访问条件和默认值仍需按来源取得证据；这些门禁未过前，不开放可受理却无处理器的 HTTP 入口，也不标来源 ready。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| [003 监控主题管理](003-监控主题管理计划.md) | S03 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 CHK-003-G4-003；仅本地预览 |
| [004 平台与连接管理](004-平台与连接管理计划.md) | S02 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 CHK-004-G4-002；非真实平台就绪 |
| [007 作品资料与上下文](007-作品资料与上下文计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 CHK-007-G4-001 |
| [009 采集任务控制](009-采集任务控制计划.md) | S02 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 CHK-009-G4-002 |
| [037 费用与资源约束](037-费用与资源约束计划.md) | S02 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 CHK-037-G4-002 |
| [002 X 专项](002-X免费采集与热点监控计划.md) | S01 已交付其明确的先行输出；只等待该切片，不等待整份 Plan 关闭 | 已取得 CHK-002-G4-001；仅受控 X 适配器 |

以上是**切片依赖**。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

`OPEN-005-001` 对受控内部切片不要求全局默认值：显式提供 UTC 窗口与每通道上限，不使用建议的 7 天/50 条冒充已校准设置。首个真实来源和定时任务前必须按合法样本关闭该来源所需的默认值、访问条件、资源及验收口径。已确定技术栈沿用 AGENTS；新增平台、付费依赖或框架等实质选择按项目规范处理。

## 3. 预计变更文件与归属

以下区分**现有复用路径和拟建路径**。只在切片实际需要时创建文件；先登记职责和结构检查，再加入代码。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| 现有 backend/app/monitors/services.py、jobs/、content/、sources/；backend/app/worker/app.py | 规则版本、任务/预算/窗口、作品发现、来源映射与处理器分别归既有领域；首个真实处理器前不预建空模块 |
| backend/app/content/schemas.py、discovery.py | S01 非 HTTP 查询快照校验与双通道 Job 命令规划；无受理/外采副作用，处理器和准入未就绪前不连接产品入口 |
| backend/app/content/discovery.py、discovery_execution.py、jobs/cursor.py、jobs/execution.py、jobs/services.py、connections/services.py | 页级作品/发现、010 范围与 checkpoint 原子提交；受控执行器按现行租约、连接版本、逐次请求及总时长预算编排多页，分类失败并沿既有 Outbox 有界延期；不注册真实 Worker/HTTP |
| backend/app/api/routers/keyword_discovery.py | 处理器与来源准入均可用后才创建；Router 经依赖注入，不直接操作 ORM |
| backend/tests/unit/test_keyword_discovery.py | 规则与状态的针对性失败/通过验证 |
| backend/tests/integration/test_keyword_discovery.py | 适用的真实 PostgreSQL/Redis/Kafka、权限、幂等或边界验证；纯前端/人工项写明适用范围 |
| frontend/src/app/discovery/；frontend/tests/e2e/keyword_discovery.spec.ts | 业务交互与真实浏览器结果 |
| backend/app/api/router.py、dependencies.py；运行时 /openapi.json（自动生成）；frontend/src/api/ | 若 HTTP 契约变化，注册路由及唯一生成链；无变化则记录不适用原因 |
| backend/database/schema.sql；根 Compose 和配置样例 | 只在确需数据/配置变化时修改；SQL 与 ORM 同批更新，禁止覆盖用户配置或删除持久卷 |
| docs/design/005-关键词主动发现设计.md；对应 Acceptance（真实验证后创建） | 开工前设计与交付后证据，使用同编号，不预填通过 |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-005-API-001 | POST /api/discovery-runs，GET /api/discovery-runs/{id} 与结果分页；输入含主题/临时查询版本、平台、Latest/Top 意图、范围及预算，返回持久任务。 |
| SPEC-005-DATA-001 | 保存实际查询、通道、排序意图、采样范围、分页停止原因和作品发现关系；同帖跨通道不重复实体。 |
| SPEC-005-JOB-001 | Latest/Top 独立进度与额度；来源拒绝原生排序时说明受限；上游乱序不凭单条旧帖结束，内部请求计预算。 |
| SPEC-005-UI-001 | 在 frontend/src/app/discovery/ 实现本项已有 PRD 流程；桌面与窄屏同时覆盖正常、空、加载、部分、错误和无权限状态，调用生成客户端，不复制服务端业务规则。 |
| SPEC-005-SEC-001 | 先检查当前身份及资源权限；配置版本不能绕过撤权、删除、停用和更严格预算。秘密不可回读，正文与外部输入只作数据。关键变更留操作者、对象、时间与版本。 |
| SPEC-005-OBS-001 | 输出来源与通道分项计数、实际排序/过滤位置和缺口；来源版本失效时按 038 停用回退，不删除已保存发现依据。 |
| SPEC-005-OPS-001 | 在隔离依赖验证后渐进启用本能力；保持历史资料可读。配置缺失或外部条件不满足时明确拒绝/延期。变更前评估数据兼容与回退，停止新增操作优先于破坏性迁移。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-005-S00-T01 | 阅读 PRD 与关联设计，关闭本期必要未决项，补同编号 Design；明确未纳入本期的分支 | 需求/设计评审记录、边界与风险 |
| TASK-005-S00-T02 | 核查先行切片，登记文件职责与主责，冻结 SPEC、样本、接口及测试命令 | 可执行切片清单、隔离资源和验证清单 |

- [x] `CHK-005-G0-001` → AC-005-001—006；`EV-005-001`（2026-09-23）：核对 PRD v1.3、003/004/007/009/010/002 Design，接受 005 Design v1.0 的 S00 与 S01 来源无关边界；OPEN-005-001 保留，内部样本显式窗口/预算，真实默认值和产品 AC 未关闭。
- [x] `CHK-005-G1-001` → AC-005-001—006；`EV-005-002`（2026-09-23）：046 S03 Acceptance 及上表六个先行切片均有证据；本机 PostgreSQL pg_isready、Redis PONG、Kafka TCP、MinIO health、API /api/ready、Web /health 均通过。既有 backend/.env 指向本机依赖；根 Compose 插值仍缺 HOTKEY_POSTGRES_PASSWORD，不能将现有服务就绪外推为重新建栈可用，本片也未重启依赖。X/国内获授权真实搜索会话与有界来源样本未具备，故真实接入门禁未过。
- [x] `CHK-005-G2-001` → AC-005-001—006；`EV-005-003`（2026-09-23）：Design §2—5 冻结来源无关的 Job/发现/窗口职责、双通道独立进度、实际查询记录、排序/过滤真实性及受控样本 v1；本 Plan §3/7 登记目标路径与测试入口。S01 先在既有领域 red-first 验证，不增表/脚本；来源专属查询语法、HTTP DTO 与运行时/浏览器验证随首个处理器冻结，不凭此项开放产品功能。

### S01 双通道与查询契约

前置：S00 完成。本切片覆盖 FR-005-001、FR-005-002；涉及的完整规则以对应 PRD 为准。

2026-09-23 先补共享作品语言字段：受控 X 响应原本已有 lang，但 SourcePost 丢弃；这会阻碍后续诚实标注语言过滤位置。只透传来源标签，不实现过滤、持久查询或 Worker；因此以下 G3/G4 仍未关闭，也不勾选 AC。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-005-S01-T01 | Red：以 AC-005-002、AC-005-003 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-005-S01-T02 | Green：先用受控适配器验证真实提交条件，再接 X 候选及已选国内来源，界面显示实际检索范围。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-005-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-005-G3-001` → AC-005-002、AC-005-003；任务 TASK-005-S01-T01：失败验证确实证明当前行为缺失；预期证据 `EV-005-004`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-005-G4-001` → AC-005-002、AC-005-003；任务 TASK-005-S01-T02、TASK-005-S01-T03：切片闭环与必要回归通过；预期证据 `EV-005-007`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

S01 语言透传子片证据（2026-09-23）：在既有 X 适配器测试中先断言 language=zh，原实现因 SourcePost 缺字段而失败；新增可空 SourcePost.language，并直接使用固定 SDK 解析的 tweet.lang，不推断缺失值。目标 27 项通过，Ruff/format/mypy 通过；首次全量运行因原 hotkey_dev_test 缺 010 已新增的 coverage_windows 表而非业务失败，改用新建一次性空库完整 schema 联验，后端 347 passed/5 skipped。QA 库验证后删除，原开发/测试库不迁移、不清空；无 API/UI/DDL/消息/代理变化，OpenAPI 生成和浏览器本子片不适用。真实语言筛选、两通道处理器和产品 AC 仍未完成。

S01 查询冻结子片证据（2026-09-23）：先新增单元测试断言双通道独立预算、仅显式上游别名、窗口/重复查询拒绝；因 `content.discovery` 不存在而 red，随后实现 `KeywordDiscoveryRunInput` 与纯规划函数。受控单元测试 15 项通过；一次性 PostgreSQL 中直接调用既有 JobService 验证两个 Job/Outbox、重试幂等、异义冲突和查询不进入 Outbox，集成测试 1 项通过。后端全量 363 passed/5 skipped，Ruff/format/mypy 通过；QA 库已删除并核实不存在，原开发库未迁移。该集成测试只是内部存储边界验证，生产 Worker 没有注册 `keyword.search`，不暴露 API、不向来源提交、不将其计为 `CHK-005-G3-001`/`G4-001` 或任何产品 AC。下一步以受控来源多页处理器验证请求计量、事务/检查点和实际提交记录，再检查合法来源准入；真实来源尚未就绪。

S01 页级持久子片证据（2026-09-23）：先增受控 PostgreSQL 测试，因页提交服务缺失而 red；随后补 Job scope 的主题+查询哈希、规则版本及连接版本，并复用 007 作品发现、010 有界游标/窗口、现有来源政策与连接屏障。同一测试验证一页旧帖后仍保存新帖、同帖跨 Latest/Top 有两条发现、页内或跨页重复不虚增任务已保存数、错误字段整页回滚、旧检查点拒绝、连接停用阻断、页预算终止与未验证终点均保持 `partial`；checkpoint 不含原始游标。专用 18 项通过，后端全量 365 passed/5 skipped，Ruff/format/mypy 通过；一次性 QA 库已删除并核实不存在，原开发库未迁移。该页服务既不访问平台，也不注册 Worker/HTTP；请求计量、实际提交、当前主题/来源准入与产品结果页仍缺，故 S01 G3/G4、全部 AC 保持未通过。

S01 受控多页/计量子片证据（2026-09-23）：先在既有 PostgreSQL 集成样本加入一页两次请求、预算拒绝、页用量与内容同事务的断言，因计量服务缺失而 red；随后实现来源无关执行器及逐请求回调。测试验证请求前的当前租约/连接/来源政策与分层预算、一页多次尝试的账本记录（真实 SDK 初始化未实采）、数量不符时整页回滚、全任务重入请求上限、游标摘要重启后两页有限重扫、Latest/Top 分开执行、页预算与无时间终点的 `partial`、停用连接在请求前拒绝、传输异常计失败用量且不写入范围。受控目标 2 项、一次性空库后端全量 365 passed/5 skipped、Ruff/format/mypy 通过；QA 库已删除并核实不存在，原开发库未迁移。没有真实 X/国内授权样本，也没有 Worker 注册、HTTP/结果页或有界时间/语言来源证明；G3/G4 与产品 AC 仍为未通过。下一步先补真实来源授权/能力与总时长门禁、运行错误映射和当前主题受理，再以合法来源接 Worker/API/UI；不以受控结果冒充产品验收。

S01 总时长/失败边界子片证据（2026-09-23）：先以受控失败测试锁定 `max_seconds` 缺失、到期后仍可能构造适配器、429 游标不可恢复及取消缺口未落库；随后将每 Job 1—90 秒总时长冻结到 scope，以首次 `started_at` 跨重入计算截止。逐请求和页间检查截止，未发请求到期记录零页 `budget_exhausted`；来源无样本认证拒绝保留明确失败类别，429 只有在一次重扫、页数/请求数、现有 Job 重试次数与截止均允许时才经 Outbox 延期，超界不派发无效重试；已发请求遇取消记失败用量、禁止未提交内容落库并留下零页 `cancelled` 缺口。测试还覆盖一页已保存后到期不伪造后续页。受控目标 30 passed，一次性空库后端全量 368 passed/5 skipped，Ruff/format/mypy 通过；专用 QA 库已删除并核实不存在，原开发库未迁移。此为内部处理器契约，不证明真实来源授权、排序/语言/时间过滤，也不开放 Worker/HTTP/结果页；S01 G3/G4 和 AC 仍未通过。下一步核准首个实际来源与当前主题受理边界，再完成正式入口。

S01 无页失败缺口子片证据（2026-09-23）：在既有受控 PostgreSQL 测试中先要求一次已发请求的传输异常留下 `partial/upstream_error`、`page_count=0`；旧实现虽结算失败用量却没有范围行，因此 red。执行器随后在确认未取消且租约有效时记录本地零页停止，不提交虚构的来源页、内容或 checkpoint；同一 Job 后续受控重试仍可从范围起点执行。目标 30 passed，一次性空库后端全量 368 passed/5 skipped，Ruff/format/mypy 通过；专用 QA 库已删除并核实不存在，原开发库未迁移。内部测试只证明缺口/重试契约，不证明平台真实可访问或完整 S01；G3/G4、AC 0/6 不变。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。

### S02 扫描与相关性准入

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 FR-005-003、FR-005-004；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-005-S02-T01 | Red：以 AC-005-004、AC-005-005 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-005-S02-T02 | Green：实现有界分页、停止原因和排除优先，保留相关低互动新帖并标明本地过滤。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-005-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-005-G3-002` → AC-005-004、AC-005-005；任务 TASK-005-S02-T01：失败验证确实证明当前行为缺失；预期证据 `EV-005-005`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-005-G4-002` → AC-005-004、AC-005-005；任务 TASK-005-S02-T02、TASK-005-S02-T03：切片闭环与必要回归通过；预期证据 `EV-005-008`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。

### S03 去重呈现与来源缺口

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 FR-005-005、FR-005-006；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-005-S03-T01 | Red：以 AC-005-002、AC-005-006 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-005-S03-T02 | Green：关联多主题和多通道，分别显示成功空结果、部分、限流和错误；接入正式采集结果页。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-005-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-005-G3-003` → AC-005-002、AC-005-006；任务 TASK-005-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-005-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-005-G4-003` → AC-005-002、AC-005-006；任务 TASK-005-S03-T02、TASK-005-S03-T03：切片闭环与必要回归通过；预期证据 `EV-005-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-005-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-005-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-005-G5-001` → AC-005-001；**总纲场景继承**：Given 关键词存在已知可访问的相关新帖和热门帖，配置了别名；When 分别运行两种发现任务并查看实际查询记录；Then 保存返回作品、搜索条件和时间；明确别名仅本地匹配还是参与上游查询，扩展请求计入预算；未支持的原生排序明确标记。预期证据 `EV-005-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-005-G5-002` → AC-005-002；**热门与最新**：Given 两个通道命中同帖且各有独有作品；When 合并结果；Then 作品不重复，保留全部通道和主题依据；预期证据 `EV-005-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-005-G5-003` → AC-005-003；**上游扩词**：Given 规则含一个本地别名和一个检索别名；When 查看查询记录；Then 仅明确启用的检索别名参与上游且计预算；预期证据 `EV-005-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-005-G5-004` → AC-005-004；**非时间顺序**：Given 一页含旧帖后又含符合窗口的新帖；When 继续扫描；Then 不因旧帖提前漏掉新帖，预算终止有记录；预期证据 `EV-005-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-005-G5-005` → AC-005-005；**低互动新帖**：Given 新帖相关但互动为零；When 发现并排序；Then 可以进入最新候选，高互动无关帖仍被排除；预期证据 `EV-005-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-005-G5-006` → AC-005-006；**部分失败**：Given 一来源成功、一来源超时；When 查看结果；Then 保留成功样本并明确失败，不把总计当全网总数；预期证据 `EV-005-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-005-G6-001` → AC-005-001、AC-005-002、AC-005-003、AC-005-004、AC-005-005、AC-005-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-005-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-005-G6-002` → AC-005-001、AC-005-002、AC-005-003、AC-005-004、AC-005-005、AC-005-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-005-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| FR-005-001 | 双通道 | TASK-005-S01-T02 | AC-005-002 | CHK-005-G4-001 |
| FR-005-002 | 查询可见 | TASK-005-S01-T02 | AC-005-003 | CHK-005-G4-001 |
| FR-005-003 | 顺序与截断 | TASK-005-S02-T02 | AC-005-004 | CHK-005-G4-002 |
| FR-005-004 | 相关性过滤 | TASK-005-S02-T02 | AC-005-005 | CHK-005-G4-002 |
| FR-005-005 | 多主题归属 | TASK-005-S03-T02 | AC-005-002 | CHK-005-G4-003 |
| FR-005-006 | 空与错 | TASK-005-S03-T02 | AC-005-006 | CHK-005-G4-003 |

父级综合场景 AC-005-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| backend/ | `uv run ruff check .`；`uv run ruff format --check .`；`uv run mypy` | 既定静态与边界要求，依赖须先由 042 锁定安装 |
| backend/ | `uv run pytest tests/unit/test_keyword_discovery.py` | 本切片规则验证 |
| backend/ | `uv run pytest tests/integration/test_keyword_discovery.py` | 隔离的真实依赖与边界验证 |
| backend/ | `uv run pytest tests/architecture` | 新模块必须受结构/依赖门禁覆盖 |
| frontend/ | `pnpm openapi:generate`、`pnpm openapi:check`、`pnpm typecheck`、`pnpm build --webpack` | HTTP 变化时从同提交运行时 /openapi.json 生成和检查客户端；按现有 package.json 的实际构建入口执行 |
| frontend/ | `pnpm test`、`pnpm lint`、`pnpm format:check`；实际页面用浏览器验证 | UI 切片适用；尚无本项 E2E 文件时不称其已运行 |
| 对应业务/验证环境 | 来源/通道计数、排序/过滤位置、停止原因和缺口；来源版本失效时按 038 停用回退 | 真实来源、B0/Q0、故障、恢复或用户证据按本项范围执行 |

通用静态检查和基础测试入口已存在；本项专用测试、契约漂移及 E2E 入口须按实际切片补齐。本文件列出验证要求，不宣称命令已运行。若某项纯人工或无对应界面，G2 写出不适用理由和替代证据；不能删掉必要质量要求来让结果通过。涉及持久化/消息用真实 PostgreSQL/Redis/Kafka，涉及文件证据用获授权隔离 MinIO；不复用生产卷或在真实平台压测。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-23 | v1.4 | 完成 S00 来源无关 Design、先行切片和本机依赖核对，冻结受控样本/目录；真实来源、S01—S04 和产品 AC 仍待执行。 |
| 2026-09-23 | v1.5 | S01 语言标签透传必要子片红绿验证；完整 S01 G3/G4、真实处理器与全部 AC 仍待执行。 |
| 2026-09-23 | v1.6 | S01 内部查询快照与双通道任务命令子片红绿/隔离库验证；不对外受理，完整 S01 和全部 AC 仍待执行。 |
| 2026-09-23 | v1.7 | S01 受控页级原子持久子片通过；真实来源多页处理器、运行契约和全部 AC 仍待执行。 |
| 2026-09-23 | v1.8 | S01 总时长跨重入、取消缺口、分类失败与有界持久延期内部子片通过；正式处理器及全部 AC 待执行。 |
| 2026-09-23 | v1.9 | 补 S01 已发请求传输异常的零页范围缺口与同 Job 重试验证；来源准入和产品 AC 不变。 |
| 2026-09-23 | v1.11 | 登记 002 S01a 的离线费用回调补片；005 正式入口和真实来源仍受 037 及开发者条件阻断。 |

文档复核（2026-09-21）：同步现有工程基线、实际检查入口和 App Router 路径；需求 ID、优先级及必要 AC 不变，未补记产品通过证据。
