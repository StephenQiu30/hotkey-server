---
layer: Plan
scope: shared
doc_no: "040"
title: 可用性与可访问性计划
status: in_progress
version: v1.4
architecture_prerequisite: "046 S03"
owner: HotKey Team
canonical_path: docs/plans/040-可用性与可访问性计划.md
design: docs/design/040-可用性与可访问性设计.md
prd: docs/prd/040-可用性与可访问性.md
source_requirement: NFR-001-014
backlog: BACKLOG.md
priority: P0
milestone: M5
---

选型修订（2026-09-18）：按用户决定统一为 PostgreSQL + Redis + Kafka；Web 位于 server/frontend，独立 App 使用 Flutter。技术基线见 [PROJECT](../../PROJECT.md)。本文状态与验收进度不变；Kafka 重试、消费位点与 Redis 失效场景需在实现前补齐设计。

# 可用性与可访问性执行计划

共同前置（2026-09-21）：遵循 [046 统一异常与响应 Design](../design/046-全局异常与响应契约设计.md)；本计划进入任何实现切片前，必须取得 [046 前置计划](046-全局异常与响应契约前置计划.md) S03 的真实通过证据。范围、研究与设计准备可先开展；既有 in_progress 不豁免，此条件与下文领域/切片依赖共同生效，不替代本计划 Design 或 AC。

日期：2026-09-25。状态：`in_progress`（S00 设计提案待评审，0/6 产品 AC）。来源：[040 PRD](../prd/040-可用性与可访问性.md)；统筹入口：[BACKLOG](../../BACKLOG.md) · [平台总计划](001-热点事件监控平台总计划.md) · [计划索引](README.md)。

## 1. 交付目标与当前差距

完成 NFR-001-014 的实现和证据闭环，保持 P0。当前已有 API/Worker/CLI 与 Web 工程底座、依赖锁和基础 CI；本项业务能力及完整验收仍未交付。底座事实与缺口见 [HANDOVER](../../HANDOVER.md)，不能把工程存在当作本项已验收。本文尚无已勾选检查项，也未建立 Acceptance。

建议责任角色：**产品＋前端＋体验验证**，实际执行人于开工时登记；从 M0 开始准备/基础接入，归属里程碑 **M5：首版质量验收与交付**。全部适用 AC 和质量门禁通过后才能计入首版完成。

本项交付不只是测试报告或页面。它包含第 4 节规格、S01—S03 具体切片、S04 的原始 AC 证据，以及可执行的停止/回退说明。PRD 的非目标、候选阈值与免费约束继续适用。

## 2. 开始条件、依赖与未决项

同编号 [Design](../design/040-可用性与可访问性设计.md) 已建立，状态为 `proposed`，不是实现批准。`OPEN-040-001` 的目标用户、主要浏览器和正式无障碍标准仍待用户确认；在关闭前不进入 S01。未通过 G0—G2 的产品代码不得开工，本轮不授权外部实采。

| 先行 Plan | 进入 S01 前所需输出 | 当前状态 |
|---|---|---|
| [042 容量与部署可重复性](042-容量与部署可重复性计划.md) | S01 已交付其明确的运行/契约前置输出；不等待整份 Plan 关闭 | 已满足：CHK-042-G4-001，EV-042-007；不代表 040 产品验收通过 |

以上是**切片依赖**。其余 PRD 关联是共同约束，不要求全部关联 Plan 先完成。来源尚未就绪时，可在隔离样本上推进内部逻辑；真实平台 AC 保留未验证，不把其依赖改成已满足。

开工时处理 PRD 中 `OPEN-040-001`，冻结会影响本切片的默认值、访问条件、资源和验收口径。已确定技术栈沿用 AGENTS；新增平台、付费依赖或框架等实质选择按项目规范处理，普通实现细节由执行者负责。

## 3. 预计变更文件与归属

以下均为**拟建/拟改路径**，不是现存实现。只在切片实际开始时创建必要文件；先登记新领域职责和结构检查，再加入代码。跨计划修改共用模块时，由总计划登记的主责方合并，其他计划补约束与验证，不能建平行实现。

| 路径或文件 | 职责 |
|---|---|
| `frontend/src/app/events/`、`monitors/`、`content/` 中实际受影响页面 | 按现有业务路由修正状态理解、键盘和窄屏；不实现尚无数据支撑的事件结果 |
| `frontend/src/components/system/` 与现有 `components/ui/` | 仅在多个页面确有重复需求时补共享语义；不新建组件库或全局覆盖样式 |
| 受影响页面旁的 `*.test.ts` / `*.test.tsx` | 复用现有 Vitest 和共置测试；新增用例覆盖已复现的用户行为 |
| `backend/`、OpenAPI、`frontend/src/api/` | 默认不变；仅在现有服务事实无法表达 PRD 状态时，经 G2 批准后同步领域 owner、契约和生成客户端 |
| `docs/design/040-可用性与可访问性设计.md`、本 Plan；Acceptance（真实验收后） | 设计及阶段任务持续维护；只在真实验证发生后建立 Acceptance |

## 4. 实施规格 SPEC

这些是可评审的执行规格；路径/接口名称为候选，G2 冻结后才是实施契约。若调整需同步测试、客户端与本文件，不私自改变 PRD 的用户语义。

| SPEC ID | 规格 |
|---|---|
| SPEC-040-API-001 | 复用 004/009 领域事实与 046 稳定错误契约；仅当数据无法表达 PRD 状态时才提议接口变更，并经 G2 同步 owner、生成链与测试。 |
| SPEC-040-DATA-001 | 使用脱敏 fixture 与最少用户任务记录；不增加产品数据库研究表或复制业务状态。 |
| SPEC-040-JOB-001 | 如页面展示进行中的任务，使用 009 已有状态并为真实异步变化提供可感知反馈；不新建后台通知/轮询系统。 |
| SPEC-040-UI-001 | 改进本质量要求影响的实际业务页面、状态或运维记录，不默认新建一个独立质量功能页面；无界面变化时以书面理由说明该 UI 分支不适用，不能跳过实际受影响界面。 |
| SPEC-040-SEC-001 | 保留现有身份、资源权限与秘密边界；研究记录不得含凭据、原始页面正文或不必要个人信息。 |
| SPEC-040-OBS-001 | 桌面、375/320 CSS像素窄屏、键盘、焦点和语义检查；5名目标或代理用户至少4名无现场指导完成核心任务的候选门槛。 |
| SPEC-040-OPS-001 | UI-only 变更不做数据库迁移；按 042 现有运行方式发布并保留可回退版本。只有 G2 证明存在 API/数据需要时才扩展相关 Plan。 |

数据/接口/前端执行顺序：先冻结字段、状态与权限 → 同步更新 schema.sql 与 ORM → 领域服务和 Worker → FastAPI/Pydantic 运行契约 → 导出并核对 OpenAPI → 生成客户端 → 页面接入 → 配置/镜像与完整验收。无 API 或数据变更的质量项在 G2 记录不适用，不强建空接口或无效 DDL。

## 5. 阶段任务与 checklist

G0=范围与设计；G1=前置输出与环境；G2=规格与目录；G3=失败验证；G4=切片实现与必要回归；G5=原始 AC 验证；G6=回退、交接与进度回写。S 表示本计划执行切片，G 表示检查类型，两者不与总体里程碑 M 混用。

### S00 开工准备

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-040-S00-T01 | 阅读 PRD 与关联设计，建立 proposed Design，列明未决项及不纳入本期的分支；用户决策到位后再关闭必要 OPEN | 设计评审记录、边界、风险及待用户确认项 |
| TASK-040-S00-T02 | 核查先行切片，登记实际页面/服务 owner 与现存验证入口；只冻结已有依据支持的范围 | 页面/事实 owner 映射和候选验证清单；G2 未通过前不冻结新接口或脚本 |

- [ ] `CHK-040-G0-001` → AC-040-001、AC-040-002、AC-040-003、AC-040-004、AC-040-005、AC-040-006；确认本期范围与 Design 已具备执行依据；预期证据 `EV-040-001`（PRD/Design 版本、必要决策及未决项结论）。
- [ ] `CHK-040-G1-001` → AC-040-001、AC-040-002、AC-040-003、AC-040-004、AC-040-005、AC-040-006；先行切片和隔离依赖可用，来源条件/真实样本不足被显式记录；预期证据 `EV-040-002`（依赖输出与环境检查）。
- [ ] `CHK-040-G2-001` → AC-040-001、AC-040-002、AC-040-003、AC-040-004、AC-040-005、AC-040-006；SPEC、目录、数据/接口变更及适用命令已冻结；预期证据 `EV-040-003`（文件清单、契约、样本与执行入口）。

### S01 任务文案与状态原型

前置：S00 完成。本切片覆盖 NFR-040-001、NFR-040-002；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-040-S01-T01 | Red：以 AC-040-002、AC-040-003 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-040-S01-T02 | Green：先冻结核心任务脚本和空/错/部分状态，随页面实现核查文案与下一步。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-040-S01-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-040-G3-001` → AC-040-002、AC-040-003；任务 TASK-040-S01-T01：失败验证确实证明当前行为缺失；预期证据 `EV-040-004`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-040-G4-001` → AC-040-002、AC-040-003；任务 TASK-040-S01-T02、TASK-040-S01-T03：切片闭环与必要回归通过；预期证据 `EV-040-007`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S02 键盘和语义基础

前置：S00 完成且 S01 的交付输出可用。本切片覆盖 NFR-040-003、NFR-040-004；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-040-S02-T01 | Red：以 AC-040-004、AC-040-005 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-040-S02-T02 | Green：在实际控件及弹窗中验证焦点、标签、错误关联和图表文本摘要。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-040-S02-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-040-G3-002` → AC-040-004、AC-040-005；任务 TASK-040-S02-T01：失败验证确实证明当前行为缺失；预期证据 `EV-040-005`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-040-G4-002` → AC-040-004、AC-040-005；任务 TASK-040-S02-T02、TASK-040-S02-T03：切片闭环与必要回归通过；预期证据 `EV-040-008`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S03 窄屏与用户验证

前置：S00 完成且 S02 的交付输出可用。本切片覆盖 NFR-040-005、NFR-040-006；涉及的完整规则以对应 PRD 为准。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-040-S03-T01 | Red：以 AC-040-006、AC-040-002、AC-040-003 的具体前提建立失败验证；断言用户结果、持久状态或质量指标，不镜像私有实现 | 有测试名、样本版本和失败原因的记录；不能因环境未启动而冒充业务失败 |
| TASK-040-S03-T02 | Green：接齐全链路后跑真实浏览器与统一受试者任务；无人测试则留未验证。 | 该范围从输入到可查看结果可运行；具体规则全部满足，失败/权限分支可见 |
| TASK-040-S03-T03 | Refactor：移除切片中的重复路径，检查领域边界、事务、生成客户端与脱敏，并复跑受影响验证 | 变更清单、绿测和接口差异；不额外增加未登记框架或空模块 |

- [ ] `CHK-040-G3-003` → AC-040-006、AC-040-002、AC-040-003；任务 TASK-040-S03-T01：失败验证确实证明当前行为缺失；预期证据 `EV-040-006`（失败测试/验证摘要与冻结输入）。
- [ ] `CHK-040-G4-003` → AC-040-006、AC-040-002、AC-040-003；任务 TASK-040-S03-T02、TASK-040-S03-T03：切片闭环与必要回归通过；预期证据 `EV-040-009`（实现版本、实际输出、绿测、适用浏览器或真实依赖证据）。

切片完成不等于整项验收通过；若依赖提供方只交付接口契约或样本，应在接入真实实现后重新验证本切片。本质量要求须随业务接入持续执行，M5 再核对当期全部对象和入口。

### S04 完整验收与交付

前置：S01—S03 通过，所有当前阶段的业务接入已完成。质量项的初始受控样本通过不能代替最终真实业务范围验证。

| 任务 | 执行内容 | 完成输出 |
|---|---|---|
| TASK-040-S04-T01 | 按下列每个原始 AC 执行，补跨切片、权限和异常验证，记录实际环境及未通过项 | 逐 AC 证据与结果，不能只引用一个全绿摘要 |
| TASK-040-S04-T02 | 演练停止/回退，整理运行交接；真实验证后建立同编号 Acceptance，回写 BACKLOG | 证据目录、已知限制、回退结果与状态记录 |

- [ ] `CHK-040-G5-001` → AC-040-001；**总纲场景继承**：Given 用户首次使用且遇到空结果或来源失效；When 完成核心流程；Then 能找到规则、状态和下一步；不把错误误认成无相关事件。预期证据 `EV-040-010`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-040-G5-002` → AC-040-002；**无指导任务**：Given 至少 5 名目标或明确标注代理用户获得统一任务；When 独立创建监控、查看事件并定位原文；Then 至少 4 人完成全部步骤；帮助次数、失败原因和样本类型可核查；预期证据 `EV-040-011`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-040-G5-003` → AC-040-003；**空与错的理解**：Given 提供无匹配、未运行、来源失效及部分结果场景；When 用户判断状态并选择动作；Then 文字解释不同，用户误解有记录，关键误导需修复后复验；预期证据 `EV-040-012`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-040-G5-004` → AC-040-004；**键盘完整路径**：Given 用户不使用鼠标；When 完成输入、弹窗确认、结果查看和返回；Then 焦点顺序合理且可见，无陷阱，关闭弹窗后焦点回到合理位置；预期证据 `EV-040-013`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-040-G5-005` → AC-040-005；**语义与颜色**：Given 隐藏颜色差异并检查辅助技术信息；When 识别错误、状态及图表结论；Then 标签和错误关联可读，状态文字或摘要足以理解，不依赖颜色；预期证据 `EV-040-014`（输入、环境、版本、实际结果与结论）。
- [ ] `CHK-040-G5-006` → AC-040-006；**窄屏和长内容**：Given 375 与 320 CSS 像素场景包含长标题、URL 和多语言评论；When 操作核心页面并缩放查看；Then 关键输入和按钮可用，长内容不遮挡，宽表仅局部可控滚动；预期证据 `EV-040-015`（输入、环境、版本、实际结果与结论）。

- [ ] `CHK-040-G6-001` → AC-040-001、AC-040-002、AC-040-003、AC-040-004、AC-040-005、AC-040-006；停止新增操作及回退/恢复路径验证，历史资料、权限与删除约束仍正确；预期证据 `EV-040-016`（前后版本、数据核对与回退结果）。
- [ ] `CHK-040-G6-002` → AC-040-001、AC-040-002、AC-040-003、AC-040-004、AC-040-005、AC-040-006；Acceptance 逐项填写，运行说明及 BACKLOG 同步；预期证据 `EV-040-017`（真实证据索引、已知限制和交付记录）。有任何必要 AC 未通过，不可勾选或把 Plan 改为 completed。

## 6. 规则到执行的追踪矩阵

| 原规则 | 内容 | 实现任务 | 原验收 | 实现检查 |
|---|---|---|---|---|
| NFR-040-001 | 任务与信息层级 | TASK-040-S01-T02 | AC-040-002 | CHK-040-G4-001 |
| NFR-040-002 | 状态与错误反馈 | TASK-040-S01-T02 | AC-040-003 | CHK-040-G4-001 |
| NFR-040-003 | 键盘与焦点 | TASK-040-S02-T02 | AC-040-004 | CHK-040-G4-002 |
| NFR-040-004 | 语义与感知 | TASK-040-S02-T02 | AC-040-005 | CHK-040-G4-002 |
| NFR-040-005 | 窄屏与长内容 | TASK-040-S03-T02 | AC-040-006 | CHK-040-G4-003 |
| NFR-040-006 | 真实可用性验证 | TASK-040-S03-T02 | AC-040-002、AC-040-003 | CHK-040-G4-003 |

父级综合场景 AC-040-001 在 S04 单独执行；不能因为单元场景通过而跳过。每个证据包含日期、代码版本、环境、输入版本、执行入口、实际结果和判定；`EV` 目前是预期证据 ID，不表示文件已存在。

## 7. 验证入口与适用环境

| 工作目录 | 拟定命令或入口 | 用途 |
|---|---|---|
| frontend/ | `pnpm test`、`pnpm lint`、`pnpm typecheck`、`pnpm format:check`、`pnpm build` | 使用 `frontend/package.json` 已有入口；按改动范围执行 |
| frontend/（仅接口变化） | `HOTKEY_OPENAPI_URL=<运行时 API> pnpm openapi:check` | 复用唯一运行时 OpenAPI 生成链；无接口变化不运行或改动客户端 |
| backend/（仅后端变化） | `uv run --frozen ruff check app tests`、`uv run --frozen mypy --strict app`、适用 `uv run --frozen pytest` | 仅对真实后端变更执行，不为前端质量项创建空测试/模块 |
| 已选定的真实浏览器与研究环境 | 桌面、375/320 CSS 像素、键盘、焦点、语义与 200% 缩放；按 PRD 统一任务记录至少 5 名样本结果 | G2 确认环境后执行；当前无 `test:e2e` 入口，不能写成已存在，也不新建临时脚本或 `.sh` |

本轮仅完成 S00 设计资料，不运行产品验收，也不声明以上实现门禁已通过。若后续确需数据/消息变更，再按所属 Plan 使用隔离依赖；不另建基础服务，不复用生产卷或在真实平台压测。人工研究不得以设计者自测替代目标用户任务。

## 8. 启用、迁移与回退

1. 变更前记录版本、配置、迁移兼容性与备份状态；无持久变更时说明无需迁移。
2. 先在隔离环境跑切片证据，再以小范围合法样本启用；来源不可用保留待就绪状态。
3. 异常时先暂停该能力的新任务/写入，保留已确认任务与历史查询；核对在途副作用。
4. 仅在数据兼容时回退程序/配置；不兼容时使用预先演练的向前修复或隔离恢复，禁止临时删除数据表/持久卷。
5. 重跑关键 AC 和当前授权/删除约束，恢复前确认预算；记录仍无法恢复的范围。

## 9. 完成定义与进度维护

全部适用 checklist 已勾选且有真实证据、原始 AC 全部通过、影响范围的回归与运行交接完整，才可将本 Plan 改为 `completed`。失败进入记录和修复任务；N/A 需写理由，不能把必需能力改 N/A。

BACKLOG 用已验收需求数统计产品进度；本文件编写完成只增加计划覆盖。共享基础模块只实现一次，质量 Plan 的完成必须覆盖当前阶段全部适用对象，不能把“测试工具已写”算作质量目标达成。

| 日期 | 版本 | 记录 |
|---|---|---|
| 2026-09-17 | v1.0 | 建立对应 PRD 的执行切片、SPEC、门禁、checklist、原 AC 映射、验证与回退计划；全部检查待执行。 |
| 2026-09-21 | v1.3 | 同步现有工程基线、检查入口和 App Router 路径；需求 ID、优先级及 AC 不变。 |
| 2026-09-25 | v1.4 | 建立 proposed Design 并将计划标为 in_progress；按当前代码收窄默认变更为既有前端与共置测试，移除重复/未存在的测试入口；OPEN-040-001 与全部产品 AC 仍开放。 |

文档复核（2026-09-25）：需求 ID、优先级及必要 AC 不变；未补记产品通过证据。
