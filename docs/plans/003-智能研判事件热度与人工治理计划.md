---
layer: Plan
scope: shared
doc_no: "003"
title: 智能研判、事件热度与人工治理计划
status: planned
version: v1.0
owner: HotKey Team
canonical_path: docs/plans/003-智能研判事件热度与人工治理计划.md
design: docs/design/003-智能研判事件热度与人工治理设计.md
prd: docs/prd/003-智能研判事件热度与人工治理.md
---

# 智能研判、事件热度与人工治理计划

## 当前实现与差距

### 当前实现

- `intelligence` 模块已有模型 Profile、运行、预算、Schema Registry、OpenAI/DeepSeek/Ollama/ONNX Provider 和 Embedding 服务；
- `monitor` 与 `ingestion` 已包含查询扩展、召回、相关性研判、反馈、校准、Embedding 与混合召回路径；
- `event` 已包含聚类、Micro Event、Storyline、实体、Claim/Evidence、Heat、生命周期、摘要和人工治理；
- PostgreSQL 已保存 AI Run、预算、Embedding、决策、Heat Snapshot、Evidence State、反馈和审计事实；
- 新写 Product Event 已由 `micro_events` v2、成员、Heat Snapshot 和 ClaimEvidence 事实承载；legacy `events/topics` 仍用于历史读取/回滚兼容；
- River Job 已承载查询扩展、相关性、摘要、Claim/Evidence 等异步任务；
- 前端已有事件列表、热点卡片、内容详情、版本工作台和治理页面。

### 主要差距

- 目标智能运行时应收敛为根目录 `agent/` 的 Python 数据分析服务，由 Go Worker 通过内部认证和版本化 HTTP 契约调用，不允许 Agent 直接读写核心存储；
- Agent 请求/响应版本、服务认证、请求体/输出限制、超时、并发、稳定错误码、Pydantic/JSON Schema 和 Evidence 白名单需要独立契约；
- 仅检查退出码不足以保证结果可信，Schema 错误、伪造 Evidence ID、Prompt Injection 回显和超长输出必须拒绝；`ai_runs` 的 queued/running/validating/retry_wait/终态与复用规则必须保持一致；
- Agent 不可用时，采集、Evidence、精确去重、确定性 Heat 和人工治理必须继续，AI 字段应显式处于未分析/待补算状态；
- 相关性、Heat、ClaimEvidence 和 Event Evidence State 必须分离，模型不能直接决定 Heat、事实真假或最终生命周期；
- Product Event 的首次聚类和后续 Refresh 必须统一使用 `micro_events` v2；legacy `events/topics` 不得无标记双写；
- 当前多 Provider、Embedding、向量和混合召回路径不能直接删除。必须先用 Fake/Golden/Shadow/Live 完成等价或更优替换验收，保留可操作回滚，再另立清理步骤；
- 人工合并、拆分、移除成员、纠正、争议、归档和重算需要不可变审计与并发保护。

## 前置门禁

1. 001 已冻结 Python Agent、无向量/RAG 和 Go Core 唯一事实拥有者边界；
2. 002 已提供稳定的 Document、Match、Evidence Reference、Event Candidate 和来源权利事实；
3. 003 Design=`accepted`、PRD=`approved`；
4. Python/生产依赖版本、内部服务认证、模型 Provider 凭据保存方式、许可、网络出口和资源上限已完成安全评审；
5. 每个 Skill 的输入、输出 JSON Schema、Evidence ID 白名单和确定性 Go 二次校验规则已批准；
6. Fake Agent、Golden Dataset 和受信任模型 Live Runner 可用；普通 CI 不依赖真实模型登录；
7. 旧 Provider/Embedding 路径、数据表、配置、消费者和前端读法已完成引用清单，回滚负责人明确。

## 阶段、依赖与出口

| 阶段 | 周次 | 依赖 | 主要工作 | 阶段出口 |
|---|---:|---|---|---|
| S01 Python Agent Runtime | W11 | 001–002 | Python 服务骨架、双端契约、内部认证、Fake Agent、请求与输出约束 | Python/Go 测试覆盖成功、超时、未认证、恶意输出 |
| S02 Skill 与降级 | W11–W12 | S01 | Monitor Compile、Relevance、Cluster/Claim/Summary 契约和待补算 | 错误结果零污染；Agent 不可用主链继续 |
| S03 Event/Heat/Evidence | W12–W13 | S02、002 | 聚类/Refresh 幂等、确定性 Heat、ClaimEvidence、Event Evidence State、生命周期和 Golden | 四类语义分离且 Heat 可解释、可复算 |
| S04 人工治理 | W13 | S03 | 合并/拆分/纠正/争议/归档、审计、并发冲突 | 每个人工变更可追踪、可重放读模型 |
| S05 双轨替换与收敛 | W13–W14 | S01–S04 | 旧 Provider/Codex/Embedding 与 Python Agent 影子比较、灰度、回滚演练 | M3：Agent 路径可用；旧路径仍可回退 |

004 只读取已冻结的 Event/Claim/Evidence/Heat Snapshot，不把报告、通知或 Vault 投影反写为研判事实。

## 预计变更文件清单

```text
backend/db/schema.sql
backend/internal/bootstrap/monitor_intent_expansion.go
backend/internal/bootstrap/relevance_command.go
backend/internal/bootstrap/document_embedding.go
backend/internal/bootstrap/monitor_intent_embedding.go
backend/internal/bootstrap/automatic_claim_evidence_composition.go
backend/internal/bootstrap/micro_event_composition.go
backend/internal/bootstrap/worker.go
backend/internal/modules/intelligence/domain/
backend/internal/modules/intelligence/application/
backend/internal/modules/intelligence/infrastructure/jobs/
backend/internal/modules/intelligence/infrastructure/agent/
backend/internal/modules/intelligence/infrastructure/provider/
backend/internal/modules/intelligence/schemas/
backend/internal/modules/monitor/application/
backend/internal/modules/monitor/infrastructure/jobs/
backend/internal/modules/ingestion/application/
backend/internal/modules/ingestion/infrastructure/jobs/
backend/internal/modules/event/domain/
backend/internal/modules/event/application/
backend/internal/modules/event/infrastructure/jobs/
backend/internal/modules/event/infrastructure/postgres/
backend/internal/modules/event/transport/http/
backend/internal/platform/config/config.go
backend/internal/platform/queue/
backend/test/_suite/internal/modules/intelligence/
backend/test/_suite/internal/modules/monitor/
backend/test/_suite/internal/modules/ingestion/
backend/test/_suite/internal/modules/event/
backend/test/fixtures/relevance/
backend/test/fixtures/event-intelligence/
backend/test/fixtures/quality/
agent/pyproject.toml
agent/src/hotkey_agent/
agent/tests/
agent/Dockerfile
backend/openapi/docs.go
docs/openapi/swagger.json
frontend/src/app/dashboard/events/page.tsx
frontend/src/app/dashboard/governance/page.tsx
frontend/src/components/dashboard/HotspotCard.tsx
frontend/src/components/dashboard/ExpansionCandidateReview.tsx
frontend/src/services/hotkey/hotkey-server/
frontend/test/unit/app/dashboard-page.test.tsx
frontend/test/unit/app/dashboard-governance-page.test.tsx
docker-compose.yml
docker-compose-prod.yml
.github/workflows/ci.yml
```

Python 服务位于根目录 `agent/`；预期新增的 Go Agent Client 应位于 `backend/internal/modules/intelligence/infrastructure/agent/` 或等价 Infrastructure 边界，不在 Domain/Application 中直接依赖 HTTP 类型。

## 测试先行任务

### S01：受控 Python Agent 服务

1. `TASK-003-S01-T01`：先以 Python 测试定义 `/healthz`、`/readyz` 和内部 `/v1/analyze` 契约，断言未认证、超大请求、未知契约/任务类型和不可信正文不能改变系统策略；再实现最小 FastAPI 服务。映射 `AC-003-001`、`AC-003-002`。
2. `TASK-003-S01-T02`：先以 Go 契约测试定义 Agent Client 的版本头、服务认证、超时、取消、请求/响应上限和稳定错误映射，再实现 Infrastructure Client；River Job 仍只携带 Run ID，Activity 重读有界 Context。映射 `AC-003-001`、`AC-003-003`。
3. `TASK-003-S01-T03`：在 CI 中加入 Python 格式、静态检查、类型检查、单元测试、依赖审计和 Agent 镜像构建；Compose 只在内部网络暴露 Agent，并让 Go readiness 区分 Agent 可选降级。映射 `AC-003-001`、`AC-003-003`。
4. `TASK-003-S01-T04`：先覆盖 Agent 返回空响应、破损 JSON、契约/Schema 版本不符、未知字段、枚举/长度越界、未知错误码和伪造 Evidence ID；断言失败/取消/非法结果不进入可复用结果，修复输出形成新 Attempt，再实现完整双端校验链。映射 `AC-003-002`。
5. `TASK-003-S01-T05`：使用至少两类 P0 分析任务与小/中/大 Context，在固定环境分别执行并发 2、3、4；实际记录任务类型、Context 字节数或 Token 数、CPU 使用、内存峰值、执行时长、成功率和失败类别，再批准默认并发。映射 `AC-003-001`。`agent-capacity.v1` 已在 Apple M5/arm64、macOS 26.6.2、Python 3.12.13 上以认证 ASGI HTTP 实测 Relevance（128/600/1,200 字节）与 ClaimEvidence（1,024/20,000/80,000 字节）共 18 组、270 请求：全部成功、零失败类别，P95 为 2.394–3.589 ms、单组 CPU 为 23.489–51.904 ms、峰值 RSS 为 53,526,528–53,870,592 bytes；并发 3/4 未给 CPU 饱和的确定性基线带来一致吞吐收益，因此批准默认并发 2，并保留 3/4 作为后续真实模型候选而非默认值。

### S02：Skill 与降级/补算

1. `TASK-003-S02-T01`：为 Monitor Compile、Relevance、Event Cluster、Claim/Evidence 和 Event Summary 建立版本化 Schema Golden；先验证 Go 生成输入和解析输出兼容。映射 `AC-003-001`、`AC-003-002`。
2. `TASK-003-S02-T02`：先写 Agent 未就绪、未认证、超时、取消、Schema 错误和预算耗尽测试，断言采集/去重/Heat 继续且 AI 状态为未分析或待补算。映射 `AC-003-003`。
3. `TASK-003-S02-T03`：先写包含 Workspace、Skill/Prompt/Input/Output Schema、Runtime、目标版本、输入 Hash 和 Evidence Set Hash 的复用键测试，断言只复用在途/成功 Run；再实现有限重试、失败码和管理员补算入口，Job 参数只传运行 ID。映射 `AC-003-002`、`AC-003-003`。
4. `TASK-003-S02-T04`：使用不可信来源文本构造 Prompt Injection 夹具，断言其命令、Evidence ID 和输出格式指令不能改变系统契约。映射 `AC-003-002`。

### S03：事件、Heat 与 Evidence

1. `TASK-003-S03-T01`：先用跨来源、转载、同源评论、近重复、晚到和重复候选夹具验证聚类幂等与成员唯一性，再实现最小聚类策略。映射 `AC-003-004`。
2. `TASK-003-S03-T02`：先写同一内容的 `relevance_score`、`heat_score`、ClaimEvidence 和 Event `evidence_state` 相互独立的领域测试，禁止模型字段或 Event 汇总状态覆盖 Claim 级引用与确定性分数。映射 `AC-003-005`。
3. `TASK-003-S03-T03`：先建立 Heat Profile 与 Golden 数据集，覆盖独立谱系根、速度、加速度、来源覆盖、按来源能力/类型归一化的可选互动、时效、`available_weight`、无互动 unknown、无时间 Baseline=`warming_up`、重算幂等和 Profile 版本。映射 `AC-003-006`。
4. `TASK-003-S03-T04`：先验证单一 origin 下多条评论/转载不能提升独立来源；ClaimEvidence 必须绑定可引用 Document Version/selector，Event Evidence State 只能从当前 ClaimEvidence 集合聚合，报告不能仅凭汇总状态批准。映射 `AC-003-005`、`AC-003-006`。
5. `TASK-003-S03-T05`：为 Product Event Refresh 建立 River 幂等测试，覆盖新成员、晚到指标、Heat/Evidence 重算、Event Update 与告警评估；同一事件版本、时间窗和 Profile 重放不重复写成员、Snapshot 或通知，legacy `events/topics` 无新写。映射 `AC-003-004`、`AC-003-006`。

### S04：人工治理

1. `TASK-003-S04-T01`：先写合并、拆分、移除成员、纠正、争议、归档和恢复的权限、资源版本冲突和不可变审计测试。映射 `AC-003-007`、`AC-003-008`。
2. `TASK-003-S04-T02`：先证明治理变更只追加 Revision/Audit 并重建读模型，不覆盖原始 Document、Observation 或 Evidence。映射 `AC-003-007`。
3. `TASK-003-S04-T03`：先覆盖治理页面正常、空、加载、错误、权限不足和并发冲突状态，再接入生成客户端。映射 `AC-003-008`。四角色可重复 UAT 已接入：Event 详情返回当前成员治理句柄与独立 Claim Version，页面通过生成客户端提交强 `If-Match`/幂等键；Viewer/Analyst 对事件治理只读且服务端零写入，Editor/Admin 可治理并得到成功回执；Analyst 只在自有 Monitor 的相关性预览/反馈路径写入。

### S05：双轨替换

1. `TASK-003-S05-T01`：盘点每个 Provider Profile、Embedding 写入/读取、召回、River Handler、API、页面、报表和运维调用方；为历史与新增数据逐项标明保留、停止新写、归档或待批准删除，并定义旧记录读兼容。映射 `AC-003-003`。
2. `TASK-003-S05-T02`：用固定 Golden 输入并行执行现有 Provider/Codex 路径和 Python Agent 路径，记录结构有效率、Evidence 精确率、人工接受率、延迟、成本和失败类别，并形成可批准的质量比较。映射 `AC-003-001`、`AC-003-003`。
3. `TASK-003-S05-T03`：先写按 Model Profile/配置切换与实际切回测试，再按内部样本→少量 Monitor→全部 P0 Monitor 灰度 Agent；任何读取方在灰度期间都能解释结果来源和读取旧记录。映射 `AC-003-003`。
4. `TASK-003-S05-T04`：调用方盘点、质量比较、数据处置、读兼容、灰度和回滚全部验收后，只停用旧 Provider 新调用；不删除代码、Schema、Embedding 表或历史向量，物理删除另立提案。映射 `AC-003-003`。

#### S05 T01 调用方与数据处置盘点（2026-08-27 仓库事实）

本节是 S05 T01 的静态代码盘点，不是 Shadow/Live 通过记录。当前 Provider Registry 只注册 OpenAI、DeepSeek、Ollama 与 ONNX。Go Codex CLI Adapter 已实现但尚未进入 ProviderName、Model Profile Schema 或生产 Registry；Python Agent 仅接入 Go Worker 的可选 Shadow 比较路径，尚未进入 Live Provider Registry 或最终决策写入，因此当前不能把 Fake/Shadow Runtime 结果解释为生产切流已完成。

| 边界 | 当前调用链与读写事实 | S05 处置 | 回滚/读兼容 |
|---|---|---|---|
| Python Agent 内部 Client | `backend/internal/modules/intelligence/infrastructure/agent/client.go` 已实现 `analysis.v1` 的认证、请求/响应上限、超时/取消、错误脱敏、身份一致性和 Evidence 白名单校验，并提供 `GenerateStructured` 到既有 `StructuredRequest` 的任务/Schema/有界 Evidence 映射；`shadow.go` 以 2 秒、并发 2 的非阻塞上限异步比较已提交的旧 Provider 结果，仅记录低基数字段和稳定错误码，不记录 Prompt、来源正文或模型输出。 | S01 T02 保持独立契约测试；只有 `HOTKEY_AGENT_SHADOW_ENABLED=true` 且 URL/Token 完整时，Go Worker 才提交 Shadow 比较，Agent 超时、失败或并发溢出均不改变旧 Provider 结果、重试、预算或业务写入。完成固定 Golden 指标、Shadow 质量报告与切回测试后，T03 才按 Profile 灰度 Live。 | 开关关闭或未配置 Agent 时 Runner 为 no-op；启用后仍不进入 Provider Registry。将开关改回 `false` 即停止新 Shadow，旧 Provider/Profile/Run 与历史读取不受影响。 |
| Provider 契约与生产注册 | `backend/internal/modules/intelligence/domain/provider.go` 定义 OpenAI/DeepSeek/Ollama/ONNX；`backend/internal/bootstrap/app.go` 只把这四类 Provider 放入 Registry。受控 Go Codex 实现位于 `backend/internal/modules/intelligence/infrastructure/provider/codex_cli.go` 与 `backend/internal/modules/intelligence/infrastructure/provider/codex_cli_provider.go`。 | T02 先让 Python Agent 与旧路径做 Golden/Shadow；T03 才可向 Runtime/Profile Schema/Registry 增加向前兼容的 Agent 选项与切换测试。 | 保留旧 Provider/Codex 实现、配置解析和 Profile 记录；切回只改已验证 Profile/配置，不改写历史 Run。 |
| 结构化 AI Run | `backend/internal/modules/intelligence/application/run_service.go` 根据 Task Type 与 Fallback Priority 选 Profile，并执行 Schema、Evidence 白名单、修复、预算和状态机；`backend/internal/bootstrap/monitor_intent_expansion.go`、`backend/internal/modules/intelligence/application/relevance_review.go`、`backend/internal/modules/intelligence/application/event_intelligence.go` 和 `backend/internal/modules/event/application/automatic_claim_evidence.go` 是已存在的结构化任务边界。 | 按 Skill/Task 逐项 Shadow，不用一个全局开关一次性切换；Agent 结果仍进入同一 Go 验证与持久化边界。 | 旧 Run 依原 Runtime/Profile/Schema 版本保留；切回后只停 Agent 新 Job，旧结果不重写。 |
| Document Embedding 写入 | `backend/internal/modules/intelligence/application/projected_embedding_service.go` 调用旧 Embedding Provider；`backend/internal/bootstrap/document_embedding.go` 在 AI Run 结算事务内写 `document_version_embeddings`，上游为 Source Document Generation。 | 保持权利决策与 Tombstone 约束；在无向量 P0 召回达标且完成切回演练前不停新写。 | Provider 不可用时返回明确 unavailable，Lexical/Structured 主链继续；历史 Receipt 与向量保留。 |
| Monitor Intent Embedding 写入 | `backend/internal/bootstrap/monitor_intent_embedding.go` 由 Intent Compiler 发起相同的 Projected Embedding，并写 `monitor_compiled_intent_embeddings`；不回退到可编辑 Draft 或 legacy monitor rules。 | 语义向量可 unavailable，但 Published Compiled Profile 的词法/结构事实继续。停写必须等待 T02/T03 证明 P0 质量与切回。 | 保留 Published Profile 及其精确 Embedding/Profile/Run 绑定；不从 legacy `monitor_embeddings` 伪造回退。 |
| P0 Hybrid Recall 读取 | `backend/internal/modules/ingestion/application/hybrid_recall.go` 必选 Lexical+Structured，Semantic 可降级；`backend/internal/modules/ingestion/infrastructure/postgres/hybrid_recall_reader.go` 读 `document_version_search_indexes` 与精确版本的 `document_version_embeddings`。 | T02 对固定样本比较含/不含 Semantic 的质量、延迟和失败分类；P0 默认最终收敛为可审计 PostgreSQL FTS/Trigram+结构化规则。 | Semantic unavailable 是显式降级，不丢弃确定性候选；切回时可恢复旧 Profile 读取。 |
| Legacy Event/Vector Recall | `backend/internal/modules/event/infrastructure/postgres/candidate_recall.go` 仍可读 `content_embeddings`/`event_embeddings` 与 legacy `events`，但未注册进当前 P0 Handler Map；Product Event 新写已只进 `micro_events` v2。 | 仅作历史读取/回滚盘点，禁止重新接成 Product Event 新写双轨。 | 保留代码与旧记录；任何再启用都需专项测试且不得写 legacy `events/topics`。 |
| River Handler | `backend/internal/bootstrap/app.go` 的 P0 Handler Map 注册 Monitor Intent Analysis、Source Document Generation、Published Match Evaluation、Automatic ClaimEvidence、Product Event Refresh 与 AI Run Recompute；Job 只传稳定 ID，Activity 重读 Context。 | Shadow/Live 必须沿现有 River 和 AI Run 状态机执行，不新建队列；Go Worker 负责调用 Agent 和提交结果。 | 关闭 Agent 新准入后，待补算 Job 保留，通过管理端重算入口在旧 Profile 上重放。 |
| API、OpenAPI 与前端 | `backend/internal/modules/intelligence/transport/http/routes.go` 提供 Admin-only Model Profile CRUD 与 AI Run Recompute；生成客户端位于 `frontend/src/services/hotkey/hotkey-server/ai.ts`。当前无第一方页面直接调用模型 Profile 管理客户端，页面只消费 Relevance/Event 等安全投影。 | T03 若增加 Agent Runtime/Profile 字段或来源投影，必须同步 DTO/OpenAPI/生成客户端和 Admin 权限测试；不为切流临时手写 API。 | 旧客户端字段保持可读，新枚举只做向前增加；服务端收紧在灰度后单独进行。 |
| 报告、页面与运维读取 | 报告模块不直接调用 Provider、Embedding、Codex 或 Agent；`backend/internal/modules/report` 只读已冻结业务事实，`backend/internal/modules/operations/infrastructure/postgres/runtime_metrics.go` 与 Governance 只聚合 AI Run Token/运行指标。 | 保持 Report/Operations 对运行时无感；T02 比较指标另存可审计的 Run/Profile 来源，不将模型原始响应写进报告。 | 切回不改报告事实；Operations 继续按 AI Run 计数，不把不同 Runtime 记录合并成无来源指标。 |

数据处置以“保留证据、停新写与物理删除分离”为原则：

| 数据资产 | 当前用途 | T01 冻结处置 |
|---|---|---|
| `ai_model_profiles` | Provider/Task/Model/预算/重试/优先级和启用状态 | 保留；T03 只做向前兼容的 Agent Runtime 新增，旧 Profile 在回滚窗口不删除。 |
| `ai_runs`、`ai_run_evidences`、`ai_budget_ledgers` | Run 状态、结果来源、Evidence 关联和预算结算 | 持续保留并按原 Profile/Runtime/Schema 读取；Shadow 与 Live 用不同 Run，不覆盖。 |
| `content_embeddings`、`monitor_embeddings`、`event_embeddings`、`topic_embeddings` | legacy Content/Monitor/Event/Topic 向量与候选召回 | 保留历史和代码回滚能力；只有 T02/T03/实际切回全过后才能在 T04 停新调用，不物理删除。 |
| `monitor_compiled_profiles` 及其 Clause/Entity 子事实 | 已发布 Monitor Intent 的可读召回契约 | 持续新写与版本化；它们不是 Embedding 临时缓存，不随 Provider 切换删除。 |
| `monitor_compiled_intent_embeddings` | Published Compiled Profile 的可选语义向量 | 保留精确 Profile/Run 绑定；无向量收敛通过 Semantic unavailable 进行，不伪造向量或删旧记录。 |
| `document_version_search_indexes` | P0 PostgreSQL FTS/Trigram 与结构化召回投影 | 持续新写、撤权/Tombstone 和可重建；它是目标主路，不属于待停的 Embedding 资产。 |
| `document_version_embeddings` | 权利绑定的 Document Version 可选语义投影 | 保留 Receipt、Run、Retention 与 Tombstone；后续停新写不跳过撤权/保留规则。 |
| legacy `events/topics/event_updates` | 历史读取与切回兼容 | 无 Product Event 新写；保留读兼容，不在 S05 删除。 |

读兼容的验收目标是：旧记录继续按原 Model Profile、Run 与向量版本可读，但不得被当作 Python Agent 新结果或无来源的当前决定。T03 必须以测试证明“选 Agent→记录 Agent Runtime/Profile/Run→切回旧 Profile→旧记录仍可解释”。本阶段禁止物理删除；表、向量、Provider/Codex 代码或历史数据的删除需另立提案，并具备备份、对账、独立批准与恢复演练。

T01 静态盘点由 `TestAIReplacementInventoryCoversCurrentCallersAndDataDisposition` 和更新后的目标运行时断言防止漂移。T02 已具备固定数据集与候选报告框架，但真实模型、双人复核和批准阈值尚未执行；T03 的灰度/切回以及 T04 的停新调用仍未完成，因此 `CHK-003-G5-001` 继续保持未勾选。

#### S01 T01–T04 实施证据（2026-08-27）

本轮证据证明 Python Agent 与 Go Infrastructure Client 的双端结构化适配已具备可复测边界，但不等同于 Shadow/Live 或生产切流通过：

- Python：`uv run ruff format --check .`、`uv run ruff check .`、`uv run mypy src`、`uv run pytest`、`uv run pip-audit` 全部通过；32 项测试通过，覆盖率 96.35%。
- Go：`go run ./test/runner test ./internal/modules/intelligence/infrastructure/agent ./test/architecture -count=1` 通过；结构化请求任务类型映射、Schema/输入/Evidence 传递、输出版本和 Embedding 明确不可用均有契约测试。
- 实际服务：本机 uvicorn `/readyz` 返回 `200`；`/v1/analyze` 对 `event-summary-output-v1` 返回 `analysis.v1`、`status=degraded`、`runtime=deterministic.v1` 和有界 Evidence ID，未写入任何业务事实。
- 容器：`docker build --tag hotkey-agent:verify .` 成功；非 root 容器 `/readyz` 与同一结构化请求均返回预期降级输出，验证镜像运行时边界。

上述证据在 2026-08-27 仅推进 S01 的实现状态；后续双端 Schema 与零写入证据见下节。`CHK-003-G5-001` 仍保持未勾选。

#### S01 T04 双端 Skill 契约证据（2026-08-28）

- Python Agent 的 `skills.py` 固定六个已批准 Skill 的任务类型、Schema 版本及 canonical 输入/输出 Schema SHA-256；`test_every_structured_skill_validates_canonical_input_and_output_schema` 逐项覆盖 Monitor Compile、Relevance、Event Cluster、Event Summary、legacy Entity/Claim 与 Atomic Claim/Evidence v2。
- `test_structured_analysis_rejects_unknown_or_task_mismatched_skill_contracts`、`test_structured_analysis_rejects_forged_input_schema_and_invalid_input`、`test_structured_analysis_rejects_invalid_or_forged_analyzer_output` 证明未知版本、任务错配、Schema 篡改、输入枚举越界、输出枚举/长度越界及伪造 Evidence ID 分别稳定返回 `AGENT_INVALID_REQUEST` 或 `AGENT_OUTPUT_INVALID`，不回显来源正文、Prompt 或内部异常。
- Go `StructuredRequest` 现在携带 canonical `InputSchema`；`RunService` 在调用前验证输入，在完成写入前再次执行输出 Schema 与 Evidence/领域 Policy 校验。`TestClientMapsStatusTimeoutAndMalformedResponsesToStableErrors`、`TestClientRejectsIdentityAndEvidenceForgery` 与 `TestClientAdaptsStructuredRequestsToVersionedAgentPayload` 固化 HTTP、错误码、身份、Evidence 和双 Schema 传递。
- `TestPromptInjectionFixtureCannotChangeEvidenceOrOutputContract`、`TestRunServiceNeverPersistsOrReusesForgedEvidenceAndRepairConsumesNewAttempt` 证明两次非法结果后 `ai_runs` 不产生可复用成功结果；`TestAutomaticClaimEvidenceServiceLeavesOperationalModelFailuresPendingWithoutBusinessWrites` 证明 `output_invalid` 不写 Claim/Evidence 业务事实。
- `make agent-skill-contract-acceptance` 将 Python 全量测试、Go 双端契约、伪造/损坏输出和零业务写入测试收敛为 `AC-003-002` 专项门禁。

#### S02 T02–T03 Agent 降级与补算证据（2026-08-28）

- `TestRunServiceKeepsPrimaryResultWhenPythonAgentIsUnavailable` 通过真实内部 Agent HTTP Client 向返回 `503` 的 Fake Agent 发起 Shadow 请求，观察到稳定 `AI_MODEL_UNAVAILABLE`，同时旧 Provider 主 Run 保持 `succeeded` 且已提交结构结果不变；`TestShadowRunnerMapsAgentFailureAndBoundsConcurrency` 覆盖失败和并发丢弃都不阻塞调用方。
- `TestAgentShadowRunnerIsOptionalAndConstructsOnlyFromValidatedInternalConfig` 证明 Agent 未启用时 Bootstrap 构造 no-op Runner；采集、确定性去重、Heat 与人工治理路径不导入 Intelligence/Agent Runtime，并分别由 `TestCollectionServiceProjectsThreeSourcePartialSuccessWithoutPersistingAggregateOutcome`、`TestContentFamilyServicePersistsOnlyFingerprintAndDecisionFacts`、`TestEventHeatServiceUsesActiveProfileWeightsAndStableSnapshot`、`TestMicroEventGovernanceServiceBuildsStablePOJOCommand` 证明可独立执行。
- `TestRunServicePersistsTerminalOperationalFailureAsPendingAnalysis` 与 `TestAutomaticClaimEvidenceServiceLeavesOperationalModelFailuresPendingWithoutBusinessWrites` 证明 Live Provider 等价故障进入显式待分析状态且不污染 Claim/Evidence；`TestAIRunRecomputeServiceSchedulesOnlyOwnedTerminalFailures` 限制仅终态失败且具有原 Job 的 Run 可补算。
- `TestAIRunRecomputeWorkerReactivatesOwningJobFromRunIDOnly` 证明补算 Job 只保存 Run ID、重新激活原拥有者 Job，并在重复调度时复用同一 Job；`make agent-degradation-acceptance` 将 Agent 503、确定性主链和恢复重放收敛为 `AC-003-003` 专项门禁。
- 本门禁只批准“Agent 不可用时不破坏当前确定性主链及可恢复性”，不批准真实模型质量、并发容量、Live Profile 切流或旧 Provider 停用；这些仍受 G3/G5 约束。

#### S05 T02 Shadow 接线证据（2026-08-28）

- `RunService` 只在旧 Provider 结果已通过 Schema/Evidence Policy 并完成主 Run 提交后调用 `StructuredShadow.Submit`；Shadow 返回值不进入 `ai_runs`、业务投影、预算或重试状态机。
- `ShadowRunner` 只有在 `HOTKEY_AGENT_SHADOW_ENABLED=true` 且内部 URL/Token 完整时，才对输入和主结果复制后异步调用 Python Agent；固定超时 2 秒、最大并发 2，并发溢出记录 `dropped`，Agent 失败只记录稳定错误码。
- 比较观察只包含 Task/Schema、`matched|different|invalid|agent_error|dropped`、JSON 有效性、SHA-256 和耗时；Bootstrap 日志不输出 SHA-256、Prompt、来源正文、模型输出或 Provider 错误文本。
- `TestRunServiceSubmitsShadowAfterPrimaryCommitWithoutChangingResult` 证明主结果先提交且 Shadow 不改变持久化事实；`TestShadowRunnerReportsSanitizedComparisonWithoutBlockingSubmit` 与 `TestShadowRunnerMapsAgentFailureAndBoundsConcurrency` 证明非阻塞、脱敏、超时/错误映射和并发背压。
- `TestAIReplacementInventoryRecordsPythonAgentMigrationBoundary` 证明 Agent 只进入可选 Shadow Bootstrap，未进入 `ProviderName` 或生产 Provider Registry。

当前实现是 Shadow 执行骨架，不是质量验收完成：固定合成 Golden 与候选报告框架已建立，但还没有真实模型的旧 Provider/Agent 双轨报告、双人人工接受率、真实模型 Live Runner、按 Monitor 灰度或实际切回记录，因此 `CHK-003-G5-001` 继续保持未勾选。

#### S05 T02 固定 Golden 候选报告证据（2026-08-28）

- `backend/test/fixtures/agent-shadow/v1/golden-dataset.json` 是版本化合成输入，逐项覆盖 `term_expansion/v1`、`relevance_review/v1`、`event_cluster/v1`、`event_summary/v1` 与 `entity_claim_extraction/v2`；当前明确记录 `annotator_count=0`，没有伪造人工验收。
- `backend/internal/modules/intelligence/application/shadow_quality.go` 对每个固定样本并发运行 baseline 与 Agent，统一校验实际模型版本、结构 Schema、Evidence Policy 和 Token 合法性，并计算结构有效率、Evidence Precision/Recall、人工接受率、P50/P95/最大延迟、可选成本与稳定失败分类。
- 报告只保留 Dataset/Output SHA-256、计数和低基数类别，不包含 Golden 输入、模型输出、Provider 错误、Prompt 或凭据；状态固定为 `candidate`、`approval_ready=false`，缺少人工复核、成本或真实 Runtime 时给出明确 Reason Code。
- `go run ./cmd/hotkey agent-quality evaluate` 是受信命令入口：baseline 只允许 OpenAI、DeepSeek、Ollama 或显式绝对路径的 Codex，Python Agent URL/Token 仅从配置读取；命令没有激活选项，也不会将 Agent 注册进生产 `ProviderRegistry`。
- `backend/internal/modules/intelligence/infrastructure/agent/client.go` 返回 Agent 响应声明的实际 Runtime Version；评估器会把实际版本与命令固定版本不一致归类为 `model_profile_invalid`，防止调用方标签冒充已验证模型。
- `TestShadowQualityEvaluatorRunsTracksConcurrentlyAndKeepsReportCandidate`、`TestShadowQualityDatasetRejectsUnknownFieldsAndTrailingDocuments`、`TestAgentQualityCommandEvaluatesFixedFiveSkillDatasetAsCandidate` 与 `TestClientAdaptsStructuredRequestsToVersionedAgentPayload` 覆盖并发、指标、数据集严格解析、脱敏、候选状态和实际 Runtime 版本绑定。

上述证据完成的是“可重复产生候选比较报告”的工程框架，不是 T02 的批准结果。真实 Provider/Agent 模型尚未在受信 Runner 执行，固定数据集尚未由至少 2 名独立复核者标注，质量阈值尚未批准，Agent Runtime 当前仍是 `deterministic.v1` 降级实现，Agent Token 成本也不可用；因此不能勾选 `CHK-003-G5-001`，也不能开始 T03 Live 灰度。

## 实施规格（SPEC）

| SPEC ID | 规格条款 | 上游 AC |
|---|---|---|
| `SPEC-003-API-001` | 智能运行、补算和治理接口使用统一 Result、稳定错误码、服务端 RBAC 和生成 OpenAPI；不得返回 Prompt、Token、原始 stderr 或内部路径。 | `AC-003-003`、`AC-003-008` |
| `SPEC-003-DATA-001` | AI Run 保存 Workspace、Skill/Prompt/Input/Output Schema/Runtime/Profile/目标版本、输入 Hash、Evidence Set Hash、状态、Attempt、稳定失败码、计量和时间；状态遵循 queued→running→validating→succeeded、retry_wait 重试和 failed/cancelled 终态，只允许在途/成功同键复用，不把完整来源正文或凭据复制进 Job 参数。 | `AC-003-001`、`AC-003-002` |
| `SPEC-003-DATA-002` | Product Event 新写只进入 `micro_events` v2。Event 成员、六分量 Heat Snapshot、ClaimEvidence、Event Evidence State、治理 Revision 和 Audit 均为分层可追溯事实；Profile 或算法变化新增版本，不静默重写历史，legacy `events/topics` 只读兼容。 | `AC-003-004` 至 `AC-003-007` |
| `SPEC-003-AGENT-001` | 根目录 `agent/` 使用 Python/FastAPI 实现无状态数据分析服务，暴露存活、就绪与内部版本化分析端点；生产依赖锁定，普通 CI 不依赖真实模型凭据。 | `AC-003-001`、`AC-003-002` |
| `SPEC-003-JOB-001` | Agent 只由 Go Worker 内 Infrastructure Client 调用；River Job 仅携带 Run ID，Activity 执行时重读 Context，并具备幂等、超时、取消和有限重试。 | `AC-003-001`、`AC-003-003` |
| `SPEC-003-JOB-002` | Go→Agent 使用内部认证和版本化 HTTP 契约，限制请求/响应大小、并发与墙钟时间；Agent 环境禁止业务存储/来源/用户凭据，Compose 不发布 Agent 宿主机端口。 | `AC-003-001`、`AC-003-002` |
| `SPEC-003-JOB-003` | Event Refresh 使用 River，并以 micro_event_id+event_version+window+Heat/Evidence Profile 派生幂等键；只读取已提交成员/指标，重算后追加 Snapshot/Event Update，再评估告警，重复执行无额外副作用。 | `AC-003-004`、`AC-003-006` |
| `SPEC-003-UI-001` | Event/治理 UI 分开展示相关性、Heat、Evidence State、AI 状态和 `warming_up`，并覆盖五态、移动端和并发冲突。 | `AC-003-005`、`AC-003-006`、`AC-003-008` |
| `SPEC-003-SEC-001` | 来源文本始终为不可信数据；输出须经 JSON、Schema、枚举、长度、Evidence 白名单和 Go 领域规则校验后才能持久化。 | `AC-003-002` |
| `SPEC-003-OBS-001` | 指标至少区分 Skill、Schema、Profile、result、failure_code、duration、queue_age、retry 和 usage；禁止高基数内容、Evidence ID 和路径标签。 | `AC-003-001`、`AC-003-003` |
| `SPEC-003-OBS-002` | 容量校准报告必须按任务类型与 Context 分层，记录环境、Context 字节数或 Token 数、CPU、内存峰值、时长、成功率、失败类别和并发 2、3、4 的结果；只允许从实测候选中批准默认并发。 | `AC-003-001` |
| `SPEC-003-OPS-001` | Python Agent 可独立禁用；禁用后确定性主链继续并产生待补算状态。旧 Provider/Codex/Embedding 只有在替换验收和回滚演练后才能停用新流量。 | `AC-003-003` |
| `SPEC-003-OPS-002` | Provider/Embedding 迁移验收必须覆盖全部调用方、固定集质量比较、历史/新增数据处置、旧记录读兼容、按 Skill/Monitor 灰度和实际切回；物理删除不与切流同版。 | `AC-003-003` |

## 执行检查清单（CHECKLIST）

- [x] `CHK-003-G3-001`（`AC-003-001`）：Python Agent 的内部网络、认证、依赖、权限和资源限制已评审，并已按任务类型/Context 实测 CPU、内存、时长及并发 2–4；证据：`agent-capacity.v1` 对 Relevance/ClaimEvidence 的小中大 Context、并发 2/3/4 共 270 个认证请求全部成功且记录 CPU、峰值 RSS、Wall/P50/P95/吞吐与空失败分类，批准 `HOTKEY_AGENT_MAX_CONCURRENCY=2`；`test_capacity_tool_reports_bounded_authenticated_analysis_case` 固化报告结构，Compose 对 Agent 保持无宿主端口和零业务存储凭据，并增加只读根文件系统、`cap_drop: ALL`、`no-new-privileges`、128 PID、1 CPU、512 MiB 与 64 MiB 临时目录上限；`TestPythonAgentCapacityAndIsolationGateMatchesAC003001` 与 `make agent-capacity-baseline` 固化可重复门禁。
- [x] `CHK-003-G4-001`（`AC-003-002`）：契约/Schema 版本、输入输出 Schema、枚举、长度、允许错误码及伪造/损坏输出在 Python 与 Go 两端均校验，失败时零业务写入；证据：`test_every_structured_skill_validates_canonical_input_and_output_schema`、`test_structured_analysis_rejects_forged_input_schema_and_invalid_input`、`test_structured_analysis_rejects_invalid_or_forged_analyzer_output`、`TestClientRejectsIdentityAndEvidenceForgery`、`TestPromptInjectionFixtureCannotChangeEvidenceOrOutputContract`、`TestRunServiceNeverPersistsOrReusesForgedEvidenceAndRepairConsumesNewAttempt`、`TestAutomaticClaimEvidenceServiceLeavesOperationalModelFailuresPendingWithoutBusinessWrites` 与 `make agent-skill-contract-acceptance`。
- [x] `CHK-003-G4-002`（`AC-003-003`）：Agent 不可用时采集、去重、Heat 和人工治理继续，补算幂等；证据：`TestRunServiceKeepsPrimaryResultWhenPythonAgentIsUnavailable`、`TestShadowRunnerMapsAgentFailureAndBoundsConcurrency`、`TestCollectionServiceProjectsThreeSourcePartialSuccessWithoutPersistingAggregateOutcome`、`TestContentFamilyServicePersistsOnlyFingerprintAndDecisionFacts`、`TestEventHeatServiceUsesActiveProfileWeightsAndStableSnapshot`、`TestMicroEventGovernanceServiceBuildsStablePOJOCommand`、`TestRunServicePersistsTerminalOperationalFailureAsPendingAnalysis`、`TestAIRunRecomputeWorkerReactivatesOwningJobFromRunIDOnly` 与 `make agent-degradation-acceptance`。
- [x] `CHK-003-G4-003`（`AC-003-004`）：重复、晚到、转载和跨来源候选不会创建重复 Event 成员；证据：`TestMicroEventClusteringGoldenCoversLineageReplayAndCandidateIdempotency`、`TestMicroEventRepositoryReusesOneMemberForSyndicatedNearDuplicateAndRepeatedMatches`、`TestMicroEventRepositoryPersistsCreateJoinReviewAndStableReplay`、`micro_event_members_active_family_uq` 数据库唯一索引。
- [ ] `CHK-003-G4-004`（`AC-003-005`）：相关性、Heat、ClaimEvidence、Event Evidence State 字段和计算方严格分离，报告逐句引用不依赖 Event 汇总状态；预期证据：领域/报告审批测试与 API/UI 快照。
- [x] `CHK-003-G4-005`（`AC-003-006`）：Heat 六分量、可选互动 unknown/可用权重、Profile 版本和 `warming_up` 行为稳定可复算；证据：`TestEventHeatV2GoldenCoversProfilesUnknownMetricsAndWarmingUp`、`TestHeatV2DoesNotInventVelocityWithoutTemporalBaseline`、`TestEventHeatRepositoryCountsAuthorizedLineageAndReplaysImmutableSnapshot`、`TestMicroEventResponseExposesHeatProfileAvailabilityAndWarmingUp`；`BenchmarkCalculateEventHeatV2` 在 Apple M5/arm64 三次为 `40.85–49.28 ns/op`、`8 B/op`、`1 allocs/op`。
- [x] `CHK-003-G4-006`（`AC-003-007`）：人工治理每次操作都有 Actor、理由、前后版本和不可变审计；证据：`TestMicroEventGovernanceMovesSplitsMergesClosesReopensAndReplays`、`TestMicroEventGovernanceRejectsUnauthorizedStaleAndConflictingCommandsAndKeepsWithdrawAuditImmutable`、`TestMicroEventGovernanceResolvesSameEventAndDifferentEventReviews` 与 `TestClaimEvidenceRepositoryPersistsQuotedRelationsAndLineageEvidenceState` 覆盖合并、拆分、移除成员、争议裁决、归档、恢复、证据纠正、版本冲突、幂等重放、审计防篡改，并逐行证明治理不会覆写原始 DocumentVersion、SourceObservation 或 ClaimEvidence。
- [x] `CHK-003-G4-007`（`AC-003-008`）：四角色服务端权限与治理页面五态一致；证据：`TestMicroEventRoutesEnforceFourRoleReadAndGovernanceBoundary` 对 Viewer/Analyst/Editor/Admin 逐一验证读取 200、Viewer/Analyst 治理 403 且零业务写入、Editor/Admin 治理 200 且各一次业务写入，`TestMicroEventRoutesRequireAuthenticationAndProtectMutations` 保留未认证和全部治理端点负向覆盖；`dashboard-event-governance-page.test.tsx` 的 11 项用例覆盖正常、空、加载、可重试错误、独立 403、Viewer/Analyst 只读、Editor/Admin 可写、409 并发冲突、生成客户端与精确版本头；`TestEventGovernanceFourRoleAndFiveStateGateMatchesAC003008` 与 `make event-governance-role-acceptance` 固化可重复验收入口。
- [x] `CHK-003-G4-008`（`AC-003-004`、`AC-003-006`）：Event Refresh 重放无重复成员/Snapshot/通知，Product Event 只写 `micro_events` v2；证据：`TestProductEventRefreshSchedulerUsesVersionWindowAndProfilesAsRiverIdentity`、`TestProductEventRefreshHandlerRejectsUnknownArgsAndDelegatesExactEnvelope`、`TestContentMetricRefreshSchedulesOnlyProductEventRiverJobs`、`TestProductEventRefreshRiverReplaysMembersMetricsSnapshotsUpdatesAndNotifications`；集成断言同一 Event 版本/窗口/Profile 重放无额外成员、Heat/Evidence Snapshot、Event Update、Alert Evaluation 或 Notification Outbox，且 legacy `events/topics/event_updates` 零新写。
- [ ] `CHK-003-G5-001`（`AC-003-001`、`AC-003-003`）：Python Agent 与旧 Provider/Codex 路径完成调用方盘点、质量比较、数据处置、旧记录读兼容、Shadow/Live 灰度和实际切回；预期证据：迁移矩阵、比较报告、灰度记录和回滚时间。

## 验证命令

```bash
cd backend
make agent-capacity-baseline
go run ./test/runner test ./internal/modules/intelligence/... ./internal/modules/monitor/... ./internal/modules/ingestion/... ./internal/modules/event/... -count=1
go run ./test/runner test -tags=integration ./internal/modules/intelligence/... ./internal/modules/event/... -count=1
go test ./test/architecture -count=1
go run ./test/runner vet ./...
sh test/tools/verify-schema.sh
go build ./...

cd ../frontend
npm run openapi:generate
npm run openapi:check
npm run typecheck
npm run test:unit
npm run build

cd ../agent
uv sync --all-extras --locked
uv run ruff format --check .
uv run ruff check .
uv run mypy src
uv run pytest
uv run pip-audit

cd ..
git diff --check
```

真实模型 Live Test 只在受信任 Runner、独立认证和固定预算下执行。普通 CI 必须使用 Fake Agent 与 Golden Dataset，且不得因为 Live 环境不可用而跳过结构、安全和降级测试。

## 数据与契约顺序

1. 先版本化 Skill 输入/输出 Schema 与 Golden，并保存失败测试；
2. 若需新增 Run、Profile 或 Snapshot 字段，只修改 canonical `schema.sql`，同步 Model、Repository、Schema 检查和非空库兼容验证；
3. 先完成 Python Agent 双端契约、Fake Agent 与 Go Infrastructure Client，再接 River Handler；
4. 先完成 JSON/Schema/Evidence/领域二次校验，再允许事务持久化；
5. Event/Heat/Evidence Domain 稳定后再暴露 API；生成 OpenAPI 和前端客户端后更新 UI；
6. Agent 先 Shadow 不写最终决定，再小流量写入可回滚 Revision，最后才切换默认 Profile；
7. 旧 Provider/Embedding 停流与删除分开；本计划不执行不可逆删除。

## 灰度、迁移与回滚

- Python Agent 通过现有 Model Profile 或等价配置按 Skill、Monitor 和环境灰度；
- Shadow 阶段保存脱敏比较指标，不覆盖现有最终决定；
- 切流阶段新旧结果写不同 Run/Revision，最终选择有明确 Profile 版本和审计；
- Agent 连续失败、质量低于门槛、延迟/成本超限或安全校验失败时，关闭 Agent 新 Job，切回已验证旧 Profile，并保留待补算队列；
- 旧 Provider 代码、配置解析、Embedding 表、向量扩展和历史数据在回滚窗口内保留；不得在同一发布中同时切流和删除；
- 只有 Replacement Acceptance 证明所有 P0 Skill、降级、质量、成本、数据兼容和回滚通过后，才能另立清理任务；
- Event/Heat 算法回滚按 Profile 版本切回，不删除新 Snapshot；人工治理事实永不回滚删除，只追加纠正 Revision。

## 完成定义（DoD）

- Python Agent 与 Go Client 通过 Fake、Golden、Shadow 和受信任 Live 四级验证；
- 错误/伪造输出零业务污染，Agent 故障可降级并幂等补算；
- 聚类/Refresh、Relevance/Heat/ClaimEvidence/Event Evidence State、Heat unknown/`warming_up` 和人工治理满足八条 AC；
- 页面五态、移动端与服务端权限一致；
- Schema、OpenAPI、生成客户端、架构检查、目标测试和构建通过；
- 除批准的根目录 `agent/` 外，未新增其他 Python/业务服务、Kafka、Temporal、Elasticsearch、Keycloak、Embedding 或 RAG；
- 未删除旧 Provider、Embedding 代码、Schema 或历史数据，回滚演练有实际证据；
- 同编号 Acceptance 留存比较指标、Live 限制、故障注入和回滚结果后，Plan 才可标为 `completed`。
