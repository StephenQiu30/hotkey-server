---
layer: Plan
scope: shared
doc_no: "025"
title: 配额限流保留与审计治理计划
status: completed
version: v1.1
owner: HotKey Team
phase: P0
canonical_path: docs/plans/archive/025-配额限流保留与审计治理计划.md
design: docs/design/archive/025-配额限流保留与审计治理设计.md
prd: docs/prd/archive/025-配额限流保留与审计治理.md
---

# 配额限流保留与审计治理计划

## 前置门禁与差距

- [x] Design v1.1 accepted，PRD v1.1 approved，003、005、007、017、019、023 与 024 已验收。
- [x] 确认 AI 预算账本已有原子 reserve/settle，来源、采集、AI 和投递用量已有事实表，不复制账本。
- [x] 确认缺口为 active monitor/manual search 硬配额、统一投影、安全保留执行、审计查询和治理界面。

## 预计变更范围

- `backend/db/schema.sql`
- `backend/internal/shared/errors/`
- `backend/internal/modules/operations/`
- `backend/internal/modules/monitor/application/`
- `backend/internal/modules/source/application/`
- `backend/internal/bootstrap/`
- `backend/test/_suite/`
- `frontend/src/app/dashboard/governance/`
- `frontend/src/app/dashboard/settings/`
- `frontend/src/app/dashboard/menuConfig.tsx`
- 生成 OpenAPI 与前端 Client

## 执行顺序

1. [x] 完成现状审计，将硬配额/观测项、唯一事实源和保留白名单写入 Design/PRD。
2. [x] 先补充配额并发、超额数据、保留 dry-run/批量/保护、审计游标与权限的失败测试。
3. [x] 新增最小 `quota_usage_ledgers`，在监控事务中检查 active 上限，在手动队列事务中原子消费日配额。
4. [x] 实现 Admin 治理聚合、保留策略列表/预览/执行与审计筛选游标，真实执行和审计同事务。
5. [x] 注册 API 与权限，生成 Swaggo/OpenAPI 和前端 Client，验证稳定 `10005` 及结构化配额数据。
6. [x] 用 shadcn/ui 官方组件组合 Admin 治理页，在监控页加入 Editor/Admin 手动搜索剩余量；补齐页面状态和单测。
7. [x] 执行后端 CI、OpenAPI、前端类型/单测/构建、Compose、差异与 React 最佳实践审查。
8. [x] 使用 `$agent-browser` 在真实全栈环境验收 Admin/Editor/Viewer、超额、空态、错误恢复、桌面/移动、键盘焦点和 WCAG。
9. [x] 写入 025 Acceptance，将三层文档归档，以 `feat(operations): 完成配额保留与审计治理` 独立提交。

## 测试矩阵

| 层级 | 必验证行为 | 对应 AC |
|---|---|---|
| 领域/应用 | 配额边界、冷却复用、保留参数/保护、权限 | AC-025-1/3/4 |
| PostgreSQL | 并发消费、事务回滚、五维聚合、dry-run、有界删除、游标 | AC-025-1/2/3/4 |
| HTTP/OpenAPI | 200/400/401/403/409/429/503，配额数据、安全 DTO | AC-025-1/2/4 |
| 前端 | 加载/空/错误/筛选/预览/二次确认/角色可见性 | AC-025-2/3/4/5 |
| 浏览器 | 真实主故事、超额、1440/390、键盘、axe | AC-025-5/6 |

## 验证命令

```bash
cd backend && make ci
cd ../frontend && npm run openapi:check && npm run typecheck && npm run test:unit && npm run build -- --webpack
cd .. && docker compose -f docker-compose.yml config --quiet
docker compose --env-file .env.prod -f docker-compose-prod.yml config --quiet
git diff --check
```

## 回滚与完成定义

回滚应用后停止新配额消费和保留入口；`quota_usage_ledgers`、已有审计和已执行的删除结果作为发生过的事实保留，不伪造回复。AC-025-1–6 全部有自动化或浏览器证据，无生成漂移和临时资产，工作树只含 025 变更并创建一个中文 Conventional Commit，方可完成。
