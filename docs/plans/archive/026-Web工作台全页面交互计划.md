---
layer: Plan
scope: frontend
doc_no: "026"
title: Web工作台全页面交互计划
status: completed
version: v1.1
owner: HotKey Team
phase: P0
canonical_path: docs/plans/archive/026-Web工作台全页面交互计划.md
design: docs/design/archive/026-Web工作台全页面交互设计.md
prd: docs/prd/archive/026-Web工作台全页面交互.md
---

# Web工作台全页面交互计划

## 前置门禁

- [x] Design v1.1 accepted、PRD v1.1 approved；002–005、007、021–025 已有 Acceptance。
- [x] 审计全部页面、生成 Client、菜单、仓库内链接、状态与现有测试；确认 026 不需要 Schema 或 API 变化。
- [x] 将过时的“reports/users 缺失”改为真实缺口：导航、旧终点、恢复路径与宽表可访问性。

## 预计变更范围

- frontend/src/app/
- frontend/src/components/ui/
- frontend/src/components/dashboard/
- frontend/src/components/notifications/
- frontend/src/layouts/BasicLayout.tsx
- frontend/src/lib/
- frontend/test/

## 执行步骤

1. [x] 先补导航结构、旧路由/链接、Profile 与订阅错误恢复、宽表区域和共享 fallback 的失败测试。
2. [x] 将八个产品页面放入主导航，Admin 页面留在角色化菜单；移动 Sheet 展示完整入口并删除 `topics`/`favorites` 页面。
3. [x] 修正通知的报告/事件深链，把内部原生 `<a>` 替换为 Next.js `Link`，保留外链和同页锚点。
4. [x] 新增 Dashboard loading/error 组合；为 Profile、订阅、事件、内容和告警补齐错误+重试且与空态互斥。
5. [x] 为所有宽表补充具名键盘滚动区域，检查 1280 桌面导航和 768/360 Sheet 的溢出、焦点与减弱动效。
6. [x] 运行 OpenAPI、TypeScript、全部单测、Webpack 构建、后端 CI、Compose、差异和 React 最佳实践审查。
7. [x] 使用 `$agent-browser` 在隔离真实全栈环境验收 Viewer/Admin 主故事、旧路由 404、错误/空/权限、三视口、键盘和 WCAG。
8. [x] 写入 026 Acceptance，归档 Design/PRD/Plan，并以 `feat(frontend): 完成工作台全页面交互` 独立提交。

## 验证

- `cd backend && make ci`
- `cd frontend && npm run openapi:check && npm run typecheck && npm run test:unit && npm run build -- --webpack`
- 启动完整环境，以浏览器验证登录→监控→采集→事件→告警→报告主故事。
- 根目录运行开发/生产 Compose 配置检查及 `git diff --check`。

## 迁移与回滚

026 不迁移数据或生产任务。回滚前端提交即可恢复旧导航；后端事实、审计、投递、模型运行与 OpenAPI 均不变化。

## 依赖与功能切片

- 前置编号：002–005、007、021–025。
- 依赖未验收时，本条只允许完成不改变对外行为的基础工作。

- [x] Slice-026-1：导航与终点——八个产品入口、Profile/Admin 角色入口、移除旧重定向、修复通知深链。
- [x] Slice-026-2：状态与恢复——共享 fallback、Profile/订阅/事件/内容/告警错误重试和空态互斥。
- [x] Slice-026-3：客户端连续性——内部 Link、URL 查询保留、桌面/移动菜单与焦点恢复。
- [x] Slice-026-4：响应式与可访问性——宽表区域、360/768/1280、Viewer/Admin、axe 与控制台。

## 完成定义

- AC-026-1–6 均有自动化或浏览器证据，且前后端/Compose 全量门禁通过。
- 仓库无 `topics`/`favorites` 页面或引用，生成物无漂移，临时服务和浏览器资产已清理。
- 工作树只含 026 变更，并创建一个符合中文 Conventional Commit 规范的独立提交。
