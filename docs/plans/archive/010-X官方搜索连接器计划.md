---
layer: Plan
scope: backend
doc_no: "010"
title: X官方搜索连接器计划
status: completed
version: v1.0
owner: HotKey Team
phase: P1
canonical_path: docs/plans/archive/010-X官方搜索连接器计划.md
design: docs/design/archive/010-X官方搜索连接器设计.md
prd: docs/prd/archive/010-X官方搜索连接器.md
---

# X官方搜索连接器计划

## 前置门禁

- [x] Design 已 accepted，PRD 已 approved。
- [x] 核对 X 官方 Recent Search、查询运算符、分页、字段和 rate-limit 文档；不使用真实 Token 或非官方接口测试。
- [x] 先保存 SourceType、Schema、查询、分页、限流、凭据、失败分类和 SSRF 的失败测试。

## 预计变更范围

- backend/internal/modules/source/infrastructure/x/
- backend/internal/modules/source/domain/
- backend/internal/modules/source/application/
- backend/db/schema.sql
- backend/test/

## 执行步骤

1. [x] 增加 `x` SourceType、固定端点、Bearer env 约束和唯一 Schema 枚举，保持 RSS/HN 兼容。
2. [x] 实现保守 512 字符查询编译、版本化 `since_id`/`next_token` checkpoint 和官方字段映射。
3. [x] 实现固定主机、DNS/重定向复验、4 MiB 响应上限、配额头和安全错误分类。
4. [x] 接入 ConnectorRegistry 与既有 Capture/CollectionRun 流水线，补齐停用、凭据和健康启用门禁。
5. [x] 更新 DTO/OpenAPI/生成 Client 和 shadcn/ui 来源配置，不创建第二套 API 或存储。
6. [x] 完成自动化、`$agent-browser`、全量门禁和同编号 Acceptance。

## 验证

- `cd backend && make ci`
- `cd frontend && npm run openapi:check && npm run typecheck && npm run test:unit && npm run build`
- 使用可控官方沙盒或 HTTP fixture 验证成功、分页、429、401、超时、恶意重定向和撤销授权。
- 根目录运行 Compose 配置检查与 `git diff --check`。

## 迁移与回滚

新来源默认 disabled，并以能力开关逐一启用。回滚时先停止该来源的新调度、等待运行任务结束，再回退应用；保留检查点、采集运行和历史证据。

## 依赖与功能切片

- 前置编号：006、007。
- 依赖未验收时，本条只允许完成不改变对外行为的基础工作。

- [x] Slice-010-1：实现「X 来源配置与健康检查」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-010-* 回归。
- [x] Slice-010-2：实现「关键词和账号查询」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-010-* 回归。
- [x] Slice-010-3：实现「分页检查点与指标快照」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-010-* 回归。
- [x] Slice-010-4：实现「授权、配额和删除/隐藏同步」，补齐实际涉及的领域、任务、API 与界面，并绑定 AC-010-* 回归。

## 完成定义

- 最近搜索窗口内不漏不重
- 禁用连接或凭据撤销后不再发起请求
- 删除、withheld 和敏感内容按官方字段处理
