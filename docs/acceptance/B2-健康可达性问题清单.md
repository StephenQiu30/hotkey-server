---
layer: acceptance
doc_no: "B2"
audience:
  - PM
  - Dev
  - QA
  - Ops
purpose: "记录部署链路、API/Web/Nginx 可达性与恢复演练结果。"
owner: "StephenQiu30"
inputs:
  - "docs/product/prd/10-部署链路与健康可达PRD.md"
  - "docs/plans/18-部署链路与健康可达计划.md"
outputs:
  - "B2 部署可达性和恢复证据"
triggers:
  - "关闭 Issue #16/#24/#25/#26 前"
downstream:
  - "docs/enterprise-p0-backlog-gap-review.md"
  - "docs/验收差距清单.md"
---

# B2 健康可达性问题清单

验收时间：2026-05-24 15:43-15:48 Asia/Shanghai  
关联 Issue：#16、#24、#25、#26

## 启动链路

执行命令：

```bash
NGINX_PUBLISHED_PORT=8088 docker compose -p hotkey_p0 -f docker-compose.prod.yml up -d --build
docker compose -p hotkey_p0 -f docker-compose.prod.yml ps
```

服务状态：

| 服务 | 状态 | 端口 |
|---|---|---|
| api | healthy | `8000:8000` |
| web | started | `3000:3000` |
| nginx | started | `8088:80` |
| postgres | healthy | `5432:5432` |
| redis | healthy | `6379:6379` |

本轮修复：

- `docker-compose.prod.yml` 增加 `api` 与 `web` 宿主端口，满足直接可达验收。
- `docker-compose.prod.yml` 增加 `nginx` 服务。
- 新增 `deploy/nginx.conf`，统一反向代理 `/api/` 到 API，其余路径到 Web。

## 可达性检查

| 检查项 | 命令 | 结果 |
|---|---|---|
| API 健康 | `curl http://127.0.0.1:8000/api/health` | `200 {"status":"ok"}` |
| Nginx API 健康 | `curl http://127.0.0.1:8088/api/health` | `200 {"status":"ok"}` |
| Web 首页 | `curl http://127.0.0.1:3000/` | `200` |
| Web 工作台 | `curl http://127.0.0.1:3000/app` | `200` |
| Nginx 首页 | `curl http://127.0.0.1:8088/` | `200` |
| Nginx 工作台 | `curl http://127.0.0.1:8088/app` | `200` |

## 恢复演练

执行命令：

```bash
docker compose -p hotkey_p0 -f docker-compose.prod.yml restart api
curl http://127.0.0.1:8000/api/health
curl http://127.0.0.1:8088/api/health
```

时间线：

| 动作 | 时间 |
|---|---|
| API restart 开始 | 2026-05-24T07:47:57Z |
| API health 恢复 | 2026-05-24T07:48:05Z |

恢复结果：API 容器 8 秒内恢复 healthy，API 直连和 Nginx 代理均返回 200。

## 验收结论

- #24 compose 启动链路验证：完成。
- #25 API/Web 可达性验收：完成。
- #26 运维恢复与回滚演练：完成 restart 恢复；镜像回滚沿用 `docker compose pull`/指定镜像 tag 后 `up -d` 流程，当前无失败镜像需回退。
