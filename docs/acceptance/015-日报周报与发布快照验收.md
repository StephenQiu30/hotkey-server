---
layer: Acceptance
doc_no: "015"
audience: [Dev, QA, Ops]
feature_area: 报告生成
purpose: 记录 PLAN-015 的可复核验收证据
canonical_path: docs/acceptance/015-日报周报与发布快照验收.md
status: review
version: v0.1
owner: HotKey Server Team
result: pending
---

# 日报、周报与发布快照验收

当前已实现按订阅时区计算日/周周期、确定性热度排序、报告构建、reports/report_items 事务持久化、发布冻结语义，以及认证后的报告列表、详情、预览与管理员发布 API。发布仅允许草稿进入已发布状态；任何后续原地写入都会被拒绝，读取始终返回冻结的 `report_items` 快照。

尚未完成：Vault 发布 Job、LLM 降级集成、跨时区 API 验收和独立复核；保持 `pending`。

```bash
go test ./internal/modules/report/... -count=1
HOTKEY_TEST_DSN='postgres:///hotkey_plan010_test?sslmode=disable' go test -tags=integration ./internal/modules/report/infrastructure/postgres -run TestReportRepositoryPersistsItemsAndFreezesPublishedVersion -count=1
HOTKEY_TEST_DSN='postgres:///hotkey_plan010_test?sslmode=disable' go test ./test/architecture -run TestOpenAPIContract -count=1
```
