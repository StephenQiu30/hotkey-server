---
layer: Acceptance
scope: shared
doc_no: "005"
title: 安全运维质量与交付验收
status: failed
version: v1.0
owner: HotKey Team
canonical_path: docs/acceptance/005-安全运维质量与交付验收.md
design: docs/design/005-安全运维质量与交付设计.md
prd: docs/prd/005-安全运维质量与交付.md
plan: docs/plans/005-安全运维质量与交付计划.md
verified_revision: "4a40911e8e38fe41ab40faa2635b168a01c13a17"
verification_date: "2026-08-30"
---

# 安全运维质量与交付验收

## 结论

本轮结论为 `failed`，表示 005 整体尚未通过，不是本轮自动化失败。受控合成环境中的 Secret 轮换与零泄露、六类不可信输入和八类入口资源边界、低基数可观测契约，以及 P0 故障告警与 Runbook 已具有可复核证据；Retention 已完成固定候选、独立审批、失败重入、统一时间围栏下的跨读取面撤权、PostgreSQL/Redis/MinIO/Vault/隔离外部 TAR 副本删除、受保护事实指纹与系谱对账复演；River 已完成领取前后、业务事务中、完成标记前与 Provider lost-ack 的统一故障复演；同一固定备份已在两个独立 Compose Project 和全新 PostgreSQL/MinIO/Vault 卷重复恢复，并通过 Schema/OpenAPI、五类资产与失败停止对账。API readiness 现已在接流量前验证运行配置、canonical Schema 和内嵌 OpenAPI；两个远端独立恢复副本的应用回切演练进一步证明三类不兼容实例零业务流量、兼容回切恢复且五类持久资产不变。RC 聚合的八个技术门禁全部通过，单机拆分 API/Worker 的 7 个真实分钟候选可用性/依赖归因也已完成；Operations 001–006 的 planned 可执行性契约已逐份验证，但全部仍待 G6 激活。哈希固定的发布清单、镜像库存、CycloneDX SBOM 和可重建契约已在同一全绿候选运行中闭合。`AC-005-002` 至 `AC-005-007` 通过。

Viewer/Analyst/Editor/Admin 完整人工 UAT、五份 Acceptance 的剩余 P0 AC、16 vCPU/64 GB/2 TB NVMe 候选上限和 W24 观察仍未完成，因此 RC 保持 `blocked`、Plan 保持 `in_progress`、PRD 保持 `approved`。重复恢复报告中的 RPO `0.494` 秒、RTO `10.119`/`8.629` 秒，以及本轮 4 逻辑 CPU/约 16 GB Runner 的 `85.714286%` 故障注入窗口可用性都是固定合成环境的真实测量，只关闭对应方法和隔离演练切片，不批准生产恢复介质、周期、99.5% SLA 或 15 分钟/2 小时无条件承诺；回环 Fake Provider 复演也不替代生产 Provider 的投递批准。

## 验证基线

| 项目 | 实际值 |
|---|---|
| 验证日期 | 2026-08-30（Asia/Shanghai） |
| 代码基线 | `4a40911e8e38fe41ab40faa2635b168a01c13a17`；Retention 固定候选 `38aa114a`，独立审批/失败重入 `098f2b7a`、`f3a9542a`，撤权读取阻断 `d2d01a88`、`86e089ad`、`6e04d674`，MinIO/Vault 删除 `71ea4d91`、`ceefd8ad`，统一围栏/删除对账 `cd8ae953`，备份侧处置 `e6e822b0`，统一撤权复演 `f13080db`；River 全断点与 Provider lost-ack 复演 `ca52b70b`；独立重复恢复 `7da9ab01`；RC `5590c4e1`；可用性 `9371390e`；测试隔离防回归 `5d5c74ee`；planned Runbook dry-run `d36d4d85`；发布清单 `b5a29046`；候选无障碍回归修复 `cdd46d17`；运行时兼容性阻断与非破坏回切 `4a40911e` |
| 项目运行 | 根 `docker-compose.yml`；Go Core、Python Agent、Web、PostgreSQL、Redis、MinIO 六个既有服务全部 `healthy`，本轮验证未重启正式项目容器 |
| 本机验证 | 复用本机 Go/Node.js 工具链；PostgreSQL 固定独立 `hotkey_test`、Redis 固定 DB 15，破坏性测试入口拒绝正式数据库/默认 Redis；`make -C backend retention-rehearsal-acceptance`、`make -C backend river-fault-rehearsal-acceptance`、`make -C backend repeated-restore-rehearsal-acceptance`、`make -C backend runbook-dry-run-acceptance`、发布清单专项测试与隔离真实生成、`make -C backend ci-test`、`make -C backend ci-static` 通过；正式项目六个服务持续 `healthy` 且未重启 |
| 远端验证 | [GitHub Actions 33299892007](https://github.com/StephenQiu30/hotkey-server/actions/runs/33299892007) 在 `4a40911e` 上将 Compose、Backend static/runtime/vulnerability、Worker recovery、Fresh-container browser 业务流/无障碍/容量/可用性/秘密扫描、Python Agent、Frontend 八个执行 Job 与 `All acceptance gates` 全部验证通过；本轮同时验证运行时兼容性阻断与非破坏应用回切，既有发布清单与 CycloneDX SBOM 聚合契约继续通过 |

## AC 状态

| AC | 状态 | 当前证据与未完成边界 |
|---|---|---|
| `AC-005-001` | `partial` | 服务端角色、所有权和会话撤销自动化已固化；完整四角色 UAT 未执行 |
| `AC-005-002` | `passed` | `EV-005-001`：合成凭据全阶段轮换、旧凭据撤销、回退与外发面零泄露 |
| `AC-005-003` | `passed` | `EV-005-002`：SSRF/重定向、Prompt Injection、路径逸出、XSS、压缩炸弹与全入口边界均在副作用前失败停止 |
| `AC-005-004` | `passed` | `EV-005-003`、`EV-005-004`：固定字段/低基数负向矩阵，以及全部 P0 告警的触发、安全关联、恢复清除与五步 Runbook |
| `AC-005-005` | `passed` | `EV-005-005`：固定候选、独立审批、同 Run 重入、统一围栏下跨读取/存储面撤权、受保护事实、隔离 TAR 副本不可读与系谱对账均通过 |
| `AC-005-006` | `passed` | `EV-005-006`：River 全断点崩溃、重试/取消/失败重放、Claim 围栏与 Provider lost-ack 的统一隔离复演通过 |
| `AC-005-007` | `passed` | `EV-005-007`、`EV-005-010`：同一固定备份的两个独立 Compose 恢复、Schema/OpenAPI/五类资产对账、实测 RPO/RTO、四类失败停止及 planned Runbook 可执行性通过 |
| `AC-005-008` | `partial` | `EV-005-008`、`EV-005-009`、`EV-005-010`、`EV-005-011`、`EV-005-012`：单机 Compose、分层 CI、RC 技术聚合、较小 Runner 容量、候选可用性口径、planned Runbook、发布物料及兼容性阻断/非破坏回切技术门禁已闭合；剩余 P0 Acceptance、候选上限硬件、UAT 和发布观察未完成 |

## 证据登记

### `EV-005-001`：合成 Secret 轮换与零泄露

- 映射：`CHK-005-G4-002`、`CHK-005-G4-010` → `AC-005-002`；
- 结果：通过；
- 证据：提交 `7b13a1ccaf7ee103f0bd4e718b8297d51b933a20`、GitHub Actions `33264824771`、[`secret-rotation-github-ubuntu-7b13a1cc.json`](evidence/005/secret-rotation-github-ubuntu-7b13a1cc.json) 和同轮 `hotkey-secret-surface-scan-v2`；
- 结论：JWT/HMAC、来源主密钥、数据库、MinIO、Identity/Delivery SMTP 与 Codex 共 8 类合成凭据完成预检、兼容、滚动、撤销和失败回退，报告不含新旧明文；本证据不宣称生产供应商凭据已轮换。

### `EV-005-002`：不可信输入与全入口失败关闭

- 映射：`CHK-005-G4-003` 至 `CHK-005-G4-009` → `AC-005-003`；
- 结果：通过；
- 证据：固定 SSRF/混淆 IP/逐跳重定向、Prompt Injection、Vault 路径/符号链接、Markdown/XSS/SVG、压缩炸弹和 `p0-request-boundaries-v1` Fixture；对应提交 `52557112`、`a4027876`、`94708947`、`1798190d`、`66920abb`、`62cdcc36` 的重复副作用前拒绝、业务事实零增量和净化审计断言；
- 远端复核：上述切片的独立远端门禁和本基线 `33267679038` 全量回归均通过。

### `EV-005-003`：固定观测字段与低基数

- 映射：`CHK-005-G4-011` → `AC-005-004`；
- 结果：通过；
- 证据：提交 `961ac970`、GitHub Actions `33173721431`，以及 `TestSafeCoreKeepsFixedLogSchemaUnderAdversarialCardinality`、`TestSafeCoreHashesStacksAndRejectsUnapprovedMessages`、`TestMetricsCollapseUntrustedValuesToBoundedLabels`、`TestAccessLogCarriesRequestAndTraceCorrelation`；
- 结论：1,000 组唯一 URL/邮件/正文/Prompt/Payload/动态字段不扩大日志 Schema、可检索维度或 Metric 标签，Request/Trace 关联仍可用。

### `EV-005-004`：P0 故障告警与 Runbook 闭环

- 映射：`TASK-005-S02-T05`、`SPEC-005-OBS-001`、`CHK-005-G5-001` → `AC-005-004`；
- 结果：通过；
- 实现证据：提交 `cfea6fab`、`5aaf148a`、`5cdeed83`、`4101d5c4`、`ec47cabe`、`aeca6612` 依次闭合 River 终态/无 Worker、Delivery `unknown`、来源认证、PG/MinIO 对账、Agent/Codex、Vault、检索、数据库 readiness 与备份失败/过期告警；
- 结构化输出证据：`TestJobRepositoryRuntimeOverviewProjectsActionableRiverAlertsWithSafeCorrelation` 输出安全 Trace/Job/Event 关联；`TestOverviewServiceAppendsBoundedUnknownDeliveryAlert`、`TestJobRepositoryRuntimeOverviewProjectsSourceEvidenceAIVaultAndSearchAlertsFromDurableFacts`、`TestDatabaseReadinessProjectsExternalAlertSignalAndClearsAfterRecovery`、`TestBackupRunFactsTriggerFailedAndStaleAlertsThenClear` 逐类断言固定 Alert ID、策略版本、Owner、影响数、阈值、安全资源 ID 和恢复清除；DTO JSON 负向断言证明 Args、正文、错误详情、路径、对象键、DSN 和凭据不输出；
- 备份事实：`hotkey-backup-run-v1` 成功清单必须且只能包含 PostgreSQL、MinIO、Vault 全量/人工区域和 River Job/Attempt 五类资产；`backup_runs` 只追加安全摘要，相同清单幂等，身份冲突、更新或删除失败停止；失败或 900 秒过期会告警，新鲜完整成功事实清除；
- Runbook：[`004-可观测性SLO与事件响应.md`](../operations/004-可观测性SLO与事件响应.md) 为每个告警固定触发、定位、处置、验证和回滚；备份契约与恢复边界详见 [`002-备份恢复与重建.md`](../operations/002-备份恢复与重建.md)；
- 远端复核：GitHub Actions `33267679038` 的完整 Backend 数据库/Schema/全量测试、Fresh-container browser 业务流与敏感面扫描、Worker recovery 及其他五个 Job 全部通过。

### `EV-005-005`：Retention、撤权与统一围栏闭环

- 映射：`TASK-005-S02-T03`、`TASK-005-S02-T04`、`SPEC-005-DATA-001`、`SPEC-005-OPS-001`、`CHK-005-G5-002` → `AC-005-005`；
- 结果：通过，`CHK-005-G5-002` 已勾选；
- 实现证据：提交 `38aa114a` 新增只追加 `retention_runs` / `retention_run_items` 和 Preview→Approve→Execute；`098f2b7a` 强制发起者与审批者独立，`f3a9542a` 允许失败后沿同一 Run/候选 Hash 重入；`d2d01a88`、`86e089ad`、`6e04d674` 使内容、热点、文档、事件详情/列表/Evidence、搜索、通知深链和缓存在当前 Rights 拒绝后失败关闭并返回 `private, no-store`；`71ea4d91` 与 `ceefd8ad` 分别按不可变路径/对象键、SHA-256 和大小删除 MinIO 原始证据及 `documents/...` Vault 自动投影，保留 PostgreSQL 血缘并追加不可改写删除审计；
- 行为证据：`TestRetentionRunRequiresFrozenPreviewApprovalAndStopsOnCandidateDrift`、独立审批与同 Run 重入测试固定候选和运行身份；`TestRawEvidenceRightsRevocationDeletesExactMinIOObjectAndKeepsImmutableLineage` 证明当前 `store_raw=deny` 覆盖未到期保留及已批准时间例外，重复删除零副作用；`TestDerivedArtifactRightsRevocationDeletesOnlyAutomaticVaultProjectionAndKeepsLineage` 先模拟 reconciliation 阻断，再删除唯一自动投影并证明 Vault 人工文件逐字不变、Artifact/Document 墓碑及路径/Hash/大小/原因审计完整；事件读取集成测试同时证明 Notification Outbox、User Notification、Notification Read Receipt 与 Delivery Attempt 前后完整 JSON 行不变；
- 围栏与备份侧证据：提交 `cd8ae953` 使统一对账固定数据库时间围栏，要求成功备份 Hash 匹配只追加 `backup_runs`，并将 MinIO/Vault 删除审计、缺失幂等、残留 `policy_blocked`、围栏后审计和保护事实收敛到同一 Run；提交 `e6e822b0` 新增双人、只追加 `hotkey-backup-retention-disposition-v1`，对所有恢复点早于最新成功删除的受影响成功备份实施失败关闭；Run 回执固定 `backup_dispositions_verified`，围栏后新登记的旧恢复点不得改变续跑结论；
- 统一复演证据：提交 `f13080db` 新增 `hotkey-retention-rights-rehearsal-v1`，在同一隔离 Fixture、策略版本 51 与数据库时间围栏内重复两次演练；API/通知深链由 `200` 变为 `404` 且始终 `private, no-store`，检索由 1 变为 0，Redis 可丢弃缓存由存在变为不存在，无预签名 URL 暴露；真实 MinIO 原始对象和两份 Vault 自动投影物理删除，Vault 人工文件 Hash 保持不变；Notification Outbox、User Notification、Read Receipt 与 Delivery Attempt 的 ID 和全行指纹前后相同；
- 备份与对账证据：隔离外部文件系统执行器物理删除包含 PostgreSQL、MinIO、Vault 和 River 事实的固定 TAR 副本并验证由可读变为不可读，独立操作者/复核者处置登记通过；Raw/Derived 重放认领均为 0，最终 `reconcile-evidence-lineage --scope all` 等价对账 3/3 健康、Finding/失败均为 0、备份处置验证数为 1；
- 远端复核：[GitHub Actions 33288799039](https://github.com/StephenQiu30/hotkey-server/actions/runs/33288799039) 的八个执行 Job 与聚合门禁全部通过；净化附件 [`retention-rights-rehearsal-github-ubuntu-f13080db.json`](evidence/005/retention-rights-rehearsal-github-ubuntu-f13080db.json) SHA-256 为 `7f3a967ac6d23b8a8043f176a3c0101f5dbf40a3bb11333ad2a153d2595b0706`，绑定完整 revision，`differences=[]` 且不包含对象定位符、正文、路径或凭据；
- 边界：本证据是自动化隔离合成 Fixture，不是生产删除审批，也不把隔离 TAR 副本称为生产备份；planned 手册可执行性已由 `CHK-005-G5-007` 关闭，但在 G6 批准前继续保持 `planned`。

### `EV-005-006`：River 全断点恢复与 Provider lost-ack

- 映射：`TASK-005-S03-T01`、`SPEC-005-JOB-001`、`CHK-005-G5-003` → `AC-005-006`；
- 结果：通过，`CHK-005-G5-003` 已勾选；
- River 断点证据：提交 `ca52b70b` 的真实子进程分别在领取前、领取后、业务事务中与幂等业务事实已提交但 River 完成标记未提交时退出；统一报告同时覆盖可重试失败、取消与永久失败后重放。领取后、事务中和完成标记前三类复演均由租约过期后重领，所有成功路径最终只有 1 个业务副作用；事务中崩溃的未提交副作用回滚，恢复前计数为 0；完成标记前崩溃在恢复前后计数均为 1，证明重放不重复写事实；
- Provider lost-ack 证据：回环 Fake Provider 接收后、本地 Delivery Attempt 提交前退出；支持回执的能力路径固定 1 次发送、1 次查询和 1 条 `succeeded` Attempt，不再次领取；不支持回执的歧义路径固定 1 次发送、0 次查询和 1 条 `unknown` Attempt，同样不盲重发。两条路径均复用稳定派发身份、拒绝 stale Token，Notification Outbox、User Notification、Read Receipt 的受保护全行指纹前后一致；
- 语义边界：报告明确声明 `bounded_at_least_once_with_fencing_idempotency_and_explicit_unknown`，并固定 `exactly_once_claimed=false`；不可确认的外部副作用保持 `unknown`，不以本地唯一键冒充外部 exactly-once；
- 远端复核：[GitHub Actions 33290016956](https://github.com/StephenQiu30/hotkey-server/actions/runs/33290016956) 的八个执行 Job 与聚合门禁全部通过；净化附件 [`river-fault-rehearsal-github-ubuntu-ca52b70b.json`](evidence/005/river-fault-rehearsal-github-ubuntu-ca52b70b.json) SHA-256 为 `cfed7cb2d3362346cbb969058dd7a39b8cc6c17f2232b8b47e625d5c30817d40`，绑定完整 revision，`differences=[]`，且不包含连接串、地址、凭据、收件人、派发键、Claim Token 或 Provider Message ID；
- 边界：该报告来自隔离 PostgreSQL、真实队列/通知仓储和无生产出口的回环 Fake Provider，只证明恢复语义与能力分支，不替代生产 Provider 的交付、回执能力或人工批准。

### `EV-005-007`：同一备份的独立重复恢复与失败停止

- 映射：`TASK-005-S03-T02`、`TASK-005-S03-T03`、`SPEC-005-OPS-003`、`CHK-005-G5-004` → `AC-005-007`；
- 结果：通过，`CHK-005-G5-004` 已勾选；
- 独立恢复证据：提交 `7da9ab01` 的复演器只使用根 `docker-compose.yml`，先在随机隔离 Source Project 生成 PostgreSQL、MinIO、Vault 人工区域与 River Job/Attempt 固定 Fixture，再将同一不可变备份 SHA-256 `d89e3e9ac815837715efbfc1dbb6bb3d876c4d2467b90b5f93c0b33954abe503` 依次恢复到 `restore-a`、`restore-b` 两个不同 Compose Project；每次均新建并最终清理 `postgres_data`、`minio_data`、`vault_data`，不停止或改写正式项目；
- 对账证据：两次恢复均通过当前 Schema 与 OpenAPI Hash 预检及 PostgreSQL 只读兼容验证；`postgres_facts`、`minio_evidence`、`vault_all_files`、`vault_manual_regions`、`river_jobs_attempts` 五类资产各自的预期/实际计数与规范化 SHA-256 全等，`differences=[]`。同轮 Worker Job 还运行既有 M4 投影恢复与联合 PostgreSQL/MinIO/Vault/River 恢复，保持投影重建前置证据连续；
- 实测时间：GitHub 托管 Ubuntu、4 逻辑 CPU 的固定合成 Fixture 中，恢复点到事故围栏的 RPO 为 `0.494` 秒，两次从独立 Project 启动到 Schema、对象、Vault 与 River 全量可读对账完成的 RTO 分别为 `10.119` 秒和 `8.629` 秒，均命中候选门槛；这些数字只描述该报告的数据规模和硬件，不升级为生产 SLA；
- 失败停止：缺失备份固定为 `backup_missing`、损坏备份固定为 `backup_checksum_mismatch`、Schema 不兼容固定为 `schema_incompatible`，三者均不创建目标且在 Mutation 前停止；合法物理包但资产期望不一致固定为 `reconciliation_failed`，只写新的隔离目标并在切流前停止。四类均 `cutover_permitted=false`、`existing_target_overwritten=false`；
- 远端复核：[GitHub Actions 33291250908](https://github.com/StephenQiu30/hotkey-server/actions/runs/33291250908) 的八个执行 Job 与聚合门禁全部通过；净化附件 [`repeated-restore-github-ubuntu-7da9ab01.json`](evidence/005/repeated-restore-github-ubuntu-7da9ab01.json) SHA-256 为 `36a778b3a12ef079331d8468ca3ff08794e739d16b6ec70dd60316fb4cfac243`，绑定完整 revision，不包含连接串、端口、容器/Project 名、对象键、Vault/宿主路径或凭据；
- 边界：该自动化 Fixture 证明恢复流程可重复及失败关闭，不是生产备份执行或恢复切流；目标部署的介质、规模、加密能力、审批与周期仍须在 G6/发布前正式批准，Operations 002 保持 `planned`。

### `EV-005-008`：RC 技术聚合与产品阻断清单

- 映射：`TASK-005-S04-T01`、`SPEC-005-OPS-003`、`CHK-005-G5-005` → `AC-005-008`；
- 结果：部分通过，`CHK-005-G5-005` 保持未勾选；
- 技术证据：[GitHub Actions 33293737954](https://github.com/StephenQiu30/hotkey-server/actions/runs/33293737954) 的 Backend static/runtime/vulnerability、Worker recovery、Frontend、Python Agent、Compose、真实浏览器业务流共 8 个必需技术门禁全部 `success`；`full_ci`、`e2e`、`performance`、`security`、`document_integrity` 均为真，P0 单机 Compose 范围检查通过且未检测到禁用基础设施；
- 净化附件：[`rc-candidate-assessment-github-ubuntu-5d5c74ee.json`](evidence/005/rc-candidate-assessment-github-ubuntu-5d5c74ee.json) SHA-256=`373046456d4ca6e16445de66525e7a65f5b026a2fb143ec6b429344e4dfa8cf6`，绑定完整 revision/run，`differences=[]`、`release_ready=false`、`status=blocked`；
- 产品阻断：该附件忠实记录 `5d5c74ee` 时 001/002/005 Acceptance 的 partial AC 和 003/004 Acceptance 缺失；本批已补建 003/004 部分验收并通过本地 RC 文档复评，将二者收敛为 `AC-003-003:partial` 和 `AC-004-008:partial`。001/002/003/004/005 仍各有未通过 P0 AC，因此不得把技术全绿解释为 RC 发布批准。

### `EV-005-009`：单机候选可用性与依赖归因

- 映射：`TASK-005-S03-T05`、`TASK-005-S04-T03`、`TASK-005-S04-T05`、`SPEC-005-OBS-002`、`SPEC-005-OPS-004`、`CHK-005-G5-006` → `AC-005-008`；
- 结果：部分通过，`CHK-005-G5-006` 保持未勾选；
- 方法：提交 `9371390e` 在全新 `hotkey-ci` Compose Project 中分离 API/Worker，只探测 `/readyz`；采用全部 Probe 均成功才计可用分钟的保守聚合，无维护窗口、无排除分钟，每个真实 60 秒分钟执行 6 次 Probe。演练器拒绝正式 `hotkey` Project，并在结束时恢复/清理隔离栈；
- 结果：`5d5c74ee` 的 GitHub 托管 Ubuntu、4 逻辑 CPU、16,372,440 KiB RAM 环境共 7 个样本/42 次 Probe；Baseline、PostgreSQL 恢复、Redis 故障、MinIO 故障、Worker 故障和全恢复 6 分钟均为 `200/0`，PostgreSQL 故障 1 分钟的 6 次 Probe 均为 `503/90001`，全部 7 个场景归因符合预期，`differences=[]`；观测窗口为 6 分钟可用/1 分钟不可用=`85.714286%`；
- 错误预算：99.5%/30 天候选预算为 216 分钟，本轮注入消耗 1 分钟、剩余 215 分钟；`target_met_in_injected_window=false` 原样保留，不隐藏注入窗口不达标结果；
- 净化附件：[`availability-github-ubuntu-5d5c74ee.json`](evidence/005/availability-github-ubuntu-5d5c74ee.json) SHA-256=`d945b04c6f6af035506f8bccf4f992782ccb1630bd4512da7bc125aa46b642d0`，`status=measured`、`approval=required`、`release_ready=false`、`scope=candidate_methodology_not_production_sla`；
- 边界：该报告证明候选分钟口径和依赖降级归因，也给出较小可运行配置的链路证据；尚未在 16 vCPU/64 GB RAM/2 TB NVMe 候选上限测量资源、拐点和余量，不覆盖 HA、生产流量或外部 Provider，因此不能关闭双配置容量门禁或批准 99.5% 生产 SLA。

### `EV-005-010`：planned Runbook 可执行性与 G6 激活边界

- 映射：`TASK-005-S04-T06`、`SPEC-005-OPS-006`、`CHK-005-G5-007` → `AC-005-007`、`AC-005-008`；
- 结果：通过，`CHK-005-G5-007` 已勾选；
- 实现证据：提交 `d36d4d85dccfc6d8370e5d6b9f6a7811b01888eb` 将 Operations 001–006 的环境参数、隔离目标、可复制命令、预期输出、失败停止点、回滚/恢复、审计字段与仓库入口收敛为固定 `hotkey-planned-runbook-dry-run-v1` 契约；
- 远端复核：[GitHub Actions 33295299129](https://github.com/StephenQiu30/hotkey-server/actions/runs/33295299129) 中 Backend static 专项演练及八个执行 Job/聚合门禁通过；净化附件 [`runbook-dry-run-github-ubuntu-d36d4d85.json`](evidence/005/runbook-dry-run-github-ubuntu-d36d4d85.json) SHA-256=`2c7ad9cfd607de359b465dfe518aa55e5a330ad88b063feb2adf1d915abdbd67`；
- 结论：六份手册的参数、隔离、命令、预期结果、停止点、回滚/恢复、审计和仓库入口八项检查全部为真，逐份及总 `differences=[]`；`activation_eligible=false`，001–006 全部留在 G6 待激活清单；
- 边界：本项只验证文档契约与仓库入口可解析，不连接真实外部 Provider，不执行生产删除、恢复、轮换或发布副作用；真实激活仍须 G6 审批和 Acceptance 演练。

### `EV-005-011`：哈希固定发布清单、SBOM 与重建契约

- 映射：`TASK-005-S04-T07`、`SPEC-005-OPS-006`、`CHK-005-G5-008` → `AC-005-008`；
- 结果：通过，`CHK-005-G5-008` 已勾选；
- 实现证据：提交 `b5a29046` 新增发布镜像库存、严格清单生成器、确定性 CycloneDX 1.6 SBOM、独立 SHA-256 及 CI 聚合门禁；提交 `cdd46d17` 修正按钮颜色的瞬态过渡，使候选桌面与 `390x844` 移动端 WCAG 复验通过；
- 远端复核：[GitHub Actions 33296358099](https://github.com/StephenQiu30/hotkey-server/actions/runs/33296358099) 的八个执行 Job 与聚合门禁全部通过。同轮材料绑定完整 revision `cdd46d1724805e60cd3dc9eecf75fce4f9a21201`：3 个应用镜像以 content-addressed image ID 固定，4 个上游镜像同时固定 image ID 和 repository digest，4 份依赖锁、Schema/OpenAPI/已生成 OpenAPI、5 份安全配置示例、Operations 001–006 和 Go/npm/Python 三类漏洞门禁齐全，`differences=[]`；
- 不可变证据：[`hotkey-release-images.json`](evidence/005/release-candidate-cdd46d17/hotkey-release-images.json) SHA-256=`aae100503526d9b5bc728e9dc72f351ca71e6f1b9f1d593a10e52649d988f595`；[`release-manifest.json`](evidence/005/release-candidate-cdd46d17/release-manifest.json) SHA-256=`4587d48b96d4ffcfa783d530070bf653175debcf76d4db03bcbc587ff0c3c2ca`，独立 [`release-manifest.sha256`](evidence/005/release-candidate-cdd46d17/release-manifest.sha256) 校验通过；SBOM SHA-256=`a86914f2786d0dcdd8cffcb25e7cc6b0cf296c39e0713a146cd3bde22396263c`、共 870 个组件。完整 SBOM 保留在同轮 CI Artifact，仓库以精确 Hash、生成器和锁文件作为无重复副本的可重建证明；
- 边界：清单明确 `approval=automated_material_verification_not_release_approval`、`release_ready=false`，并保留 `remaining_p0_acceptance`、`upper_bound_capacity`、`uat_signoff`、`release_observation` 四项阻断；材料完整不等于发布批准。

### `EV-005-012`：运行时兼容性阻断与非破坏应用回切

- 映射：`TASK-005-S06-T01`、`TASK-005-S06-T03`、`CHK-005-G6-003` → `AC-005-008`；
- 结果：技术证据通过，G6 发布批准未完成，`CHK-005-G6-003` 保持未勾选；
- 实现证据：提交 `4a40911e` 将 API `/readyz` 固定为依赖、运行配置、canonical PostgreSQL Schema、内嵌 Swagger 2.0/API `1.0`/Capabilities/Bearer 合同的失败关闭检查；任一不兼容统一返回 `503/90001`，不向响应暴露底层 Schema 差异、配置值、数据库错误或凭据；
- 远端复演：[GitHub Actions 33299892007](https://github.com/StephenQiu30/hotkey-server/actions/runs/33299892007) 的八个执行 Job 与 `All acceptance gates` 全部成功；其中 `Worker recovery acceptance` 在 4 逻辑 CPU Ubuntu Runner 上完成两个独立 Compose 恢复副本。每个副本分别注入 Schema、OpenAPI、配置不兼容，三类结果均为 readiness `503`、`admitted_business_requests=0`、`mutation_started=false`；兼容回切后均为 readiness `200` 且只放行 1 个受控 Capabilities 请求；
- 非破坏对账：每个副本的 `postgres_facts`、`minio_evidence`、`vault_all_files`、`vault_manual_regions`、`river_jobs_attempts` 均为预期/实际计数 `2/2` 且 SHA-256 全等，逐副本及总 `differences=[]`；既有缺失、损坏、Schema 不兼容和资产差异四类失败停止仍全部在切流前阻断；
- 不可变证据：[`repeated-restore-and-release-rollback-github-ubuntu-4a40911e.json`](evidence/005/repeated-restore-and-release-rollback-github-ubuntu-4a40911e.json) SHA-256=`d194184d69d98cd6eedfb8c2f3f5d1fa4b8f37aa7d2a4e3076ce4c494309ea96`，与 CI 原始 Artifact 逐字节一致，绑定完整 revision `4a40911e8e38fe41ab40faa2635b168a01c13a17`，且敏感面扫描无连接串、地址、Project/容器名、宿主路径或凭据；
- 边界：该结果只证明隔离合成 Fixture 中的技术阻断和无损回切，不执行生产流量切换，不替代回滚责任人、UAT、候选上限硬件或 W24 观察批准。

## 实际验证命令

```bash
make -C backend observability-alert-acceptance
make -C backend observability-cardinality-acceptance
make -C backend retention-rehearsal-acceptance
make -C backend river-fault-rehearsal-acceptance
make -C backend repeated-restore-rehearsal-acceptance
make -C backend runbook-dry-run-acceptance
make -C backend release-manifest-acceptance
make -C backend schema
make -C backend ci-static
(cd frontend && node --test test/operations/availability-rehearsal.test.mjs)
(cd backend && go run ./test/runner test ./internal/modules/operations/... -count=1)
(cd backend && go run ./test/runner test ./internal/platform/database/model -run '^TestSpecsHaveUniqueTablesAndColumns$' -count=1)
(cd backend && go run ./test/runner test -tags=integration -p=1 ./internal/modules/source/infrastructure/postgres ./internal/modules/ingestion/infrastructure/postgres -count=1)
(cd frontend && npm run typecheck)
(cd frontend && npm run test:unit)
(cd frontend && npm run build)
```

远端 `Backend test acceptance` 额外执行完整 `make ci-runtime`，并在 PostgreSQL 16 上验证空库初始化、Schema 指纹、幂等、历史升级/回滚和后端全量测试。

## Schema、API、配置与部署影响

- Schema 在既有只追加 `backup_runs` 之外新增 `retention_runs`、`retention_run_items`、`derived_artifact_deletion_audits` 与 `backup_retention_dispositions`，并为 `evidence_lineage_reconciliation_runs` 固定已验证备份处置数；保存策略/候选边界、不可变 Vault 路径/Hash/大小、运行状态、独立操作/复核者、计数、时间和稳定失败码；触发器禁止删除、候选项/审计/备份处置改写及非法终态迁移；
- 新增 `hotkey maintenance expire-derived-artifacts --batch-size <n> --apply --confirm-delete`，且既有 `expire-raw-evidence` 同样从 PostgreSQL 当前 Rights 与不可变血缘计算候选，不接受任意对象前缀或 Vault 路径；
- 新增 `hotkey maintenance record-backup --manifest <path>` 与 `record-backup-retention-disposition --manifest <path>`，只读取不可变严格清单，不执行备份或备份删除本身；
- Runtime Overview 复用现有安全 DTO；Retention API 由旧直接执行入口收敛为 Preview、Approve、Execute 三段式路径，OpenAPI 与前端生成客户端已同步且无漂移；
- 运维请求边界升级为 `p0-request-boundaries-v2`，覆盖全部三段 Retention 路径；
- 不新增密钥、外部服务、宿主端口或环境变量；Python `agent/` 的数据分析边界未改变；
- planned Runbook 演练只新增仓库契约校验与 CI 证据，不变更 Schema/OpenAPI，不激活任何生产操作入口；
- 发布清单只读取入库的配置示例与脱敏门禁结果，不读取实际 `.env`、容器 `Config/Env` 或凭据；新增的仅为 CI Artifact 与证据文件，不变更运行时 Schema/OpenAPI 或服务编排；
- API readiness 新增只读运行配置、Schema 和内嵌 OpenAPI 精确兼容性检查；不新增 Schema、公开 API、配置项、外部服务或持久写入，检查失败只使当前实例退出流量面；
- 正式项目继续由 Docker Compose 运行，本轮只用本机已安装环境运行测试，没有为验证重复重启项目。

## 已知限制与后续停止条件

- `CHK-005-G5-005`、`CHK-005-G5-006` 与 `CHK-005-G6-001`–`CHK-005-G6-003` 保持未勾选；planned Runbook 可执行性和发布物料完整性已闭合，但剩余 P0 Acceptance、候选上限容量、UAT 和发布观察完成前，005 不得改为 `passed`；
- `ALERT-DB-UNAVAILABLE` 由数据库外 readiness/Metric 投影，数据库不可用时不依赖同一数据库的 Overview 自我报告；
- 备份执行器必须每次追加运行事实；隔离固定 Fixture 的实测 RPO/RTO 不批准生产介质、频率、规模或 SLA，目标部署未复演前不得扩大结论；
- 002 真实 RSS/HN/X 授权冒烟与 003 真实 Provider/Agent 质量对比仍受外部凭据、查询与审查输入阻塞，不使用 Fixture 替代。

## 回滚验证

- 应用可回滚到实现前版本；旧版本忽略新的 `backup_runs`、`retention_runs`、`retention_run_items` 与 `derived_artifact_deletion_audits` 事实，不删除这些表或历史；
- 新清单或备份执行器失败时只追加失败/修复事实，不更新或删除历史；
- Retention 代码回滚不得复活旧直接清理旁路，也不得删除已冻结 Run/Item；生产删除在后续 Runbook 与 G6 审批完成前继续停用；
- 回滚后重新执行 readiness、Schema/OpenAPI 兼容与告警查询；任一数据差异、告警消失或敏感字段输出都必须阻断后续发布。

## 验收人

- 自动化与证据登记：Codex；
- 产品范围与后续 UAT/生产批准：待产品负责人及对应审批人执行；
- 当前结论：`failed`（部分 AC 通过，005 整体未通过）。
