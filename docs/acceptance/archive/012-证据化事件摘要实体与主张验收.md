---
layer: Acceptance
doc_no: "012"
audience: [Dev, QA]
feature_area: 事件智能
purpose: 记录 PLAN-012 的可复核验收证据
canonical_path: docs/acceptance/archive/012-证据化事件摘要实体与主张验收.md
status: accepted
version: v1.0
owner: HotKey Server Team
result: accepted
---

# 证据化事件摘要、实体与主张验收

## 范围与提交

- PRD：`docs/prd/archive/012-证据化事件摘要实体与主张.md`
- Plan：`docs/plans/archive/012-证据化事件摘要实体与主张计划.md`
- 主要提交：`0b5397d`（结构化契约）、`7ca77f2`（AI Run）、`ce542ed`（可追溯持久化）、`ea1abbf`（安全 API）、`59e85fe`（离线评测基线）。

## 验收证据

| 验收项 | 证据 | 结论 |
|---|---|---|
| 证据绑定与安全结构 | Event Summary/Claim 输出只接受本次 active evidence；模型提供的摘录会被可信快照替换 | 通过 |
| 幻觉和撤回证据 | `test/fixtures/event-intelligence/v1/acceptance.json` 覆盖无引用结论、快照外引用和已撤回 evidence；三条摘要、两条提取候选均被拒绝且不会持久化 | 通过 |
| 无 Provider 降级 | fixture 验证摘要返回代表 Content 的 `fallback-v1`，提取不写入任何事实 | 通过 |
| 人工/API 边界 | 只读 intelligence 查询需认证；重生成仅 editor/admin；响应不含 prompt、provider、credential 或原始结构化结果 | 通过 |
| 总体回归 | PostgreSQL 16 + pgvector、Redis DB 15 上的完整质量门禁 | 通过 |

执行的关键命令：

```bash
go run ./test/runner test ./internal/modules/event/application \
  ./internal/modules/intelligence/application -run EventIntelligenceEvaluation -count=1

HOTKEY_TEST_DSN='postgres://root@localhost:5432/hotkey_test?sslmode=disable' \
HOTKEY_TEST_REDIS_URL='redis://127.0.0.1:6379/15' \
make ci
make clean
```

`make ci` 已通过 OpenAPI 漂移校验、Vet、真实 PostgreSQL 运行时验证、集中测试套件、构建、架构/仓库校验和 Schema 重复执行。评测不调用真实模型、网络或开发数据。River 编排、Vault 投影、报告与投递仍由下游 PLAN-013–016 负责，不属于本验收范围。
