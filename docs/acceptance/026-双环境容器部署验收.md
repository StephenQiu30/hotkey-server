---
layer: Acceptance
doc_no: "026"
audience: [Dev, QA, Ops]
feature_area: 容器部署
purpose: 验收 Server 双环境 Compose 与生产镜像
canonical_path: docs/acceptance/026-双环境容器部署验收.md
status: review
conclusion: pending
version: v0.3
owner: HotKey Server Team
inputs:
  - docs/design/020-双环境容器部署设计.md
  - docs/prd/026-双环境容器部署.md
  - docs/plans/026-双环境容器部署计划.md
outputs:
  - PLAN-026 长期验收结论
---

# 双环境容器部署验收

## 实施前 RED/GREEN 映射

| 验收项 | RED 或负测 | GREEN / 通过标准 | 状态 |
|---|---|---|---|
| 文件、镜像与环境契约 | 三个 `TestDocker*` 因容器文件缺失或契约不符失败 | 定向架构测试全部通过；Go 1.26、非 root、CA/时区/wget、项目名与镜像标签均被断言 | pending |
| 生产配置隔离 | 缺 `.env.prod` 时生产 `config --quiet` 非 0；空密钥时生产服务启动失败 | 生产 Compose 只引用 `.env.prod`，完整配置可解析和启动 | pending |
| 数据库状态机 | 非空不兼容库的 `db-init` 非 0且占位表保留 | 空库并发两个初始化器均为 0；兼容库重复执行为 0 | pending |
| Bucket 幂等 | MinIO 不可用时 init 保持重试而非伪成功 | MinIO 可用后连续两次 init 均为 0，目标 Bucket 存在 | pending |
| 日常与生产健康 | 任一 `/readyz` 非 200 或容器不健康即失败 | 两个独立项目同时运行，两个 `/readyz` 均为 200 | pending |
| 生产网络与关闭 | 任一基础设施 `docker compose port` 返回发布端口即失败 | 仅 Server HTTP 发布；StopTimeout 为 30；生产镜像用户非 root | pending |
| 工程回归 | Schema/OpenAPI 漂移、测试或门禁任一非 0即失败 | `make lint/test/build/validate/schema-verify/openapi-check`、`git diff --check` 全部为 0 | pending |

## 实施后证据占位

- 目标提交：pending
- Docker / Compose 版本：pending
- 静态 RED：pending
- 静态 GREEN：pending
- 两份 Compose 原生命令构建与启动：pending
- 完整 Go 回归：pending
- Schema / OpenAPI 零差异：pending
- 非主要实施者最终复核：pending

## 结论

等待 PLAN-026 readiness 复审、测试先行实施、真实容器验证和独立最终复核。
