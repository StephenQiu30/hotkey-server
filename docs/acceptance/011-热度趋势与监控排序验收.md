---
layer: Acceptance
doc_no: "011"
audience: [Dev, QA, Ops]
feature_area: 热度与排序
purpose: 记录 PLAN-011 的可复核验收证据
canonical_path: docs/acceptance/011-热度趋势与监控排序验收.md
status: accepted
version: v0.4
owner: HotKey Server Team
result: passed
---

# 热度趋势与监控排序验收

提交 `46ac2e7` 完成来源 capability profile 的 draft/published/archived 状态机、管理员创建/发布/归档理由审计、已发布记录不可改写、同一 source_type 的单一已发布版本，以及供 Event 使用的窄只读端口。

提交 `b90e478` 完成 PLAN-011 其余范围：以真实 active、非 duplicate、有效证据为输入的 1/6/24h 同步重算；稳定的 `evidence_set_hash` 与 `capability_profile_set_hash`；档案切换追加历史快照；Design-010 六分量热度、EMA 趋势和 Monitor 排序；Event/Monitor 原子投影；内容进入或删除后的 Event 所有权刷新桥接；安全的 API/OpenAPI 原因码与版本字段；以及版本化 Top-20 recall/false-positive 基线。

Cron/River 的调度、去重、重试与长期线上质量仍由 PLAN-013/017 验收，不属于 PLAN-011 的同步重算交付范围。

## 当前证据

| 提交 | 范围 | 已验证证据 |
|---|---|---|
| `46ac2e7` | capability profile 的状态机、管理员 API/RBAC、审计、事务锁、Schema/OpenAPI | `HOTKEY_TEST_DSN="$HOTKEY_TEST_DSN" HOTKEY_TEST_REDIS_URL="$HOTKEY_TEST_REDIS_URL" go test -tags=integration ./internal/modules/source/application ./internal/modules/source/infrastructure/postgres ./internal/modules/operations/infrastructure/postgres ./internal/platform/database -count=1`；`HOTKEY_TEST_DSN="$HOTKEY_TEST_DSN" HOTKEY_TEST_REDIS_URL="$HOTKEY_TEST_REDIS_URL" make ci` |
| `b90e478` | 版本化 Event 快照、热度/趋势/排序、同步触发、Event/Monitor 投影、内容刷新桥接、Schema/OpenAPI、Top-20 基线 | `HOTKEY_TEST_DSN="$HOTKEY_TEST_DSN" HOTKEY_TEST_REDIS_URL="$HOTKEY_TEST_REDIS_URL" go test -race -tags=integration ./internal/modules/source/... ./internal/modules/event/... ./tests/architecture -run 'Metric|Heat|Trend|Ranking|Recompute|OpenAPI|Schema' -count=1`；`HOTKEY_TEST_DSN="$HOTKEY_TEST_DSN" HOTKEY_TEST_REDIS_URL="$HOTKEY_TEST_REDIS_URL" make ci` |

上述验收记录是静态的治理证据，不参与服务启动或请求处理；运行时不读取或校验此 Markdown 文件。

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-07-17 | 初始验收草案，记录存在的脚手架与待补项。 |
| v0.2 | 2026-07-17 | 按 PLAN-011 准入复审把能力档案、真实证据同步重算、档案集合哈希快照身份、Design-010 公式和 Top-20 基线写为最终验收边界。 |
| v0.3 | 2026-07-17 | 记录 `46ac2e7` 完成能力档案、管理员审核、审计、事务锁、Schema/OpenAPI 与完整 CI；其余热度重算和 Top-20 验收仍待实施。 |
| v0.4 | 2026-07-17 | `b90e478` 完成 PLAN-011 同步重算、历史快照、投影、API/OpenAPI、内容刷新桥接与 Top-20 基线，验收通过。 |
