---
layer: acceptance
doc_no: "B1"
audience:
  - PM
  - Dev
  - QA
  - Ops
purpose: "记录企业级 P0 生产环境回放验收结果与凭据可用性边界。"
owner: "StephenQiu30"
inputs:
  - "docs/product/prd/09-生产环境回放验收PRD.md"
  - "docs/plans/17-生产环境回放与凭据验收计划.md"
outputs:
  - "B1 回放命令、结果和不可达凭据证据"
triggers:
  - "关闭 Issue #15/#21/#22/#23 前"
downstream:
  - "docs/enterprise-p0-backlog-gap-review.md"
  - "docs/验收差距清单.md"
---

# B1 回放证据包

验收时间：2026-05-24 15:45-15:47 Asia/Shanghai  
关联 Issue：#15、#21、#22、#23

## 环境与凭据检查

运行环境使用 `docker compose -p hotkey_p0 -f docker-compose.prod.yml up -d --build`。

| 项目 | 状态 | 结论 |
|---|---|---|
| PostgreSQL | 已配置并启动 | `postgres:16-alpine` healthy，schema 自动初始化 |
| Redis | 已配置并启动 | `redis:7-alpine` healthy |
| OpenAI 兼容模型 | 未配置真实密钥 | `AI_PROVIDER_ERROR_STRATEGY=fallback` 生效，回放使用本地 fallback |
| X/Twitter | 未配置 `X_API_BEARER_TOKEN` | 真实外部源不可达，记录为凭据缺失 |
| Bing | 未配置 `BING_SEARCH_API_KEY` | 真实外部源不可达，记录为凭据缺失 |
| SMTP | 未配置 `SMTP_*` | 通知路径落库为 `skipped`，原因：`SMTP is not configured.` |

## 回放执行

执行动作：

```bash
curl -X POST /api/auth/register
curl -X POST /api/keywords
curl -X POST /api/check-runs
curl -X POST /api/reports
```

关键结果：

| 对象 | 结果 |
|---|---|
| 注册用户 | 成功，生成 Bearer token |
| 关键词 | 创建 2 个回放关键词，后续权限演练新增 1 个管理员关键词 |
| 检查任务 | `id=1`，`trigger_type=manual-p0-acceptance` |
| 检查状态 | `completed_with_errors` |
| 成功数 | `176` |
| 失败数 | `1` |
| 错误摘要 | AI fallback 被使用；真实模型密钥缺失导致降级 |
| 日报 | `id=1`，`status=skipped`，`hotspot_count=10` |
| 邮件通知 | report notification `status=skipped`，原因 `SMTP is not configured.` |

数据库复核：

```text
keywords=3
sources=6
hotspots=176
ai_analyses=176
notifications=112
check_runs=1
reports=1
```

## 验收结论

- #21 环境与凭据检查：完成。真实外部凭据缺失已记录，不作为代码阻塞。
- #22 生产环境真实回放执行：完成。PostgreSQL、API、回放任务、报告与通知落库链路均可复验。
- #23 真实外部调用验证：有条件完成。X/Twitter、Bing、OpenAI、SMTP 未配置真实凭据；系统在缺失凭据时按 fallback/skipped 策略不中断主闭环。

剩余运行建议：生产交付前由运维填入真实外部凭据后，复用本文命令再次取得真实第三方成功记录。
