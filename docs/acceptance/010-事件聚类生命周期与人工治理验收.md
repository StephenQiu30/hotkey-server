---
layer: Acceptance
doc_no: "010"
audience: [Dev, QA, Ops]
feature_area: 事件治理
purpose: 保存 PLAN-010 事件聚类、生命周期和人工治理的可复核验收证据
canonical_path: docs/acceptance/010-事件聚类生命周期与人工治理验收.md
status: review
version: v0.3
owner: HotKey Server Team
inputs:
  - docs/prd/010-事件聚类生命周期与人工治理.md
  - docs/plans/010-事件聚类生命周期与人工治理计划.md
  - docs/design/009-事件发现聚类与生命周期设计.md
outputs:
  - PLAN-010 验收结论与长期回归门禁
triggers:
  - PLAN-010 Task 1–6 完成
  - 聚类、生命周期、合并拆分或人工锁回归结论变化
downstream:
  - docs/prd/010-事件聚类生命周期与人工治理.md
  - docs/plans/010-事件聚类生命周期与人工治理计划.md
result: pending
---

# 事件聚类、生命周期与人工治理验收

## 当前结论

本文件是实施前验收模板。PLAN-010 尚未开工，当前结论为 `pending`；在 PRD/Plan 独立评审通过并进入 `ready` 前，不得提交 event 实现代码。

## 验收范围与禁止越界

- 只验收已通过 Monitor 相关性判断的 active Content 到 Event 的聚类、成员、生命周期、合并、拆分、证据重算和人工锁。
- 不验收热度趋势、实体/Claim、AI 摘要、队列/调度、知识库、报告投递或新增 AI task type。
- 合并/拆分只验证 `event_contents`、`monitor_events` 和 `merged_into_id`；实体、Claim、Topic、Report 只验证 `downstream_reconcile_required` 审计事件，不验证其迁移实现。
- 记录准确实现提交 SHA、Schema/record/OpenAPI 变更；不记录真实 DSN、密钥、Provider 原文或本机绝对路径。

## 验收环境与 Fixture

- PostgreSQL/Redis 使用可丢弃 fixture：`HOTKEY_TEST_DSN='postgres:///hotkey_plan010_test?sslmode=disable'`、`HOTKEY_TEST_REDIS_URL='redis://127.0.0.1:6379/15'`。
- 固定数据集版本：`testdata/clustering/v1`（中英文同一事件、同名实体不同事件、跨时间窗口、转载/更正/撤回、迟到内容、硬冲突、合并和拆分）。实施时补充 SHA、样本数、分组切分和标注冲突决议。
- 若实现使用 MinIO 原始证据，必须使用专用 disposable fixture 并在验收后明确停止；否则不得声称 MinIO 已验收。

## 红灯证据（实施前必须保留）

| 验收项 | 命令 | 预期失败信号 |
|---|---|---|
| 领域与幂等 | `go test ./internal/modules/event/domain ./internal/modules/event/application -run 'Test(Cluster|Event|Lifecycle|Merge|Split)' -count=1` | 事件领域包/契约不存在或固定测试失败 |
| 候选边界 | `go test ./internal/modules/event/... -run 'TestRecallCandidateLimit|TestClusteringEvaluation' -count=1` | 候选未限制 50 或 F1/固定标签断言未满足 |
| 事务故障 | `go test -tags=integration ./internal/modules/event/... -run 'TestMerge|TestSplit|TestConcurrent' -count=1` | 事务、锁或回滚测试失败 |

红灯只能由真实缺失行为产生，禁止跳过、删除或放宽测试断言；实现完成后同一命令必须转为 GREEN。

## 必须通过的 GREEN 证据

| 能力 | 证据 |
|---|---|
| 聚类质量与候选上限 | 固定集 F1 ≥ 75%；词法/Monitor ≤16、时间/来源 ≤12、事实指纹 ≤10、同 profile 向量 ≤12，合并去重后每条 Content 精排候选 ≤50；硬冲突不得被向量分数覆盖；无 Event embedding 时有明确规则降级 |
| 决策审计与归属 | `event_clustering_decisions` 保存每个候选的版本、feature_input_hash、通道/排名、五项分数、证据、actor 和 reason；`event_governance_audits` 保存前后状态、source/target、expected_version；非 duplicate Content 只有一个当前 Event 归属 |
| 幂等与稳定键 | 相同输入重跑不创建重复 Event/成员；标题、摘要、成员变化不改变 event_key；独立证据仍保留 |
| 生命周期与证据 | detected/active/cooling/closed/archived/rejected/merged 迁移、迟到、撤回和删除重算均有状态/原因/版本记录 |
| 人工治理 | 事件字段和成员 `manual_locked` 在自动重算、重试和并发写入后保持有效；人工拒绝不会被重复建议 |
| 合并/拆分 | canonical root、防环、目标已 merged、重复关系、人工锁、失败重试和 `downstream_reconcile_required` 均有证据；成员、Monitor 关系、审计、merged_into_id 在单事务内迁移，故障注入回滚后不存在半迁移关系 |
| API 与安全 | `GET /api/v1/events`、`GET /api/v1/events/:id`、`GET /api/v1/events/:id/contents` 为 viewer；`POST /api/v1/events/:id/contents/:content_id/lock` 为 editor，支持 `locked`/解锁；生命周期/合并/拆分为 admin；merge 校验源/目标版本，split 校验源事件和逐成员版本；陈旧目标、陈旧成员、锁定成员和并发操作均返回 409 且无部分写入；错误有 400/404/409 numeric code，不泄露正文、Prompt、密钥、Provider 原文或 SQL |

## 运行命令（完成后填写提交和摘要）

```bash
go test ./internal/modules/event/domain ./internal/modules/event/application -count=1
go test ./internal/modules/event/... -run 'ClusteringEvaluation|RecallBound|Lifecycle|ManualLock' -count=1
HOTKEY_TEST_DSN='postgres:///hotkey_plan010_test?sslmode=disable' \
HOTKEY_TEST_REDIS_URL='redis://127.0.0.1:6379/15' \
go test -race -tags=integration ./internal/modules/event/... \
  ./internal/platform/database/... ./tests/architecture -count=1
HOTKEY_TEST_DSN='postgres:///hotkey_plan010_test?sslmode=disable' \
HOTKEY_TEST_REDIS_URL='redis://127.0.0.1:6379/15' make ci
make clean
git diff --check
git status --short
```

## Schema、OpenAPI 和回滚证据

实施时必须记录完整 `db/schema.sql`、database record/catalog、约束和索引测试；API 变更必须同步 `docs/openapi/swagger.json` 并通过架构 OpenAPI 校验。Schema 升级/回退写入 `docs/operations/plan010-schema-upgrade.md`，只针对可丢弃数据库演练，不运行时 DDL，不记录真实凭据。

## 独立复审与归档条件

- [ ] 非主要编写者复核实现提交范围、Plan/PRD 契约、Schema/record/catalog、OpenAPI、并发/回滚、权限/脱敏和上述命令。
- [ ] Reviewer 结论为 `APPROVED`，无未解决 Critical/Important。
- [ ] 填写准确 baseline、实现 SHA、测试摘要、残余风险和未执行项目。
- [ ] 仅在上述条件全部满足后，将本文件改为 `status: accepted`、`result: accepted`，并把 PRD/Plan 归档为 `archived/done`。

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-07-17 | 创建实施前验收模板，固定范围、红灯/绿灯、聚类评测、事务/权限/脱敏和独立复审门禁；尚未执行。 |
| v0.2 | 2026-07-17 | 按独立复核补齐决策/审计 Schema、当前归属唯一性、四通道召回与向量降级、关系所有权、canonical 合并拆分和具体 API 权限；仍待独立复审。 |
| v0.3 | 2026-07-17 | 补齐成员级锁/解锁 endpoint、多资源 expected_version、逐成员拆分重验及陈旧/并发 409 无部分写入门禁；仍待独立复审。 |
