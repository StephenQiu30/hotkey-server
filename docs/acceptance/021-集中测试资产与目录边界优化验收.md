---
layer: Acceptance
doc_no: "021"
audience: [Dev, QA, Ops]
feature_area: 工程目录与测试基础设施
purpose: 验收集中 fixture、runner 物化与目录防回流门禁
canonical_path: docs/acceptance/021-集中测试资产与目录边界优化验收.md
status: review
conclusion: pending
version: v0.5
owner: HotKey Server Team
inputs:
  - docs/prd/021-集中测试资产与目录边界优化.md
  - docs/plans/021-集中测试资产与目录边界优化计划.md
outputs:
  - PLAN-021 长期验收结论
triggers:
  - PLAN-021 开始实施
downstream:
  - docs/acceptance/README.md
---

# 集中测试资产与目录边界优化验收

## 实施前标准审核

| 项目 | 状态 | 证据 |
|---|---|---|
| 只迁移纯测试资产，不改业务包 | passed | 独立 Plan Review APPROVED |
| runner 发现、冲突和清理边界 | passed | 独立 Plan Review APPROVED |
| 原包内 testdata 读取语义保持 | passed | 独立 Plan Review APPROVED |
| 架构防回流与回归命令完整 | passed | 独立 Plan Review APPROVED |

## RED/GREEN 证据

| 类型 | 命令/证据 | 状态 |
|---|---|---|
| 目录 RED | `go test ./test/architecture -run TestGreenfieldLayout -count=1` | passed：迁移前准确报告 5 个位于 `internal` 的 `testdata` 目录 |
| runner RED | `go run ./test/runner test ./test/runner -run 'Test(SuiteEntries\|Materialize\|RunCleansLinksAfterChildFailure)' -count=1` | passed：实现前因缺少 `suiteEntries`、`run` 编译失败 |
| runner GREEN | `go run ./test/runner test ./test/runner -count=1` | passed |
| 目录 GREEN | `go test ./test/architecture -run TestGreenfieldLayout -count=1` | passed |
| fixture 回归 | Plan Task 4 精确命令覆盖 event、ingestion、intelligence、RSS、Hacker News 六个实际消费包 | passed |
| 运行时 Schema 保护 | SchemaRegistry 定向测试通过，`git diff --exit-code -- internal/modules/intelligence/schemas` 零差异 | passed |
| 工程门禁 | `make lint`、`make build`、`make validate`、`git diff --check` | passed |
| 生成物保护 | `git diff --exit-code -- db/schema.sql docs/openapi` | passed |
| 目录残留 | `internal` 无 `testdata`，runner 退出后 `internal` 与 `test/runner` 无符号链接，集中区包含 10 个 fixture 文件 | passed |
| 完整 CI | `make test` 在入口处因未配置隔离 `HOTKEY_TEST_DSN` 被阻止；Redis/外部依赖未执行 | blocked |
| 独立实现复审 | 无 P0/P1/P2 | pending |

## 结论

`pending`：实现和本地可执行门禁已完成，等待独立实现复审；需要数据库的完整 CI 保持 blocked，不将其记为通过。

## 变更记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v0.1 | 2026-08-01 | 建立实施前验收模板，等待独立评审。 |
| v0.2 | 2026-08-01 | 按独立评审补充子进程失败清理和运行时 Schema 保护验收项。 |
| v0.3 | 2026-08-01 | 记录 runner/目录 GREEN 与全包回归的真实 PostgreSQL 环境阻塞，fixture 回归命令收窄后等待复审。 |
| v0.4 | 2026-08-01 | 记录精确 fixture 回归、工程门禁、生成物保护和目录残留检查结果，完整 CI 因缺少隔离数据库保持 blocked。 |
| v0.5 | 2026-08-01 | 按独立实现复审修复子进程失败时清理错误静默及绝对路径泄露，并增加组合故障回归测试。 |
