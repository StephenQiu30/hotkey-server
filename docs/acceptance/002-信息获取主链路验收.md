---
layer: Acceptance
scope: epic
doc_no: "002"
title: 信息获取主链路验收
status: failed
version: v0.5
date: 2026-09-27
owner: HotKey Team
canonical_path: docs/acceptance/002-信息获取主链路验收.md
prd: docs/prd/002-信息获取主链路需求.md
design: docs/design/002-信息获取主链路设计.md
---

# M1 信息获取主链路验收

本记录登记 Plan 001、002、033 的技术交付与 M1 仍未验收的边界。实施基线为 `88a3628d`，当前工作树未提交；Claude 于 2026-09-27 在本机新建空库 `hotkey_test` 执行真实 PostgreSQL 复验，结果由用户提供。Plan 001、002、033 的本卡技术交付已完成；M1 产品验收及父级 AC-002-002/004/005/006/007 均未最终通过，因此本里程碑 Acceptance 保持 `failed`。

G0 代码基线：`connections/presets.py` 仅有来源端点配置、能力和单个日预算，B 站无执行策略；`connections/schemas.py` 只有严格的非秘密 `SourceConnectionConfig`，无节奏 DTO；`connections/services.py` 的预设应用已共用调用方事务并按准入/保留/组件/预算变化升连接版本，但版本只保存端点 `config`，且重应用会重新启用停用连接；`jobs/services.py` 的窗口由预算 policy ID 与起点定位，预设预算键虽稳定，但同来源/指标/窗口可另建预算键。这些均以当前工作树修改前的 HEAD `88a3628d` 为准。

## Plan 002 局部证据

| 检查 | 当前结果 | 证据与缺口 |
|---|---|---|
| CHK-002-G0-001 | 通过 | 历史 `68f1b02b:docs/acceptance/046-全局异常与响应契约验收.md` 记录 S03 全项 passed，含 AC-046-006 真实本地上游；当前连接版本仅有 `config`，预设仅有日预算，B 站节奏仍在 scheduler/config 硬编码。stash@{0} 的 `SourceCollectionCadence` 未按当前字段/事务持久化，未应用。 |
| CHK-002-G1-001 | 通过 | `SourceExecutionPolicy` 的字段与跨午夜判定冻结；连接版本增加独立非秘密 JSONB 快照，来源预算保持稳定策略行与窗口，保留库先走 051。 |
| CHK-002-G2-001 | 完成，保留证据边界 | 非法字段先行测试实施前因缺 `SourceExecutionPolicy` 失败；后续 psycopg 方言测试先复现 `Jsonb(None)` 再验证修复。预算换版与半写事务的测试先行保存并在真实库转绿；当时没有它们的实施前真实库 Red 运行记录。 |
| CHK-002-001 | 通过 | `test_source_presets.py` 覆盖字段、B 站上限、跨午夜、四关键词来源和六榜默认快照；psycopg 方言绑定测试确认可空策略的 Python `None` 落为 SQL NULL。与三份集成测试一起进入 EV-002-001 的 106 passed。 |
| CHK-002-002 | 通过 | `test_source_connections.py` 在真实隔离 PostgreSQL 验证幂等、升版、旧版策略读取及注入失败后的全事务回滚；见 EV-002-001。 |
| CHK-002-003 | 通过 | `test_resource_budgets.py` 验证升版不返还窗口额度，`test_monitor_topics.py` 验证安全停用不能由预设重应用解除；无预算写入时连接不启用的场景由连接集成测试覆盖；见 EV-002-001。 |
| CHK-002-004 | 通过，限配置核对 | 真实库集成测试覆盖公开 HN 预设两次幂等应用、非秘密策略升版、旧版快照、预算账本和失败回滚；CLI 输出经代码审查确认脱敏，未另跑独立 CLI 端到端命令；见 EV-002-001/003。应用预设不等于来源采集验收。 |
| CHK-002-G3-001 | 通过 | CHK-002-001—003 的受控断言在真实库复验中通过；按指定连接版本读取策略可供后续卡消费，见 EV-002-001。 |
| CHK-002-G4-001 | 通过 | 本地 Ruff/mypy 与 EV-002-001/002 的隔离库 pytest 通过；HTTP 响应契约未变，索引 C 不触发。 |
| CHK-002-G5-001 | 通过，限配置核对 | CHK-002-004 的真实库版本/账本与 CLI 代码脱敏核对完成，见 EV-002-001/003；不含来源采集或重试实效。 |
| CHK-002-G6-001 | 已记录 | 下节分别列 AC-002-002/007 的策略交付和未验条件，父级 AC 保持未通过。 |

## EV-002 真实库与受控证据

- **EV-002-001（2026-09-27，Claude 执行，用户提供结果）**：本机新建空库 `hotkey_test`，用 `psql -X --set ON_ERROR_STOP=on -f backend/database/schema.sql` 原子应用当前 Schema。`backend/tests/integration/test_source_connections.py`、`test_resource_budgets.py`、`test_monitor_topics.py` 与 `backend/tests/unit/test_source_presets.py` 合计 **106 passed、0 failed**。此前暴露的 JSONB `None` 绑定和多行保留策略测试断言均已修复并纳入这次复验。
- **EV-002-002（2026-09-27，Claude 执行，用户提供结果）**：`HOTKEY_TEST_DATABASE_URL` 指向上述隔离库的全量 pytest 为 **743 passed、17 skipped、0 failed**。17 项跳过均为本卡外部环境：Kafka 4（`test_worker_messaging.py:20`、`test_webpage_persistence.py:1031`、`test_job_reliability.py:1405/1292`，缺 `HOTKEY_TEST_KAFKA_BOOTSTRAP_SERVERS`）；Redis/MinIO 生命周期 2（`test_data_lifecycle.py:536/414`）；隔离浏览器 6（`test_browser_collection.py`，未显式启用隔离浏览器服务）；备份恢复 5（`test_backup_restore.py` 的 4 项缺 PostgreSQL/MinIO 专用配置，`:181` 缺 MinIO 配置）。这些跳过不属于 Plan 002 验证条件。
- **EV-002-003（2026-09-27，Claude 代码核对，用户提供结论）**：`app/cli/commands.py` 的 `apply_source_preset` 成功输出仅含 `source_key`、`connection_id`、`version`、`capabilities`，不输出 `secret_ref` 或 `config`。版本、策略和账本的实际数据库断言由 EV-002-001 覆盖；未另跑独立 CLI 端到端命令。

Codex 沙箱先前运行 `ruff format --check .`、`ruff check .`、`mypy` 均通过；无 `HOTKEY_TEST_DATABASE_URL` 时的全量 pytest 为 557 passed、203 skipped，仅作为本地静态与非数据库回归证据。Plan 002 的真实数据库结论以 EV-002-001/002 为准。

本卡未改变任何 HTTP 路由、响应模型或 API Schema 引用，`SourcePresetApplyView` 保持原字段；索引 C 的 OpenAPI 客户端生成条件未触发。新策略 DTO 当前只用于预设写入和服务端按版本读取。

Codex 沙箱自身仍无法连接 Docker/PostgreSQL，未触碰保留业务库；这不影响 EV-002-001/002 在本机独立隔离库取得的结果。测试库结果不作为生产保留库迁移或来源真实采集证据。

## 父级 AC 与剩余条件

- AC-002-002：固定版本策略、预设事务与真实库受控验证完成；031 调度实际消费、逐来源真实连接和共同 72 小时窗口未验。
- AC-002-007：来源稳定预算键与真实库受控验证完成；032 人工/自动重试周期与真实重试未验。
- 回滚只针对本工作树变更；`schema.sql` 仅用于全新空库，保留库按 051 恢复演练处理。未修改或重置任何既有预算账本。

## Plan 033 到期窗口与采集计量事实（技术交付完成）

G0：历史 `68f1b02b:docs/acceptance/046-全局异常与响应契约验收.md` 的 046 S03 真实通过记录沿用；Plan 002 的 `SourceExecutionPolicy` 已按连接版本保存，来源预算的稳定策略行和窗口已在本工作树。实施前 `monitor_schedules` 仅保留 `next_run_at`，`coverage_windows` 只有 Job 执行范围，均不能表示尚未受理的到期点。

G1：新增 `collection_due_windows` 的 `(owner_id, schedule_key, due_at)` 唯一约束；`[window_start, window_end)` 和 UTC 到期点分开保存。`pending/accepted/skipped/missed` 有数据库配对约束；静默/停用自动跳过，预算/限流由调用方显式跳过，停机补窗保留 `scheduler_interrupted`。受理须在调用方创建 Job 的同一事务中填写 operation/job ID，并核对 owner、来源能力和冻结的连接版本。`policy_snapshot` 使用非秘密 `SourceExecutionPolicy` DTO。Plan 031 负责将调度器接入此事务入口；本卡没有开启新扫描入口。

G2：先新增 `test_collection_coverage.py`，在实现前运行指定 pytest 得到 `ModuleNotFoundError: No module named 'jobs.coverage'`。其后增加同到期点并发、跨午夜静默、停机补窗、旧版本快照、受理回滚、owner/UTC、真实零与未知、跨 Job 内容去重和标注状态用例；在既有增量窗口及预算测试增加第二页失败/重放与半写回滚用例。沙箱没有真实库 Red/Green 证据；后续隔离库 Green 见 EV-002-004/005。

| 检查 | 当前结果 | 证据与缺口 |
|---|---|---|
| CHK-033-001 | 通过，限隔离库 | `test_collection_coverage.py` 的到期唯一键、并发、恢复、静默、版本、UTC 与受理回滚断言在 EV-002-004/005 的新建 `hotkey_test` 中通过。 |
| CHK-033-002 | 通过，限受控计量 | `test_incremental_windows.py` 的第二页失败和重放、`test_resource_budgets.py` 的预算半写回滚、`test_collection_coverage.py` 的请求尝试/实扣及零与未知断言在 EV-002-004 通过；评论缓存零网络和跨主题去重所在的 `test_keyword_discovery.py` 随 EV-002-005 全量通过。逐来源真实计量仍待后续 Plan。 |
| CHK-033-003 | 通过，限隔离库 DTO | `test_collection_coverage.py` 中无 Job、owner 隔离、预算截断 `partial` 缺口和标注状态断言在 EV-002-004 通过；固定窗确认仍要求可信 terminal evidence。 |
| CHK-033-004 | 通过，限本卡技术边界 | EV-002-004/005 在全新隔离 PostgreSQL 中验证 Schema、计量 SQL/DTO、事务回滚与受控重放；007/008/035—038 的逐来源真实对账和 72 小时产品结论不由本卡关闭。 |

**EV-002-004（2026-09-27，Claude 在本机隔离库执行，结果由用户提供）**：新建空库 `hotkey_test`，以 `psql -X --set ON_ERROR_STOP=on -f backend/database/schema.sql` 成功应用含 `BEGIN`/`COMMIT` 的当前 Schema。Plan 033 指定的 `test_collection_coverage.py`、`test_incremental_windows.py`、`test_resource_budgets.py`，连同本轮同类 SQL 测试修复涉及的 `test_backup_restore.py`、`test_provenance_replay.py`、`test_webpage_persistence.py`，合计 **72 passed、6 skipped、0 failed**。6 项跳过属于未配置的 MinIO/Kafka 专用环境；未提供 DDL 失败注入后无残留表的独立记录。

**EV-002-005（2026-09-27，Claude 在本机隔离库执行，结果由用户提供）**：同一当前工作树的全量 pytest 为 **758 passed、17 skipped、0 failed**；跳过项属于未配置的 Kafka、MinIO、Redis、隔离浏览器专用环境，用户确认与本卡无关。`test_keyword_discovery.py` 随全量测试通过。Plan 033 的 G3、G4、G5 据本卡适用的真实 PostgreSQL/受控重放证据关闭；未运行的 Kafka 专用测试不作为本卡 Kafka 行为通过证据。

上述三份额外修复文件不属 SPEC-033，仅是在同类 `text()` 内嵌 JSON 排查时改用绑定参数，已随 EV-002-004/005 验证；无需另开 Issue。G6 仅记录 AC-002-006 的到期/计量技术部分，Plan 033 为 `completed`，不表示逐来源真实对账或 AC-002-006 最终关闭。

本地静态与非数据库证据：`ruff check .`、`ruff format --check .`、`mypy` 通过；无 `HOTKEY_TEST_DATABASE_URL` 的指定三份 pytest 为 **1 passed、48 skipped**，全量 pytest 为 **559 passed、216 skipped**。真实库结果以 EV-002-004/005 为准；父级 AC 与 M1 连续 72 小时结论仍保持 `failed`。

## Plan 001 主题与关键词规则（2026-09-27，本卡技术交付完成）

G0：历史 `68f1b02b:docs/acceptance/046-全局异常与响应契约验收.md` 的 S03 全项 `passed`，Plan 002/033 的技术交付见上文。`PRD 002 FR-002-001` 要求任一/全部/排除、来源选择和默认 1800/最短 600 秒。实施前文件清单：`backend/app/monitors/{schemas,models,services}.py`、`backend/app/api/routers/monitor_topics.py`、`backend/database/schema.sql`、`backend/tests/{unit,integration}/test_monitor_topics.py`、`backend/app/connections/{presets,services,schemas}.py`、`backend/app/worker/scheduler.py`；前端为 `keyword-group-field.tsx`、`topic-settings-fields.tsx`、`topic-rule-preview.tsx`、`topic-form.tsx`、`topic-editor.tsx`、`openapi2ts.config.ts` 及生成 `src/api`。既有路由含 POST/GET/PATCH/preview/clone/pause/resume/archive，规则预览复用 `evaluate_monitor_rules`，owner/CSRF、默认频率和初始暂停已有；版本仅存三组关键词，来源及间隔不升版，保存仅检查预设，恢复未核对预算，UI 无不可用来源原因。

G1：采集版本固定三组规范化关键词、排序去重的来源键和请求间隔；只有这三者变化才升版。名称和报告/推送偏好不改采集版本。`MonitorTopicService` 拥有创建、更新、克隆、生命周期事务；`connections` 提供当前预设/准入/执行策略，`jobs` 提供预算只读快照；Router 只处理认证和 HTTP。创建/更新来源需当前已应用搜索预设、批准的准入策略及启用的执行策略；恢复额外要求每个所选来源和适用全局网络预算仍有至少一次请求额度。配置就绪不代表来源已有持久读取证据。主题间隔与来源最短间隔取最大值写当前调度投影；Plan 031 仍负责其后调度入口的通用消费。Schema 扩展只在新空库使用，保留库须按恢复流程处理。

G2：先扩展单元用例，实施前 `uv run pytest tests/unit/test_monitor_topics.py -q` 得 **23 passed、1 failed**，失败点为版本缺 `source_keys`；中文子串、ASCII 整词（`AI` 不命中 `daily`）、排除优先、规范化/去重与预览同函数已有和新增用例覆盖。随后扩展集成测试，新增来源/间隔升版快照、旧 version 409 无部分写、未应用/撤销准入、禁用预算、运行中追加来源的预算拦截、重复恢复调度不改写、归档不可恢复；既有 POST/PATCH/克隆/归档/无副作用预览用例保留。沙箱无数据库时这些测试仅被收集并跳过；本机隔离库 Green 见 EV-002-006，集成测试实施前的数据库 Red 记录仍未提供。

| 检查 | 结果 | 证据和待办 |
|---|---|---|
| CHK-001-G0-001 | 通过 | 046 S03、PRD/Design、路由、模块、预设及差距如上。 |
| CHK-001-G1-001 | 通过，限合同冻结 | 接口/DTO/版本/事务/准入/预算和目录归属如上；同步 PROJECT、Design 001/002、AGENTS。 |
| CHK-001-G2-001 | 通过，保留 Red 证据边界 | 单元测试有实施前 Red；扩展的集成用例在 EV-002-006 的真实隔离库中 15 passed。未提供实施前数据库 Red 运行记录。 |
| CHK-001-G3-001 | 通过，限本卡技术交付 | `schema.sql`、ORM、服务、路由、前端设置/预览/错误状态已实现；EV-002-006 验证真实库集成测试，EV-002-007 验证运行 OpenAPI 重新生成客户端。 |
| CHK-001-G4-001 | 部分，技术门禁通过 | 后端 Ruff/mypy 的沙箱记录与 EV-002-006 的真实库全量 pytest、EV-002-007 的前端 lint/test/typecheck/build 及运行 OpenAPI 生成共同覆盖本卡技术门禁；未提供独立 `pnpm openapi:check` 通过记录或真实浏览器交互记录。 |
| CHK-001-G5-001 | 部分，受控配置链路通过 | 真实库集成测试核对保存、预览、版本冲突、克隆和归档；前端组件测试与真实构建通过。页面手工交互/截图未单独提供；真实获准来源首周期由 007/035—037 提供输入并在 009 汇合。 |
| CHK-001-G6-001 | 已记录本卡结论 | 本机技术结果与仍未验证的页面手工路径、真实来源首周期分别记录；AC-002-002/004/005 及 M1 产品验收未关闭。 |

受控结果：`backend/` 中 `uv run ruff format --check .` 为 193 files already formatted，`uv run ruff check .`、`uv run mypy` 通过；`uv run pytest -q` 为 **561 passed、221 skipped**，其中 `test_monitor_topics.py` **15 skipped**，对应单元/集成指定命令为 **24 passed、15 skipped**。`frontend/` 直接调用已安装的 `eslint .`、`tsc --noEmit` 通过；`vitest run --exclude 'src/app/api/[[...path]]/route.test.ts'` 为 **48 passed**，新主题测试 5 passed。完整 `vitest run` 为 50 passed、2 failed，失败均是既有代理测试监听 `127.0.0.1` 被沙箱 `EPERM` 拒绝。`next build` 的 Turbopack 因创建进程/绑定端口 `EPERM` 中止；`next build --webpack` 因代理端口 `127.0.0.1:7897` 拒绝连接，无法获取 Geist 字体。`pnpm` 在此前端目录启动脚本无输出且持续等待，实际使用相同的 `node_modules/.bin` 命令执行验证。

OpenAPI 沙箱记录：沙箱禁止 Uvicorn 绑定 `127.0.0.1:8867`，当时从 `create_app().openapi()` 导出 Schema，核对 `POST /api/topics` 的 422 引用 `ErrorView`，以该 Schema 运行 `openapi2ts` 并格式化 `src/api`；连续两次生成的文件哈希一致。该快照并非最终运行路由版本：本机运行后端重新生成时发现 `createMonitorTopic`、`updateMonitorTopic` 的客户端说明文字落后于最终路由，已按 EV-002-007 修正。此前 `git diff --exit-code -- frontend/src/api` 返回 1，不能据此把独立 `pnpm openapi:check` 记为通过。

**EV-002-006（2026-09-27，Claude 在本机真实 PostgreSQL 执行，结果由用户提供）**：全新隔离库 `hotkey_test` 使用 `psql -X --set ON_ERROR_STOP=on -f backend/database/schema.sql` 原子应用当前 Schema。`backend/tests/integration/test_monitor_topics.py` 为 **15 passed、0 failed**；全量后端 pytest 为 **765 passed、17 skipped、0 failed**。17 项跳过属于未配置的 Kafka、MinIO、Redis 和隔离浏览器专用环境，与 Plan 001 的主题配置验证无关。本次未附原始终端日志。

**EV-002-007（2026-09-27，Claude 在本机完整前端构建环境执行，结果由用户提供）**：`pnpm test` 全量 **52 passed、0 failed**；`pnpm typecheck`、`pnpm lint` 均通过，后者 0 错误/警告；`pnpm build` 成功生成 **13 条路由**。沙箱中两个代理测试的 `EPERM` 在本机未复现。指向 `hotkey_test` 的运行后端 `GET /openapi.json` 为 200；对该运行端点执行 `pnpm openapi:generate` 后，修正 `frontend/src/api/jiankongzhuti.ts` 中创建/编辑主题的两处说明文字，其余 8 个生成 API 文件未变化。本次未附独立 `pnpm openapi:check` 命令输出、原始终端日志或浏览器页面截图。

Plan 001 按“受控技术交付、真实库和真实前端构建通过”的范围标记 `completed`。真实浏览器中的 owner 会话仍需单独核对保存刷新、`AI`/中文/排除预览无外部请求、不可用来源原因、422 字段提示、双标签 409 保留草稿、401 回登录、重复提交、克隆暂停及归档不可恢复，并保留截图与请求 ID；当前用户提供的是本机前端构建与测试结果，不将其记作这些浏览器交互已通过。真实获准来源首周期、原帖与空/失败联验留给 007/035—037 并在 009 汇合；AC-002-002/004/005 不因本卡完成而关闭。
