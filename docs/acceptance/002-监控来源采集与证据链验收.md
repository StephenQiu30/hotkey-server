---
layer: Acceptance
scope: shared
doc_no: "002"
title: 监控来源采集与证据链验收
status: failed
version: v1.0
owner: HotKey Team
canonical_path: docs/acceptance/002-监控来源采集与证据链验收.md
design: docs/design/002-监控来源采集与证据链设计.md
prd: docs/prd/002-监控来源采集与证据链.md
plan: docs/plans/002-监控来源采集与证据链计划.md
verified_revision: "113fd8bf523f2bb150346320c75fd4ca890f721a"
verification_date: "2026-08-29"
---

# 监控来源采集与证据链验收

## 结论

本轮结论为 `failed`，表示 002 整体尚未通过，而不是本轮自动化失败。Monitor 发布与调度准入、三来源资源边界、部分成功、Evidence 血缘、去重/时间语义、四点 Worker 恢复、角色/五态/OpenAPI 已具有可复核证据；固定容量候选报告也已保存。

提交 `113fd8bf523f2bb150346320c75fd4ca890f721a` 进一步闭合了采集前当前 `fetch` Rights 显式允许、拒绝时零连接器解析与脱敏审计，以及 X 官方端点公网解析先于托管凭据解密、预算和拨号的边界。该切片只把 `AC-002-002` 推进为 `partial`：RSS/HN/X 真实授权冒烟和来源/凭据/预算/扫描/Evidence 管理动作的完整审计矩阵仍未完成，所以 `CHK-002-G3-002` 保持未勾选；生产同构容量复测未完成，所以 `CHK-002-G5-001` 也保持未勾选。

## 验证基线

| 项目 | 实际值 |
|---|---|
| 验证日期 | 2026-08-29（Asia/Shanghai） |
| 代码基线 | `113fd8bf523f2bb150346320c75fd4ca890f721a` |
| 项目运行 | 根 `docker-compose.yml`；Go Core、Python Agent、Web、PostgreSQL、Redis、MinIO 共 6 个既有服务保持运行且均为 `healthy`，本轮未重启项目容器 |
| 本机验证 | 复用本机锁定 Go 工具链与已运行 PostgreSQL/Redis；隔离进程环境执行后端完整 `make ci`，OpenAPI、vet、build、架构、仓库、数据库空库/幂等 Schema、全量测试和 `govulncheck` 全部通过 |
| 远端验证 | [GitHub Actions 33250215548](https://github.com/StephenQiu30/hotkey-server/actions/runs/33250215548)：提交 `113fd8bf` 的 8 个必需执行 Job 与最终 `All acceptance gates` 均为 `success`；Fresh Browser 合成 Secret 扫描、Backend 全量验收与 Worker 恢复门禁均通过 |

## 证据登记

### `EV-002-001`：Monitor 不可变发布与调度准入

- 映射：`CHK-002-G3-001` → `AC-002-001`；
- 结果：通过；
- 证据：`make monitor-publication-acceptance`，以及 `TestMonitorServicePublishesImmutableConfigurationAndCoordinatesSourceLifecycle`、`TestMonitorServiceFirstDraftAndPublishConcurrencyAndAuditRollback`、`TestCompiledIntentProfileHashIsStableAcrossEquivalentPreviewRuns`、`TestIntentPublicationRepositoryPromotesSuccessfulExactPreviewInsidePublishTransaction`、`TestCollectionSchedulerSkipsPublishedMonitorWithoutReadyCompiledProfile`；
- 结论：相同输入/版本产生相同 Profile Hash，发布事务绑定不可变 ready Compiled Profile，未发布或未 ready 不排队，同窗口稳定 Job 去重。

### `EV-002-002`：三来源八维资源边界

- 映射：`CHK-002-G3-003`、`CHK-002-G4-006` → `AC-002-009`；
- 结果：通过；
- 证据：`make source-resource-limit-acceptance`；`rss-resource-limits-v1`、`hacker-news-resource-limits-v1`、`x-api-resource-limits-v1`；三类 `ResourceLimitsStopBeforeNextExternalOrEvidenceSideEffect`、`ResourceLimitsBoundConnectReadAndWallClock` 与 `TestExternalRequestBudgetEnforcesUTCSourceProfileQuotaAtomically`；
- 结论：连接、读取、墙钟、页数、条数、累计字节、重试和每日配额均有有限值；越界在下一外部或持久副作用前停止。

### `EV-002-003`：三来源部分成功事实

- 映射：`CHK-002-G4-001` → `AC-002-003`；
- 结果：通过；
- 证据：`TestCollectionServiceProjectsThreeSourcePartialSuccessWithoutPersistingAggregateOutcome` 与 `TestGroupMonitorScansDerivesPartialSuccessFromThreeIndependentSourceFacts`；
- 结论：RSS 一条成功继续 Normalize、HN 成功零条、X 限流失败，Application 从三个独立 Target 派生 `partial_success`，数据库拒绝持久化冲突的整体状态。

### `EV-002-004`：Evidence 血缘、到期与例外

- 映射：`CHK-002-G4-002` → `AC-002-004`；
- 结果：通过；
- 证据：`TestEvidenceLineageSampleTraversesEventDocumentObservationAndRejectsCurrentAuthorizationMismatch`、`TestEvidenceLineageReconciliationQuarantinesDigestMismatchAndNeverMarksItHealthy`、`TestEvidenceLineageReconciliationHonorsAuditableApprovedRetentionException`、Citation 生命周期集成测试和 `DocumentVersionWorkspace` 页面状态测试；
- 结论：Event/Document 可回到 Observation 与保留期内 MinIO Hash；摘要不一致会隔离，过期元数据/删除审计和批准例外可追溯。

### `EV-002-005`：去重与时间语义

- 映射：`CHK-002-G4-003` → `AC-002-005`；
- 结果：通过；
- 证据：`TestDocumentVersionRepositoryResolvesExactIdentityInExternalURLHashOrder`、`TestDecideDuplicateNearTextRequiresSameSourceWindowAndStrictSimilarity`、`TestNormalizeCapturedItemPreservesUnknownPublishedAtAndMapsHackerNewsComment`、`TestContentRepositoryPersistsUnknownPublishedAtAsNull` 和 Event 聚类幂等/未知时间拒绝测试；
- 结论：External ID、规范 URL、正文 Hash 与版本化近重复策略保持分层；未知发布时间仍为 `null`，发现时间不伪造发布时间。

### `EV-002-006`：四点 Worker 恢复

- 映射：`CHK-002-G4-004` → `AC-002-006`；
- 结果：通过；
- 证据：`make worker-recovery-acceptance`；领取后、MinIO 写后、Observation/文档任务提交后、Vault 投影写后四个真实子进程非零退出 Fixture；
- 结论：新 Worker 通过租约回收继续同一稳定身份，PostgreSQL、MinIO、Vault 与 River 事实均无重复副作用。

### `EV-002-007`：角色、五态与契约一致性

- 映射：`CHK-002-G4-005` → `AC-002-007`；
- 结果：通过；
- 证据：`TestAnalystCanManageOnlyAnOwnedMonitor`、Source/Collection/Content 路由四角色矩阵、来源/Monitor/内容页面五态测试、`TestGeneratedOpenAPIRegistryMatchesCommittedArtifact`、前端 `openapi:check` 与生成客户端测试；
- 结论：页面与直接 API 权限一致，凭据/Object Key 不进入安全 DTO；Swaggo、发布 OpenAPI 与生成客户端无漂移。

### `EV-002-008`：固定隔离环境容量候选

- 映射：`CHK-002-G5-001` → `AC-002-008`；
- 结果：部分通过，检查项保持未勾选；
- 证据：[`collection-capacity-macos-arm64-6f5a3e03.json`](evidence/002/collection-capacity-macos-arm64-6f5a3e03.json) 固定 50 个活跃 Monitor、100 个启用连接、50,000 条候选事实与 20 个并发 Job，报告为零错误且不含 DSN、凭据或私密响应；
- 边界：报告排除真实 Connector 网络时延、Provider 限流、公网 TLS、冷缓存和生产身份查询，不构成生产 SLO 或容量批准。

### `EV-002-009`：采集前 fetch Rights 与 X 凭据延迟边界

- 映射：`TASK-002-S02-T03` → `SPEC-002-SEC-001` → `AC-002-002` → `CHK-002-G3-002`；
- 结果：部分通过，检查项保持未勾选；
- 失败证据：新增红测首先证明 Collection 旧执行路径会在没有当前 `fetch` Rights 显式允许时直接解析 Connector，且托管 X 凭据在 Connector Resolve 阶段被提前解密；
- 实现证据：提交 `113fd8bf523f2bb150346320c75fd4ca890f721a` 增加 `CollectionAdmissionGate` 与 PostgreSQL 当前 endpoint `fetch` 决策读取，只有最高优先级全部显式 `allow` 才放行；无决策、`unknown`、`deny` 或冲突均拒绝。生产 Bootstrap 强制注入准入门，X 托管凭据改为请求边界回调，并在 Fetch、Lookup 与 Health 中先校验官方 HTTPS 路径和全部 DNS 结果为公网地址；
- 拒绝证据：`TestCollectionFetchRightsDenialStopsBeforeConnectorResolutionAndPersistsSanitizedAudit` 证明拒绝时 Connector Resolve 为 0、业务事实计数不变、失败 Run 可诊断且只追加 `fetch_rights_not_permitted` 净化审计；`TestRightsDecisionReaderResolvesCurrentEndpointFetchConservatively` 固定 allow/unknown 冲突、历史时间点 allow 和无决策 unknown；
- 凭据证据：`TestConnectorRegistryDefersManagedCredentialDecryptionUntilTheRequestBoundary` 证明 Resolve/Validate 均不解密；`TestXEndpointPolicyRejectsUnsafeResolutionBeforeCredentialBudgetOrDial` 证明私网解析时凭据读取、预算调用和拨号均为 0；架构契约固定 Collection 与 X 的调用顺序；
- 验证：Source、Ingestion、架构专项回归与隔离进程环境的完整后端 `make ci` 均通过；远端 CI `33250215548` 的 8 个必需执行 Job 和最终汇总门禁全部通过；
- 未完成边界：尚未形成 RSS/HN/X 的完整 Capability/Rights/Credential/Budget/Rate Limit/Endpoint 准入矩阵，尚未对来源、凭据、预算、扫描、Evidence 五类管理动作完成统一审计查询与合成 Secret 负向扫描，也未执行三来源真实授权冒烟，因此不得关闭 `AC-002-002` 或 `CHK-002-G3-002`。

## AC 结果

| AC | 结果 | 说明 |
|---|---|---|
| `AC-002-001` | passed | 见 `EV-002-001` |
| `AC-002-002` | partial | 见 `EV-002-009`；当前 fetch Rights 与 X 凭据延迟边界已闭合，完整准入/管理审计矩阵和真实授权冒烟未完成 |
| `AC-002-003` | passed | 见 `EV-002-003` |
| `AC-002-004` | passed | 见 `EV-002-004` |
| `AC-002-005` | passed | 见 `EV-002-005` |
| `AC-002-006` | passed | 见 `EV-002-006` |
| `AC-002-007` | passed | 见 `EV-002-007` |
| `AC-002-008` | partial | 见 `EV-002-008`；固定隔离候选已测，真实来源、冷缓存和生产同构复测未完成 |
| `AC-002-009` | passed | 见 `EV-002-002` |

## 实际命令与结果摘要

```text
targeted: go run ./test/runner test -p=1 ./internal/modules/source/application ./internal/modules/source/infrastructure/postgres ./internal/modules/source/infrastructure ./internal/modules/source/infrastructure/x -run 'Test(CollectionAdmission|CurrentCollectionFetchRightsResultRejectsDuplicate|CollectionFetchRightsDenial|RightsDecisionReaderResolvesCurrentEndpointFetch|ConnectorRegistryDefersManagedCredential|XEndpointPolicyRejectsUnsafeResolution)' -count=1
module regression: go run ./test/runner test -p=1 ./internal/modules/source/... ./internal/modules/ingestion/... ./test/architecture -count=1
backend full gate: isolated process environment with only HOTKEY_TEST_DSN/HOTKEY_TEST_REDIS_URL; make ci
runtime observation: docker compose ps（只读复核既有 6 个服务，未停止或重启）
```

## 未完成项与停止条件

- `CHK-002-G3-002`：补齐 RSS/HN/X 完整准入顺序、五类管理动作脱敏审计和合成 Secret 负向扫描，并在受信任环境完成三来源真实授权冒烟；
- `CHK-002-G5-001`：保存真实授权冒烟的脱敏状态/计数/延迟、冷缓存和生产同构硬件容量报告并取得批准；
- 缺少任何真实来源授权时必须继续保持默认禁用，不得用 Fixture、模拟响应或本机候选容量替代真实授权证据。

以上任一项未完成时，本 Acceptance 不得改为 `passed`，002 Plan/PRD 不得改为 `completed/implemented`。

## 影响与回滚验证

- Schema 与 OpenAPI 未变化；运行路径新增 Source Collection 的强制当前 `fetch` Rights 查询，缺少显式 allow 会在 Connector、凭据、预算和网络前失败关闭；
- X 受管凭据不再在 Connector Resolve/Validate 时解密，只在官方端点公网解析通过后的请求边界读取；RSS/HN 与其他 Connector 的既有运行行为未在本切片扩大修改；
- 回滚若移除准入门或恢复 X 早期解密，`TestSourceCollectionAdmissionPrecedesCredentialsBudgetAndNetwork`、拒绝审计测试和凭据调用计数测试会失败；不得通过回滚补写、覆盖或删除既有 Rights、Run、Audit、Evidence 或 Checkpoint 事实；
- 本轮不重启本机项目，不改动 Docker 拓扑；测试只使用本机工具链与可丢弃测试库。

## 验收人

- Owner：HotKey Team；
- 自动化执行：Codex；
- 整体结论：failed（`AC-002-002`、`AC-002-008` 仍为 partial）。
