---
layer: Acceptance
scope: shared
doc_no: "002"
title: 监控来源采集与证据链验收
status: failed
version: v1.0
owner: HotKey Team
canonical_path: docs/acceptance/002-监控来源采集与证据链验收.md
design: docs/design/002-监控来源采集与证据链设计.md
prd: docs/prd/002-监控来源采集与证据链.md
plan: docs/plans/002-监控来源采集与证据链计划.md
verified_revision: "d28642092ce6148e59272f5054d752a7aebc15c0"
verification_date: "2026-08-29"
---

# 监控来源采集与证据链验收

## 结论

本轮结论为 `failed`，表示 002 整体尚未通过，而不是本轮自动化失败。Monitor 发布与调度准入、三来源资源边界、部分成功、Evidence 血缘、去重/时间语义、四点 Worker 恢复、角色/五态/OpenAPI 已具有可复核证据；固定容量候选报告也已保存。

提交 `113fd8bf523f2bb150346320c75fd4ca890f721a` 闭合了采集前当前 `fetch` Rights 显式允许、拒绝时零连接器解析与脱敏审计，以及 X 官方端点公网解析先于托管凭据解密、预算和拨号的边界。提交 `9ea2b30b08d6cf50bb8cc4d072bd0bb069f91288` 把来源、凭据、预算、扫描和 Evidence 五类管理动作接入统一审计查询，并用合成 Secret 证明拒绝路径和存储表面零残留。提交 `6ff813bc8e90bd673d24d8d50b1ef076d5a70a04` 已补齐 RSS/HN/X 的 Capability→当前 Rights→凭据状态→日预算/分钟限流预检→Endpoint/SSRF→请求矩阵，并覆盖定时采集、即时搜索、X 指标刷新和管理员健康探测四条入口。提交 `88d4a9dca1e92c2694ee505fe18572aeed71de3d` 增加只在受信任环境显式运行的三来源真实冒烟执行器；提交 `d28642092ce6148e59272f5054d752a7aebc15c0` 在不减少覆盖的前提下让隔离测试包保守双并行，恢复远端全量门禁。当前环境没有三个已启用真实连接、对应当前 Rights 和 X 凭据，真实授权冒烟未执行。`AC-002-002` 仍为 `partial` 的唯一剩余产品边界是三来源真实授权冒烟；生产同构容量复测也未完成，所以 `CHK-002-G3-002` 与 `CHK-002-G5-001` 均保持未勾选。

## 验证基线

| 项目 | 实际值 |
|---|---|
| 验证日期 | 2026-08-29（Asia/Shanghai） |
| 代码基线 | `d28642092ce6148e59272f5054d752a7aebc15c0` |
| 项目运行 | 根 `docker-compose.yml`；Go Core、Python Agent、Web、PostgreSQL、Redis、MinIO 共 6 个既有服务保持运行且均为 `healthy`，本轮未重启项目容器 |
| 本机验证 | 复用本机锁定 Go 工具链与已运行 PostgreSQL/Redis；隔离进程环境的完整 `make ci`、双包并行后的完整 `make ci-runtime`、live-smoke 专项测试/Vet 与架构门禁全部通过；本轮可丢弃 `hotkey_test` 及子库复验后已清理且剩余数为 0 |
| 远端验证 | [GitHub Actions 33256271696](https://github.com/StephenQiu30/hotkey-server/actions/runs/33256271696)：提交 `d2864209` 的 8 个必需执行 Job 与最终 `All acceptance gates` 均为 `success`；Backend 全量 Schema/测试在 6 分 41 秒完成，静态检查、漏洞检查、Fresh Browser、Worker 恢复、前端、Python Agent 与 Compose 门禁均通过 |

## 证据登记

### `EV-002-001`：Monitor 不可变发布与调度准入

- 映射：`CHK-002-G3-001` → `AC-002-001`；
- 结果：通过；
- 证据：`make monitor-publication-acceptance`，以及 `TestMonitorServicePublishesImmutableConfigurationAndCoordinatesSourceLifecycle`、`TestMonitorServiceFirstDraftAndPublishConcurrencyAndAuditRollback`、`TestCompiledIntentProfileHashIsStableAcrossEquivalentPreviewRuns`、`TestIntentPublicationRepositoryPromotesSuccessfulExactPreviewInsidePublishTransaction`、`TestCollectionSchedulerSkipsPublishedMonitorWithoutReadyCompiledProfile`；
- 结论：相同输入/版本产生相同 Profile Hash，发布事务绑定不可变 ready Compiled Profile，未发布或未 ready 不排队，同窗口稳定 Job 去重。

### `EV-002-002`：三来源八维资源边界

- 映射：`CHK-002-G3-003`、`CHK-002-G4-006` → `AC-002-009`；
- 结果：通过；
- 证据：`make source-resource-limit-acceptance`；`rss-resource-limits-v1`、`hacker-news-resource-limits-v1`、`x-api-resource-limits-v1`；三类 `ResourceLimitsStopBeforeNextExternalOrEvidenceSideEffect`、`ResourceLimitsBoundConnectReadAndWallClock` 与 `TestExternalRequestBudgetEnforcesUTCSourceProfileQuotaAtomically`；
- 结论：连接、读取、墙钟、页数、条数、累计字节、重试和每日配额均有有限值；越界在下一外部或持久副作用前停止。

### `EV-002-003`：三来源部分成功事实

- 映射：`CHK-002-G4-001` → `AC-002-003`；
- 结果：通过；
- 证据：`TestCollectionServiceProjectsThreeSourcePartialSuccessWithoutPersistingAggregateOutcome` 与 `TestGroupMonitorScansDerivesPartialSuccessFromThreeIndependentSourceFacts`；
- 结论：RSS 一条成功继续 Normalize、HN 成功零条、X 限流失败，Application 从三个独立 Target 派生 `partial_success`，数据库拒绝持久化冲突的整体状态。

### `EV-002-004`：Evidence 血缘、到期与例外

- 映射：`CHK-002-G4-002` → `AC-002-004`；
- 结果：通过；
- 证据：`TestEvidenceLineageSampleTraversesEventDocumentObservationAndRejectsCurrentAuthorizationMismatch`、`TestEvidenceLineageReconciliationQuarantinesDigestMismatchAndNeverMarksItHealthy`、`TestEvidenceLineageReconciliationHonorsAuditableApprovedRetentionException`、Citation 生命周期集成测试和 `DocumentVersionWorkspace` 页面状态测试；
- 结论：Event/Document 可回到 Observation 与保留期内 MinIO Hash；摘要不一致会隔离，过期元数据/删除审计和批准例外可追溯。

### `EV-002-005`：去重与时间语义

- 映射：`CHK-002-G4-003` → `AC-002-005`；
- 结果：通过；
- 证据：`TestDocumentVersionRepositoryResolvesExactIdentityInExternalURLHashOrder`、`TestDecideDuplicateNearTextRequiresSameSourceWindowAndStrictSimilarity`、`TestNormalizeCapturedItemPreservesUnknownPublishedAtAndMapsHackerNewsComment`、`TestContentRepositoryPersistsUnknownPublishedAtAsNull` 和 Event 聚类幂等/未知时间拒绝测试；
- 结论：External ID、规范 URL、正文 Hash 与版本化近重复策略保持分层；未知发布时间仍为 `null`，发现时间不伪造发布时间。

### `EV-002-006`：四点 Worker 恢复

- 映射：`CHK-002-G4-004` → `AC-002-006`；
- 结果：通过；
- 证据：`make worker-recovery-acceptance`；领取后、MinIO 写后、Observation/文档任务提交后、Vault 投影写后四个真实子进程非零退出 Fixture；
- 结论：新 Worker 通过租约回收继续同一稳定身份，PostgreSQL、MinIO、Vault 与 River 事实均无重复副作用。

### `EV-002-007`：角色、五态与契约一致性

- 映射：`CHK-002-G4-005` → `AC-002-007`；
- 结果：通过；
- 证据：`TestAnalystCanManageOnlyAnOwnedMonitor`、Source/Collection/Content 路由四角色矩阵、来源/Monitor/内容页面五态测试、`TestGeneratedOpenAPIRegistryMatchesCommittedArtifact`、前端 `openapi:check` 与生成客户端测试；
- 结论：页面与直接 API 权限一致，凭据/Object Key 不进入安全 DTO；Swaggo、发布 OpenAPI 与生成客户端无漂移。

### `EV-002-008`：固定隔离环境容量候选

- 映射：`CHK-002-G5-001` → `AC-002-008`；
- 结果：部分通过，检查项保持未勾选；
- 证据：[`collection-capacity-macos-arm64-6f5a3e03.json`](evidence/002/collection-capacity-macos-arm64-6f5a3e03.json) 固定 50 个活跃 Monitor、100 个启用连接、50,000 条候选事实与 20 个并发 Job，报告为零错误且不含 DSN、凭据或私密响应；
- 边界：报告排除真实 Connector 网络时延、Provider 限流、公网 TLS、冷缓存和生产身份查询，不构成生产 SLO 或容量批准。

### `EV-002-009`：采集前 fetch Rights 与 X 凭据延迟边界

- 映射：`TASK-002-S02-T03` → `SPEC-002-SEC-001` → `AC-002-002` → `CHK-002-G3-002`；
- 结果：部分通过，检查项保持未勾选；
- 失败证据：新增红测首先证明 Collection 旧执行路径会在没有当前 `fetch` Rights 显式允许时直接解析 Connector，且托管 X 凭据在 Connector Resolve 阶段被提前解密；
- 实现证据：提交 `113fd8bf523f2bb150346320c75fd4ca890f721a` 增加 `CollectionAdmissionGate` 与 PostgreSQL 当前 endpoint `fetch` 决策读取，只有最高优先级全部显式 `allow` 才放行；无决策、`unknown`、`deny` 或冲突均拒绝。生产 Bootstrap 强制注入准入门，X 托管凭据改为请求边界回调，并在 Fetch、Lookup 与 Health 中先校验官方 HTTPS 路径和全部 DNS 结果为公网地址；
- 拒绝证据：`TestCollectionFetchRightsDenialStopsBeforeConnectorResolutionAndPersistsSanitizedAudit` 证明拒绝时 Connector Resolve 为 0、业务事实计数不变、失败 Run 可诊断且只追加 `fetch_rights_not_permitted` 净化审计；`TestRightsDecisionReaderResolvesCurrentEndpointFetchConservatively` 固定 allow/unknown 冲突、历史时间点 allow 和无决策 unknown；
- 凭据证据：`TestConnectorRegistryDefersManagedCredentialDecryptionUntilTheRequestBoundary` 证明 Resolve/Validate 均不解密；`TestXEndpointPolicyRejectsUnsafeResolutionBeforeCredentialBudgetOrDial` 证明私网解析时凭据读取、预算调用和拨号均为 0；架构契约固定 Collection 与 X 的调用顺序；
- 验证：Source、Ingestion、架构专项回归与隔离进程环境的完整后端 `make ci` 均通过；远端 CI `33250215548` 的 8 个必需执行 Job 和最终汇总门禁全部通过；
- 未完成边界：尚未形成 RSS/HN/X 的完整 Capability/Rights/Credential/Budget/Rate Limit/Endpoint 准入矩阵，也未执行三来源真实授权冒烟，因此不得关闭 `AC-002-002` 或 `CHK-002-G3-002`。

### `EV-002-010`：五类管理审计与 Secret 零残留

- 映射：`TASK-002-S02-T03` → `SPEC-002-SEC-001` → `AC-002-002` → `CHK-002-G3-002`；
- 结果：部分通过，检查项保持未勾选；
- 实现证据：提交 `9ea2b30b08d6cf50bb8cc4d072bd0bb069f91288` 新增 `source_credential.changed`、`source_budget.updated`、`collection.manual_requested`、`collection.retry_requested` 和 `source.health_checked` 动作；来源凭据创建/旋转/移除、预算变更、手动采集、重试和健康检查都在与业务事实同一 PostgreSQL 事务中写入审计；Evidence 保留执行继续使用 `retention.executed`；
- 查询证据：`TestGovernanceAuditQueryCoversFiveSourceManagementCategoriesWithoutSyntheticSecrets` 通过统一 Governance Audit 查询同时取回来源、凭据、预算、扫描和 Evidence 五类动作；`TestSourceServiceCreatesAndRotatesEncryptedManagedCredential`、`TestSourceServiceAuditsBudgetConfigurationChangeWithoutPersistingConfiguration`、`TestManualCollectionUsesDurableCooldownAndActivePublishedTargets` 和 `TestCollectionControlListsRetriesAndPersistsSafeHealth` 固定各写入路径；
- 负向证据：凭据保存拒绝合成 Secret 注入并验证数据库零残留；审计查询逐条断言不含合成 Token，预算审计不保存原始配置，手动采集的 cooldown 复用请求也会单独留审计；
- 验证：`make source-management-audit-acceptance`、Source/Operations 回归、架构门禁与后端完整 `make ci` 均通过；远端 CI `33251371397` 的 8 个必需执行 Job 和最终汇总门禁全部通过；
- 未完成边界：本证据只关闭五类管理审计与合成 Secret 负向门禁，不代替 RSS/HN/X 完整准入矩阵或真实授权冒烟。

### `EV-002-011`：三来源统一准入矩阵与入口旁路闭合

- 映射：`TASK-002-S02-T03` → `SPEC-002-SEC-001` → `AC-002-002` → `CHK-002-G3-002`；
- 结果：自动化准入矩阵通过，真实授权冒烟未执行，检查项保持未勾选；
- 实现证据：提交 `6ff813bc8e90bd673d24d8d50b1ef076d5a70a04` 把 Capability、当前 `fetch` Rights、托管凭据状态、每日 Budget/每分钟 Rate Limit 非消耗预检固化为统一 Application 准入门，并在 Connector 内继续执行 Endpoint/逐跳 SSRF Policy、物理请求预算原子 Reserve 和请求边界凭据读取；定时采集、即时搜索、X 指标刷新和管理员健康探测均先准入再解析 Connector；
- 凭据证据：`TestManagedCredentialStatusChecksMetadataAndKeyReadinessWithoutResolvingPlaintext` 只读取 `key_version`，覆盖无记录、当前 Key、无 Key、错误 Key 版本和 Previous Key 轮换窗口，过程中不读取密文、不返回或解密明文；`TestConnectorRegistryDefersManagedCredentialDecryptionUntilTheRequestBoundary` 继续固定 Connector 解析零解密；
- 配额证据：`source_request_usage_ledgers` 以向前兼容方式增加分钟窗口与计数；`TestExternalRequestBudgetEnforcesPerMinuteRateLimitAtomicallyWithoutConsumingDailyBudget` 证明并发下分钟上限不超卖且分钟拒绝不消耗每日预算；`TestCollectionRequestBudgetStatusMapsEveryP0ProfileWithoutConsumingARequest` 证明准入预检不占用真实请求额度；RSS/HN/X 在每次拨号、重试、重定向和 X Lookup 前执行同一原子 Reserve；
- 旁路证据：`TestCollectionFetchRightsDenialStopsBeforeConnectorResolutionAndPersistsSanitizedAudit`、`TestCollectionHealthAdmissionDenialStopsBeforeConnectorResolutionAndPersistsSafeAudit`、`TestInstantSearchAdmissionDenialStopsBeforeConnectorResolution` 与 `TestXMetricRefreshAdmissionDenialStopsBeforeConnectorResolution` 证明四条生产入口拒绝时 Connector 解析/网络为 0；健康探测只放宽“禁用/不可用来源可恢复”，不放宽已删除、Rights、凭据或预算门禁；
- 顺序证据：`TestCollectionAdmissionRejectsInvalidCapabilityBeforeRightsLookup`、`TestCollectionAdmissionChecksManagedCredentialStatusAfterRights`、`TestCollectionAdmissionChecksBudgetAndRateLimitAfterCredentialStatus`、X Endpoint 负向测试及 `TestSourceCollectionAdmissionPrecedesCredentialsBudgetAndNetwork` 架构契约共同固定完整顺序；生产 Fx 图显式绑定同一预算状态适配器，Bootstrap 图测试通过；
- 验证：`make source-admission-matrix-acceptance`、`make source-resource-limit-acceptance source-management-audit-acceptance` 与完整 `make ci` 本机通过；远端 CI `33253811195` 的 8 个必需执行 Job 和最终 `All acceptance gates` 全部通过；
- 未完成边界：该证据关闭代码级准入矩阵及旁路，不冒充外部 Provider 授权。仍须在受信任环境对 RSS/HN/X 执行真实授权冒烟并保存脱敏状态、计数和延迟，才能勾选 `CHK-002-G3-002`。

### `EV-002-012`：真实来源冒烟执行入口与诚实预检

- 映射：`TASK-002-S02-T04` → `AC-002-002`、`AC-002-008` → `CHK-002-G3-002`、`CHK-002-G5-001`；
- 结果：执行器与安全边界通过，真实授权冒烟未执行，两个检查项保持未勾选；
- 实现证据：提交 `88d4a9dca1e92c2694ee505fe18572aeed71de3d` 增加 `backend/test/tools/source-live-smoke` 与受信任环境专用的 `make source-live-smoke`。执行器先通过真实 Go API 读取并核对三个唯一 Source ID、类型、启用/删除状态和 X 凭据状态；全部预检通过后才依次执行三个 Health，全部 Health 通过后才分别调用 `/api/v1/search`，每个来源必须恰有一个成功状态且结果数大于 0；普通 CI 不运行外部冒烟；
- 失败关闭：必须显式提供 `I_CONFIRM_AUTHORIZED_P0_SOURCES`、Loopback HTTP 或 HTTPS API Origin、临时 Admin Bearer、三个真实 Source ID、批准查询、环境标识、完整 40 位小写 Revision 和全新报告路径；任一身份、Rights、凭据、Health 或配置错误都会停止后续阶段。全程 5 分钟、单请求 60 秒、响应 1 MiB 上限、禁止跟随重定向，报告以 `0600` 和排他创建写入，禁止覆盖既有证据；
- 脱敏证据：报告只允许版本、审批要求、环境、Revision、时间、查询字节数、Source 类型/ID、启用/凭据布尔值、阶段状态、计数、延迟和白名单错误码，不保存 Token、查询内容或 Hash、Base URL、来源名称、Endpoint、结果 Payload 或 API 原始消息；
- 自动化证据：`TestExecuteUsesRealAPIBoundariesAndWritesOnlySanitizedEvidence`、`TestLoadConfigFailsClosedBeforeNetworkOrOutput`、`TestExecuteRedactsAPIFailuresAndDoesNotFollowRedirect`、`TestExecuteStopsBeforeExternalProbeWhenSourceIdentityIsWrong`、`TestWriteReportNeverOverwritesEvidence` 与 `TestStableProviderCodeAllowsOnlyApplicationCodes` 覆盖成功路径、Secret Canary 零输出、配置缺失零网络/零文件、重定向拒绝、身份错配零外部 POST、不可覆盖和错误码白名单；
- 当前环境预检：运行中的业务库对 `rss`、`hacker_news`、`x` 的已启用连接计数分别为 0；运行容器的环境变量名称只有来源主密钥和 DoH 配置，没有 X 来源凭据配置项。未提供显式确认时执行 `make source-live-smoke` 以退出码 2 在网络与输出前失败，消息为 `HOTKEY_SOURCE_LIVE_SMOKE_CONFIRM is required`，没有生成报告；
- 验证：live-smoke 专项测试/Vet、架构门禁和本机完整 `make ci` 通过。远端 `33255119495` 的产品执行面没有断言失败，但旧 `ci-test -p=1` 因 GitHub Runner 性能波动触发 20 分钟总时限；提交 `d28642092ce6148e59272f5054d752a7aebc15c0` 依据每测试独立 PostgreSQL 数据库边界改为保守双包并行，完整本机 `make ci-runtime` 通过，远端 CI `33256271696` 的 8 个必需执行 Job 与最终门禁全部通过，后端全量耗时 6 分 41 秒；
- 未完成边界：当前没有真实授权输入，所以没有保存 live-smoke 报告，也没有关闭任何 Checklist。必须由受信任操作者配置三个真实连接、当前 Rights、X 凭据和批准查询后实际运行，Fixture、公共未授权端点或仅执行器测试均不能替代该证据。

## AC 结果

| AC | 结果 | 说明 |
|---|---|---|
| `AC-002-001` | passed | 见 `EV-002-001` |
| `AC-002-002` | partial | 见 `EV-002-009`、`EV-002-010`、`EV-002-011`、`EV-002-012`；完整准入矩阵、入口旁路、五类管理审计和受信执行入口已闭合，只剩三来源真实授权冒烟 |
| `AC-002-003` | passed | 见 `EV-002-003` |
| `AC-002-004` | passed | 见 `EV-002-004` |
| `AC-002-005` | passed | 见 `EV-002-005` |
| `AC-002-006` | passed | 见 `EV-002-006` |
| `AC-002-007` | passed | 见 `EV-002-007` |
| `AC-002-008` | partial | 见 `EV-002-008`、`EV-002-012`；固定隔离候选和受信执行入口已测，真实来源、冷缓存和生产同构复测未完成 |
| `AC-002-009` | passed | 见 `EV-002-002` |

## 实际命令与结果摘要

```text
targeted: make source-admission-matrix-acceptance
regression: make source-resource-limit-acceptance source-management-audit-acceptance
backend full gate: isolated process environment with only HOTKEY_TEST_DSN/HOTKEY_TEST_REDIS_URL; make ci
live-smoke runner tests: go run ./test/runner test ./test/tools/source-live-smoke -count=1; go run ./test/runner vet ./test/tools/source-live-smoke
trusted live-smoke preflight: make source-live-smoke -> exit 2 before network/output because HOTKEY_SOURCE_LIVE_SMOKE_CONFIRM is required
runtime observation: docker compose ps（只读复核既有 6 个服务，未停止或重启）
```

## 未完成项与停止条件

- `CHK-002-G3-002`：代码级完整准入顺序、入口旁路和受信执行器已由 `EV-002-011`、`EV-002-012` 闭合；仍须在受信任环境完成三来源真实授权冒烟；
- `CHK-002-G5-001`：`EV-002-012` 当前只有诚实预检，没有真实报告；仍须保存真实授权冒烟的脱敏状态/计数/延迟、冷缓存和生产同构硬件容量报告并取得批准；
- 缺少任何真实来源授权时必须继续保持默认禁用，不得用 Fixture、模拟响应或本机候选容量替代真实授权证据。

以上任一项未完成时，本 Acceptance 不得改为 `passed`，002 Plan/PRD 不得改为 `completed/implemented`。

## 影响与回滚验证

- OpenAPI 未变化；Schema 只向 `source_request_usage_ledgers` 增加可幂等回填的分钟窗口与计数，既有每日用量事实保留；分钟限流拒绝不会消耗每日额度；
- 定时采集、即时搜索、X 指标刷新和健康探测现在共享同一准入门；托管凭据状态只读 Key 版本元数据，X 明文仍只在官方端点公网解析通过后的请求边界读取；
- 回滚若移除任一入口准入、分钟原子账本或恢复 X 早期解密，`make source-admission-matrix-acceptance`、`TestSourceCollectionAdmissionPrecedesCredentialsBudgetAndNetwork` 和 Schema/数据库门禁会失败；不得通过回滚补写、覆盖或删除既有 Rights、用量、Run、Audit、Evidence 或 Checkpoint 事实；
- 本轮不重启本机项目，不改动 Docker 拓扑；测试只使用本机工具链与可丢弃测试库。`hotkey_test` 与全部 `hotkey_it_*` 子库在复验后均已删除，剩余数为 0；
- live-smoke 是显式、只读、受信任执行入口，不进入普通 CI；它不会创建来源、补写 Rights、注入凭据或以公共未授权端点绕过真实验收。

## 验收人

- Owner：HotKey Team；
- 自动化执行：Codex；
- 整体结论：failed（`AC-002-002`、`AC-002-008` 仍为 partial）。
