{
  "scope": "S02-T03A visible-page activity polling",
  "date": "2026-09-16",
  "environment": {
    "database": "PostgreSQL 16.15 disposable Compose database",
    "broker": "RabbitMQ 4.1.8 disposable Compose broker",
    "schema_revision": "0019_collection_retry_budget",
    "browser": "Playwright Chromium against the final production frontend image",
    "source": "no external source requests"
  },
  "red": {
    "result": "1 failed",
    "assertion": "a submitted diagnostic job remained queued for the full 20 second UI timeout without manual refresh",
    "observed_row": "queued with zero attempts"
  },
  "green": {
    "poll_interval_seconds": 2,
    "single_in_flight_chain": true,
    "hidden_page": "no new job or collection-run list requests during a 4.5 second hidden interval",
    "visible_resume": "the queued row advanced to succeeded without a manual refresh",
    "terminal_stop": "job and collection-run request counts stayed unchanged across two 4.5 second observation windows",
    "terminal_refresh": "one complete workspace refresh ran after activity reached a terminal state",
    "backend": "170 passed with 2 upstream deprecation warnings",
    "ruff": "158 files formatted and linted",
    "mypy": "131 source and verification files",
    "dependency_audit": "no known Python vulnerabilities; production frontend audit 0",
    "contract": "FastAPI OpenAPI snapshot and UmiOpenAPI generated client unchanged and current",
    "frontend": "contract, boundary, negative boundary, Prettier and TypeScript/Vite checks passed",
    "browser": "3 Chromium tests passed on a fresh synthetic database",
    "compose_prefork_attempts": 1,
    "proxy_replacement_status": 401,
    "backup_restore": {
      "schema_revision": "0019_collection_retry_budget",
      "dump_bytes": 90427,
      "withdrawal_replay_idempotent": true
    },
    "shutdown": "worker=0, backend=143, scheduler=0; no SIGKILL"
  },
  "remote_ci": {
    "status": "pending"
  },
  "test_isolation": {
    "first_red_environment_attempt": "the custom web port was outside the configured Origin allowlist and was excluded from product evidence",
    "first_green_rerun": "a prior disposable browser run left synthetic event data; only the hotkey-s02poll volumes were removed",
    "first_full_browser_attempt": "Swagger used its default API port instead of the isolated mapping; the complete three-test run passed after HOTKEY_API_URL was set",
    "final_project": "hotkey-s02poll"
  },
  "limitations": [
    "The activity state sequence used a controlled browser route after a real authenticated diagnostic submission so queued and terminal timing was deterministic.",
    "The real scheduler, RabbitMQ and Celery prefork pipeline was verified separately in the same disposable Compose stack with attempts=1.",
    "No external platform request, existing MinIO instance, real user content or seven-day observation was used.",
    "The HTTP/OpenAPI contract, generated frontend client, request budget and source admission configuration are unchanged.",
    "This slice advances TASK-007-S02-T03 runtime visibility but does not complete EV-007-003 or the full P0 goal."
  ]
}
