---
layer: Plan
scope: issue
doc_no: "005"
title: 标注异常与有效结论执行计划
status: planned
version: v1.0
date: 2026-09-26
owner: HotKey Team
canonical_path: docs/plan/005-标注异常与有效结论执行计划.md
prd: docs/prd/002-信息获取主链路需求.md
design: docs/design/002-信息获取主链路设计.md
architecture_prerequisite: "046 S03"
source_task: TASK-002-S03-T01
---

# Plan 005：标注异常与有效结论

## 范围与需求

开发库 738 条标注中有 3 条 `related` 为空，成因未明。先只读定位输入、模型输出校验、写入与重放路径，再保存失败用例并修复。源自 [PRD 002](../prd/002-信息获取主链路需求.md) `FR-002-005`、`NFR-001-105/108`，贡献 `AC-002-006/008` 的状态正确性；60 分钟有效率由 Plan 006/009 复算。预计复核 `backend/app/analysis/{services,schemas}.py`、Worker 分析处理器及 tests；若确需数据约束，先同步 Design 与唯一 `schema.sql`。

## SPEC

| ID | 规格 |
|---|---|
| `SPEC-005-DATA-001` | 明确相关、明确不相关、待处理、调用失败、模型输出无效和空值状态；只有带理由的有效结论计为已判断。 |
| `SPEC-005-JOB-001` | 同一内容的分析重放和恢复幂等；失败或空结果不得覆盖已有有效结论，真实异常保留可定位原因。 |
| `SPEC-005-OBS-001` | 分别可查询待处理、失败、无效/空值、有效结论数量及版本；抽样只保存脱敏诊断，不记录敏感正文。 |

## 验收与 Checklist

Given：3 条已知异常和受控有效、空、失败、重放输入。When：只读定位并运行修复前后回归，再用真实 Codex 样本核对。Then：异常路径有解释和稳定状态，重放不污染有效结论，空值/失败不进入有效率分子。

- [ ] `CHK-005-G0-001`：核对 046 S03、PRD/Design 和 3 条异常的只读证据；记录精确文件及差距。
- [ ] `CHK-005-G1-001`：确定 `SPEC-005-DATA-001`、`SPEC-005-JOB-001`、`SPEC-005-OBS-001` 的输出校验、持久状态与重放事务边界。
- [ ] `CHK-005-G2-001`：保存可重复的空值、无效输出、调用失败和旧有效结论重放的失败测试。
- [ ] `CHK-005-G3-001`：修复并逐项核对三项 SPEC，不将缺失值补成虚假的相关或不相关。
- [ ] `CHK-005-G4-001`：运行 `uv run ruff check .`、`uv run mypy`、`uv run pytest`；真实 PostgreSQL/Kafka 下核对重放。
- [ ] `CHK-005-G5-001`：真实内容与 Codex 运行后逐项核对标注、理由、状态及异常分母。
- [ ] `CHK-005-G6-001`：记录 `AC-002-006/008` 局部结果；十来源及 60 分钟比例仍由 Plan 009 判断。

证据记录版本、库、内容身份、状态转换与命令摘要，不存敏感正文。失败时停用受影响分析路径并保留待处理任务、旧有效结果和异常记录；父级 AC 不因异常数下降自动通过。
