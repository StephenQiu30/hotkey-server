# AI 热点监控工具验收标准

本验收标准以当前仓库实现为主线：先满足 MVP 运行闭环，再分步推进企业级补齐。
如果目标是企业级 P0 能力，则按 `openspec/changes/archive/2026-05-14-enterprise-p0-hotspot-platform/tasks.md` 的任务验收。

## 适用边界（2026）

- 本阶段以 `docs/product/产品需求文档.md` 的 `MVP` 范围为准。
- `docs/product/prd/00-企业级AI热点监控平台PRD.md` 为企业级扩展方向，仅在明确启动企业级 P0 时执行。
- 企业级条目仅在明确启动 `enterprise-p0-hotspot-platform` 任务流后纳入阻断性验收；否则仅保留为规划记录。
- 不验收旧实现兼容性、旧数据库迁移兼容和旧 OpenAPI 契约。

## 企业级 P0 追加验收（仅在启动 enterprise 变更时评估）

### 当前 P0 已具备（需证据）

- [x] A1. Provider 注册与可插拔链路完整且有最少 2 条来源接入实现。
- [x] A3. 核心抓取与 AI 分析链路失败隔离；单源失败不影响全量执行。
- [x] A5. RSS 端点可提供至少：关键词/用户/热门（keyword/user/trending）三类入口。
- [x] A6. 报告系统支持 Markdown 输出，HTML 预览路径可用，发送失败记录可读。
- [x] A7. 日志与监控具备可检索的抓取、AI、邮件、任务指标，当前以轻量运行时快照 `/api/ops/metrics` 提供请求总数、状态码分桶、429 次数与活跃限流客户端数。

### 企业级 P0 缺口复核（待企业级启动后执行）

- [x] A2. LLM Provider 抽象可切换，主模型异常时有降级/跳过策略且有可观察日志。 ✅ 已补齐（待企业级运行实测）
- [x] A4. 热点聚类、去重、版本化元数据可检索回溯。 ✅ 已补齐（待企业级运行实测）
- [x] A8. 安全增强项（JWT、RBAC、限流）按企业级任务分批验收。 ✅ 已补齐（待企业级运行实测）

补充说明：企业级 P0 的“待补齐”已从“开发缺口”转为“运行级缺口”，即：

- 真实配置下的端到端验收（PostgreSQL、X/Twitter、Bing、模型、SMTP）；
- 生产部署链路与真实流量下的观察（Compose 可用性、/api/health 与 Nginx 入站可达）；
- 角色权限策略与异常路径的运营化验证（日志留存、故障演练）。

执行映射：
- B1（真实环境回放）：`docs/product/prd/09-生产环境回放验收PRD.md` / `docs/plans/17-生产环境回放与凭据验收计划.md`
- B2（部署与可达）：`docs/product/prd/10-部署链路与健康可达PRD.md` / `docs/plans/18-部署链路与健康可达计划.md`
- B3（告警闭环）：`docs/product/prd/11-监控告警闭环PRD.md` / `docs/plans/19-监控告警最小闭环演练计划.md`
- C1（权限运维）：`docs/product/prd/12-权限边界运维化PRD.md` / `docs/plans/20-权限边界与运维手册计划.md`
- C2（告警阈值）：`docs/product/prd/13-告警阈值标准化PRD.md` / `docs/plans/21-告警阈值与变更流程计划.md`

## 1. 已通过自动化或静态确认

- [x] 后端服务入口已切换为 `server`，不再以 `apps/api` 作为后端交付路径。
- [x] 除 `openspec/changes/archive` 历史归档外，不再保留运行时 `apps/` 目录与 `apps/api` 文本引用。
- [x] Next.js 前端位于 `hotkey-web` 仓库（当前页面入口按项目约定可访问）。
- [x] PostgreSQL 是当前 P0 唯一数据库。
- [x] `sql/001_init_schema.sql` 是 schema 事实源，包含 `keywords`、`sources`、`hotspots`、`ai_analyses`、`notifications`、`check_runs`、`reports`、`settings`。
- [x] SQLAlchemy models 与 `sql/001_init_schema.sql` 保持核心表一致。
- [x] RSS、Hacker News、X/Twitter、Bing、Bilibili、Sogou-style 具备 adapter 路径。
- [x] X/Twitter 未配置 Token 时跳过来源并不中断流程。
- [x] Bing 未配置 Key 时跳过来源并不中断流程。
- [x] AI 查询扩展可在本地降级路径生成 2-5 个去重查询词。
- [x] AI 分析结果包含真实性、相关性分数、相关性理由、关键词命中、重要性和摘要。
- [x] `active`/`filtered` 阈值过滤规则已实现：`relevance_score >= RELEVANCE_THRESHOLD` 且 `is_real is not False` 才进入 `active`。
- [x] `filtered` 热点不发送事件邮件、不进入日报/周报。
- [x] SMTP 缺失时系统仍可运行，并记录 `skipped`。
- [x] `/api/search` 可执行即时搜索，搜索结果不入库。
- [x] `/api/reports` 可生成 daily/weekly 报告，并只聚合 `active` 热点。
- [x] `/api/daily-reports` 不再注册，已移除兼容别名。
- [x] `/rss/trending`、`/rss/keyword/{keyword_name}`、`/rss/user/{user_id}` 可访问并返回 XML。
- [x] 前端提供核心页面：关键词、来源、热点、搜索、任务、报告、通知和设置。

## 2. 需真实配置后验收

- [ ] 空 PostgreSQL 通过 `sql/001_init_schema.sql` 初始化并完成一次端到端检查。
- [ ] 使用真实 X API v2 Recent Search Token 验证 X/Twitter 来源。
- [ ] 使用真实 Bing Search Key 验证 Bing 来源。
- [ ] 使用真实 OpenAI 兼容模型配置验证查询扩展和热点分析。
- [ ] 使用真实 SMTP 配置验证事件邮件和报告邮件发送。
- [ ] 后端与 PostgreSQL 同时启动后，使用浏览器验证工作台创建关键词、搜索、报告生成与发送链路。

## 3. 人工功能验收

- [ ] 可以创建、更新、删除、启停关键词。
- [ ] 同一 URL + 来源组合不能重复入库。
- [ ] 热点可按关键词、来源、重要性、时间范围筛选。
- [ ] 热点可按时间、相关性或重要性排序。
- [ ] 可手动触发热点检查。
- [ ] 可简单定时触发热点检查。
- [ ] 报告内容以本地 Markdown 模板生成；AI 报告增强不作为当前 P0 必需。

## 4. 非目标确认（当前阶段）

- [x] P0 不引入多租户。
- [x] P0 不引入复杂权限和分级计费。
- [x] P0 不引入实时推送。
- [x] P0 不引入向量库。
- [x] P0 不强制依赖 Celery/Redis。
- [x] P0 不兼容旧表结构、旧内存仓库或旧 bootstrap 示例数据。
