---
layer: PRD
scope: backend
doc_no: "012"
title: 搜狗授权来源适配
status: implemented
version: v1.1
owner: HotKey Team
phase: P2
canonical_path: docs/prd/archive/012-搜狗授权来源适配.md
design: docs/design/archive/012-搜狗授权来源适配设计.md
plan: docs/plans/archive/012-搜狗授权来源适配计划.md
---

# 搜狗授权来源适配

## 用户问题

参考项目通过搜狗网页搜索采集热点；搜狗公开的“开放平台”主要用于站方向搜狗提交结构化数据，不构成可读取搜索结果的官方 API。

## 产品目标

让用户明确看见搜狗能力因缺少可验证的读取授权而不可用，并让管理员知道解除阻塞所需材料；系统不得创建或调度伪搜狗来源。

## 范围

- 不可调度的“搜狗授权搜索”能力卡片
- 书面授权、端点、凭据、配额、分页、删除同步与契约测试准入清单
- 搜狗官方开放平台说明链接
- 后端禁止 HTML 搜索页抓取的架构门禁

## 用户故事

- 作为编辑者，我能选择真实可用的来源能力并预览查询结果。
- 作为管理员，我能配置授权、健康、配额和失败处理，而看不到明文密钥。
- 作为阅读者，我能识别原始事实、派生内容和不可用来源。

## 功能要求

- FR-012-1：不得解析搜狗搜索结果页、绕过验证码或复用浏览器 Cookie。
- FR-012-2：SourceType 可在授权契约落地后加入；在此之前 UI 显示“需要授权”，且不写入可调度连接。
- FR-012-3：授权适配器必须实现固定端点、凭据引用、速率限制、分页、删除同步和原始字段证据。
- FR-012-4：若无读取授权，以站点官方 RSS/Atom 或其他已批准来源覆盖信息需求，不声称数据来自搜狗。
- FR-012-5：管理员和只读用户均可查看阻塞原因与官方依据；界面不得提供创建、探测、启用或伪造授权状态的操作。
- FR-012-6：只有准入清单全部满足、沙盒契约测试通过且新设计被验收后，才能加入来源枚举和 Connector Registry。

## 非功能要求

- 外部请求必须具备固定端点、DNS/重定向复验、超时、限流和最大响应限制。
- 凭据只保存 `env:NAME` 引用；日志、错误、证据和前端不得暴露 Token。
- 采集、重试和人工触发保持幂等，可在 CollectionRun 中追踪。

## 非目标

不保证第三方未授权或已退役能力与参考项目保持表面一致。

## 验收标准

- AC-012-1：未配置授权合同时系统无法发布搜狗来源
- AC-012-2：没有任何 HTML 搜索页抓取实现
- AC-012-3：授权版本通过契约测试和新一轮验收后才可加入来源注册
- AC-012-4：来源页显示“需要授权”、阻塞原因、官方依据与完整准入清单，且不存在搜狗创建、探测或启用操作
- AC-012-5：管理员与 viewer 的桌面和移动页面无横向溢出，权限语义和可访问性验收通过
