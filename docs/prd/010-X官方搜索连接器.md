---
layer: PRD
scope: backend
doc_no: "010"
title: X官方搜索连接器
status: draft
version: v1.0
owner: HotKey Team
phase: P1
canonical_path: docs/prd/010-X官方搜索连接器.md
design: docs/design/010-X官方搜索连接器设计.md
plan: docs/plans/010-X官方搜索连接器计划.md
---

# X官方搜索连接器

## 用户问题

当前仓库没有 X/Twitter 连接器；参考项目通过 Twitter 搜索提供关键词监听。

## 产品目标

使用 X API v2 Recent Search 获取最近 7 天公开帖子，使关键词、账号和语言规则进入统一采集流水线。

## 范围

- X 来源配置与健康检查
- 关键词和账号查询
- 分页检查点与指标快照
- 授权、配额和删除/隐藏同步

## 用户故事

- 作为编辑者，我能选择真实可用的来源能力并预览查询结果。
- 作为管理员，我能配置授权、健康、配额和失败处理，而看不到明文密钥。
- 作为阅读者，我能识别原始事实、派生内容和不可用来源。

## 功能要求

- FR-010-1：只调用官方 `/2/tweets/search/recent` 或经单独批准的全量归档接口，使用 env Bearer Token。
- FR-010-2：QueryPlanner 编译关键词、短语、from、lang、排除转帖/回复等运算符，并受套餐长度和运算符能力约束。
- FR-010-3：以 since_id 和 next_token 维护检查点，映射作者、正文、时间、会话、引用、媒体和公开指标。
- FR-010-4：配额耗尽返回 rate_limited 并按 reset 时间延迟，不抓取 x.com 页面作为回退。

## 非功能要求

- 外部请求必须具备固定端点、DNS/重定向复验、超时、限流和最大响应限制。
- 凭据只保存 `env:NAME` 引用；日志、错误、证据和前端不得暴露 Token。
- 采集、重试和人工触发保持幂等，可在 CollectionRun 中追踪。

## 非目标

不保证第三方未授权或已退役能力与参考项目保持表面一致。

## 验收标准

- AC-010-1：最近搜索窗口内不漏不重
- AC-010-2：禁用连接或凭据撤销后不再发起请求
- AC-010-3：删除、withheld 和敏感内容按官方字段处理
